# STATE — Estado del proyecto

Última actualización: 2026-07-05 · **Fases 1 y 2 COMPLETAS · Fase 3 INICIADA por decisión #24 (F3-01 completo; API pública sigue sin exponer) · re-auditoría pospuesta por el propietario** · **Auditoría Independiente v1: integrada; lote AUD-1 ejecutado** (`docs/audits/`)

## Hitos

- **Fase 0 APROBADA por el propietario (2026-07-04)** — decisión #16. ADRs 0001–0010 aceptados.
- **País inicial: Colombia** (decisión #15, ex PEND-001). Implicaciones MVP: COP como moneda principal de sandbox; el MockProvider incluirá un método asíncrono tipo PSE; candidatos de proveedor para Fase 5: Wompi/PayU/dLocal/Mercado Pago. La matriz de jurisdicción queda por verificar con fuentes y revisión legal (bloquea solo Fase 5).
- **F1-01 y F1-02 completados** (apps + config + CI; primer run de CI verde en GitHub Actions).
- **F1-03 completado (2026-07-04)**: modelo de identidad/tenancy real — migración `0003_identity_tenancy.sql` (`tenants`→`organizations`, `users` global con política RLS por membresía compartida, `memberships` RBAC, `merchants` con defaults Colombia) + paquete `@fluvia/identity` (plano plataforma y plano tenant) con 15 tests de integración: aislamiento cross-tenant de merchants/orgs/users, rollback atómico de alta de organización, unicidad case-insensitive de email, escritura de `users` denegada al rol app, inmutabilidad de las tablas nuevas y `authenticate_api_key` verificada tras el rename. Suite total: 61/61.
- **F1-04a completado (2026-07-04)**: primera sub-entrega de auth — migración `0004_auth_sessions.sql` (sessions con hash SHA-256, tokens de verificación de un solo uso, lockout en users, rol dedicado `fluvia_auth` con acceso exclusivo al plano de credenciales y REVOKE a app/worker) + `@fluvia/auth` (scrypt versionado, login uniforme anti-enumeración con igualación de timing, lockout configurable, sesiones revocables, `auth_list_memberships`) + endpoints `/v1/auth/*` con mapeo estable de errores y ZodError→400. Bug real detectado y corregido por los tests: el contador de lockout se perdía en el ROLLBACK del propio error — ahora se commitea antes de lanzar. Suite total: **92/92** + smoke test HTTP del flujo completo (register→verify→login→session→bad-login→logout). Pendiente F1-04b (MFA TOTP + step-up).
- **F1-04c completado (2026-07-04)**: API keys con scopes y entorno test/live (migración 0005; secreto mostrado una única vez, solo hash+prefijo en BD, last_used_at con throttle) + RBAC declarativo (`packages/identity/src/rbac.ts`, matriz publicada en access-control.md y verificada celda a celda por test) + middleware de dos planos en el API (sesión+rol por organización para dashboard; API key+scope para integración; planos NO intercambiables) + primeros endpoints tenant-scoped reales: `/v1/organizations[...]` (org, members, merchants CRUD, api-keys) y `/v1/account`. Decisión de seguridad: no existe scope para gestionar API keys — una key robada no puede escalar. Suite total: **111/111** incl. tests BOLA (404 indistinguible), RBAC por rol y scopes.
- **F1-05 completado (2026-07-04)**: audit log append-only — migración `0006_audit_log.sql` (tabla inmutable por triggers Y por grants, RLS por plano: tenant para app, INSERT-only sin tenant para fluvia_auth) + `@fluvia/audit` (evento en la MISMA transacción que la acción, redacción recursiva de claves sensibles, lector paginado). Acciones auditadas: api_key.created/revoked (risk high), merchant.created/updated (con before/after), user.registered/email_verified, auth.login_succeeded/login_failed/account_locked/logout/sessions_revoked. Permiso nuevo `audit:read` (owner/admin/finance/analyst) + endpoint `GET /v1/organizations/:orgId/audit-events` con cursor. Suite total: **120/120**.
- **F1-06 completado (2026-07-04)** → **Gate Multi-tenant 🟢 técnico**: suite ampliada de tenant-escape (UPDATE por PK ajena, UPDATE masivo, INSERT…SELECT, JOINs, sondas EXISTS, agregados, fuga de contexto en la MISMA conexión del pool, SET ROLE denegado, worker sin DELETE) + **meta-tests estructurales** contra pg_catalog (toda tabla futura con tenant_id debe tener RLS forzado y política; ningún rol de runtime con DELETE; app/worker sin privilegios sobre credenciales) + `withPlatformOperation` como único bypass sancionado (razón obligatoria, auditoría atómica de riesgo alto, rollback conjunto). Límite residual documentado (SQL arbitrario ⇒ cambio de GUC) en multi-tenancy.md §6.5 y threat model. Suite total: **135/135**.
- **F2-01+F2-02 completados (2026-07-04) — arranca el núcleo financiero**: migración `0007_ledger_core.sql` — enlace causal (`source_type/source_id`) y de reversión (`reverses_tx_id` con FK) en `ledger_transactions`; `balance_projections` separada con RLS+inmutabilidad (y `ledger_accounts` contraída a pura definición); **invariantes a nivel de motor**: constraint triggers diferidos que hacen imposible commitear un asiento desbalanceado por (tx, moneda) — probado con SQL crudo de superusuario — ni compensar un hueco USD con COP, ni dejar cabeceras sin asientos. Suite total: **144/144**.
- **F2-03 completado (2026-07-04)**: `@fluvia/ledger` — `LedgerService.postTransaction` implementa el posting normativo íntegro: validación previa de balanceo por moneda (feedback rápido; la BD re-verifica al COMMIT), idempotencia del asiento con **replay verificado por huella canónica** (mismo key + payload distinto ⇒ `IdempotencyConflictError`), locking pesimista de cuentas en orden total anti-deadlock, proyecciones versionadas con guard optimista y retry limitado (deadlock/serialization incluidos), y evento `ledger.transaction.posted` al outbox EN la misma transacción. `createAccount` crea cuenta+proyección atómicamente; `verifyProjection` es la primitiva del audit-replay. 10 tests de integración: captura con split de fees, bucket pending, carrera de 5 posts concurrentes con el mismo key (exactamente 1 aplicado), 25 transferencias concurrentes con orden aleatorio de cuentas sin drift (proyección == recomputado). Suite total: **154/154**.
- **Auditoría Independiente v1 integrada + lote AUD-1 completado (2026-07-04)**: PR documental #1 verificado (solo 4 archivos bajo `docs/audits/independent-audit-v1/`, inmutables) y mergeado sin tocar los hallazgos; HEAD al integrar == commit auditado `a665b4f` (cero drift). Reconciliación evidencia-por-hallazgo en `docs/audits/audit-integration-plan-v1.md` (0 P0 · 10 P1 · 16 P2 · 3 P3) y estado vivo en `audit-closure-register-v1.md`. **Lote AUD-1 (remediación inmediata)**: migración `0008_audit_remediations.sql` — FK compuesta `(account_id, tenant_id, currency)` en `ledger_entries` (AUD-P1-001, con validación previa de datos), `idempotency_keys` re-clavada a `(tenant_id, endpoint, key)` (AUD-P1-009), REVOKE total al worker sobre ledger/proyecciones/api_keys (adelanto AUD-P1-007, mitiga AUD-P2-004) + guard race-safe `nonNegativeAccounts` con `InsufficientBalanceError` aplicado a release/refund (AUD-P1-010) + huella idempotente causal completa en replay (AUD-P2-001) + bloqueo de API keys `live` (`LiveKeysDisabledError`, AUD-P2-003, decisión #19) + anti-mezcla de entornos en `dbUrlsFromEnv` (AUD-P2-014) + README real (AUD-P3-001), bloques "Estado de implementación" en 6 docs de diseño (AUD-P3-002) y reservas-como-cuentas (AUD-P2-013, decisión #18). Veredicto del auditor adoptado: continuar; pagos públicos/checkout/proveedor/webhooks externos/live congelados hasta cerrar bloqueantes. Nuevas decisiones humanas abiertas: PEND-004/005/006.
- **F1-04b completado (2026-07-04) — MFA + STEP-UP + RATE LIMITING; LOS 5 P1 DEL AUDITOR CERRADOS**: PEND-005 resuelta (decisión #23: TOTP + backup codes, sin SMS). Migración `0014` (secreto TOTP cifrado en users, `mfa_challenges`, `mfa_backup_codes` — plano fluvia_auth con FORCE RLS, meta-test ampliado a 5 tablas) + TOTP RFC 6238 propio (SHA-1/6 dígitos/30 s, verificado contra los vectores del Apéndice B del RFC) + cifrado AES-256-GCM del secreto en reposo (clave `MFA_SECRET_KEY` con anti-mezcla en config). Flujo: password válido con MFA ⇒ reto de 5 min (fluvia_mfa_, un solo éxito) ⇒ TOTP o backup code ⇒ sesión con `mfa_verified_at`. Garantías probadas: **anti-replay por step** (un código jamás vale dos veces), backup codes de un solo uso que decrementan el contador, fallos MFA alimentan el MISMO lockout que el password (hasta bloquear, probado), desactivar exige TOTP (un backup code NO basta). **Step-up**: `keys:manage` exige verificación MFA ≤15 min cuando el usuario tiene MFA (403 `mfa_step_up_required` sobre HTTP, refresco vía `/v1/auth/mfa/step-up`; usuarios sin MFA no se bloquean hasta PEND-006). **Rate limiting** por email/IP en `/v1/auth/*` (429 `rate_limited` + Retry-After; ventana fija in-memory — Nivel C documentado: store compartido antes del sandbox compartido). Catálogo v1 +5 códigos (golden regenerado). 23 tests nuevos. Suite total: **278/278**.
- **F1-07 completado (2026-07-05) — observabilidad base**: paquete `@fluvia/observability` propio (counter/gauge/histogram + exposición Prometheus 0.0.4, guard de cardinalidad de 500 series con contador de descartes, tests de formato/escape). API instrumentada: `fluvia_http_requests_total{method,route,status}` + histograma de duración — la ruta es SIEMPRE la plantilla registrada y los paths sin match colapsan en `unmatched` (anti-cardinalidad probado); `GET /metrics` sin datos de tenant (test verifica ausencia de UUIDs). Worker: servidor mínimo `/health`+`/metrics` en `WORKER_METRICS_PORT` (default 9464), métricas de relay (delivered/retried/dead por ciclo vía `onStats`), drift (gauge de cuentas en drift + contador de chequeos vía `onCheck`) y heartbeats. Regla probada en los tres hooks: un observador de métricas que lanza JAMÁS rompe al proceso observado. Alertas baseline en `observability.md` §4 (drift>0 crítica, dead>0, worker sin latido, 5xx, p99, 429s). **Desviación registrada**: OTel diferido hasta collector real (F6) — declarar trazas sin backend sería capacidad simulada; correlación hoy = `request_id` de extremo a extremo (header→logs→sobre de error→audit_log). AUD-P2-007 pasa a PARCIAL (pata observabilidad; restore/runbooks siguen en F4-06/F6). Suite total: **298/298**.
- **F1-09 completado (2026-07-05) — purga por clasificación de datos + guard de roles (cierra AUD-P2-008)**: migración `0015` — la política ENTERA vive en `purge_technical_data()` (SECURITY DEFINER SIN parámetros: nadie puede ensanchar el alcance): 4 clases técnicas con retenciones fijas (idempotency_keys al vencer; sesiones/tokens de verificación +7 días; retos MFA +1 día), auditoría atómica `platform.technical_purge` con conteos por clase EN la misma transacción (solo purgas efectivas — un no-op no genera ruido append-only), EXECUTE solo del worker. El trigger de las tablas técnicas cambia a `fluvia_forbid_mutation_technical()`: mismo bloqueo (incluso superusuario, probado) salvo dentro de la transacción del job (GUC local); las clases financieras conservan el trigger ORIGINAL — probado que el escape NO existe para ledger/organizations/audit_events ni con el GUC activo. **AUD-P2-008 RESUELTO**: `fluvia_assert_dev_role_creation()` antes de cada `CREATE ROLE ... PASSWORD` (meta-test: 5 altas / 5 con guard) + el runner inyecta `fluvia.environment` — probado end-to-end en BD efímera: en 'production' la migración ABORTA sin registro parcial; en 'test' aplica. `TechnicalPurgeJob` en el worker (PURGE_* en config, métricas `fluvia_technical_purge_*`). R-12 pasa a Mitigado. Suite total: **310/310**.
- **F1-10 completado (2026-07-05) — seeds deterministas; FASE 1 COMPLETA**: `@fluvia/seeds` + `pnpm seed`. IDs UUID v5 sobre claves fijas (misma corrida = mismos ids), identidad vía ON CONFLICT DO NOTHING y ledger por la VÍA NORMATIVA (`PostingService.ensureChart` + capture/release con idempotency keys fijos: re-ejecutar = replay exacto, CERO filas nuevas — probado contando orgs/users/memberships/merchants/tx/asientos antes y después). Historia de demo: 500.000 COP capturados (fee proveedor 14.500, fee plataforma 19.500), 300.000 liberados → pending 180.500 / available 300.000 (verificado). Guard duro: `SeedEnvironmentError` fuera de local/test ANTES de tocar la BD (probado con pools rotos) — los passwords de demo jamás llegan a sandbox/staging/production; abrir sandbox se decidirá con PEND-006. Passwords de demo verificados autenticables con `verifyPassword`. Suite total: **315/315**.
- **AUD-P2-015 completado + diseño de F3-01 listo (2026-07-05)**: (1) migración `0016` — API keys con **HMAC-SHA256 server-side**: `key_hash_version` (1=sha256 legado, 2=HMAC con pepper `API_KEY_HMAC_SECRET`, 64 hex, anti-mezcla en config), claves nuevas nacen v2, `authenticate_api_key(hmac, legacy)` autentica ambas vías y **promueve v1→v2 en la misma llamada** (upgrade perezoso probado: fila legada simulada → autentica → queda v2 con HMAC; la vía sha256 deja de matchear). Un dump de `api_keys` ya no permite validación offline de claves — el pepper jamás se persiste. Prerequisito técnico de PEND-006 cumplido. (2) `docs/design/f3-01-payment-intents.md`: diseño aprobado para ejecutar F3-01 el día del veredicto — schema 0017 (12 estados, FK compuestas, attempts), FSM hecha cumplir EN el motor (tabla de transiciones + trigger), una sola fuente de verdad (mapa TS → seed generado, patrón golden) y meta-test triple doc↔TS↔DDL + matriz de ilegales como superusuario (cerrará AUD-P2-011). Suite total: **316/316**.
- **F3-01 completado (2026-07-05) — ARRANCA FASE 3 por decisión #24 (propietario adelanta F3-01; re-auditoría se solicitará más adelante; endpoints públicos SIGUEN congelados)**: migración `0017` ejecutando el diseño aprobado — guard de tabla vacía, 12 estados, FK compuesta intent↔merchant↔tenant (un intent no puede referenciar el merchant de otro tenant ni como superusuario, probado), CHECKs `captured ≤ amount` / `refunded ≤ captured` (probados), `payment_attempts` con FSM propia. **FSM hecha cumplir EN el motor**: tablas de transiciones inmutables (seed GENERADO desde el mapa TS por `gen-fsm-seed.ts` — una sola fuente de verdad) + triggers `FLUVIA_INVALID_TRANSITION`. `@fluvia/payments-core`: mapas declarativos intent/attempt/refund, `PaymentIntentService` (RLS, FOR UPDATE, version++, timestamps terminales, `failure_code` de catálogo, evento `payment_intent.<estado>` al outbox EN la misma tx). **Meta-test triple (cierra AUD-P2-011)**: doc(mermaid §1/§2/§3) ↔ TS ↔ DDL con igualdad exacta, y la matriz COMPLETA de motor (144 pares intent + 64 attempt) probada con SQL de superusuario. Cierra también **AUD-P2-002**. Mermaids de attempt/refund añadidos al doc (antes solo prosa). Suite total: **332/332**.
- **F3-02 completado (2026-07-05) — primeros endpoints de DINERO, idempotentes de nacimiento**: `POST /v1/payment_intents`, `GET` (retrieve/list), `POST .../cancel` en el plano de integración (API key + scopes `payments:write`/`read`; los planos sesión↔API key siguen sin cruzarse). TODA mutación exige `Idempotency-Key` (capa F2-09: claim+efecto+respuesta en UNA transacción — el servicio de intents expone variantes client-bound para componer): replay exacto probado sobre HTTP, 422 `idempotency_key_reuse` con payload distinto, y un efecto fallido (merchant ajeno → 404 indistinguible) revierte el claim SIN quemar la key (probado). FSM sobre HTTP: cancelar un intent cancelado con key nueva = 409 `invalid_state_transition` (código nuevo; catálogo v1 → **35 códigos**, golden regenerado). Contrato `docs/api/openapi.v1.json` (parcial, versionado) + test spec↔servidor (cada path declarado existe Y exige auth; control anti-catch-all). **Desviaciones registradas**: `confirm` diferido a F3-03 (confirmar sin proveedor = pago irresoluble = capacidad simulada) y `customers` a F3-05 (su primer consumidor). AUD-P1-002 pasa a PARCIAL. Suite total: **348/348**.
- **F3-03 parte síncrona completada (2026-07-05) — EL CICLO DE PAGO SANDBOX ESTÁ VIVO**: `MockPaymentProvider` (tokenización simulada tipo tarjetas de prueba: `tok_approve`/`tok_decline`/`tok_decline_insufficient`/`tok_timeout`; referencias deterministas; contract tests) + `PaymentConfirmationService` en dos fases (V4 Nivel A): fase 1 DENTRO de la tx de la idempotency key (intent→processing por la FSM + attempt en submitting — el replay devuelve exactamente lo mismo y no re-ejecuta), proveedor FUERA de toda tx, y registro atómico del resultado: aprobado ⇒ `capturePayment` + attempt succeeded + intent succeeded + `amount_captured`, TODO en una transacción vía `onPosted` (probado: merchant.pending crece exacto; el asiento referencia causalmente al attempt; re-ejecutar tras crash = no-op, 1 solo asiento); rechazado ⇒ failed con `failure_code` estable y CERO asientos; **timeout ⇒ attempt `indeterminate`** y el intent queda processing — nada lo resuelve por asunción (V4 §23, probado). `POST /v1/payment_intents/:id/confirm` con contrato ASÍNCRONO (responde processing+attempt_id; GET muestra el final) — spec OpenAPI actualizado y cubierto por el test de contrato. Fees en 0: inventar pricing sería simular un modelo comercial inexistente (PEND-002). Suite total: **363/363**.
- **F3-03b completado (2026-07-05) — F3-03 CERRADO; el inbox durable tiene su primer handler real**: `tok_pse` (método asíncrono PSE-like, Colombia) ⇒ attempt `submitted` con referencia del proveedor. **Endpoint HTTP de ingesta** `/v1/providers/mock/webhook` (cierra el pendiente de F2-12): sin API key — la credencial ES la firma HMAC sobre los bytes crudos (parser raw scoped), tolerancia ±5 min, dedup race-safe por (provider, event_id); firma inválida ⇒ 401 del catálogo y CERO persistencia (probado). **Primer handler real del inbox**: schema Zod estricto (lo no conforme → DLQ) → `resolveFromProvider` — la ÚNICA vía legítima para cerrar attempts `submitted` o `indeterminate` (V4 §23) — con la MISMA captura contable atómica de la vía síncrona (misma idempotency key del ledger ⇒ doble procesamiento imposible); webhooks tardíos/contradictorios ⇒ `ignored_out_of_order` sin re-abrir nada (probado end-to-end sobre HTTP con el MISMO cableado del worker). `InboxProcessor` en el worker (rol mínimo `fluvia_inbox` para el claim; el handler actúa como la aplicación con RLS; INBOX_* en config; métricas `fluvia_inbox_*` + alerta dead). Config: `INBOX_DATABASE_URL` y `MOCK_WEBHOOK_SECRET` entran a la cadena anti-mezcla. Suite total: **372/372**.
- **F3-04 completado (2026-07-05) — robustez del plano de attempts**: (1) migración `0018` — `sweep_payment_attempts()` (definer SIN parámetros, familia purga/drift): barrido `submitting→indeterminate` con lease de 5 min — cierra la ventana de crash entre fases de F3-03; un attempt barrido queda con desenlace DESCONOCIDO y solo lo cierra la fuente verificada (probado: tras el barrido, el webhook lo resuelve normal); el fresco no se toca (probado); auditoría atómica `payment_attempt.swept_indeterminate` con los ids; salud de indeterminados (total + envejecidos >30 min). (2) `AttemptsWatchdog` en el worker (ATTEMPTS_WATCHDOG_* en config; gauges `fluvia_payment_attempts_*` + alerta baseline: `indeterminate_aged > 0` = dinero en desenlace desconocido, intervenir). (3) `ResilientProvider` — decorador sobre CUALQUIER adapter: timeout duro (proveedor colgado ⇒ ProviderTimeoutError ⇒ indeterminate) + circuit breaker (umbral de fallos consecutivos, cooldown, half-open con sonda única, probado con reloj inyectado; los declines NO cuentan como fallos). Distinción clave probada: **circuito abierto = la petición JAMÁS se envió = fallo LIMPIO** `provider_unavailable` sin asiento ni ambigüedad — solo el desenlace desconocido va a indeterminate. Breaker in-memory Nivel C (como el rate limiter; store compartido con PEND-006). Suite total: **385/385**.
- **F3-07 completado (2026-07-05) — los eventos que emitimos llegan a los comercios**: migración `0019` — `webhook_endpoints`/`webhook_events`/`webhook_attempts` (append-only, RLS forzado) + rol `fluvia_webhook` con ventanas ADR-0011 (el fan-out lo hace `fluvia_relay`; la entrega, un rol propio con UPDATE por columna; lección F2-12 reaplicada: ON CONFLICT exige SELECT sobre las columnas del árbitro). `@fluvia/webhooks`: catálogo de **13 topics** con meta-test doc §5↔código; firma `Fluvia-Signature: v1=` HMAC-SHA256 sobre `{ts}.{event_id}.{body}` con **rotación dual** (el secreto anterior firma durante 24 h — probado que AMBAS firmas verifican); secretos `whsec_` cifrados **AES-256-GCM en reposo** (un dump no permite forjar webhooks); **SSRF guard §4 probado**: https+443 fuera de local, denylist de TODAS las IPs resueltas (privadas/loopback/link-local/CGNAT/multicast/v4-mapped), conexión PINEADA a la IP validada en CADA intento (anti DNS-rebinding), sin redirects, IP registrada por attempt; deliverer claim-lease (mismo patrón del relay) con calendario 0s/30s/2m/10m/1h/6h/24h→dead y endpoint deshabilitado ⇒ cola muere SIN tráfico de red (probado). API `/v1/webhook_endpoints` (create/list/rotate/disable, scope `webhooks:manage`): el secreto se entrega UNA sola vez y el listado jamás lo expone (probado); cross-tenant = 404 indistinguible. Métrica `fluvia_webhook_deliveries_total{result}` + alerta dead. Probado end-to-end contra PG real y un receptor HTTP real. Desviaciones: reenvío manual auditado → F3-09; auto-disable con notificación → F6. Suite total: **409/409**.
- **F3-08 completado (2026-07-05) — refunds end-to-end; el ciclo de dinero es reversible**: migración `0020` — tabla `refunds` (FK compuesta intent↔tenant, append-only, RLS forzado) + FSM `refund_transitions` hecha cumplir EN el motor con el patrón golden de 0017 (seed generado desde el mapa TS; meta-test triple doc §3↔TS↔DDL + matriz COMPLETA de 36 pares como superusuario). `RefundService` en dos fases (mismo esqueleto que confirm, V4 Nivel A): fase 1 DENTRO de la tx de la idempotency key — bajo `FOR UPDATE` del intent valida estado (succeeded|partially_refunded) y monto contra lo REMANENTE (capturado − aplicado − refunds en vuelo), inserta el refund en `created`; el lock del intent serializa refunds concurrentes del mismo intent (Σ refunds ≤ capturado race-safe). Fase 2 FUERA de toda tx: (2a) `refund.request` reserva merchant.available→refund.liability con guard de no-negatividad EN el motor (AUD-P1-010) — sin disponible el refund se CANCELA limpio SIN contactar al proveedor (desenlace conocido); (2b) proveedor → aprobado: `refund.settle` (liability→provider.clearing) + refund succeeded + `amount_refunded += monto` + intent partially_refunded/refunded, TODO en una tx vía `onPosted`; rechazado/circuito abierto: `refund.cancel` (reserva devuelta ÍNTEGRA al comercio) + refund failed; **throw o `pending` (aceptación asíncrona) = desenlace DESCONOCIDO: el refund pasa a `indeterminate` con la reserva RETENIDA — nada lo resuelve por asunción NI por re-envío (V4 §23); SOLO `resolveFromProvider` (fuente verificada, espeja attempts) lo cierra en succeeded/failed**. Idempotencia por keys `refund:<id>:request|settle|cancel` (re-ejecutar = replay, cero doble asiento; probado). Endpoints `POST/GET /v1/refunds` idempotentes (scope payments:write/read), catálogo +`refund_amount_exceeds_remaining` (**36 códigos**, golden regen), topic `refund.canceled` añadido al catálogo de webhooks (los 5 `refund.*` llegan al comercio por F3-07). Probado contra PG real + ledger: todos los desenlaces del dinero con balances exactos y cero drift, incluida la resolución verificada de un `indeterminate`. **Desviación registrada** (espeja F3-03→F3-04): la ingesta de webhooks de refund del proveedor y un watchdog de refunds `indeterminate` envejecidos quedan para F4 (conciliación); hoy `execute` corre una vez desde el endpoint y un refund atascado conserva su cupo (conservador: jamás sobre-reembolsa). Revisión adversarial multi-agente (4 lentes × verificación escéptica) sobre el flujo del dinero: cero defectos confirmados. **Cierra AUD-P1-002 por completo** (payments core real: intents+attempts+confirm+refunds E2E). Suite total: **436/436**.
- **F3-05a completado (2026-07-05) — customers; arranca la fundación de checkout**: migración `0021` — tabla `customers` (RLS forzado, append-only con baja lógica `deleted_at`, clave compuesta (id, tenant_id) para los FK futuros de checkout sessions y payment links). Recurso diferido desde F3-02 con su primer consumidor real ya a la vista (el checkout de F3-05). Semántica tipo Stripe: **el email NO es único** (personas/negocios distintos comparten correo — probado que se permiten duplicados), se exige al menos uno de email/name/phone (un customer vacío suele ser bug del integrador), metadata validada ≤50 claves con límites de longitud (bolsa del comercio, jamás interpretada por Fluvia). `CustomerService` en `@fluvia/identity`: create/get/list con DTO whitelisted (jamás spread de fila — sin `tenant_id`/`deleted_at` al cliente), **update parcial** (solo las claves presentes cambian; `null` limpia email/name/phone/description; metadata REEMPLAZA la bolsa completa) y **softDelete idempotente** (baja lógica, desaparece de get/list, borrar dos veces no es error). Endpoints `/v1/customers` create/get/list/update/delete con scopes `customers:write`/`read` (el scope ya existía desde F1-04c); `CustomerNotFoundError`→`not_found` sin código nuevo (golden regen solo del mapa domain, 36 códigos); OpenAPI +5 paths cubiertos por el test de contrato. Probado contra PG real: validación, update parcial, baja idempotente y aislamiento cross-tenant (404 indistinguible por RLS). Alcance: F3-05a es la fundación backend; **checkout sessions (F3-05b) y la UI Next.js alojada (F3-05c) siguen** — la UI no puede construirse contra una API inexistente sin simular capacidad (Nivel A). Suite total: **458/458**.
- **F3-05b completado (2026-07-05) — checkout sessions (el recurso)**: migración `0022` — tabla `checkout_sessions` (FK compuestas al payment_intent y al customer opcional, RLS forzado, append-only) + FSM `open→completed|expired` hecha cumplir EN el motor (patrón golden de 0017/0020/0021; meta-test triple doc §5↔TS↔DDL + matriz COMPLETA de 9 pares como superusuario). Reconcilié el mermaid §5 (tenía prosa con un estado `canceled` inexistente — un buyer que cancela se redirige a `cancel_url` pero la sesión sigue `open` hasta expirar, semántica Stripe). `CheckoutSessionService` en `@fluvia/payments-core`: create idempotente (capa F2-09) SOLO sobre un intent no resuelto (un intent terminal es error de estado `invalid_state_transition`), `client_secret` `cs_<random>` entregado UNA vez con **solo su hash sha256 en reposo** (un dump no permite secuestrar sesiones), TTL acotado 5 min–24 h (default 24 h), customer opcional pre-validado bajo RLS (ajeno/borrado → error limpio, no violación de FK cruda); get/list con DTO whitelisted (jamás expone el secreto). Endpoints `/v1/checkout_sessions` create/get/list (scopes payments:write/read); config `CHECKOUT_BASE_URL` (no secreto, default dev); `CheckoutSessionNotFoundError`→not_found y `CheckoutSessionInvalidCustomerError`→validation_error sin códigos nuevos (36); OpenAPI +2 paths. Probado contra PG real: creación idempotente con replay exacto sobre HTTP, guard de estado del intent, client_secret una vez + hash en reposo, aislamiento cross-tenant. **Alcance**: F3-05b es el RECURSO; el flujo alojado (retrieval por client_secret, disparo de completed/expired y sus eventos `checkout_session.*`) es **F3-05c**, registrado como desviación (espeja el diferimiento de barridos F3-03→F3-04). Suite total: **476/476**.
- **F3-05c-i completado (2026-07-05) — backend del flujo alojado; los eventos `checkout_session.*` cobran productor**: migración `0023` — `checkout_session_authenticate(session_id, secret_hash)→tenant_id` SECURITY DEFINER: autenticación **cross-tenant por `client_secret`** (la página alojada no trae contexto de tenant), mismo patrón que `authenticate_api_key`; devuelve solo el tenant_id, nada más. `CheckoutSessionService.getByClientSecret`: autentica (secreto/id equivocado = mismo not-found que inexistente, anti-enumeración) → ya con el tenant scoped, **sincroniza el estado de forma perezosa e idempotente bajo `FOR UPDATE` de la sesión**: open→completed si el intent asociado tuvo éxito (sella `completed_at`), open→expired si venció el TTL, emitiendo `checkout_session.completed|expired` al outbox (buildEnvelope en TS — valida el sobre AL emitir, mismo schema que valida el relay; el productor de esos topics del catálogo F3-07 ya existe) → devuelve una **vista REDACTADA** (estado de la sesión + id/estado/monto/moneda del intent, jamás el secreto, el tenant ni internos del comercio). Endpoint `GET /v1/checkout_sessions/:id/status` **sin API key** (la credencial es el `client_secret` en header `X-Checkout-Client-Secret`; 404 del catálogo para secreto ausente/malo; excluido del contrato OpenAPI igual que la ingesta de webhooks del proveedor). Probado contra PG real: vista redactada, completado con evento único + idempotencia (segunda consulta no re-emite), expiración con evento único, intent-succeeded gana sobre TTL vencido, y el ciclo completo sobre HTTP (crear sesión → confirmar el intent con `tok_approve` por la vía existente → `/status` refleja completed). **Pendiente F3-05c-ii**: barrido de entrega garantizada para sesiones que nadie consulta (espeja `sweep_payment_attempts`) + UI Next.js alojada. Suite total: **483/483**.
- **F3-05c-ii completado (2026-07-05) — entrega garantizada de eventos de checkout**: migración `0024` — `sweep_checkout_sessions()` SECURITY DEFINER (familia purga/drift/attempts): completa las sesiones `open` cuyo intent tuvo éxito y expira las vencidas que **NADIE consultó** (la sincronización perezosa de F3-05c-i solo dispara si el comprador vuelve a la página; si cierra la pestaña tras pagar, este barrido cierra el hueco), emitiendo `checkout_session.completed|expired` al outbox. El sobre común se construye EN SQL (`jsonb_build_object` con `evt_<uuid>`, `occurred_at` ISO-8601, resource/data) — **probado que valida contra el MISMO `EventEnvelopeSchema` que el relay exige antes de despachar** (un sobre malformado sería veneno). Las dos ramas del `WITH` son DISJUNTAS (una fila jamás la tocan ambos UPDATE); un intent con éxito gana sobre un TTL vencido (coherente con la vía perezosa). `CheckoutSessionWatchdog` en el worker (mismo patrón que `AttemptsWatchdog`; config `CHECKOUT_WATCHDOG_ENABLED/INTERVAL_MS`, métrica `fluvia_checkout_sessions_swept_total{result}`, observador que jamás rompe el job). Idempotente con la vía perezosa bajo el guard `status='open'` (probado: segunda corrida no re-emite). Probado contra PG real vía el rol `fluvia_worker`. **Pendiente F3-05c-iii**: UI Next.js alojada (i18n es/en, WCAG AA) — primer app frontend del monorepo. Suite total: **489/489**.
- **F3-05c-iii completado (2026-07-05) — confirm alojado (backend); el checkout ya es pagable desde la página del comprador**: endpoint `POST /v1/checkout_sessions/:id/confirm` **sin API key** (la credencial es el `client_secret` en header; el navegador del comprador JAMÁS sostiene la API key del comercio). `CheckoutSessionService.confirmByClientSecret`: autentica (0023) → mismo patrón de dos fases que el confirm de la API (V4 Nivel A): fase 1 (intent→processing + attempt `submitting`) bajo `FOR UPDATE` de la sesión, proveedor FUERA de toda tx, luego sincroniza la sesión (completed + evento si el intent tuvo éxito). **Idempotente ante doble submit** (el comprador hace doble clic): si el intent ya no es confirmable (una confirmación previa lo dejó en processing/terminal), devuelve la vista actual SIN crear un segundo attempt — probado que attempts=1 tras dos confirms. Probado contra PG real + HTTP: `tok_approve`→sesión completed + intent succeeded; `tok_decline`→intent failed y la sesión sigue open (el intent terminal expirará por TTL — el checkout es de un intento por sesión, coherente con la FSM del intent; un reintento = nuevo intent+sesión); secreto equivocado→404 anti-enumeración; token faltante→400. La ruta va sin API key, excluida del contrato OpenAPI como `/status` y la ingesta del proveedor. Con esto **el flujo de pago alojado es funcional extremo a extremo por backend** (crear sesión → la página consulta `/status` → paga con `/confirm` → completa + webhook). **Pendiente F3-05c-iv**: la UI Next.js alojada (presentación). Suite total: **496/496**.
- **F3-05c-iv completado (2026-07-05) — UI de checkout alojada; F3-05 COMPLETO**: `apps/checkout`, el PRIMER app frontend del monorepo (Next.js 15 App Router + React 19 — **subí de Next 14 a 15 porque el gate `pnpm audit --audit-level high` del CI rechazaba el advisory crítico de Next 14**; con 15.5.16 el árbol queda limpio). Página `/c/[id]`: lee el `client_secret` del **fragmento** de la URL (`#…`, no llega a los logs del servidor), consume `GET /status` y `POST /confirm` vía **route handlers server-side** (el navegador jamás conoce la URL de la API ni sostiene una API key — el proxy reenvía el secreto). i18n es/en con un diccionario propio (la superficie no justifica una librería). **WCAG AA**: `main`/`h1`, `fieldset`/`legend` para los métodos, `label` explícito, estado en región `aria-live` con foco al resultado, contraste AA (ratios ≥4.5:1 documentados en el CSS), targets táctiles ≥44px. **Verificación**: tests de componente + accesibilidad estructural (jsdom + `@testing-library/react` + `axe-core`) **CI-gated** (render, i18n, la interacción de pago aprobado/rechazado, cero violaciones axe); y el **E2E de navegador full-stack verificado LOCALMENTE** (Chromium real preinstalado → route handlers → API de Fluvia → PG: renderiza `$ 50.000` → clic Pagar → «¡Pago completado!», con captura). El CI **no tiene navegador** (no corre `playwright install`), así que el E2E de navegador es local por diseño — documentado en `apps/checkout/e2e/README.md`; añadí `.next`/`.prettierignore` para no escanear artefactos generados. Con esto **F3-05 (checkout) está COMPLETO extremo a extremo**: customers → recurso → flujo alojado (status/confirm) → entrega garantizada → UI. Suite total: **501/501** (496 backend + 5 componente); `pnpm audit --audit-level high` limpio (1 moderate).
- **F3-06 completado (2026-07-05, backend) — payment links; reutilizan TODO el checkout**: migración `0025` — tabla `payment_links` (plantilla "págame" reutilizable: FK compuesta al merchant, RLS forzado, append-only) + función `payment_link_resolve(id)` SECURITY DEFINER cross-tenant que devuelve monto/moneda/merchant SOLO de links ACTIVOS (inexistente o deshabilitado = 0 filas, anti-enumeración). `PaymentLinkService` en `@fluvia/payments-core`: create idempotente (F2-09, merchant validado bajo RLS)/get/list/disable, y **`createSessionFromLink` PÚBLICO** — resuelve el link y genera un `payment_intent` + `checkout_session` **FRESCOS en UNA transacción atómica** (reutiliza el recurso de checkout de F3-05; **multi-uso**: cada apertura del link es una sesión distinta con su propio intent, probado). Endpoints `/v1/payment_links` create/get/list/disable (scope payments:write/read, creación idempotente) + `POST :id/sessions` **sin API key** (abrir el link; deshabilitado/inexistente = mismo 404; excluido del contrato OpenAPI). Probado **end-to-end sobre HTTP**: crear link → `POST :id/sessions` (público) → sesión pagable → `GET /status` refleja el monto del link → `POST /confirm` con `tok_approve` → completed. Sin códigos nuevos (36); OpenAPI +3 paths de gestión. **Pendiente F3-06-b** (frontend, thin): página `/l/[id]` del app que llama a `/sessions` y redirige a `/c/{sessionId}#{secret}`. Suite total: **515/515**.
- **F3-06-b completado (2026-07-05) — UI del payment link; el link cierra end-to-end para el comprador**: página pública `/l/[id]` en `apps/checkout` — server component (`export const dynamic = 'force-dynamic'`) que hace el `POST /v1/payment_links/:id/sessions` **server-side** (F3-06, sin API key) y **redirige** el navegador al flujo alojado `/c/{sessionId}#{clientSecret}` (F3-05c-iv). El `client_secret` viaja en el **fragmento** del `Location` del 307: el servidor lo emite una vez y NUNCA lo vuelve a ver en la navegación posterior a `/c` (el fragmento no se envía al servidor). Un link inválido/deshabilitado/API caída → misma vista de indisponibilidad `LinkUnavailable` (mismo mensaje anti-enumeración que la vista `not_found` del checkout). La lógica pura `resolveLinkSession` se factorizó aparte para **probarla en CI sin navegador** (jsdom + axe, 6 tests: destino con fragmento, preservación de `?lang=en`, endpoint correcto, 404→null, API caída→null, body incompleto→null, vista de indisponibilidad accesible). **La redirección real con fragmento a través del navegador** se verificó LOCALMENTE (Chromium preinstalado → Next `:3100` → API `:3000` → PG): abrir `/l/{id}` → `307` con `Location: /c/{sid}#cs_…` → el navegador conserva el fragmento → checkout carga `$ 77.000` → clic Pagar → «¡Pago completado!». El CI no tiene navegador, así que el E2E de navegador es local por diseño (documentado en `apps/checkout/e2e/README.md`). Sin cambios de esquema ni de dependencias (reusa Next 15/React 19/axe ya presentes). Suite total: **521/521**.
- **F3-09a completado (2026-07-05) — visibilidad de la cola de webhooks + reenvío manual auditado de eventos `dead`**: primera rebanada del dashboard (F3-09), capacidad backend NUEVA (no hay read API de la cola de webhooks hasta ahora) que además cierra el diferido de F3-07 (reenvío manual). Migración `0026`: columna `resent_from_event_id` en `webhook_events` + función SECURITY DEFINER acotada `webhook_event_resend(event_id, tenant_id)` — `fluvia_app` tiene SELECT sobre `webhook_events`/`webhook_attempts` (privilegios por defecto de 0002) pero NO INSERT/UPDATE (revocados en 0019: la cola la escribe el relay); el reenvío entra por la función (ventana ADR-0011). `WebhookEventService` en `@fluvia/webhooks`: `list` (filtros endpoint/status), `get` (con `payload` + historial de intentos) y `resend` — **reenviar NO resucita el evento muerto** (clase append-only, estado terminal inmutable): clona (tenant, endpoint, topic, payload) como un evento `pending` FRESCO (attempts=0, entra por el ciclo normal del deliverer) enlazado vía `resent_from_event_id`, con un audit event `webhook_event.resent` (nueva acción del catálogo) escrito en la MISMA transacción. Endpoints `GET /v1/webhook_events` (scope `read`), `GET /v1/webhook_events/:id`, `POST /v1/webhook_events/:id/resend` (scope `webhooks:manage`); solo `dead` se reenvía (si no, `invalid_state_transition`). `WebhookEventNotFoundError`→not_found, `WebhookEventNotDeadError`→invalid_state_transition (sin códigos de catálogo nuevos, 36; +2 domain codes → 48; golden regen); OpenAPI +3 paths. Probado vs PG real (12 tests: 6 de servicio + 6 de ruta HTTP): lectura RLS por tenant, reenvío que clona+enlaza+audita atómicamente, aislamiento cross-tenant (404), guard de solo-`dead` (409), scope requerido. Suite total: **536/536**.
- **F2-09 + F2-10 completados (2026-07-04) — GATE IDEMPOTENCIA 🟢 TÉCNICO; FASE 2 COMPLETA**: `@fluvia/idempotency` + migración 0013 (`expires_at`, purga en F1-09). Diseño: el claim de la key se inserta EN LA MISMA transacción que el efecto y la respuesta se persiste `completed` antes del COMMIT — crash pre-COMMIT ⇒ ni key ni efecto (reintento limpio); post-COMMIT ⇒ replay exacto del status+body persistidos sin re-ejecutar el handler (ambos probados). Duplicado concurrente: espera acotada sobre el índice único (`lock_timeout`) — si el primero commitea a tiempo, replaya; si no, 409 `processing_in_flight` (probado con pg_sleep). Payload distinto (hash canónico sha256 con claves ordenadas) ⇒ 422 `idempotency_key_reuse` sin ejecutar. Carrera 8 concurrentes ⇒ exactamente 1 efecto; property: toda secuencia de reintentos ⇒ efectos==1. Sin Redis en el camino (ADR-0006). Códigos del contrato añadidos al catálogo v1 (aditivo, golden actualizado; categoría nueva `unprocessable_error` 422). HTTP end-to-end probado con el patrón exacto que cablearán los endpoints de pagos (F3-02). Cierra **AUD-P1-003** — 4 de los 5 P1 bloqueantes del auditor cerrados (queda solo P1-006/F1-04b). Suite total: **255/255**.
- **F1-08 completado (2026-07-04) — taxonomía de errores del API, AUD-P2-009 cerrado**: catálogo v1 cerrado en `apps/api/src/error-catalog.ts` (26 códigos en 8 categorías con familia HTTP coherente, verificada por meta-test) + **golden en git como contrato** (`error-catalog.v1.json`: cualquier cambio breaking rompe el contract test y exige bump de versión consciente). Sobre estable `{type, code, message, details?, request_id}` para TODA respuesta de error: dominio, validación Zod con details, JSON malformado, 413/415, 404 y 5xx opacos. Regla dura probada con fuga sembrada: el `message` interno de los errores de dominio va SOLO a logs — el cliente recibe el texto público del catálogo; y la forma default de Fastify (`{statusCode,…}`) es irrepresentable (test de control contra Fastify crudo). Reservado `rate_limited` (429) para F1-04b. Doc espejo: `docs/architecture/api-errors.md`. Desbloquea F2-09 por el DAG. Suite total: **241/241**.
- **F2-07 + F2-08 completados (2026-07-04) — GATE LEDGER 🟢 TÉCNICO**: (1) `reverseTransaction` (F2-07): asiento espejo completo por la vía normativa íntegra; **reversión única a nivel de motor** (índice único parcial 0012 — probado con 4 reversiones concurrentes de la misma tx: exactamente 1 gana); prohibido revertir reversiones; razón humana obligatoria + evento `ledger.transaction_reversed` (riesgo alto) en la MISMA transacción vía hook atómico `onPosted` (solo primera aplicación, jamás en replay); idempotente — el test cazó un bug real (el replay chocaba con el pre-check "ya revertida") corregido comparando la key de la reversión existente. Suma neta exacta probada con golden. (2) Suite formal de concurrencia (F2-08, `concurrency.test.ts`): PRNG determinista (mulberry32, seed 20260704, configurable por env) — 120 transferencias aleatorias concurrentes con conservación exacta y cero drift, presión de deadlock con pares opuestos A↔B, carrera masiva de idempotencia (10 keys × 5 posts ⇒ exactamente 10 aplicados), presión mixta postings+rebuilds+reversal concurrentes. **Baseline local: 120 postings en ~800 ms (~151 tx/s) con pool de 12 conexiones** — referencia, no SLO. Con F2-02…F2-08 verdes, el Gate Ledger queda técnicamente completo. Suite total: **233/233**.
- **F2-05 + F2-06 completados (2026-07-04) — el Gate Ledger avanza a falta de F2-07/F2-08**: (1) `rebuildProjection` (F2-05): reconstrucción race-safe bajo el MISMO lock de cuenta que el posting, avanza `version` (postings en vuelo reintentan), reporta `drifted` before/after; property test con 20 transferencias y rebuilds CONCURRENTES sin drift residual. (2) Detección programada: función `ledger_projection_drift()` (SECURITY DEFINER — única ventana del rol worker, denegada al rol app, mismo patrón que `authenticate_api_key`) + `ProjectionDriftWatcher` en `apps/worker` (DRIFT_CHECK_* en config; alerta = log error hasta F1-07). Cierra **AUD-P2-004**. (3) `scripts/verify-ledger-invariants.sql` (F2-06): auditoría AUTOCONTENIDA externa al ORM — 6 checks (doble partida por tx/moneda, montos>0, coherencia cuenta-tenant-moneda, cabeceras huérfanas, reversiones cross-tenant, drift) — nuevo paso de CI vía psql tras la suite; detecta corrupción sembrada como superusuario y vuelve a verde tras reparación explícita (probado). Cierra **AUD-P2-012**. Decisión de semántica documentada: drift silencioso = fila de proyección viva que miente; fila ausente = fallo visible (`getBalance` lanza) + `rebuildProjection` la materializa. Se corrigió de paso un test de F2-02 que dejaba una proyección incoherente como residuo. Suite total: **224/224**.
- **F2-12 completado (2026-07-04) — inbox durable, AUD-P1-005 cerrado**: migración `0010_inbox_provider_events.sql` + `@fluvia/inbox`. Ingesta (`InboxIngestService`, rol app con SOLO INSERT + columnas del árbitro): límite de tamaño, firma HMAC-SHA256 con timestamp FIRMADO y comparación en tiempo constante verificada ANTES de persistir (firma inválida = cero filas), headers allowlisted, dedup `UNIQUE (provider, provider_event_id)` race-safe (6 entregas concurrentes ⇒ exactamente 1 fila, probado). Procesador (`InboxProcessor`, rol `fluvia_inbox` mínimo, patrón ADR-0011): claim-lease SKIP LOCKED, registro por provider {schema Zod, handler}, veneno→dead + copia REDACTADA en la DLQ (probado que `card_token` llega como `[REDACTED]`), `ignored_out_of_order` como estado terminal para fuera-de-orden, backoff con jitter, provider sin handler→dead recuperable, y replay de plataforma auditado. 18 tests nuevos (2 procesadores concurrentes sin doble procesamiento incluido). Suite total: **215/215**.
- **F2-11 completado (2026-07-04) — el outbox cobra vida**: migración `0009_outbox_relay_role.sql` + `@fluvia/events` + `@fluvia/outbox` + ADR-0011. (1) **Envelope común** (AUD-P2-005): todo evento sale con `event_id/schema_version/occurred_at/producer/resource/data`, validado con Zod al emitir Y al despachar; el ledger ya publica `ledger.transaction.posted` con el sobre. (2) **Relay multi-worker**: claim atómico `FOR UPDATE SKIP LOCKED` donde el claim ES el lease (crash ⇒ la fila vuelve sola al expirar; sin estados nuevos ni reaper), publicación FUERA de la transacción (Nivel A), backoff exponencial con jitter, veneno→dead sin tocar el publisher, barrido de zombies, y **dead→pending solo vía replay de plataforma auditado con razón e ids reales**. (3) **AUD-P1-007 CERRADO**: rol `fluvia_relay` sin BYPASSRLS (políticas RLS explícitas solo sobre `outbox_events`, UPDATE por columna a los 6 campos de despacho), `fluvia_worker` reducido a cascarón sin privilegios ni default privileges, y meta-test permanente: NINGÚN rol de runtime con BYPASSRLS. Publisher actual = log estructurado (sandbox; la entrega real llega con F2-12/F3-07 — sin capacidades declaradas de más). 12 tests de integración del relay incl. 2 workers concurrentes sin doble entrega. Suite total: **195/195**.
- **F2-04 completado (2026-07-04)**: Chart of Accounts ejecutable — `CHART_OF_ACCOUNTS` con las 13 cuentas del diseño (semántica contable verificada por test), `PostingService.ensureChart` (aprovisionamiento idempotente por merchant+moneda: 8 platform-scope + 5 merchant-scope) y reglas de posting tipadas: `capturePayment` (modelo bruto sandbox v1, 5 asientos, margen Fluvia = Ff−Fp), `releaseSettlement`, `requestRefund`, `settleRefund`. Combinaciones fuera de catálogo irrepresentables. **Desviación registrada**: captura en 1 tx atómica (no 2 como el borrador de Fase 0) — atomicidad > trazabilidad-por-partes; doc actualizado como espejo del código; pricing final depende de PEND-002. 8 golden tests con montos exactos incl. ciclo completo captura→liquidación→refund con cero drift. Suite total: **162/162**.

## Resumen ejecutivo

Fluvia es una capa de software y orquestación de pagos (no banco, no custodio, no procesador certificado). La Fase 0 entregó el paquete completo de decisión (auditoría del prompt, PRD, arquitectura, ledger, FSMs, estrategias, threat model, ADRs, backlog+DAG, gates) validado con un spike contra PostgreSQL 16 real. La Fase 1 arrancó con la plataforma mínima: apps `api`/`worker` con configuración tipada que falla rápido, y pipeline de CI completo con Postgres real, validación de migraciones, secret scanning, audit de dependencias y SBOM.

## Evidencia del incremento F1-01/F1-02 (formato §47, 2026-07-04)

```
pnpm lint            → OK (ESLint 9 flat config; no-explicit-any=error)
pnpm format:check    → OK (Prettier)
pnpm build           → 5/5 paquetes typecheck OK
pnpm test            → 46/46 PASS
                       config  6/6  (defaults, coerción, anti-mezcla de credenciales)
                       db      9/9  (RLS, inmutabilidad, authenticate_api_key — PG16 real)
                       money  20/20
                       api     7/7  (health, ready contra BD real, ready 503 con BD caída,
                                     correlation-id con sanitización, sobre de error 404)
                       worker  4/4  (checkReady rol worker, heartbeat, stop limpio, start idempotente)
Smoke test manual    → servidor arrancado: /health OK, /ready OK (BD real),
                       404 con sobre estable, x-request-id eco verificado
```

## Estado por componente

| Componente                                                                                                                                                | Estado real                                                                               |
| --------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| Documentación Fase 0 (30 entregables §52)                                                                                                                 | Completado y aprobado                                                                     |
| `packages/money`, `packages/db` (spike)                                                                                                                   | Completado; promoción formal en F2-01/F1-03                                               |
| `packages/config`                                                                                                                                         | **Completado** (F1-01)                                                                    |
| `apps/api` esqueleto (health/ready, correlation, sobre de error)                                                                                          | **Completado** (F1-01) — sin recursos de negocio aún                                      |
| `apps/worker` esqueleto (readiness, heartbeat, shutdown)                                                                                                  | **Completado** (F1-01) — sin consumidores aún                                             |
| CI (`.github/workflows/ci.yml`)                                                                                                                           | **Completado** (F1-02) — pendiente de verse verde en GitHub Actions en el primer push     |
| `packages/identity` + migración 0003 (organizations/users/memberships/merchants)                                                                          | **Completado** (F1-03)                                                                    |
| `packages/auth` + migración 0004 + endpoints `/v1/auth/*`                                                                                                 | **Completado F1-04a** (registro, verificación, login, lockout, sesiones)                  |
| RBAC + API keys + endpoints organizations/merchants/api-keys + /v1/account (F1-04c)                                                                       | **Completado**                                                                            |
| `packages/audit` + migración 0006 + endpoint de auditoría (F1-05)                                                                                         | **Completado**                                                                            |
| Suite tenant-escape + meta-tests + bypass controlado (F1-06)                                                                                              | **Completado** — Gate Multi-tenant 🟢 técnico                                             |
| `@fluvia/ledger` (posting normativo + Chart of Accounts + reglas tipadas, F2-03/04)                                                                       | **Completado** — reforzado por lote AUD-1 (guardas de saldo, FK compuesta, huella causal) |
| Integración Auditoría v1 (plan + registro de cierre + lote AUD-1 + migración 0008)                                                                        | **Completado 2026-07-04**                                                                 |
| `@fluvia/events` (envelope común) + `@fluvia/outbox` (relay + replay auditado) + rol `fluvia_relay` (F2-11, ADR-0011)                                     | **Completado 2026-07-04**                                                                 |
| `@fluvia/inbox` (ingesta firmada + dedup + procesador + DLQ + replay) + rol `fluvia_inbox` (F2-12)                                                        | **Completado 2026-07-04**                                                                 |
| Rebuild/drift de proyecciones (F2-05) + invariantes SQL externas en CI (F2-06)                                                                            | **Completado 2026-07-04**                                                                 |
| Compensaciones vía servicio (F2-07) + suite formal de concurrencia (F2-08) — **Gate Ledger 🟢 técnico**                                                   | **Completado 2026-07-04**                                                                 |
| Taxonomía de errores del API con catálogo versionado + contract tests (F1-08)                                                                             | **Completado 2026-07-04**                                                                 |
| `@fluvia/idempotency` + crash-recovery (F2-09/10) — **Gate Idempotencia 🟢 técnico; Fase 2 completa**                                                     | **Completado 2026-07-04**                                                                 |
| MFA TOTP + step-up + rate limiting (F1-04b) — **AUD-P1-006 cerrado; 5/5 P1 del auditor**                                                                  | **Completado 2026-07-04**                                                                 |
| Observabilidad base (F1-07) — métricas Prometheus API+worker, health/metrics del worker, alertas baseline                                                 | **Completado 2026-07-05**                                                                 |
| Purga auditada de datos técnicos + guard de roles (F1-09) — **AUD-P2-008 cerrado; R-12 mitigado**                                                         | **Completado 2026-07-05**                                                                 |
| Seeds deterministas de demo (F1-10) — `pnpm seed`, solo local/test                                                                                        | **Completado 2026-07-05**                                                                 |
| API keys HMAC server-side (AUD-P2-015) — prerequisito de PEND-006                                                                                         | **Completado 2026-07-05**                                                                 |
| Núcleo de pagos F3-01 — schema real + FSM en motor + servicio de transiciones (`@fluvia/payments-core`)                                                   | **Completado 2026-07-05**                                                                 |
| API de payment intents F3-02 — endpoints idempotentes create/get/list/cancel + OpenAPI parcial                                                            | **Completado 2026-07-05**                                                                 |
| Confirm + MockProvider + attempts con captura contable (F3-03 parte síncrona)                                                                             | **Completado 2026-07-05**                                                                 |
| Flujo asíncrono PSE-like + ingesta HTTP firmada + primer handler del inbox (F3-03b)                                                                       | **Completado 2026-07-05**                                                                 |
| Robustez de attempts: barrido+salud de indeterminados, timeout, circuit breaker (F3-04)                                                                   | **Completado 2026-07-05**                                                                 |
| Webhooks salientes F3-07 — firma con rotación dual, SSRF guard con pinning, deliverer claim-lease, API de endpoints                                       | **Completado 2026-07-05**                                                                 |
| Refunds E2E F3-08 — asiento compensatorio por la vía normativa, dos fases idempotentes, desenlaces del dinero (aprobado/insuficiente/rechazo/desconocido) | **Completado 2026-07-05**                                                                 |
| Customers F3-05a — recurso con metadata validada, update parcial, baja lógica, aislamiento por tenant (fundación de checkout)                             | **Completado 2026-07-05**                                                                 |
| Checkout sessions F3-05b — recurso con FSM en el motor, create idempotente, client_secret hasheado, TTL acotado                                           | **Completado 2026-07-05**                                                                 |
| Backend del flujo alojado F3-05c-i — retrieval por client_secret + completado/expiración con eventos checkout_session.*                                   | **Completado 2026-07-05**                                                                 |
| Entrega garantizada de eventos de checkout F3-05c-ii — sweep_checkout_sessions() + watchdog                                                               | **Completado 2026-07-05**                                                                 |
| Confirm alojado (backend) F3-05c-iii — POST /confirm por client_secret, dos fases, idempotente                                                            | **Completado 2026-07-05**                                                                 |
| UI de checkout alojada F3-05c-iv — apps/checkout (Next.js 15, i18n, WCAG AA), E2E de navegador verificado local                                           | **Completado 2026-07-05**                                                                 |
| Payment links F3-06 (backend) — plantilla que genera checkout sessions bajo demanda, apertura pública, reutiliza F3-05                                    | **Completado 2026-07-05**                                                                 |
| UI del payment link F3-06-b — `/l/[id]` genera la sesión server-side y redirige a `/c/{sid}#{secret}`, E2E de navegador verificado local                    | **Completado 2026-07-05**                                                                 |
| Cola de webhooks + reenvío `dead` F3-09a — read API (`/v1/webhook_events`) + `resend` auditado (clona el muerto como pending fresco), cierra diferido de F3-07 | **Completado 2026-07-05**                                                                 |
| Dashboard frontend (F3-09b), SDK (F3-10), conciliación (F4)…                                                                                              | No construido                                                                             |

## Bloqueadores

1. PEND-002 (pricing) — bloquea solo el motor de fees (Fase 4).
2. F0-VER (verificación en vivo de licencias de referencias) — antes de adoptar código externo.
3. Runs #1–#10 de CI verificados verdes (uno por commit hasta el auditado); cada incremento nuevo debe registrar su run verde antes de declararse completado. `pnpm audit` puede reportar advisories nuevos en cualquier momento — tratarlos según Gate Seguridad, no silenciarlos.
4. Congelado hasta re-auditoría + PEND-006: **exposición pública** de la API de pagos, checkout y webhooks (construidos en sandbox por decisión #24, jamás expuestos), y todo uso `live` (creación de keys live bloqueada por código).

## Próximo incremento propuesto

**Los 5 hallazgos P1 de la auditoría están cerrados con evidencia — el criterio de re-auditoría se cumplió por completo**: es el momento de que el propietario la solicite (el paquete de entrada es `docs/audits/audit-closure-register-v1.md` + `audit-integration-plan-v1.md`). F1-07 (observabilidad base) quedó completado el 2026-07-05: la pata de observabilidad de AUD-P2-007 está cubierta (restore/runbooks siguen en F4-06/F6) y el drift watcher/relay ya alimentan métricas con alertas baseline definidas (`observability.md` §4). F3-08 quedó completado: el ciclo de dinero es reversible extremo a extremo (refunds con asiento compensatorio, desenlaces conocido/desconocido probados) y **AUD-P1-002 queda cerrado por completo** — el núcleo de pagos real (intents + attempts + confirm + refunds) existe y está probado. Con esto los incrementos de dinero de la Fase 3 (F3-01/02/03/04/07/08) están cerrados. F3-05c-iv quedó completado: **F3-05 (checkout session app) está COMPLETO** — customers, recurso de checkout sessions, flujo alojado (status/confirm por client_secret), entrega garantizada de eventos y la UI Next.js alojada, verificada en navegador real. Es también el **primer app frontend del monorepo** y establece el patrón (Next.js 15 App Router, route handlers como proxy, i18n propio, WCAG AA, tests jsdom+axe en CI + E2E de navegador local). F3-06 quedó completado (backend): los payment links generan checkout sessions pagables bajo demanda, reutilizando TODO F3-05; probado end-to-end (abrir link → pagar → completado). F3-06-b quedó completado: la página pública `/l/[id]` del app de checkout genera la sesión server-side y **redirige** el navegador a `/c/{sessionId}#{clientSecret}` (el `client_secret` en el fragmento del `Location`, fuera de los logs) — **el payment link cierra end-to-end para el comprador**, verificado en navegador real (`/l/{id}` → `$ 77.000` → Pagar → completado). Con esto la superficie de checkout+links de la Fase 3 está completa de cara al comprador. F3-09a quedó completado: primera rebanada del dashboard como capacidad backend NUEVA — el read API de la cola de webhooks (`/v1/webhook_events`) + el **reenvío manual auditado de eventos `dead`** (clona el muerto como un evento `pending` fresco, con audit `webhook_event.resent` atómico), que cierra el diferido de F3-07. Próximo incremento propuesto: **F3-09b — frontend del dashboard** (nuevo app `apps/dashboard`, o rutas en un app admin): plano de operación del comercio autenticado por API key/sesión que lista intents/refunds/checkout sessions/payment links + la cola de webhooks, con la acción de **reenvío** de eventos `dead` sobre el API de F3-09a — establece el patrón de superficie de operación como F3-05 estableció el de checkout. ALTERNATIVA: **F3-10 SDK TS generado del OpenAPI** (cliente tipado del contrato ya versionado, backend puro sin nuevo frontend). NOTA: superficie pública congelada hasta re-auditoría + PEND-006; se construye en sandbox. Decisiones humanas abiertas: PEND-002, PEND-004, PEND-006.
