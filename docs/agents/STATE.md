# STATE — Estado del proyecto

Última actualización: 2026-07-11 · **🔎 F5.0 (investigación legal/compliance) EN EJECUCIÓN DOCUMENTAL** (decisión #30): paquete `docs/compliance/f5-0-legal-research.md` + `legal-questions-checklist.md` con fuentes oficiales (GNU/CC, Función Pública, SFC, UIAF, DIAN; consulta 2026-07-11) y estado epistémico por afirmación; matriz jurisdiccional con columna «Verificación» — **ninguna fila cerrada legalmente, sin conclusiones legales**. Hallazgo bisagra (pregunta-para-abogado): pasarelas de pago NO vigiladas por la SFC per se; el régimen de adquirente no vigilado (Decreto 1692/2020) exige segregar fondos si se recauda — la clasificación no-custodia de Fluvia es pregunta para abogado. **F5.1–F5.4 NO autorizadas; F5.0 no cambia runtime ni gates; no hay proveedor real; producción/live/sandbox compartido bloqueados; freeze #24 vigente.** · **📋 PLAN DE PREPARACIÓN DE FASE 5 VERSIONADO** (opción B, decisión #29): `docs/phases/f5-preparation-plan.md` registra el plan estratégico/documental — **Fase 5 NO iniciada**; las etapas F5.0→F5.4 requieren autorización humana separada por etapa (primera inversión recomendada: solo F5.0, verificación legal/matriz, sin código); freeze #24 / live / producción / sandbox compartido siguen bloqueados. · **✅ F6 APROBADA para el alcance `sandbox cerrado / hardening sandbox`** (decisión #28) — ratificada por **Hermes** (delta final + ratificación corta de F6-DELTA-001) sobre el **baseline aprobado `claude/new-session-haeo7h` @ `8f126c4049653b11e6a46903c68d9138e8c2a4ab`** (merge del PR #30, CI run #368 VERDE); **0 P0/P1/P2 del delta F6**; RA-F6-001…005 + F6-DELTA-001 CERRADOS. **La aprobación NO autoriza producción, Fase 5, `live`, proveedor real, dinero real, exposición pública, sandbox compartido, release público/comercial, ni levantar el freeze #24. La siguiente etapa (Fase 5) es una DECISIÓN HUMANA separada del propietario** — no iniciar F5 sin autorización explícita; MockProvider sigue siendo el único proveedor; producción sigue no autorizada; revisión legal LGPLv3 antes del primer release. · **DELTA AUDIT FINAL de Hermes ejecutado sobre `claude/new-session-haeo7h` @ `69a69ed99e860e799dd69858a367e9ef1340464b`** (merge commit del PR #29, CI run #365 VERDE): ratificó RA-F6-002/003/005; RA-F6-001 sigue cerrado por el delta parcial previo; **dejó RA-F6-004 PARCIALMENTE ABIERTO por un P2 nuevo — F6-DELTA-001** (el reporte `licenses:report` contradecía las decisiones humanas de `license-exceptions.json` marcando las 2 restringidas aceptadas como «sin decisión registrada»). **F6-DELTA-001 CORREGIDO** en `fix/f6-delta-001-license-report-decisions`: `check-licenses.mjs` unifica la fuente única de excepciones (`accepted` Map) usada por `check` y `report`, el reporte imprime el bloque completo de cada decisión (aprobado por Celestino Briceño, obligaciones, reviewBy, «no autoriza producción», nota LGPLv3), con guard anti-regresión interno de dientes probados; `licenses:check --strict` sigue en PASS, sin cambios de deps/lockfiles/código. **F6 sigue SIN aprobarse hasta la ratificación corta de Hermes sobre F6-DELTA-001.** · **RA-F6-001 CERRADO** (delta audit aprobó — alcance limitado a ese hallazgo —, PR #23 mergeado, CI run #327 VERDE) · **RA-F6-002 CERRADO** (override mínimo `postcss@8.4.31 → 8.5.16`, PR `fix/ra-f6-002-postcss-cve`; audit limpio; pendiente ratificación del delta final) · **RA-F6-004 CERRADO** (reporte+política+check `--strict` en CI; las 2 licencias restringidas — `caniuse-lite` CC-BY-4.0 y `@img/sharp-libvips-linux-x64` LGPL-3.0-or-later — fueron **aceptadas como excepciones controladas por el propietario el 2026-07-10**, con obligaciones registradas y **revisión legal de la LGPL obligatoria antes del primer release público/comercial**; la aceptación NO autoriza producción) · **RA-F6-005 CERRADO** (meta-test `chart-nonneg-sync.test.ts` con dientes probados; cobertura chart↔[9] confirmada exacta) · **RA-F6-003 CERRADO** (reconciliación docs-only de `production-gates.md`: estadios sandbox cerrado/compartido/producción separados, gates al estado real, checks [1]–[9]; PR `docs/ra-f6-003-gates-reconciliation`) — **los 5 RA-F6 cerrados a nivel interno; falta la RATIFICACIÓN del delta audit final** (paquete de entrada en el registro §Paquete delta final; baseline pre-003: `8ae36461…`, el definitivo será el merge commit del PR de 003) (detalle y plan en registro §Re-auditoría F6 delta) · **F6 NO se declara aprobado** hasta cerrar o aceptar formalmente los abiertos · **STACK F4+F6 MERGEADO** — los PRs #2–#10 se mergearon EN ORDEN a `claude/new-session-haeo7h` (decisión #26; merge commits, CI verde tras cada uno, runs #281/#304/#305/#306/#309/#314/#317/#318/#319); **baseline de re-auditoría: commit `d60e45956bef4c1cc696ff4e3cca31c8bc8346d1`** (run #319 VERDE) · **Fases 1, 2, 3 y 4 COMPLETAS — Gate Conciliación cumplido** (reconciliación + panel admin + reserves/fees/settlement/payout/disputas + observabilidad/watchdogs + los 7 runbooks operativos **probados en drill**) · **LISTO PARA RE-AUDITORÍA** (paquete: `docs/audits/audit-closure-register-v1.md` + `audit-integration-plan-v1.md` + este STATE) · API pública y todo uso `live` **siguen congelados** (decisión #24) hasta re-auditoría + PEND-006 · **Auditoría v1: 0 P1 abiertos, 0 P2 abiertos** — AUD-P2-007 **RESUELTO** en sus tres patas (el _restore drill_ F6 · Gate Restore quedó ejecutado **PASS 7/7**) · **re-auditoría v2 remediada** (0 P0/P1 nuevos; `re-audit-v2-delta.md` + §Re-auditoría v2 del registro) (`docs/audits/`)

## Hitos

- **Fase 0 APROBADA por el propietario (2026-07-04)** — decisión #16. ADRs 0001–0010 aceptados.
- **País inicial: Colombia** (decisión #15, ex PEND-001). Implicaciones MVP: COP como moneda principal de sandbox; el MockProvider incluirá un método asíncrono tipo PSE; candidatos de proveedor para Fase 5: Wompi/PayU/dLocal/Mercado Pago. La matriz de jurisdicción queda por verificar con fuentes y revisión legal (bloquea solo Fase 5).
- **F1-01 y F1-02 completados** (apps + config + CI; primer run de CI verde en GitHub Actions).
- **F1-03 completado (2026-07-04)**: modelo de identidad/tenancy real — migración `0003_identity_tenancy.sql` (`tenants`→`organizations`, `users` global con política RLS por membresía compartida, `memberships` RBAC, `merchants` con defaults Colombia) + paquete `@fluvia/identity` (plano plataforma y plano tenant) con 15 tests de integración: aislamiento cross-tenant de merchants/orgs/users, rollback atómico de alta de organización, unicidad case-insensitive de email, escritura de `users` denegada al rol app, inmutabilidad de las tablas nuevas y `authenticate_api_key` verificada tras el rename. Suite total: 61/61.
- **F1-04a completado (2026-07-04)**: primera sub-entrega de auth — migración `0004_auth_sessions.sql` (sessions con hash SHA-256, tokens de verificación de un solo uso, lockout en users, rol dedicado `fluvia_auth` con acceso exclusivo al plano de credenciales y REVOKE a app/worker) + `@fluvia/auth` (scrypt versionado, login uniforme anti-enumeración con igualación de timing, lockout configurable, sesiones revocables, `auth_list_memberships`) + endpoints `/v1/auth/*` con mapeo estable de errores y ZodError→400. Bug real detectado y corregido por los tests: el contador de lockout se perdía en el ROLLBACK del propio error — ahora se commitea antes de lanzar. Suite total: **92/92** + smoke test HTTP del flujo completo (register→verify→login→session→bad-login→logout). Pendiente F1-04b (MFA TOTP + step-up).
- **F1-04c completado (2026-07-04)**: API keys con scopes y entorno test/live (migración 0005; secreto mostrado una única vez, solo hash+prefijo en BD, last_used_at con throttle) + RBAC declarativo (`packages/identity/src/rbac.ts`, matriz publicada en access-control.md y verificada celda a celda por test) + middleware de dos planos en el API (sesión+rol por organización para dashboard; API key+scope para integración; planos NO intercambiables) + primeros endpoints tenant-scoped reales: `/v1/organizations[...]` (org, members, merchants CRUD, api-keys) y `/v1/account`. Decisión de seguridad: no existe scope para gestionar API keys — una key robada no puede escalar. Suite total: **111/111** incl. tests BOLA (404 indistinguible), RBAC por rol y scopes.
- **F1-05 completado (2026-07-04)**: audit log append-only — migración `0006_audit_log.sql` (tabla inmutable por triggers Y por grants, RLS por plano: tenant para app, INSERT-only sin tenant para fluvia_auth) + `@fluvia/audit` (evento en la MISMA transacción que la acción, redacción recursiva de claves sensibles, lector paginado). Acciones auditadas: api_key.created/revoked (risk high), merchant.created/updated (con before/after), user.registered/email_verified, auth.login_succeeded/login_failed/account_locked/logout/sessions_revoked. Permiso nuevo `audit:read` (owner/admin/finance/analyst) + endpoint `GET /v1/organizations/:orgId/audit-events` con cursor. Suite total: **120/120**.
- **F1-06 completado (2026-07-04)** → **Gate Multi-tenant 🟢 técnico**: suite ampliada de tenant-escape (UPDATE por PK ajena, UPDATE masivo, INSERT…SELECT, JOINs, sondas EXISTS, agregados, fuga de contexto en la MISMA conexión del pool, SET ROLE denegado, worker sin DELETE) + **meta-tests estructurales** contra pg_catalog (toda tabla futura con tenant_id debe tener RLS forzado y política; ningún rol de runtime con DELETE; app/worker sin privilegios sobre credenciales) + `withPlatformOperation` como único bypass sancionado (razón obligatoria, auditoría atómica de riesgo alto, rollback conjunto). Límite residual documentado (SQL arbitrario ⇒ cambio de GUC) en multi-tenancy.md §6.5 y threat model. Suite total: **135/135**.
- **F2-01+F2-02 completados (2026-07-04) — arranca el núcleo financiero**: migración `0007_ledger_core.sql` — enlace causal (`source_type/source_id`) y de reversión (`reverses_tx_id` con FK) en `ledger_transactions`; `balance_projections` separada con RLS+inmutabilidad (y `ledger_accounts` contraída a pura definición); **invariantes a nivel de motor**: constraint triggers diferidos que hacen imposible commitear un asiento desbalanceado por (tx, moneda) — probado con SQL crudo de superusuario — ni compensar un hueco USD con COP, ni dejar cabeceras sin asientos. Suite total: **144/144**.
- **F2-03 completado (2026-07-04)**: `@fluvia/ledger` — `LedgerService.postTransaction` implementa el posting normativo íntegro: validación previa de balanceo por moneda (feedback rápido; la BD re-verifica al COMMIT), idempotencia del asiento con **replay verificado por huella canónica** (mismo key + payload distinto ⇒ `IdempotencyConflictError`), locking pesimista de cuentas en orden total anti-deadlock, proyecciones versionadas con guard optimista y retry limitado (deadlock/serialization incluidos), y evento `ledger.transaction.posted` al outbox EN la misma transacción. `createAccount` crea cuenta+proyección atómicamente; `verifyProjection` es la primitiva del audit-replay. 10 tests de integración: captura con split de fees, bucket pending, carrera de 5 posts concurrentes con el mismo key (exactamente 1 aplicado), 25 transferencias concurrentes con orden aleatorio de cuentas sin drift (proyección == recomputado). Suite total: **154/154**.
- **Auditoría Independiente v1 integrada + lote AUD-1 completado (2026-07-04)**: PR documental #1 verificado (solo 4 archivos bajo `docs/audits/independent-audit-v1/`, inmutables) y mergeado sin tocar los hallazgos; HEAD al integrar == commit auditado `a665b4f` (cero drift). Reconciliación evidencia-por-hallazgo en `docs/audits/audit-integration-plan-v1.md` (0 P0 · 10 P1 · 16 P2 · 3 P3) y estado vivo en `audit-closure-register-v1.md`. **Lote AUD-1 (remediación inmediata)**: migración `0008_audit_remediations.sql` — FK compuesta `(account_id, tenant_id, currency)` en `ledger_entries` (AUD-P1-001, con validación previa de datos), `idempotency_keys` re-clavada a `(tenant_id, endpoint, key)` (AUD-P1-009), REVOKE total al worker sobre ledger/proyecciones/api_keys (adelanto AUD-P1-007, mitiga AUD-P2-004) + guard race-safe `nonNegativeAccounts` con `InsufficientBalanceError` aplicado a release/refund (AUD-P1-010) + huella idempotente causal completa en replay (AUD-P2-001) + bloqueo de API keys `live` (`LiveKeysDisabledError`, AUD-P2-003, decisión #19) + anti-mezcla de entornos en `dbUrlsFromEnv` (AUD-P2-014) + README real (AUD-P3-001), bloques "Estado de implementación" en 6 docs de diseño (AUD-P3-002) y reservas-como-cuentas (AUD-P2-013, decisión #18). Veredicto del auditor adoptado: continuar; pagos públicos/checkout/proveedor/webhooks externos/live congelados hasta cerrar bloqueantes. Nuevas decisiones humanas abiertas: PEND-004/005/006.
- **F1-04b completado (2026-07-04) — MFA + STEP-UP + RATE LIMITING; LOS 5 P1 DEL AUDITOR CERRADOS**: PEND-005 resuelta (decisión #23: TOTP + backup codes, sin SMS). Migración `0014` (secreto TOTP cifrado en users, `mfa_challenges`, `mfa_backup_codes` — plano fluvia_auth con FORCE RLS, meta-test ampliado a 5 tablas) + TOTP RFC 6238 propio (SHA-1/6 dígitos/30 s, verificado contra los vectores del Apéndice B del RFC) + cifrado AES-256-GCM del secreto en reposo (clave `MFA_SECRET_KEY` con anti-mezcla en config). Flujo: password válido con MFA ⇒ reto de 5 min (fluvia_mfa_, un solo éxito) ⇒ TOTP o backup code ⇒ sesión con `mfa_verified_at`. Garantías probadas: **anti-replay por step** (un código jamás vale dos veces), backup codes de un solo uso que decrementan el contador, fallos MFA alimentan el MISMO lockout que el password (hasta bloquear, probado), desactivar exige TOTP (un backup code NO basta). **Step-up**: `keys:manage` exige verificación MFA ≤15 min cuando el usuario tiene MFA (403 `mfa_step_up_required` sobre HTTP, refresco vía `/v1/auth/mfa/step-up`; usuarios sin MFA no se bloquean hasta PEND-006). **Rate limiting** por email/IP en `/v1/auth/*` (429 `rate_limited` + Retry-After; ventana fija in-memory — Nivel C documentado: store compartido antes del sandbox compartido). Catálogo v1 +5 códigos (golden regenerado). 23 tests nuevos. Suite total: **278/278**.
- **F1-07 completado (2026-07-05) — observabilidad base**: paquete `@fluvia/observability` propio (counter/gauge/histogram + exposición Prometheus 0.0.4, guard de cardinalidad de 500 series con contador de descartes, tests de formato/escape). API instrumentada: `fluvia_http_requests_total{method,route,status}` + histograma de duración — la ruta es SIEMPRE la plantilla registrada y los paths sin match colapsan en `unmatched` (anti-cardinalidad probado); `GET /metrics` sin datos de tenant (test verifica ausencia de UUIDs). Worker: servidor mínimo `/health`+`/metrics` en `WORKER_METRICS_PORT` (default 9464), métricas de relay (delivered/retried/dead por ciclo vía `onStats`), drift (gauge de cuentas en drift + contador de chequeos vía `onCheck`) y heartbeats. Regla probada en los tres hooks: un observador de métricas que lanza JAMÁS rompe al proceso observado. Alertas baseline en `observability.md` §4 (drift>0 crítica, dead>0, worker sin latido, 5xx, p99, 429s). **Desviación registrada**: OTel diferido hasta collector real (F6) — declarar trazas sin backend sería capacidad simulada; correlación hoy = `request_id` de extremo a extremo (header→logs→sobre de error→audit_log). AUD-P2-007 pasa a PARCIAL (pata observabilidad; restore/runbooks siguen en F4-06/F6). Suite total: **298/298**.
- **F1-09 completado (2026-07-05) — purga por clasificación de datos + guard de roles (cierra AUD-P2-008)**: migración `0015` — la política ENTERA vive en `purge_technical_data()` (SECURITY DEFINER SIN parámetros: nadie puede ensanchar el alcance): 4 clases técnicas con retenciones fijas (idempotency_keys al vencer; sesiones/tokens de verificación +7 días; retos MFA +1 día), auditoría atómica `platform.technical_purge` con conteos por clase EN la misma transacción (solo purgas efectivas — un no-op no genera ruido append-only), EXECUTE solo del worker. El trigger de las tablas técnicas cambia a `fluvia_forbid_mutation_technical()`: mismo bloqueo (incluso superusuario, probado) salvo dentro de la transacción del job (GUC local); las clases financieras conservan el trigger ORIGINAL — probado que el escape NO existe para ledger/organizations/audit_events ni con el GUC activo. **AUD-P2-008 RESUELTO**: `fluvia_assert_dev_role_creation()` antes de cada `CREATE ROLE ... PASSWORD` (meta-test: 5 altas / 5 con guard) + el runner inyecta `fluvia.environment` — probado end-to-end en BD efímera: en 'production' la migración ABORTA sin registro parcial; en 'test' aplica. `TechnicalPurgeJob` en el worker (PURGE_* en config, métricas `fluvia_technical_purge_*`). R-12 pasa a Mitigado. Suite total: **310/310**.
- **F1-10 completado (2026-07-05) — seeds deterministas; FASE 1 COMPLETA**: `@fluvia/seeds` + `pnpm seed`. IDs UUID v5 sobre claves fijas (misma corrida = mismos ids), identidad vía ON CONFLICT DO NOTHING y ledger por la VÍA NORMATIVA (`PostingService.ensureChart` + capture/release con idempotency keys fijos: re-ejecutar = replay exacto, CERO filas nuevas — probado contando orgs/users/memberships/merchants/tx/asientos antes y después). Historia de demo: 500.000 COP capturados (fee proveedor 14.500, fee plataforma 19.500), 300.000 liberados → pending 180.500 / available 300.000 (verificado). Guard duro: `SeedEnvironmentError` fuera de local/test ANTES de tocar la BD (probado con pools rotos) — los passwords de demo jamás llegan a sandbox/staging/production; abrir sandbox se decidirá con PEND-006. Passwords de demo verificados autenticables con `verifyPassword`. Suite total: **315/315**.
- **AUD-P2-015 completado + diseño de F3-01 listo (2026-07-05)**: (1) migración `0016` — API keys con **HMAC-SHA256 server-side**: `key_hash_version` (1=sha256 legado, 2=HMAC con pepper `API_KEY_HMAC_SECRET`, 64 hex, anti-mezcla en config), claves nuevas nacen v2, `authenticate_api_key(hmac, legacy)` autentica ambas vías y **promueve v1→v2 en la misma llamada** (upgrade perezoso probado: fila legada simulada → autentica → queda v2 con HMAC; la vía sha256 deja de matchear). Un dump de `api_keys` ya no permite validación offline de claves — el pepper jamás se persiste. Prerequisito técnico de PEND-006 cumplido. (2) `docs/design/f3-01-payment-intents.md`: diseño aprobado para ejecutar F3-01 el día del veredicto — schema 0017 (12 estados, FK compuestas, attempts), FSM hecha cumplir EN el motor (tabla de transiciones + trigger), una sola fuente de verdad (mapa TS → seed generado, patrón golden) y meta-test triple doc↔TS↔DDL + matriz de ilegales como superusuario (cerrará AUD-P2-011). Suite total: **316/316**.
- **F3-01 completado (2026-07-05) — ARRANCA FASE 3 por decisión #24 (propietario adelanta F3-01; re-auditoría se solicitará más adelante; endpoints públicos SIGUEN congelados)**: migración `0017` ejecutando el diseño aprobado — guard de tabla vacía, 12 estados, FK compuesta intent↔merchant↔tenant (un intent no puede referenciar el merchant de otro tenant ni como superusuario, probado), CHECKs `captured ≤ amount` / `refunded ≤ captured` (probados), `payment_attempts` con FSM propia. **FSM hecha cumplir EN el motor**: tablas de transiciones inmutables (seed GENERADO desde el mapa TS por `gen-fsm-seed.ts` — una sola fuente de verdad) + triggers `FLUVIA_INVALID_TRANSITION`. `@fluvia/payments-core`: mapas declarativos intent/attempt/refund, `PaymentIntentService` (RLS, FOR UPDATE, version++, timestamps terminales, `failure_code` de catálogo, evento `payment_intent.<estado>` al outbox EN la misma tx). **Meta-test triple (cierra AUD-P2-011)**: doc(mermaid §1/§2/§3) ↔ TS ↔ DDL con igualdad exacta, y la matriz COMPLETA de motor (144 pares intent + 64 attempt) probada con SQL de superusuario. Cierra también **AUD-P2-002**. Mermaids de attempt/refund añadidos al doc (antes solo prosa). Suite total: **332/332**.
- **F3-02 completado (2026-07-05) — primeros endpoints de DINERO, idempotentes de nacimiento**: `POST /v1/payment_intents`, `GET` (retrieve/list), `POST .../cancel` en el plano de integración (API key + scopes `payments:write`/`read`; los planos sesión↔API key siguen sin cruzarse). TODA mutación exige `Idempotency-Key` (capa F2-09: claim+efecto+respuesta en UNA transacción — el servicio de intents expone variantes client-bound para componer): replay exacto probado sobre HTTP, 422 `idempotency_key_reuse` con payload distinto, y un efecto fallido (merchant ajeno → 404 indistinguible) revierte el claim SIN quemar la key (probado). FSM sobre HTTP: cancelar un intent cancelado con key nueva = 409 `invalid_state_transition` (código nuevo; catálogo v1 → **35 códigos**, golden regenerado). Contrato `docs/api/openapi.v1.json` (parcial, versionado) + test spec↔servidor (cada path declarado existe Y exige auth; control anti-catch-all). **Desviaciones registradas**: `confirm` diferido a F3-03 (confirmar sin proveedor = pago irresoluble = capacidad simulada) y `customers` a F3-05 (su primer consumidor). AUD-P1-002 pasa a PARCIAL. Suite total: **348/348**.
- **F3-03 parte síncrona completada (2026-07-05) — EL CICLO DE PAGO SANDBOX ESTÁ VIVO**: `MockPaymentProvider` (tokenización simulada tipo tarjetas de prueba: `tok_approve`/`tok_decline`/`tok_decline_insufficient`/`tok_timeout`; referencias deterministas; contract tests) + `PaymentConfirmationService` en dos fases (V4 Nivel A): fase 1 DENTRO de la tx de la idempotency key (intent→processing por la FSM + attempt en submitting — el replay devuelve exactamente lo mismo y no re-ejecuta), proveedor FUERA de toda tx, y registro atómico del resultado: aprobado ⇒ `capturePayment` + attempt succeeded + intent succeeded + `amount_captured`, TODO en una transacción vía `onPosted` (probado: merchant.pending crece exacto; el asiento referencia causalmente al attempt; re-ejecutar tras crash = no-op, 1 solo asiento); rechazado ⇒ failed con `failure_code` estable y CERO asientos; **timeout ⇒ attempt `indeterminate`** y el intent queda processing — nada lo resuelve por asunción (V4 §23, probado). `POST /v1/payment_intents/:id/confirm` con contrato ASÍNCRONO (responde processing+attempt_id; GET muestra el final) — spec OpenAPI actualizado y cubierto por el test de contrato. Fees en 0: inventar pricing sería simular un modelo comercial inexistente (PEND-002). Suite total: **363/363**.
- **F3-03b completado (2026-07-05) — F3-03 CERRADO; el inbox durable tiene su primer handler real**: `tok_pse` (método asíncrono PSE-like, Colombia) ⇒ attempt `submitted` con referencia del proveedor. **Endpoint HTTP de ingesta** `/v1/providers/mock/webhook` (cierra el pendiente de F2-12): sin API key — la credencial ES la firma HMAC sobre los bytes crudos (parser raw scoped), tolerancia ±5 min, dedup race-safe por (provider, event_id); firma inválida ⇒ 401 del catálogo y CERO persistencia (probado). **Primer handler real del inbox**: schema Zod estricto (lo no conforme → DLQ) → `resolveFromProvider` — la ÚNICA vía legítima para cerrar attempts `submitted` o `indeterminate` (V4 §23) — con la MISMA captura contable atómica de la vía síncrona (misma idempotency key del ledger ⇒ doble procesamiento imposible); webhooks tardíos/contradictorios ⇒ `ignored_out_of_order` sin re-abrir nada (probado end-to-end sobre HTTP con el MISMO cableado del worker). `InboxProcessor` en el worker (rol mínimo `fluvia_inbox` para el claim; el handler actúa como la aplicación con RLS; INBOX_* en config; métricas `fluvia_inbox_*` + alerta dead). Config: `INBOX_DATABASE_URL` y `MOCK_WEBHOOK_SECRET` entran a la cadena anti-mezcla. Suite total: **372/372**.
- **F3-04 completado (2026-07-05) — robustez del plano de attempts**: (1) migración `0018` — `sweep_payment_attempts()` (definer SIN parámetros, familia purga/drift): barrido `submitting→indeterminate` con lease de 5 min — cierra la ventana de crash entre fases de F3-03; un attempt barrido queda con desenlace DESCONOCIDO y solo lo cierra la fuente verificada (probado: tras el barrido, el webhook lo resuelve normal); el fresco no se toca (probado); auditoría atómica `payment_attempt.swept_indeterminate` con los ids; salud de indeterminados (total + envejecidos >30 min). (2) `AttemptsWatchdog` en el worker (ATTEMPTS_WATCHDOG_* en config; gauges `fluvia_payment_attempts_*` + alerta baseline: `indeterminate_aged > 0` = dinero en desenlace desconocido, intervenir). (3) `ResilientProvider` — decorador sobre CUALQUIER adapter: timeout duro (proveedor colgado ⇒ ProviderTimeoutError ⇒ indeterminate) + circuit breaker (umbral de fallos consecutivos, cooldown, half-open con sonda única, probado con reloj inyectado; los declines NO cuentan como fallos). Distinción clave probada: **circuito abierto = la petición JAMÁS se envió = fallo LIMPIO** `provider_unavailable` sin asiento ni ambigüedad — solo el desenlace desconocido va a indeterminate. Breaker in-memory Nivel C (como el rate limiter; store compartido con PEND-006). Suite total: **385/385**.
- **F3-07 completado (2026-07-05) — los eventos que emitimos llegan a los comercios**: migración `0019` — `webhook_endpoints`/`webhook_events`/`webhook_attempts` (append-only, RLS forzado) + rol `fluvia_webhook` con ventanas ADR-0011 (el fan-out lo hace `fluvia_relay`; la entrega, un rol propio con UPDATE por columna; lección F2-12 reaplicada: ON CONFLICT exige SELECT sobre las columnas del árbitro). `@fluvia/webhooks`: catálogo de **13 topics** con meta-test doc §5↔código; firma `Fluvia-Signature: v1=` HMAC-SHA256 sobre `{ts}.{event_id}.{body}` con **rotación dual** (el secreto anterior firma durante 24 h — probado que AMBAS firmas verifican); secretos `whsec_` cifrados **AES-256-GCM en reposo** (un dump no permite forjar webhooks); **SSRF guard §4 probado**: https+443 fuera de local, denylist de TODAS las IPs resueltas (privadas/loopback/link-local/CGNAT/multicast/v4-mapped), conexión PINEADA a la IP validada en CADA intento (anti DNS-rebinding), sin redirects, IP registrada por attempt; deliverer claim-lease (mismo patrón del relay) con calendario 0s/30s/2m/10m/1h/6h/24h→dead y endpoint deshabilitado ⇒ cola muere SIN tráfico de red (probado). API `/v1/webhook_endpoints` (create/list/rotate/disable, scope `webhooks:manage`): el secreto se entrega UNA sola vez y el listado jamás lo expone (probado); cross-tenant = 404 indistinguible. Métrica `fluvia_webhook_deliveries_total{result}` + alerta dead. Probado end-to-end contra PG real y un receptor HTTP real. Desviaciones: reenvío manual auditado → F3-09; auto-disable con notificación → F6. Suite total: **409/409**.
- **F3-08 completado (2026-07-05) — refunds end-to-end; el ciclo de dinero es reversible**: migración `0020` — tabla `refunds` (FK compuesta intent↔tenant, append-only, RLS forzado) + FSM `refund_transitions` hecha cumplir EN el motor con el patrón golden de 0017 (seed generado desde el mapa TS; meta-test triple doc §3↔TS↔DDL + matriz COMPLETA de 36 pares como superusuario). `RefundService` en dos fases (mismo esqueleto que confirm, V4 Nivel A): fase 1 DENTRO de la tx de la idempotency key — bajo `FOR UPDATE` del intent valida estado (succeeded|partially_refunded) y monto contra lo REMANENTE (capturado − aplicado − refunds en vuelo), inserta el refund en `created`; el lock del intent serializa refunds concurrentes del mismo intent (Σ refunds ≤ capturado race-safe). Fase 2 FUERA de toda tx: (2a) `refund.request` reserva merchant.available→refund.liability con guard de no-negatividad EN el motor (AUD-P1-010) — sin disponible el refund se CANCELA limpio SIN contactar al proveedor (desenlace conocido); (2b) proveedor → aprobado: `refund.settle` (liability→provider.clearing) + refund succeeded + `amount_refunded += monto` + intent partially_refunded/refunded, TODO en una tx vía `onPosted`; rechazado/circuito abierto: `refund.cancel` (reserva devuelta ÍNTEGRA al comercio) + refund failed; **throw o `pending` (aceptación asíncrona) = desenlace DESCONOCIDO: el refund pasa a `indeterminate` con la reserva RETENIDA — nada lo resuelve por asunción NI por re-envío (V4 §23); SOLO `resolveFromProvider` (fuente verificada, espeja attempts) lo cierra en succeeded/failed**. Idempotencia por keys `refund:<id>:request|settle|cancel` (re-ejecutar = replay, cero doble asiento; probado). Endpoints `POST/GET /v1/refunds` idempotentes (scope payments:write/read), catálogo +`refund_amount_exceeds_remaining` (**36 códigos**, golden regen), topic `refund.canceled` añadido al catálogo de webhooks (los 5 `refund.*` llegan al comercio por F3-07). Probado contra PG real + ledger: todos los desenlaces del dinero con balances exactos y cero drift, incluida la resolución verificada de un `indeterminate`. **Desviación registrada** (espeja F3-03→F3-04): la ingesta de webhooks de refund del proveedor y un watchdog de refunds `indeterminate` envejecidos quedan para F4 (conciliación); hoy `execute` corre una vez desde el endpoint y un refund atascado conserva su cupo (conservador: jamás sobre-reembolsa). Revisión adversarial multi-agente (4 lentes × verificación escéptica) sobre el flujo del dinero: cero defectos confirmados. **Cierra AUD-P1-002 por completo** (payments core real: intents+attempts+confirm+refunds E2E). Suite total: **436/436**.
- **F3-05a completado (2026-07-05) — customers; arranca la fundación de checkout**: migración `0021` — tabla `customers` (RLS forzado, append-only con baja lógica `deleted_at`, clave compuesta (id, tenant_id) para los FK futuros de checkout sessions y payment links). Recurso diferido desde F3-02 con su primer consumidor real ya a la vista (el checkout de F3-05). Semántica tipo Stripe: **el email NO es único** (personas/negocios distintos comparten correo — probado que se permiten duplicados), se exige al menos uno de email/name/phone (un customer vacío suele ser bug del integrador), metadata validada ≤50 claves con límites de longitud (bolsa del comercio, jamás interpretada por Fluvia). `CustomerService` en `@fluvia/identity`: create/get/list con DTO whitelisted (jamás spread de fila — sin `tenant_id`/`deleted_at` al cliente), **update parcial** (solo las claves presentes cambian; `null` limpia email/name/phone/description; metadata REEMPLAZA la bolsa completa) y **softDelete idempotente** (baja lógica, desaparece de get/list, borrar dos veces no es error). Endpoints `/v1/customers` create/get/list/update/delete con scopes `customers:write`/`read` (el scope ya existía desde F1-04c); `CustomerNotFoundError`→`not_found` sin código nuevo (golden regen solo del mapa domain, 36 códigos); OpenAPI +5 paths cubiertos por el test de contrato. Probado contra PG real: validación, update parcial, baja idempotente y aislamiento cross-tenant (404 indistinguible por RLS). Alcance: F3-05a es la fundación backend; **checkout sessions (F3-05b) y la UI Next.js alojada (F3-05c) siguen** — la UI no puede construirse contra una API inexistente sin simular capacidad (Nivel A). Suite total: **458/458**.
- **F3-05b completado (2026-07-05) — checkout sessions (el recurso)**: migración `0022` — tabla `checkout_sessions` (FK compuestas al payment_intent y al customer opcional, RLS forzado, append-only) + FSM `open→completed|expired` hecha cumplir EN el motor (patrón golden de 0017/0020/0021; meta-test triple doc §5↔TS↔DDL + matriz COMPLETA de 9 pares como superusuario). Reconcilié el mermaid §5 (tenía prosa con un estado `canceled` inexistente — un buyer que cancela se redirige a `cancel_url` pero la sesión sigue `open` hasta expirar, semántica Stripe). `CheckoutSessionService` en `@fluvia/payments-core`: create idempotente (capa F2-09) SOLO sobre un intent no resuelto (un intent terminal es error de estado `invalid_state_transition`), `client_secret` `cs_<random>` entregado UNA vez con **solo su hash sha256 en reposo** (un dump no permite secuestrar sesiones), TTL acotado 5 min–24 h (default 24 h), customer opcional pre-validado bajo RLS (ajeno/borrado → error limpio, no violación de FK cruda); get/list con DTO whitelisted (jamás expone el secreto). Endpoints `/v1/checkout_sessions` create/get/list (scopes payments:write/read); config `CHECKOUT_BASE_URL` (no secreto, default dev); `CheckoutSessionNotFoundError`→not_found y `CheckoutSessionInvalidCustomerError`→validation_error sin códigos nuevos (36); OpenAPI +2 paths. Probado contra PG real: creación idempotente con replay exacto sobre HTTP, guard de estado del intent, client_secret una vez + hash en reposo, aislamiento cross-tenant. **Alcance**: F3-05b es el RECURSO; el flujo alojado (retrieval por client_secret, disparo de completed/expired y sus eventos `checkout_session.*`) es **F3-05c**, registrado como desviación (espeja el diferimiento de barridos F3-03→F3-04). Suite total: **476/476**.
- **F3-05c-i completado (2026-07-05) — backend del flujo alojado; los eventos `checkout_session.*` cobran productor**: migración `0023` — `checkout_session_authenticate(session_id, secret_hash)→tenant_id` SECURITY DEFINER: autenticación **cross-tenant por `client_secret`** (la página alojada no trae contexto de tenant), mismo patrón que `authenticate_api_key`; devuelve solo el tenant_id, nada más. `CheckoutSessionService.getByClientSecret`: autentica (secreto/id equivocado = mismo not-found que inexistente, anti-enumeración) → ya con el tenant scoped, **sincroniza el estado de forma perezosa e idempotente bajo `FOR UPDATE` de la sesión**: open→completed si el intent asociado tuvo éxito (sella `completed_at`), open→expired si venció el TTL, emitiendo `checkout_session.completed|expired` al outbox (buildEnvelope en TS — valida el sobre AL emitir, mismo schema que valida el relay; el productor de esos topics del catálogo F3-07 ya existe) → devuelve una **vista REDACTADA** (estado de la sesión + id/estado/monto/moneda del intent, jamás el secreto, el tenant ni internos del comercio). Endpoint `GET /v1/checkout_sessions/:id/status` **sin API key** (la credencial es el `client_secret` en header `X-Checkout-Client-Secret`; 404 del catálogo para secreto ausente/malo; excluido del contrato OpenAPI igual que la ingesta de webhooks del proveedor). Probado contra PG real: vista redactada, completado con evento único + idempotencia (segunda consulta no re-emite), expiración con evento único, intent-succeeded gana sobre TTL vencido, y el ciclo completo sobre HTTP (crear sesión → confirmar el intent con `tok_approve` por la vía existente → `/status` refleja completed). **Pendiente F3-05c-ii**: barrido de entrega garantizada para sesiones que nadie consulta (espeja `sweep_payment_attempts`) + UI Next.js alojada. Suite total: **483/483**.
- **F3-05c-ii completado (2026-07-05) — entrega garantizada de eventos de checkout**: migración `0024` — `sweep_checkout_sessions()` SECURITY DEFINER (familia purga/drift/attempts): completa las sesiones `open` cuyo intent tuvo éxito y expira las vencidas que **NADIE consultó** (la sincronización perezosa de F3-05c-i solo dispara si el comprador vuelve a la página; si cierra la pestaña tras pagar, este barrido cierra el hueco), emitiendo `checkout_session.completed|expired` al outbox. El sobre común se construye EN SQL (`jsonb_build_object` con `evt_<uuid>`, `occurred_at` ISO-8601, resource/data) — **probado que valida contra el MISMO `EventEnvelopeSchema` que el relay exige antes de despachar** (un sobre malformado sería veneno). Las dos ramas del `WITH` son DISJUNTAS (una fila jamás la tocan ambos UPDATE); un intent con éxito gana sobre un TTL vencido (coherente con la vía perezosa). `CheckoutSessionWatchdog` en el worker (mismo patrón que `AttemptsWatchdog`; config `CHECKOUT_WATCHDOG_ENABLED/INTERVAL_MS`, métrica `fluvia_checkout_sessions_swept_total{result}`, observador que jamás rompe el job). Idempotente con la vía perezosa bajo el guard `status='open'` (probado: segunda corrida no re-emite). Probado contra PG real vía el rol `fluvia_worker`. **Pendiente F3-05c-iii**: UI Next.js alojada (i18n es/en, WCAG AA) — primer app frontend del monorepo. Suite total: **489/489**.
- **F3-05c-iii completado (2026-07-05) — confirm alojado (backend); el checkout ya es pagable desde la página del comprador**: endpoint `POST /v1/checkout_sessions/:id/confirm` **sin API key** (la credencial es el `client_secret` en header; el navegador del comprador JAMÁS sostiene la API key del comercio). `CheckoutSessionService.confirmByClientSecret`: autentica (0023) → mismo patrón de dos fases que el confirm de la API (V4 Nivel A): fase 1 (intent→processing + attempt `submitting`) bajo `FOR UPDATE` de la sesión, proveedor FUERA de toda tx, luego sincroniza la sesión (completed + evento si el intent tuvo éxito). **Idempotente ante doble submit** (el comprador hace doble clic): si el intent ya no es confirmable (una confirmación previa lo dejó en processing/terminal), devuelve la vista actual SIN crear un segundo attempt — probado que attempts=1 tras dos confirms. Probado contra PG real + HTTP: `tok_approve`→sesión completed + intent succeeded; `tok_decline`→intent failed y la sesión sigue open (el intent terminal expirará por TTL — el checkout es de un intento por sesión, coherente con la FSM del intent; un reintento = nuevo intent+sesión); secreto equivocado→404 anti-enumeración; token faltante→400. La ruta va sin API key, excluida del contrato OpenAPI como `/status` y la ingesta del proveedor. Con esto **el flujo de pago alojado es funcional extremo a extremo por backend** (crear sesión → la página consulta `/status` → paga con `/confirm` → completa + webhook). **Pendiente F3-05c-iv**: la UI Next.js alojada (presentación). Suite total: **496/496**.
- **F3-05c-iv completado (2026-07-05) — UI de checkout alojada; F3-05 COMPLETO**: `apps/checkout`, el PRIMER app frontend del monorepo (Next.js 15 App Router + React 19 — **subí de Next 14 a 15 porque el gate `pnpm audit --audit-level high` del CI rechazaba el advisory crítico de Next 14**; con 15.5.16 el árbol queda limpio). Página `/c/[id]`: lee el `client_secret` del **fragmento** de la URL (`#…`, no llega a los logs del servidor), consume `GET /status` y `POST /confirm` vía **route handlers server-side** (el navegador jamás conoce la URL de la API ni sostiene una API key — el proxy reenvía el secreto). i18n es/en con un diccionario propio (la superficie no justifica una librería). **WCAG AA**: `main`/`h1`, `fieldset`/`legend` para los métodos, `label` explícito, estado en región `aria-live` con foco al resultado, contraste AA (ratios ≥4.5:1 documentados en el CSS), targets táctiles ≥44px. **Verificación**: tests de componente + accesibilidad estructural (jsdom + `@testing-library/react` + `axe-core`) **CI-gated** (render, i18n, la interacción de pago aprobado/rechazado, cero violaciones axe); y el **E2E de navegador full-stack verificado LOCALMENTE** (Chromium real preinstalado → route handlers → API de Fluvia → PG: renderiza `$ 50.000` → clic Pagar → «¡Pago completado!», con captura). El CI **no tiene navegador** (no corre `playwright install`), así que el E2E de navegador es local por diseño — documentado en `apps/checkout/e2e/README.md`; añadí `.next`/`.prettierignore` para no escanear artefactos generados. Con esto **F3-05 (checkout) está COMPLETO extremo a extremo**: customers → recurso → flujo alojado (status/confirm) → entrega garantizada → UI. Suite total: **501/501** (496 backend + 5 componente); `pnpm audit --audit-level high` limpio (1 moderate).
- **F3-06 completado (2026-07-05, backend) — payment links; reutilizan TODO el checkout**: migración `0025` — tabla `payment_links` (plantilla "págame" reutilizable: FK compuesta al merchant, RLS forzado, append-only) + función `payment_link_resolve(id)` SECURITY DEFINER cross-tenant que devuelve monto/moneda/merchant SOLO de links ACTIVOS (inexistente o deshabilitado = 0 filas, anti-enumeración). `PaymentLinkService` en `@fluvia/payments-core`: create idempotente (F2-09, merchant validado bajo RLS)/get/list/disable, y **`createSessionFromLink` PÚBLICO** — resuelve el link y genera un `payment_intent` + `checkout_session` **FRESCOS en UNA transacción atómica** (reutiliza el recurso de checkout de F3-05; **multi-uso**: cada apertura del link es una sesión distinta con su propio intent, probado). Endpoints `/v1/payment_links` create/get/list/disable (scope payments:write/read, creación idempotente) + `POST :id/sessions` **sin API key** (abrir el link; deshabilitado/inexistente = mismo 404; excluido del contrato OpenAPI). Probado **end-to-end sobre HTTP**: crear link → `POST :id/sessions` (público) → sesión pagable → `GET /status` refleja el monto del link → `POST /confirm` con `tok_approve` → completed. Sin códigos nuevos (36); OpenAPI +3 paths de gestión. **Pendiente F3-06-b** (frontend, thin): página `/l/[id]` del app que llama a `/sessions` y redirige a `/c/{sessionId}#{secret}`. Suite total: **515/515**.
- **F3-06-b completado (2026-07-05) — UI del payment link; el link cierra end-to-end para el comprador**: página pública `/l/[id]` en `apps/checkout` — server component (`export const dynamic = 'force-dynamic'`) que hace el `POST /v1/payment_links/:id/sessions` **server-side** (F3-06, sin API key) y **redirige** el navegador al flujo alojado `/c/{sessionId}#{clientSecret}` (F3-05c-iv). El `client_secret` viaja en el **fragmento** del `Location` del 307: el servidor lo emite una vez y NUNCA lo vuelve a ver en la navegación posterior a `/c` (el fragmento no se envía al servidor). Un link inválido/deshabilitado/API caída → misma vista de indisponibilidad `LinkUnavailable` (mismo mensaje anti-enumeración que la vista `not_found` del checkout). La lógica pura `resolveLinkSession` se factorizó aparte para **probarla en CI sin navegador** (jsdom + axe, 6 tests: destino con fragmento, preservación de `?lang=en`, endpoint correcto, 404→null, API caída→null, body incompleto→null, vista de indisponibilidad accesible). **La redirección real con fragmento a través del navegador** se verificó LOCALMENTE (Chromium preinstalado → Next `:3100` → API `:3000` → PG): abrir `/l/{id}` → `307` con `Location: /c/{sid}#cs_…` → el navegador conserva el fragmento → checkout carga `$ 77.000` → clic Pagar → «¡Pago completado!». El CI no tiene navegador, así que el E2E de navegador es local por diseño (documentado en `apps/checkout/e2e/README.md`). Sin cambios de esquema ni de dependencias (reusa Next 15/React 19/axe ya presentes). Suite total: **521/521**.
- **F3-09a completado (2026-07-05) — visibilidad de la cola de webhooks + reenvío manual auditado de eventos `dead`**: primera rebanada del dashboard (F3-09), capacidad backend NUEVA (no hay read API de la cola de webhooks hasta ahora) que además cierra el diferido de F3-07 (reenvío manual). Migración `0026`: columna `resent_from_event_id` en `webhook_events` + función SECURITY DEFINER acotada `webhook_event_resend(event_id, tenant_id)` — `fluvia_app` tiene SELECT sobre `webhook_events`/`webhook_attempts` (privilegios por defecto de 0002) pero NO INSERT/UPDATE (revocados en 0019: la cola la escribe el relay); el reenvío entra por la función (ventana ADR-0011). `WebhookEventService` en `@fluvia/webhooks`: `list` (filtros endpoint/status), `get` (con `payload` + historial de intentos) y `resend` — **reenviar NO resucita el evento muerto** (clase append-only, estado terminal inmutable): clona (tenant, endpoint, topic, payload) como un evento `pending` FRESCO (attempts=0, entra por el ciclo normal del deliverer) enlazado vía `resent_from_event_id`, con un audit event `webhook_event.resent` (nueva acción del catálogo) escrito en la MISMA transacción. Endpoints `GET /v1/webhook_events` (scope `read`), `GET /v1/webhook_events/:id`, `POST /v1/webhook_events/:id/resend` (scope `webhooks:manage`); solo `dead` se reenvía (si no, `invalid_state_transition`). `WebhookEventNotFoundError`→not_found, `WebhookEventNotDeadError`→invalid_state_transition (sin códigos de catálogo nuevos, 36; +2 domain codes → 48; golden regen); OpenAPI +3 paths. Probado vs PG real (12 tests: 6 de servicio + 6 de ruta HTTP): lectura RLS por tenant, reenvío que clona+enlaza+audita atómicamente, aislamiento cross-tenant (404), guard de solo-`dead` (409), scope requerido. Suite total: **536/536**.
- **F3-09b-i completado (2026-07-05) — plano de LECTURA del dashboard de operación (por sesión + membresía)**: segunda rebanada de F3-09. El dashboard es para un operador HUMANO, así que autentica por **SESIÓN** y su organización (= tenant) sale de su **MEMBRESÍA** (`security.session` + `security.org(...)`), no por API key — los recursos hasta ahora solo se leían con API key. Por Nivel A, el plano de lectura por sesión es el prerequisito honesto de cualquier UI del dashboard (no construir UI sobre capacidad inexistente). Nuevo permiso RBAC **`payments:read`** (matriz de `rbac.ts` + meta-test celda-a-celda + `docs/security/access-control.md` actualizados; lo tiene TODO rol — es dato de tenant de solo lectura, todos ya tienen `org:read`). Rutas `GET /v1/organizations/:orgId/{payment_intents,refunds,checkout_sessions,payment_links,webhook_events}` (list + detail) que **reutilizan los MISMOS servicios y serializers** que el plano de API key (serializers exportados; una sola forma de recurso, cero duplicación de lógica). Solo LECTURA: la única escritura de operación (reenvío de webhooks `dead`) sigue en el plano de API key (F3-09a, `webhooks:manage`); el frontend la ejercerá por esa vía. Probado vs PG real (5 tests): datos creados por el plano de API key y leídos por el de sesión, plano completo cableado+guardado (las 5 vistas), no-miembro→404, sin sesión→401, aislamiento por tenant (un miembro de otra org no ve el intent ajeno ni por id). Sin migración ni dependencias nuevas; el plano de sesión queda excluido del contrato OpenAPI como el resto de rutas de sesión. Suite total: **541/541**.
- **F3-09b-ii completado (2026-07-05) — frontend del dashboard de operación**: tercera rebanada de F3-09 y **SEGUNDO app frontend del monorepo** (`apps/dashboard`, Next.js 15 App Router + React 19, patrón de `apps/checkout`). **Login por sesión**: `/login` (client) → route handler `/api/session` que proxya el login a la API y, si hay sesión, fija la cookie **httpOnly** `fluvia_session` — el token NUNCA llega al JS del navegador; MFA queda fuera de alcance (si la cuenta lo exige, se avisa, no se simula, V4 Nivel A). `/` (server component) lee la cookie, sin sesión → `/login`, con sesión lista las organizaciones del operador (su membresía) enlazando a `/o/[orgId]`. `/o/[orgId]` (server component) trae las 5 vistas del plano de lectura F3-09b-i con el token de la cookie (server-side; el navegador jamás conoce la URL de la API) y renderiza `DashboardView`: tablas accesibles de payment intents / refunds / checkout sessions / payment links / cola de webhooks, montos formateados por moneda/locale (reusa `formatAmount` de checkout), badges por estado. **SOLO lectura** (la acción de reenvío `dead` llega en F3-09b-iii). i18n es/en, WCAG AA (main/h1, `th scope`, `caption`, foco, contraste AA). Tests **CI-gated**: lógica pura del cliente API server-side (login incl. MFA-required sin fingir sesión, orgs, agregación de las 5 listas con degradación por recurso) + componente/accesibilidad (jsdom + axe) = **12 tests**. **E2E de navegador full-stack verificado LOCALMENTE** (Chromium real → route handlers/server components → API `:3000` → PG: login con credenciales reales → cookie httpOnly → picker «Dash E2E Org» → panel `/o/{orgId}` con el intent `$ 123.000` (badge `created`) y el payment link `$ 45.000` (badge `active`); captura), documentado en `apps/dashboard/e2e/README.md`; el CI no tiene navegador. Sin migración; deps ya presentes (Next 15/React 19/axe). Suite total: **553/553**.
- **F3-09b-iii completado (2026-07-05) — reenvío `dead` desde el panel; el dashboard es superficie de OPERACIÓN, no solo observación**: cuarta rebanada de F3-09. **Backend**: nuevo permiso RBAC `webhooks:manage` (espeja el scope de API key homónimo en el plano de SESIÓN — son enums distintos, misma capacidad en planos distintos; solo owner/admin/developer, los roles que gestionan integración; matriz de `rbac.ts` + meta-test celda-a-celda + `access-control.md` actualizados). Ruta `POST /v1/organizations/:orgId/webhook_events/:id/resend` bajo `security.session`+`security.org('webhooks:manage')` que reusa `WebhookEventService.resend` (F3-09a) auditando como `actorType: user` (no `api_key`). **Frontend**: botón «Reenviar» (client component `ResendButton`) en la cola de webhooks del panel, mostrado SOLO para eventos `dead` y SOLO si el rol puede reenviar (hint de UX `canResendRole` = owner/admin/developer, espejo del RBAC; el API es la fuente de verdad y da 403 a quien no lo tenga). Postea al route handler `/api/orgs/[orgId]/webhook-events/[id]/resend` que reenvía la cookie de sesión server-side; en éxito muestra «Reenviado ✓» y recarga para reflejar el evento `pending` fresco. Tests **CI-gated**: 3 de ruta HTTP (admin→201 con `resent_from_event_id`, read_only→403 por falta de `webhooks:manage`, no-dead→409/no-miembro→404) + 3 de frontend (canResendRole por rol, visibilidad del botón según canResend, POST al route handler + «Reenviado»). **E2E de navegador verificado LOCALMENTE** (Chromium: login owner → panel → clic Reenviar en el evento `dead` → `201` → «Reenviado ✓» → recarga con «Cola de webhooks (2)» = el `dead` (con su botón) + el `pending` fresco (sin acción); captura). Sin migración; deps ya presentes. Suite total: **559/559**.
- **F3-10 completado (2026-07-05) — SDK TypeScript del contrato OpenAPI**: `@fluvia/sdk`, cliente tipado del **plano de integración (API key)**, sin dependencias de runtime (usa `fetch` global inyectable vía `fetchImpl` — funciona en Node y navegador). `FluviaClient({ baseUrl, apiKey })` con namespaces `paymentIntents` (create/get/list/confirm/cancel) / `refunds` / `customers` (create/get/list/update/delete) / `checkoutSessions` / `paymentLinks` / `webhookEndpoints` (create/list/rotate/disable) / `webhookEvents` (list/get/resend) — cubre las **27 (método, ruta)** del contrato. Tipos de recurso que espejan la forma pública del API; `FluviaApiError` expone el sobre estable (F1-08: `status`/`code`/`requestId`/`details`); las creaciones que exigen idempotencia (intents/refunds/checkout/links) **autogeneran** un `Idempotency-Key` (reintentar es seguro), con override opcional. **`test/contract.test.ts`**: el conjunto de rutas del SDK (`SDK_ROUTES`) es EXACTAMENTE igual al del contrato `openapi.v1.json` — añadir una ruta al spec sin método en el SDK (o al revés) rompe CI, así el SDK jamás deriva del contrato que el CI ya verifica spec↔servidor. **`test/sdk.test.ts`**: ejerce el SDK contra el API REAL en proceso — el `fetch` inyectado delega en `app.inject` (Fastify), así cada llamada pasa por auth/idempotencia/dominio reales (NO un mock): crear intent → confirm (contrato asíncrono: `processing`, luego GET `succeeded`) → refund; customers CRUD; payment links; rotación de webhook endpoints (secreto una vez); errores tipados (404 `not_found`, 401). README con ejemplos. 10 tests (2 contrato + 8 e2e). Sin migración ni dependencias externas nuevas. Suite total: **569/569**.
- **F3-11a completado (2026-07-05) — CORS + cabeceras de seguridad del API (AUD-P2-016)**: primera rebanada del hardening de despliegue de la Fase 3. **CORS de allowlist explícita**: config `CORS_ALLOWED_ORIGINS` (lista por comas, no secreto, default **VACÍO** = ningún cross-origin — el default seguro, porque `apps/checkout`/`apps/dashboard` llaman al API SERVER-SIDE, no desde el navegador; `*` habilita cualquiera). Un hook `onRequest` hace eco del `Origin` permitido con `Access-Control-Allow-Origin` + `Vary: Origin`, y responde el **preflight** OPTIONS (204 con `Access-Control-Allow-Methods`, `Access-Control-Allow-Headers` incl. `idempotency-key`/`authorization`, `Access-Control-Max-Age`) SOLO para orígenes permitidos — un origen ajeno no recibe ACAO y el navegador lo bloquea. **Cabeceras de seguridad en TODA respuesta** (hook `onSend`): `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy: no-referrer`, CSP `default-src 'none'; frame-ancestors 'none'` (el API devuelve JSON → se bloquea al máximo), `Cross-Origin-Resource-Policy: same-origin`, y `Strict-Transport-Security` SOLO fuera de local/test (local es http). 7 tests: presencia de headers, HSTS ausente en test, CORS default niega cross-origin, eco de origen permitido + Vary, rechazo de origen ajeno, preflight permitido (con method/header allowances) y preflight denegado (sin ACAO). Sin migración ni dependencias nuevas. Suite total: **576/576**.
- **F3-11b completado (2026-07-05) — empaquetado de despliegue (AUD-P3-003); con F3-11a cierra F3-11 → hardening de Fase 3 COMPLETO**: `Dockerfile` multi-stage ÚNICO del backend (API + worker comparten imagen; el servicio elige la app con `command` en compose — ambos corren el monorepo vía tsx). Stage `build`: `pnpm install --frozen-lockfile` + `pnpm build` (valida tipos dentro de la imagen). Stage `runtime`: slim, corre como usuario **no-root** (`node`); el pnpm pineado (`packageManager` 10.33.0) se hornea en la capa base con `corepack prepare … --activate` para que el runtime no lo descargue. `.dockerignore` (jamás copia node_modules ni artefactos — arquitectura/host equivocados). `docker-compose.yml` extendido con un **profile `app`** (el `docker compose up` por defecto sigue siendo solo PG+Redis para desarrollo en el host): servicio `migrate` (one-shot idempotente, crea los roles de mínimo privilegio + esquema) del que dependen `api` (:3000) y `worker` con `service_completed_successfully`, todos cableados por env a los roles (host = servicio `postgres`; el profile es de desarrollo, usa las contraseñas de rol de local). **Verificado con Docker REAL** (el daemon se levantó en el sandbox): `docker build` OK (install + `pnpm build` de 20 paquetes dentro de la imagen), `migrate` aplicó las migraciones en el contenedor contra un PG containerizado (creó roles + esquema), el API arrancó **como `node` (no-root)** y sirvió `/health` (`env: local`) + `/ready` (`ready` → conecta a la BD con rol `fluvia_app`) con los security headers de F3-11a presentes, y el worker arrancó limpio (purga/inbox/attempts+checkout watchdog/webhook deliverer + métricas en 9464). El CI no corre docker, así que esta verificación es local (como el E2E de navegador). Sin cambios de código (solo `Dockerfile`, `.dockerignore`, `docker-compose.yml`). Suite total: **576/576** (sin cambios).
- **F4-01a completado (2026-07-05) — motor de conciliación (núcleo)**: arranca el bloque de dominio de dinero de la Fase 4. Concilia lo que Fluvia CREE liquidado (intentos `succeeded` con `provider_ref` en `payment_attempts`) contra el REPORTE DE LIQUIDACIÓN del proveedor — el siguiente invariante de dinero real tras refunds. Migración `0027`: `settlement_reports` (lote del proveedor: tenant/proveedor/moneda/periodo/estado) + `settlement_lines` (lo que el proveedor dice que liquidó: `provider_ref`+monto+fee) + `reconciliation_entries` (resultado por referencia), las tres **RLS por tenant + append-only**. `@fluvia/reconciliation` `ReconciliationService`: `createReport`, `addLines` (ingesta idempotente por `(report, provider_ref)` vía `ON CONFLICT DO NOTHING`), `reconcile`, `getReport`, `listEntries`. El motor `reconcile` corre un **FULL OUTER JOIN** entre las líneas del reporte y los intentos `succeeded` del tenant (mismo proveedor/moneda, `resolved_at` DENTRO del periodo del reporte — acota el universo del ledger) por `provider_ref`, y clasifica cada referencia: **matched** (ambos, montos iguales) / **amount_mismatch** (ambos, difieren) / **missing_in_ledger** (línea sin intento) / **missing_at_provider** (intento sin línea); persiste `reconciliation_entries` + marca el reporte `reconciled`, todo en UNA transacción. **Append-only**: un reporte se concilia UNA sola vez (`ReportAlreadyReconciledError`; guard `FOR UPDATE` sobre estado `open`). Per-tenant bajo RLS, SIN definer (la liquidación cross-tenant a nivel plataforma queda como refinamiento futuro; el modelo actual atribuye la liquidación por tenant). 4 tests vs PG real: las 4 clases de discrepancia en una sola corrida + intento fuera de periodo excluido, idempotencia de `addLines`, guard de conciliar-una-vez, aislamiento por tenant. Sin dependencias externas nuevas. Suite total: **580/580**.
- **F4-01b completado (2026-07-05) — gestión de conciliación (API de integración)**: expone el motor F4-01a por el plano de API key. `ReconciliationService` +`listReports`/`getSummary`. Rutas: `POST /v1/settlement_reports` (crea un reporte, `payments:write`), `POST :id/lines` (carga las líneas del reporte del proveedor — ≤1000, ingesta idempotente por `(report, provider_ref)`), `POST :id/reconcile` (dispara el motor, devuelve el resumen de discrepancias), `GET :id` (reporte + resumen agregado), `GET` (lista), `GET :id/entries` (discrepancias con filtro por status; `read`). `SettlementReportNotFoundError`→not_found, `ReportAlreadyReconciledError`→invalid_state_transition (sin códigos de catálogo nuevos, 36; +2 domain codes → 50; golden regen). 4 tests de ruta HTTP: cargar líneas → conciliar → resumen con las 4 clases + `GET` con status `reconciled` y la discrepancia `missing_in_ledger` con su monto, reconciliar-dos-veces→409 `invalid_state_transition`, scope `payments:write` requerido para crear + aislamiento por tenant (404), listar. **Intencionalmente FUERA del contrato OpenAPI v1** por ahora (superficie nueva, como los planos público y de sesión) — no cascada al SDK. Sin migración ni deps nuevas. Suite total: **584/584**.
- **F4-01c completado (2026-07-05) — vista de conciliación en el dashboard**: cierra el bucle de conciliación de cara al operador (ver + drill-down de discrepancias). **Backend**: plano de LECTURA por SESIÓN — `GET /v1/organizations/:orgId/settlement_reports` (lista), `/:id` (reporte + resumen agregado), `/:id/entries` (discrepancias con filtro por status), bajo `security.session`+`security.org('payments:read')`, reutilizando `ReconciliationService` y los serializers exportados de F4-01b (una sola forma de recurso). **Frontend** (`apps/dashboard`): enlace «Conciliación» en la cabecera del panel → `/o/[orgId]/reconciliation` (server component que lista los reportes: proveedor, periodo, estado) → `/o/[orgId]/reconciliation/[reportId]` (server component con el **resumen** — contadores por clase con colores — + la **tabla de discrepancias** con referencia, estado (badge), monto ledger y monto proveedor por moneda/locale). Solo lectura. i18n es/en, WCAG AA. Tests **CI-gated**: 2 de ruta de sesión (lista/detalle+resumen/entries + aislamiento por tenant 404) + 5 de frontend (componentes list/detail, axe, fetchers con degradación a [] y filtro por status). **E2E de navegador full-stack verificado LOCALMENTE** (Chromium: login → «Conciliación» → lista con el reporte `mock` → detalle con resumen (Conciliados=1, Falta en el ledger=1) y la tabla `er_match`→matched $60.000/$60.000, `er_phantom`→missing_in_ledger ledger «—» proveedor $9.000; captura). El settlement report con discrepancias se sembró por el plano de API key (F4-01b) — sin endpoint de sandbox nuevo. Sin migración; deps ya presentes. Suite total: **591/591**.
- **F4-02 completado (2026-07-06) — conciliación batch/continua (worker)**: lleva la conciliación de "a demanda" (F4-01b) a "continua". Migración `0028`: función `sweep_settlement_reports()` **SECURITY DEFINER** sin parámetros (misma familia que `sweep_payment_attempts`/`sweep_checkout_sessions`; alcance imposible de ensanchar), **EXECUTE solo para `fluvia_worker`** (que no tiene privilegios de tabla). Concilia cross-tenant, en un solo statement set-based, los reportes cuyo **periodo YA CERRÓ** (`open` + `period_end <= now()` — el disparador natural de "reporte sellado"; mientras el periodo sigue abierto llegan líneas/liquidaciones y conciliar antes sería prematuro): FULL OUTER JOIN líneas del proveedor ⋈ intentos `succeeded` del ledger (clasificación **idéntica** al motor per-tenant F4-01a), inserta `reconciliation_entries`, marca los reportes `reconciled` (TODOS los vencidos, incluso los vacíos). **Lease** vía `FOR UPDATE SKIP LOCKED` (dos barridos concurrentes jamás tocan el mismo reporte; la UNIQUE de entries lo garantizaría, pero el skip lo evita antes). **Idempotente** con la conciliación manual (ambas bajo el guard `status='open'`; la manual deja `reconciled`, fuera del barrido). `apps/worker` `ReconciliationWatchdog` (mismo patrón que attempts/checkout: gate `RECONCILIATION_WATCHDOG_ENABLED`/`_INTERVAL_MS` en `@fluvia/config`, `runOnce`/`start`/`stop`, observador `onResult` cuyos errores JAMÁS rompen el job) invoca la función, expone métricas y **ALERTA ante discrepancias** (cualquier entry ≠ `matched` = dinero real sin cuadrar → log error). Métricas nuevas (`observability.md` §2): `fluvia_settlement_reports_reconciled_total` (counter), `fluvia_reconciliation_entries_total{status}` (counter), `fluvia_reconciliation_discrepancies_last` (gauge, 0 = cuadrado) + alerta ALTA nueva. 6 tests vs PG real: `runOnce` devuelve enteros ≥0, concilia un reporte sellado cross-tenant clasificando las 4 clases, **NO toca un periodo abierto** (period_end futuro), idempotencia (reporte ya conciliado no se re-barre ni duplica), `discrepancyCount` suma las 3 clases no-matched, observador que explota no rompe el job. `@fluvia/reconciliation` agregado como devDependency del worker (solo para sembrar en tests; `main.ts` solo invoca la función SQL). Docs espejo: `reconciliation.md` §4/§5, `observability.md` §2/§4. Suite total: **597/597**.
- **F4-03a completado (2026-07-06) — casos operativos de conciliación (materialización + ciclo)**: lleva la conciliación de "detectar + alertar" (F4-02) a "casos rastreables que un operador trabaja". Migración `0029`: tabla `operational_cases` (RLS por tenant, **mutable** con ciclo `open→acknowledged→resolved` + `version` para lock optimista; sin DELETE/TRUNCATE — registro auditable) enlazada a la `reconciliation_entry` de origen vía **FK compuesta por tenant** (nueva UNIQUE `(id, tenant_id)` en `reconciliation_entries`) + snapshot inmutable de la evidencia. La materialización es un **trigger `AFTER INSERT` sobre `reconciliation_entries`** (`materialize_reconciliation_case`): crea un caso por cada entry `!= matched`, automático para AMBAS vías de conciliación (motor per-tenant F4-01a bajo `fluvia_app`, y barrido F4-02 dentro del SECURITY DEFINER como postgres) sin duplicar lógica y atómico con la entry; idempotente (`UNIQUE(reconciliation_entry_id)` + `ON CONFLICT DO NOTHING`). Severidad por clase: `missing_in_ledger`→**critical**, `amount_mismatch`/`missing_at_provider`→**high**. `@fluvia/reconciliation` `OperationalCaseService` (`list`/`get`/`acknowledge`/`resolve`) bajo RLS con auditoría `operational_case.acknowledged`/`.resolved` (2 acciones nuevas en `@fluvia/audit`). **Nivel A: resolver es DOCUMENTAL — NO mueve dinero ni ajusta saldos**; el ajuste monetario con **four-eyes** sobre umbral es F4-03b (ni IA ni automatización autorizan dinero real solas, V4 §30). Plano de API key: `GET /v1/operational_cases` (filtros status/severity), `GET :id`, `POST :id/acknowledge` (payments:write), `POST :id/resolve` (payments:write, resolución obligatoria). `OperationalCaseNotFoundError`→not_found, `InvalidCaseTransitionError`→invalid_state_transition (sin códigos de catálogo nuevos, 36; +2 domain → 52; golden regen). 8 tests vs PG real: 5 de servicio (materialización un-caso-por-discrepancia con mapeo de severidad + matched no crea caso, ack→resolve con nota obligatoria + auditoría, actor api_key deja `resolved_by_user_id` null, re-resolver→error, aislamiento por tenant) + 3 de ruta HTTP (materializar→ack→resolve→409 re-resolver, scope payments:write + resolución vacía→400, aislamiento por tenant 404). `@fluvia/audit` agregado como dep de `@fluvia/reconciliation`. Fuera del contrato OpenAPI v1 por ahora. Suite total: **605/605**.
- **F4-03b completado (2026-07-06) — ajuste monetario con four-eyes (Nivel C)**: cierra el §30 ("todo ajuste pasa por asiento con caso, razón, actor y aprobación; four-eyes para montos sobre umbral"). Migración `0030`: tabla `case_adjustments` (RLS por tenant, mutable `proposed→applied|rejected` + `version`; sin DELETE/TRUNCATE) enlazada al caso por **FK compuesta por tenant** (nueva UNIQUE `(id, tenant_id)` en `operational_cases`). **FOUR-EYES EN LA BD** (no solo del servicio): `CHECK (NOT requires_second_approval OR approved_by_user_id <> proposed_by_user_id)` — ni un bug del servicio puede saltárselo; `proposed_by_user_id` NOT NULL (proponer es humano); índice parcial único `WHERE status <> 'rejected'` (a lo sumo un ajuste vivo por caso). `@fluvia/ledger` `PostingService.postReconAdjustment` postea un **asiento compensatorio real** `recon.differences ↔ suspense` (ambas platform/transitorias del chart existente; sin merchant; `reason='reconciliation'`). `@fluvia/reconciliation` `CaseAdjustmentService` (`propose`/`approve`/`reject`/`get`/`listForCase`): **propone** un humano; **aprueba** un segundo humano DISTINTO sobre umbral (`FOUR_EYES_THRESHOLD_MINOR`, default 0 = SIEMPRE) — al aprobar, postea el asiento + resuelve el caso + auditoría, TODO atómico vía `onPosted` del ledger e idempotente por `case_adj:{id}` (doble aprobación no duplica el asiento). **Invariante Nivel A**: `actorType='api_key'` (máquina/IA) → `HumanActorRequiredError`; auto-aprobación sobre umbral → `SelfApprovalError`. 3 acciones de auditoría nuevas (`operational_case.adjustment_proposed`/`.adjustment_applied`/`.adjustment_rejected`). Config `FOUR_EYES_THRESHOLD_MINOR`; deps `@fluvia/ledger`+`@fluvia/money` en `@fluvia/reconciliation`. **Entregado como MOTOR** (sin rutas HTTP, como F4-01a antes de F4-01b): 6 tests vs PG real — máquina rechazada, sobre-umbral proponente-no-autoaprueba + segundo humano aplica con asiento balanceado (débitos==créditos) + caso resuelto, bajo-umbral autoaprueba, re-aprobar aplicado→error con UN solo asiento, rechazo libera para nueva propuesta (índice parcial), aislamiento por tenant. La exposición por sesión + UI (four-eyes por dos miembros sobre HTTP) es F4-03c. Suite total: **611/611**.
- **F4-03c completado (2026-07-06) — casos y ajustes por SESIÓN (four-eyes real sobre HTTP; backend)**: expone el motor F4-03a/b al operador HUMANO del panel. Permiso RBAC nuevo `reconciliation:manage` (owner/admin/**finance** — el rol de conciliación; matriz + meta-test celda-a-celda + `access-control.md`). Rutas de sesión en `dashboard.ts` bajo `security.session`+membresía: `GET /v1/organizations/:orgId/operational_cases` (list, `payments:read`) + `/:id` (detalle **con sus ajustes**), `POST /:id/acknowledge` y `/:id/resolve` (`reconciliation:manage`); `GET/POST /:id/adjustments` (list/propose), `POST /case_adjustments/:id/approve` y `/reject` (`reconciliation:manage`). **FOUR-EYES REAL SOBRE HTTP**: el proponente es `req.identity.userId`; el aprobador sobre umbral debe ser un usuario distinto — `SelfApprovalError`→**409 `four_eyes_required`** (código de catálogo NUEVO; 37 catálogo, +4 domain → 56; golden regen). Una API key jamás alcanza este plano (autorizar dinero es humano). `CaseAdjustmentService` cableado en `app.ts` con `postingService` + `config.fourEyesThresholdMinor`. 4 tests de ruta HTTP vs PG real con **DOS miembros `finance` distintos**: U1 acknowledge→propone→**U1 auto-aprueba→409 four_eyes_required**→U2 aplica (200, `ledger_transaction_id` no nulo)→caso `resolved` con el ajuste en el detalle; rol sin permiso (read_only)→403 `insufficient_permissions`; no-miembro→404; rechazo por U2 + read_only lista (payments:read). Sin migración ni deps nuevas. Falta solo la UI del panel (F4-03c-ii). Suite total: **615/615**.
- **F2-09 + F2-10 completados (2026-07-04) — GATE IDEMPOTENCIA 🟢 TÉCNICO; FASE 2 COMPLETA**: `@fluvia/idempotency` + migración 0013 (`expires_at`, purga en F1-09). Diseño: el claim de la key se inserta EN LA MISMA transacción que el efecto y la respuesta se persiste `completed` antes del COMMIT — crash pre-COMMIT ⇒ ni key ni efecto (reintento limpio); post-COMMIT ⇒ replay exacto del status+body persistidos sin re-ejecutar el handler (ambos probados). Duplicado concurrente: espera acotada sobre el índice único (`lock_timeout`) — si el primero commitea a tiempo, replaya; si no, 409 `processing_in_flight` (probado con pg_sleep). Payload distinto (hash canónico sha256 con claves ordenadas) ⇒ 422 `idempotency_key_reuse` sin ejecutar. Carrera 8 concurrentes ⇒ exactamente 1 efecto; property: toda secuencia de reintentos ⇒ efectos==1. Sin Redis en el camino (ADR-0006). Códigos del contrato añadidos al catálogo v1 (aditivo, golden actualizado; categoría nueva `unprocessable_error` 422). HTTP end-to-end probado con el patrón exacto que cablearán los endpoints de pagos (F3-02). Cierra **AUD-P1-003** — 4 de los 5 P1 bloqueantes del auditor cerrados (queda solo P1-006/F1-04b). Suite total: **255/255**.
- **F1-08 completado (2026-07-04) — taxonomía de errores del API, AUD-P2-009 cerrado**: catálogo v1 cerrado en `apps/api/src/error-catalog.ts` (26 códigos en 8 categorías con familia HTTP coherente, verificada por meta-test) + **golden en git como contrato** (`error-catalog.v1.json`: cualquier cambio breaking rompe el contract test y exige bump de versión consciente). Sobre estable `{type, code, message, details?, request_id}` para TODA respuesta de error: dominio, validación Zod con details, JSON malformado, 413/415, 404 y 5xx opacos. Regla dura probada con fuga sembrada: el `message` interno de los errores de dominio va SOLO a logs — el cliente recibe el texto público del catálogo; y la forma default de Fastify (`{statusCode,…}`) es irrepresentable (test de control contra Fastify crudo). Reservado `rate_limited` (429) para F1-04b. Doc espejo: `docs/architecture/api-errors.md`. Desbloquea F2-09 por el DAG. Suite total: **241/241**.
- **F2-07 + F2-08 completados (2026-07-04) — GATE LEDGER 🟢 TÉCNICO**: (1) `reverseTransaction` (F2-07): asiento espejo completo por la vía normativa íntegra; **reversión única a nivel de motor** (índice único parcial 0012 — probado con 4 reversiones concurrentes de la misma tx: exactamente 1 gana); prohibido revertir reversiones; razón humana obligatoria + evento `ledger.transaction_reversed` (riesgo alto) en la MISMA transacción vía hook atómico `onPosted` (solo primera aplicación, jamás en replay); idempotente — el test cazó un bug real (el replay chocaba con el pre-check "ya revertida") corregido comparando la key de la reversión existente. Suma neta exacta probada con golden. (2) Suite formal de concurrencia (F2-08, `concurrency.test.ts`): PRNG determinista (mulberry32, seed 20260704, configurable por env) — 120 transferencias aleatorias concurrentes con conservación exacta y cero drift, presión de deadlock con pares opuestos A↔B, carrera masiva de idempotencia (10 keys × 5 posts ⇒ exactamente 10 aplicados), presión mixta postings+rebuilds+reversal concurrentes. **Baseline local: 120 postings en ~800 ms (~151 tx/s) con pool de 12 conexiones** — referencia, no SLO. Con F2-02…F2-08 verdes, el Gate Ledger queda técnicamente completo. Suite total: **233/233**.
- **F2-05 + F2-06 completados (2026-07-04) — el Gate Ledger avanza a falta de F2-07/F2-08**: (1) `rebuildProjection` (F2-05): reconstrucción race-safe bajo el MISMO lock de cuenta que el posting, avanza `version` (postings en vuelo reintentan), reporta `drifted` before/after; property test con 20 transferencias y rebuilds CONCURRENTES sin drift residual. (2) Detección programada: función `ledger_projection_drift()` (SECURITY DEFINER — única ventana del rol worker, denegada al rol app, mismo patrón que `authenticate_api_key`) + `ProjectionDriftWatcher` en `apps/worker` (DRIFT_CHECK_* en config; alerta = log error hasta F1-07). Cierra **AUD-P2-004**. (3) `scripts/verify-ledger-invariants.sql` (F2-06): auditoría AUTOCONTENIDA externa al ORM — 6 checks (doble partida por tx/moneda, montos>0, coherencia cuenta-tenant-moneda, cabeceras huérfanas, reversiones cross-tenant, drift) — nuevo paso de CI vía psql tras la suite; detecta corrupción sembrada como superusuario y vuelve a verde tras reparación explícita (probado). Cierra **AUD-P2-012**. Decisión de semántica documentada: drift silencioso = fila de proyección viva que miente; fila ausente = fallo visible (`getBalance` lanza) + `rebuildProjection` la materializa. Se corrigió de paso un test de F2-02 que dejaba una proyección incoherente como residuo. Suite total: **224/224**.
- **F2-12 completado (2026-07-04) — inbox durable, AUD-P1-005 cerrado**: migración `0010_inbox_provider_events.sql` + `@fluvia/inbox`. Ingesta (`InboxIngestService`, rol app con SOLO INSERT + columnas del árbitro): límite de tamaño, firma HMAC-SHA256 con timestamp FIRMADO y comparación en tiempo constante verificada ANTES de persistir (firma inválida = cero filas), headers allowlisted, dedup `UNIQUE (provider, provider_event_id)` race-safe (6 entregas concurrentes ⇒ exactamente 1 fila, probado). Procesador (`InboxProcessor`, rol `fluvia_inbox` mínimo, patrón ADR-0011): claim-lease SKIP LOCKED, registro por provider {schema Zod, handler}, veneno→dead + copia REDACTADA en la DLQ (probado que `card_token` llega como `[REDACTED]`), `ignored_out_of_order` como estado terminal para fuera-de-orden, backoff con jitter, provider sin handler→dead recuperable, y replay de plataforma auditado. 18 tests nuevos (2 procesadores concurrentes sin doble procesamiento incluido). Suite total: **215/215**.
- **F2-11 completado (2026-07-04) — el outbox cobra vida**: migración `0009_outbox_relay_role.sql` + `@fluvia/events` + `@fluvia/outbox` + ADR-0011. (1) **Envelope común** (AUD-P2-005): todo evento sale con `event_id/schema_version/occurred_at/producer/resource/data`, validado con Zod al emitir Y al despachar; el ledger ya publica `ledger.transaction.posted` con el sobre. (2) **Relay multi-worker**: claim atómico `FOR UPDATE SKIP LOCKED` donde el claim ES el lease (crash ⇒ la fila vuelve sola al expirar; sin estados nuevos ni reaper), publicación FUERA de la transacción (Nivel A), backoff exponencial con jitter, veneno→dead sin tocar el publisher, barrido de zombies, y **dead→pending solo vía replay de plataforma auditado con razón e ids reales**. (3) **AUD-P1-007 CERRADO**: rol `fluvia_relay` sin BYPASSRLS (políticas RLS explícitas solo sobre `outbox_events`, UPDATE por columna a los 6 campos de despacho), `fluvia_worker` reducido a cascarón sin privilegios ni default privileges, y meta-test permanente: NINGÚN rol de runtime con BYPASSRLS. Publisher actual = log estructurado (sandbox; la entrega real llega con F2-12/F3-07 — sin capacidades declaradas de más). 12 tests de integración del relay incl. 2 workers concurrentes sin doble entrega. Suite total: **195/195**.
- **F2-04 completado (2026-07-04)**: Chart of Accounts ejecutable — `CHART_OF_ACCOUNTS` con las 13 cuentas del diseño (semántica contable verificada por test), `PostingService.ensureChart` (aprovisionamiento idempotente por merchant+moneda: 8 platform-scope + 5 merchant-scope) y reglas de posting tipadas: `capturePayment` (modelo bruto sandbox v1, 5 asientos, margen Fluvia = Ff−Fp), `releaseSettlement`, `requestRefund`, `settleRefund`. Combinaciones fuera de catálogo irrepresentables. **Desviación registrada**: captura en 1 tx atómica (no 2 como el borrador de Fase 0) — atomicidad > trazabilidad-por-partes; doc actualizado como espejo del código; pricing final depende de PEND-002. 8 golden tests con montos exactos incl. ciclo completo captura→liquidación→refund con cero drift. Suite total: **162/162**.

## Resumen ejecutivo

Fluvia es una capa de software y orquestación de pagos (no banco, no custodio, no procesador certificado). La Fase 0 entregó el paquete completo de decisión (auditoría del prompt, PRD, arquitectura, ledger, FSMs, estrategias, threat model, ADRs, backlog+DAG, gates) validado con un spike contra PostgreSQL 16 real. La Fase 1 arrancó con la plataforma mínima: apps `api`/`worker` con configuración tipada que falla rápido, y pipeline de CI completo con Postgres real, validación de migraciones, secret scanning, audit de dependencias y SBOM.

## Evidencia del incremento F1-01/F1-02 (formato §47, 2026-07-04)

```
pnpm lint            → OK (ESLint 9 flat config; no-explicit-any=error)
pnpm format:check    → OK (Prettier)
pnpm build           → 5/5 paquetes typecheck OK
pnpm test            → 46/46 PASS
                       config  6/6  (defaults, coerción, anti-mezcla de credenciales)
                       db      9/9  (RLS, inmutabilidad, authenticate_api_key — PG16 real)
                       money  20/20
                       api     7/7  (health, ready contra BD real, ready 503 con BD caída,
                                     correlation-id con sanitización, sobre de error 404)
                       worker  4/4  (checkReady rol worker, heartbeat, stop limpio, start idempotente)
Smoke test manual    → servidor arrancado: /health OK, /ready OK (BD real),
                       404 con sobre estable, x-request-id eco verificado
```

## Estado por componente

| Componente                                                                                                                                                                                                                                                                                                                                                    | Estado real                                                                               |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| Documentación Fase 0 (30 entregables §52)                                                                                                                                                                                                                                                                                                                     | Completado y aprobado                                                                     |
| `packages/money`, `packages/db` (spike)                                                                                                                                                                                                                                                                                                                       | Completado; promoción formal en F2-01/F1-03                                               |
| `packages/config`                                                                                                                                                                                                                                                                                                                                             | **Completado** (F1-01)                                                                    |
| `apps/api` esqueleto (health/ready, correlation, sobre de error)                                                                                                                                                                                                                                                                                              | **Completado** (F1-01) — sin recursos de negocio aún                                      |
| `apps/worker` esqueleto (readiness, heartbeat, shutdown)                                                                                                                                                                                                                                                                                                      | **Completado** (F1-01) — sin consumidores aún                                             |
| CI (`.github/workflows/ci.yml`)                                                                                                                                                                                                                                                                                                                               | **Completado** (F1-02) — pendiente de verse verde en GitHub Actions en el primer push     |
| `packages/identity` + migración 0003 (organizations/users/memberships/merchants)                                                                                                                                                                                                                                                                              | **Completado** (F1-03)                                                                    |
| `packages/auth` + migración 0004 + endpoints `/v1/auth/*`                                                                                                                                                                                                                                                                                                     | **Completado F1-04a** (registro, verificación, login, lockout, sesiones)                  |
| RBAC + API keys + endpoints organizations/merchants/api-keys + /v1/account (F1-04c)                                                                                                                                                                                                                                                                           | **Completado**                                                                            |
| `packages/audit` + migración 0006 + endpoint de auditoría (F1-05)                                                                                                                                                                                                                                                                                             | **Completado**                                                                            |
| Suite tenant-escape + meta-tests + bypass controlado (F1-06)                                                                                                                                                                                                                                                                                                  | **Completado** — Gate Multi-tenant 🟢 técnico                                             |
| `@fluvia/ledger` (posting normativo + Chart of Accounts + reglas tipadas, F2-03/04)                                                                                                                                                                                                                                                                           | **Completado** — reforzado por lote AUD-1 (guardas de saldo, FK compuesta, huella causal) |
| Integración Auditoría v1 (plan + registro de cierre + lote AUD-1 + migración 0008)                                                                                                                                                                                                                                                                            | **Completado 2026-07-04**                                                                 |
| `@fluvia/events` (envelope común) + `@fluvia/outbox` (relay + replay auditado) + rol `fluvia_relay` (F2-11, ADR-0011)                                                                                                                                                                                                                                         | **Completado 2026-07-04**                                                                 |
| `@fluvia/inbox` (ingesta firmada + dedup + procesador + DLQ + replay) + rol `fluvia_inbox` (F2-12)                                                                                                                                                                                                                                                            | **Completado 2026-07-04**                                                                 |
| Rebuild/drift de proyecciones (F2-05) + invariantes SQL externas en CI (F2-06)                                                                                                                                                                                                                                                                                | **Completado 2026-07-04**                                                                 |
| Compensaciones vía servicio (F2-07) + suite formal de concurrencia (F2-08) — **Gate Ledger 🟢 técnico**                                                                                                                                                                                                                                                       | **Completado 2026-07-04**                                                                 |
| Taxonomía de errores del API con catálogo versionado + contract tests (F1-08)                                                                                                                                                                                                                                                                                 | **Completado 2026-07-04**                                                                 |
| `@fluvia/idempotency` + crash-recovery (F2-09/10) — **Gate Idempotencia 🟢 técnico; Fase 2 completa**                                                                                                                                                                                                                                                         | **Completado 2026-07-04**                                                                 |
| MFA TOTP + step-up + rate limiting (F1-04b) — **AUD-P1-006 cerrado; 5/5 P1 del auditor**                                                                                                                                                                                                                                                                      | **Completado 2026-07-04**                                                                 |
| Observabilidad base (F1-07) — métricas Prometheus API+worker, health/metrics del worker, alertas baseline                                                                                                                                                                                                                                                     | **Completado 2026-07-05**                                                                 |
| Purga auditada de datos técnicos + guard de roles (F1-09) — **AUD-P2-008 cerrado; R-12 mitigado**                                                                                                                                                                                                                                                             | **Completado 2026-07-05**                                                                 |
| Seeds deterministas de demo (F1-10) — `pnpm seed`, solo local/test                                                                                                                                                                                                                                                                                            | **Completado 2026-07-05**                                                                 |
| API keys HMAC server-side (AUD-P2-015) — prerequisito de PEND-006                                                                                                                                                                                                                                                                                             | **Completado 2026-07-05**                                                                 |
| Núcleo de pagos F3-01 — schema real + FSM en motor + servicio de transiciones (`@fluvia/payments-core`)                                                                                                                                                                                                                                                       | **Completado 2026-07-05**                                                                 |
| API de payment intents F3-02 — endpoints idempotentes create/get/list/cancel + OpenAPI parcial                                                                                                                                                                                                                                                                | **Completado 2026-07-05**                                                                 |
| Confirm + MockProvider + attempts con captura contable (F3-03 parte síncrona)                                                                                                                                                                                                                                                                                 | **Completado 2026-07-05**                                                                 |
| Flujo asíncrono PSE-like + ingesta HTTP firmada + primer handler del inbox (F3-03b)                                                                                                                                                                                                                                                                           | **Completado 2026-07-05**                                                                 |
| Robustez de attempts: barrido+salud de indeterminados, timeout, circuit breaker (F3-04)                                                                                                                                                                                                                                                                       | **Completado 2026-07-05**                                                                 |
| Webhooks salientes F3-07 — firma con rotación dual, SSRF guard con pinning, deliverer claim-lease, API de endpoints                                                                                                                                                                                                                                           | **Completado 2026-07-05**                                                                 |
| Refunds E2E F3-08 — asiento compensatorio por la vía normativa, dos fases idempotentes, desenlaces del dinero (aprobado/insuficiente/rechazo/desconocido)                                                                                                                                                                                                     | **Completado 2026-07-05**                                                                 |
| Customers F3-05a — recurso con metadata validada, update parcial, baja lógica, aislamiento por tenant (fundación de checkout)                                                                                                                                                                                                                                 | **Completado 2026-07-05**                                                                 |
| Checkout sessions F3-05b — recurso con FSM en el motor, create idempotente, client_secret hasheado, TTL acotado                                                                                                                                                                                                                                               | **Completado 2026-07-05**                                                                 |
| Backend del flujo alojado F3-05c-i — retrieval por client_secret + completado/expiración con eventos checkout_session.*                                                                                                                                                                                                                                       | **Completado 2026-07-05**                                                                 |
| Entrega garantizada de eventos de checkout F3-05c-ii — sweep_checkout_sessions() + watchdog                                                                                                                                                                                                                                                                   | **Completado 2026-07-05**                                                                 |
| Confirm alojado (backend) F3-05c-iii — POST /confirm por client_secret, dos fases, idempotente                                                                                                                                                                                                                                                                | **Completado 2026-07-05**                                                                 |
| UI de checkout alojada F3-05c-iv — apps/checkout (Next.js 15, i18n, WCAG AA), E2E de navegador verificado local                                                                                                                                                                                                                                               | **Completado 2026-07-05**                                                                 |
| Payment links F3-06 (backend) — plantilla que genera checkout sessions bajo demanda, apertura pública, reutiliza F3-05                                                                                                                                                                                                                                        | **Completado 2026-07-05**                                                                 |
| UI del payment link F3-06-b — `/l/[id]` genera la sesión server-side y redirige a `/c/{sid}#{secret}`, E2E de navegador verificado local                                                                                                                                                                                                                      | **Completado 2026-07-05**                                                                 |
| Cola de webhooks + reenvío `dead` F3-09a — read API (`/v1/webhook_events`) + `resend` auditado (clona el muerto como pending fresco), cierra diferido de F3-07                                                                                                                                                                                                | **Completado 2026-07-05**                                                                 |
| Plano de lectura del dashboard F3-09b-i — `/v1/organizations/:orgId/*` por sesión+membresía (`payments:read`), reutiliza servicios/serializers del plano API key                                                                                                                                                                                              | **Completado 2026-07-05**                                                                 |
| Frontend del dashboard F3-09b-ii — `apps/dashboard` (Next.js 15), login por sesión (cookie httpOnly) → panel de las 5 vistas de lectura, E2E de navegador verificado local                                                                                                                                                                                    | **Completado 2026-07-05**                                                                 |
| Reenvío `dead` desde el panel F3-09b-iii — permiso RBAC `webhooks:manage` + ruta de sesión + botón «Reenviar» (client), E2E de navegador verificado local                                                                                                                                                                                                     | **Completado 2026-07-05**                                                                 |
| SDK TS del OpenAPI F3-10 — `@fluvia/sdk`, cliente tipado del plano de API key, contract test SDK⇄spec + e2e contra el API real en proceso                                                                                                                                                                                                                     | **Completado 2026-07-05**                                                                 |
| CORS + security headers del API F3-11a — allowlist explícita + preflight + CSP/HSTS/nosniff en toda respuesta                                                                                                                                                                                                                                                 | **Completado 2026-07-05**                                                                 |
| Empaquetado de despliegue F3-11b — Dockerfile multi-stage (no-root) + compose profile `app` (migrate→api+worker), verificado con Docker real local                                                                                                                                                                                                            | **Completado 2026-07-05**                                                                 |
| Motor de conciliación F4-01a — `@fluvia/reconciliation`: casa intentos `succeeded` vs settlement report del proveedor, clasifica 4 discrepancias, RLS+append-only                                                                                                                                                                                             | **Completado 2026-07-05**                                                                 |
| Gestión de conciliación F4-01b — API de API key (crear reporte / cargar líneas / reconcile / resumen / discrepancias)                                                                                                                                                                                                                                         | **Completado 2026-07-05**                                                                 |
| Vista de conciliación en el dashboard F4-01c — plano de lectura por sesión + páginas `/o/:orgId/reconciliation[/:reportId]`, E2E de navegador verificado local                                                                                                                                                                                                | **Completado 2026-07-05**                                                                 |
| Conciliación batch/continua F4-02 — `ReconciliationWatchdog` + `sweep_settlement_reports()` (0028, definer/worker): concilia reportes con periodo cerrado cross-tenant, lease + idempotencia + métricas + alerta de discrepancias                                                                                                                             | **Completado 2026-07-06**                                                                 |
| Casos operativos F4-03a — `operational_cases` (0029) + trigger de materialización por discrepancia + `OperationalCaseService` (ciclo open→ack→resolved, documental) + plano API key                                                                                                                                                                           | **Completado 2026-07-06**                                                                 |
| Ajuste monetario con four-eyes F4-03b (Nivel C) — `case_adjustments` (0030, four-eyes por CHECK) + `PostingService.postReconAdjustment` (asiento recon.differences↔suspense) + `CaseAdjustmentService` (propose/approve/reject, atómico)                                                                                                                      | **Completado 2026-07-06** (motor)                                                         |
| Casos+ajustes por sesión F4-03c (backend) — permiso `reconciliation:manage` + rutas de sesión `operational_cases`/`case_adjustments` con four-eyes real sobre HTTP (dos miembros)                                                                                                                                                                             | **Completado 2026-07-06**                                                                 |
| UI del panel de casos/ajustes F4-03c-ii — lista + detalle con evidencia de discrepancia y formulario de ajuste (proponer/aprobar/rechazar con four-eyes visible), jsdom+axe                                                                                                                                                                                   | **Completado 2026-07-06**                                                                 |
| Panel admin F4-04 — enlaces «Comercios» (`/o/:orgId/merchants`) y «Eventos» (gateado por `canReadAudit`) en el dashboard                                                                                                                                                                                                                                      | **Completado 2026-07-06**                                                                 |
| Reserves/fees/payout-accounts F4-05 — reserves (0031) + payout accounts (0032) + **fees** (`FlatBpsFeeSchedule` 2%, cierra PEND-002, decisión #25)                                                                                                                                                                                                            | **Completado 2026-07-06**                                                                 |
| Runbooks operativos + DRILLS F4-06 — 7 runbooks (`docs/ops/runbooks/`) anclados a métricas/endpoints reales; **6 accionables ejecutados en drill** (`apps/api/drills/`, invariantes verdes): reconciliation 9/9, aged-disputes 7/7, indeterminate-payment 6/6, outbox-inbox-stuck 7/7, worker-down 5/5, webhook-dead-letter 5/5; audit-investigation de facto | **Completado 2026-07-07**                                                                 |
| Payouts (money out) F4-07 — motor (0033) + API key + watchdog (0034) + webhook del banco + dashboard UI + re-drive de `requested` atascados (0035)                                                                                                                                                                                                            | **Completado 2026-07-06**                                                                 |
| Disputas/chargebacks (money clawed back) F4-08 — motor+FSM (0036) + API key + webhook del banco (0037, idempotencia por `provider_ref`) + dashboard UI + acción de evidencia por sesión                                                                                                                                                                       | **Completado 2026-07-07**                                                                 |
| Entrega de eventos de dinero F4-09 — `payout.*`/`dispute.*` graduados a topics activos de webhook (meta-test doc↔código + E2E)                                                                                                                                                                                                                                | **Completado 2026-07-06**                                                                 |
| Observabilidad + watchdog de disputas F4-10 — `sweep_disputes()` (0038, health-only) + `DisputesWatchdog` + gauges `fluvia_disputes_held`/`_aged` + alerta + runbook `aged-disputes.md`                                                                                                                                                                       | **Completado 2026-07-06**                                                                 |
| **Fase 4 COMPLETA — Gate Conciliación cumplido; LISTO PARA RE-AUDITORÍA**                                                                                                                                                                                                                                                                                     | **2026-07-07**                                                                            |
| Restore drill (backup/restore) F6 · Gate Restore — `pg_dump`→restore→`FLUVIA_INVARIANTS_OK`+paridad→drift detectado+`rebuildProjection`→RLS preservado (`drill:restore`, PASS 7/7); runbook `backup-restore.md`. **Cierra la pata restante de AUD-P2-007**                                                                                                    | **Completado 2026-07-07**                                                                 |

## Bloqueadores

1. ~~PEND-002 (pricing)~~ **RESUELTO 2026-07-06** → fee de plataforma 2% por transacción (decisión #25; motor F4-05c construido). Ya no bloquea nada.
2. F0-VER (verificación en vivo de licencias de referencias) — antes de adoptar código externo.
3. Runs #1–#10 de CI verificados verdes (uno por commit hasta el auditado); cada incremento nuevo debe registrar su run verde antes de declararse completado. `pnpm audit` puede reportar advisories nuevos en cualquier momento — tratarlos según Gate Seguridad, no silenciarlos.
4. Congelado hasta re-auditoría + PEND-006: **exposición pública** de la API de pagos, checkout y webhooks (construidos en sandbox por decisión #24, jamás expuestos), y todo uso `live` (creación de keys live bloqueada por código). **Con la Fase 4 cerrada, la re-auditoría ya es solicitable por el propietario** (paquete de entrada en `docs/audits/`).

## Estado: Fase 4 CERRADA — lista para re-auditoría

**Fases 1–4 completas.** El **Gate Conciliación** de la Fase 4 está cumplido de punta a punta: conciliación (motor F4-01a + continua F4-02 + casos F4-03a + four-eyes F4-03b + UI F4-03c/ii), **panel admin** (F4-04), las abstracciones contables de dinero como recursos gestionados — **reserves/fees** (F4-05, cierra PEND-002 al 2%), **payouts money-out** (F4-07: motor + API + watchdog + webhook del banco + UI + re-drive) y **disputas money-clawed-back** (F4-08: motor+FSM + webhook del banco con idempotencia por `provider_ref` + UI + acción de evidencia por sesión) — la **entrega** de esos eventos al comercio por webhook firmado (F4-09), el **monitoreo** (F4-10: watchdog + gauges + alerta de disputas envejecidas) y, cerrando el criterio de salida, **los 7 runbooks operativos probados en drill** (F4-06a/b): los 6 accionables ejecutados contra Postgres 16 real con invariantes verdes (`reconciliation-discrepancy` 9/9, `aged-disputes` 7/7, `indeterminate-payment` 6/6, `outbox-inbox-stuck` 7/7, `worker-down` 5/5, `webhook-dead-letter` 5/5) + `audit-investigation` de facto. Todo en sandbox; nada expuesto públicamente ni `live`.

**Re-auditoría**: ya ocurrió (v2, read-only, `re-audit-v2-delta.md`) y sus hallazgos accionables están **remediados** (§Re-auditoría v2 del registro: 0 P0/P1 nuevos, V2-R1/R2/R4/R6 RESUELTO, resto documentado/diferido). Paquete: `docs/audits/audit-closure-register-v1.md` (estado vivo por hallazgo + §Re-auditoría v2 + §Evidencia CI run-a-run) + `audit-integration-plan-v1.md` + este STATE + `docs/ops/runbooks/`. Hallazgos v1: **0 P1 abiertos** (10/10 RESUELTOS), **0 P2 abiertos** — **AUD-P2-007 RESUELTO** (el restore drill F6 · Gate Restore quedó ejecutado, PASS 7/7).

**Fase 6 (hardening) en curso**: (1) **restore drill + worker-down drill** (Gate Restore/resiliencia) ✅ — el restore cierra AUD-P2-007 y **ambos corren AHORA en CI por commit** (TM-04; el worker-down —reinicio→drena backlog→detecta+repara drift, solo-PG, drenado dirigido por objetivo robusto al backlog heredado de la suite— era el último drill que quedaba manual) ⇒ Gate Restore/resiliencia 🟢 técnico y verde solo (resta solo la infra de backup de prod); (2) **threat model actualizado** ✅ — `docs/security/threat-model.md` reescrito por STRIDE contra el sistema implementado (89 controles citados) con el **backlog de riesgo residual F6** en su §5 (fuente única de las siguientes patas de endurecimiento); de paso se corrigió un doc-drift (`multi-tenancy.md §4` afirmaba `fluvia_worker` con BYPASSRLS — falso desde ADR-0011); (3) **TM-01** ✅ — meta-test que convierte la no-negatividad del ledger en gate de cobertura; (4) **TM-06** ✅ — guard de datos de tarjeta CONSTRUIDO (`card-data-guard.ts` + hook `preValidation`: PAN por IIN+Luhn / campos de tarjeta/CVV por nombre → 422 `card_data_not_allowed` + incidente sin el valor + métrica; 12 tests, cero falsos positivos en la suite de 193) — `pci-scope.md` §3 pasa de afirmación a capacidad probada. (5) **TM-02** ✅ — el step-up ya NO es no-op sin MFA: re-autenticación por password (`/v1/auth/step-up/password`, `sessions.password_verified_at`, migración 0040; comparte el lockout del login, auditada; el password jamás sustituye al TOTP con MFA — probado sobre HTTP). (6) **TM-05** ✅ — derecho al olvido por pseudonimización IMPLEMENTADO para customers (`CustomerService.erase` + `POST /v1/customers/:id/erase` + SDK + OpenAPI: PII sobrescrita irreversiblemente, fila/FKs/contabilidad intactas, audit `customer.pii_erased` riesgo alto en la misma tx, idempotente, probado sobre HTTP incl. no-fuga en ninguna columna). (7) **TM-03** ✅ (limiter) — `RedisFixedWindowLimiter` compartido entre instancias (Lua INCR+PEXPIRE atómico, fail-open logueado, dep `redis` exacta + service container redis:7 en CI; e2e: dos apps Fastify comparten presupuesto → 429 en la otra instancia); el circuit breaker del proveedor queda como seguimiento al escalar el worker. **Con esto los 6 P2 del threat model §5 están cerrados.** (8) **Bundle SSRF/webhooks** ✅ — cierra **V2-N2** de la re-auditoría v2 (V2-N4 sigue diferido/documentado, con su invariante núcleo DB-enforced): el target SSRF lleva TODAS las IPs validadas y el deliverer hace failover entre ellas SOLO cuando la conexión jamás se estableció (cap 3, sin re-resolver; jamás si el socket conectó o hubo respuesta HTTP — ese borde de doble entrega lo encontró la revisión adversarial del bundle y se corrigió antes de mergear), `rejectUnauthorized:true` explícito probado contra un receptor TLS self-signed (que jamás ve el payload firmado, aun con `NODE_TLS_REJECT_UNAUTHORIZED=0`), rate limit por IP del ingest público en `onRequest` (mismo backend inyectable de TM-03; keying por `req.ip` ⇒ `trustProxy` acotado es bloqueante de PEND-006, registrado en threat model §5) y test HTTP del body >1 MiB (413, cero persistencia). (9) **Bundle de cadena de suministro** ✅ — la fila «Secretos/cadena» del §5 cerrada salvo secret-manager: **grype gated en CI** (rompe el build ante High/Critical no aceptadas; catalogando el MISMO árbol que el SBOM publicado — el gate es real; el SBOM sigue siendo el inventario para el auditor), registro de aceptación versionado (`.grype.yaml` vacío + proceso en `docs/security/vulnerability-acceptance.md`), **`.gitleaks.toml` versionado** (excepciones por code review), **imagen base pineada por digest**, **Dependabot semanal** (npm agrupado + actions + docker), y **redacción del logger ampliada y PROBADA** (`log-redaction.test.ts`: paths nuevos `x-checkout-client-secret`/`set-cookie`, objeto `LOG_REDACT` compartido app↔test + no-fuga end-to-end). **SHA-pinning COMPLETO de la cadena de CI/build** ✅ — las 6 actions de `ci.yml` ancladas por SHA de commit (comentario de versión para Dependabot) y el frontend BuildKit `docker/dockerfile` (que BuildKit descarga y EJECUTA) anclado por digest; cada pin CONGELA el commit/digest que el tag mutable resuelve hoy (freeze verificado tag-a-tag, no upgrade), cerrando en toda la cadena el vector ya cerrado en la imagen base; con esto la fila «Secretos/cadena» del §5 queda cerrada salvo secret-manager (ADR). (10) **Suite SQLi/parametrización + roles deny-by-default** ✅ — respalda el límite documentado del RLS (fila Multi-tenant del §5): candado ESTÁTICO (`sql-parameterization.test.ts` — en un literal SQL de sentencia completa toda `${}` es un identificador vetado o el número de config de `SET LOCAL`, nunca un valor; y prohíbe concatenar o componer queries desde fragmentos `${}` iniciales; + lint de que ninguna ruta instancia el pool admin), suite BEHAVIORAL (`sql-injection.test.ts` — payloads hostiles round-trip como dato, tabla intacta, RLS respetado, validación Zod bloquea los campos tipados) y meta-tests de roles DENY-BY-DEFAULT (enumeran TODOS los `fluvia_%` de `pg_catalog`; rol nuevo = build roto; ninguno super/bypassrls/createrole ni con DELETE/TRUNCATE EFECTIVO —vía `has_table_privilege`, cubre grants a PUBLIC y heredados—). (11) **Idempotencia endurecida** ✅ — ENDURECE la fila del §5 (mecanismo hecho; la fila queda «Endurecido», no cerrada — el valor de retención es una decisión del propietario): retención de keys AHORA configurable (`IDEMPOTENCY_RETENTION_HOURS`, default 24 h, fijada explícita en `expires_at` por el servicio) con la regla documentada «>= ventana de retry del cliente» (el valor final lo fija el dueño antes de PEND-006), + `IdempotencyWatchdog`/`sweep_idempotency_orphans()` (migr. 0041) que barren y ALERTAN sobre claims `in_progress` envejecidos (>1 h) — huérfanos que bloqueaban su key hasta la purga sin visibilidad; gauges + alerta baseline. (12) **Auth — ciclo de vida de sesión endurecido** ✅ — endurece PARCIAL la fila Auth del §5 con las dos piezas sin dependencia de email: **idle-timeout** de sesión (`SESSION_IDLE_TIMEOUT_MS`, default 30 min; `authenticateSession` invalida una sesión ociosa antes del expiry absoluto de 24 h y refresca el reloj en cada uso) y **`POST /v1/auth/logout-all`** (revoca TODAS las sesiones del usuario, auditado — el control que faltaba y el hook para revoke-all-al-cambiar-credencial). Diferido HONESTAMENTE por acoplamiento: neutralizar el oráculo de enumeración de `register` y el flujo de recovery necesitan el **canal de email** (sin él, un register neutral dejaría al usuario legítimo sin señal ni cuenta), y revoke-all-automático necesita primero un flujo de cambio de credencial; rotación de pepper/MFA-key es pata operacional. (13) **Ledger — hash-chain de tamper-evidence** ✅ (migr. 0042) — ENDURECE la fila Ledger del §5: el «superuser salta triggers» pasa de indetectable a DETECTABLE. Checkpoints append-only encadenados por sha256 sobre `ledger_entries`+cabecera (orden total por `seq` GENERATED ALWAYS), sellado LAZY en dos fases con **horizonte de txid** (un candidato solo finaliza cuando toda tx en vuelo al registrarlo terminó — cero falsos positivos por commits fuera de orden, probado con una tx larga abierta; cero contención en el posting), sellador `LedgerCheckpointer` en el worker (gauges `fluvia_ledger_chain_*`) y verificación como check **[7]** de `verify-ledger-invariants.sql` (autocontenido: CI por commit + copia del restore drill — la cadena sobrevive pg_dump/restore y se verifica sobre el backup). Dientes probados (`ledger-hash-chain.test.ts`, 10 tests): el ENCUBRIMIENTO PERFECTO (borrar una tx balanceada completa — invisible para [1..6]) rompe [7]; también editar un asiento o un checkpoint sellado; round-trip de reparación byte-exacta (lección: capturar `created_at::text` — el Date de JS trunca microsegundos). Límites honestos documentados: tamper-EVIDENCE (no prevention); borrar la cadena ENTERA o TRUNCAR el sufijo = [7] verde trivial → alerta de gauge-que-cae + anclaje externo como extensión. **Endurecido por revisión adversarial** (18 hallazgos, 5 dimensiones; commit de remediación aparte): advisory lock que serializa selladores concurrentes (evita forjar un checkpoint con prev_hash equivocado — [7] roto permanente), `t.tenant_id` en el canónico (re-atribuir una cabecera sellada era invisible), auto-sanado tras DR (candidato con horizon de otro cluster se evicta en vez de atascar el sellado), gauge `fluvia_ledger_chain_unsealed_seq` + alerta de estancamiento (el sellador falla en silencio), y los tests de dientes migrados a `session_replication_role=replica` de SESIÓN (el `DISABLE TRIGGER` global corría con los ~9 archivos de test en paralelo — CI-breaking; verificado 3× en paralelo con la cadena intacta). (14) **Ledger — anclaje EXTERNO del chain_hash** ✅ (migr. 0043) — cierra el hueco honesto de [7]: borrar la cadena ENTERA o truncar su sufijo HASTA el último tip anclado dejaba [7] verde trivial (solo lo cubría la alerta de gauge-que-cae). `anchor_ledger_chain()` (DEFINER, EXECUTE solo del worker) publica el tip (`upto_seq`, `chain_hash`) a un almacén append-only SEPARADO (`ledger_chain_anchors` + evento `ledger.chain_anchored` en `audit_events` — segundo trail que viaja por el canal de exportación de auditoría), y el nuevo check **[8]** de `verify-ledger-invariants.sql` rompe DURO cuando un anchor no tiene checkpoint vivo (truncado/borrado) o su `chain_hash` difiere (re-sellado) — CI por commit + copia del restore drill (los anchors sobreviven pg_dump/restore, probado). `LedgerChainAnchorer` en el worker LOGUEA el tip (artefacto exportable) + gauges `fluvia_ledger_chain_anchors_total`/`_anchored_upto_seq`; el estancamiento del anchorador se detecta CRUZANDO gauges (`_sealed_upto_seq` sigue creciendo aunque el anchorador caiga — alerta observability §4). Dientes probados: borrar la cadena ENTERA deja [1..7] verdes y SOLO [8] rompe (`ledger-hash-chain.test.ts`), + min-privilegio y append-only. **Endurecido por revisión adversarial** (5 lentes + verificación escéptica): se eliminó un gauge `anchor_gap` INERTE (el anchorador ancla el tip actual en UNA llamada ⇒ su rezago auto-reportado es siempre 0; el estancamiento se detecta cruzando gauges) y se precisó el alcance honesto — [8] protege HASTA el último anchor (el horizonte aún no anclado lo cubre la alerta de rezago), y el anchor vive EN la BD: un superusuario que manipule los TRES almacenes (cadena + anchors + audit) sigue indetectable sin una copia OFFSITE (responsabilidad del operador; no hay infra externa en sandbox — V4 Nivel A, sin capacidad simulada). (15) **Secretos/cadena — ADR de secret-manager** ✅ (ADR-0012, **Propuesto**) — cierra la parte «ADR» del residual «Secretos/cadena» del §5 (regla 6 de `secrets-management.md`). Decide el ENFOQUE agnóstico de proveedor: el secret manager inyecta los secretos como **variables de entorno al arranque** (la app conserva su contrato de env validado + anti-mezcla; **cero SDK en runtime**, cero lock-in — coherente con ADR-0001), imagen Docker sin secretos, y rotación por **clave versionada** con re-hash/re-cifrado perezoso (el patrón v1→v2 que las API keys ya implementan) por clase de secreto. **Límite honesto (Nivel A)**: es artefacto de DISEÑO — no hay integración de secret manager en el sandbox (no hay infra externa; no se simula); el runtime + el vendor concreto se fijan en el despliegue real (F5). (16) **Secretos/cadena — rotación EFECTIVA de la clave de cifrado de webhooks** ✅ (ADR-0012, PRIMERA pata de rotación con CÓDIGO) — `WEBHOOK_SECRET_ENC_KEY` ya se rota SIN downtime ni cambio de esquema: un **keyring** (clave actual + retiradas) descifra probando cada clave y el **tag de AES-GCM disambigua** (una clave ajena jamás autentica), así que no hay que versionar el blob ni añadir columnas; el deliverer usa el keyring (`WEBHOOK_SECRET_ENC_KEY_RETIRED` en config, con anti-mezcla) y `reencryptWebhookSecrets` (CLI admin `pnpm --filter @fluvia/webhooks rotate:webhook-key`, cross-tenant, transaccional, IDEMPOTENTE) migra los blobs a la clave actual para poder eliminar la retirada. Runbook `webhook-enc-key-rotation.md` (**4 fases sin ventana**: pre-sembrar la clave nueva como solo-descifra en TODA la flota ANTES de promoverla → intercambio → barrido → eliminar la retirada tras el `--check`; verificación, rollback: NUNCA borrar una clave con blobs vivos). Probado vs PG real (`crypto.test.ts` 7 + `rotate.test.ts` 6): trial-decrypt por tag, re-cifrado que preserva el claro cross-tenant, fila mixta (secret actual + prev retirado), idempotencia, filtro por tenant; el secreto `whsec_` del comercio NO cambia (solo su cifrado en reposo). Sin migración (keyring por config). **Endurecido por revisión adversarial** (4 lentes + verificación escéptica): (a) el barrido pasó a **lotes por keyset con cotas de tiempo por transacción** (statement/lock/idle como `withTenantTransaction` — el camino crudo `pool.connect()` no las heredaba y podía colgar reteniendo los locks `FOR UPDATE` de toda la tabla, bloqueando la gestión de endpoints durante el barrido entero); los locks se sueltan entre lotes; (b) **resiliencia por fila**: un blob que ninguna clave del keyring descifra (clave ausente / corrupto) se REPORTA en `failed`/`failedIds` y el barrido SIGUE, en vez de que una sola fila aborte la plataforma; (c) **prueba de vuelo de SOLO LECTURA** (`inspectWebhookSecretKeys` / `rotate:webhook-key --check`: `underRetired=0` y `undecryptable=0`) como gate independiente antes de eliminar una clave (sin mutar); (d) `tryDecrypt` deja surgir el error de FORMATO de clave (no lo enmascara como «clave equivocada»). El **hueco de honestidad del «sin downtime»** —una ventana de indescifrable en el rolling restart de la flota mixta si se promovía la clave nueva sin pre-sembrarla— se CERRÓ con la pre-siembra de 4 fases (toda instancia viva ya tiene la clave nueva en su keyring antes de que alguna cifre con ella). (17) **Secretos/cadena — rotación EFECTIVA de la clave de cifrado de secretos TOTP** ✅ (ADR-0012, SEGUNDA pata de rotación con CÓDIGO) — `MFA_SECRET_KEY` ya se rota con el MISMO mecanismo probado en webhook-enc, reutilizando el enfoque: un **keyring** (clave actual + retiradas) descifra probando cada clave y el **tag de AES-GCM disambigua**; `MFA_SECRET_KEY_RETIRED` en config (anti-mezcla, mismo `required()`/eval-order que la de webhooks); el `AuthService` descifra `users.totp_secret_enc`/`totp_pending_secret_enc` con el keyring (cifra los enrolamientos nuevos con la actual); y `reencryptMfaSecrets` (CLI con el rol **`fluvia_auth` de MÍNIMO PRIVILEGIO** — NO superusuario, a diferencia del barrido de webhooks: `users` es global y `fluvia_auth` ya lee/escribe toda la tabla, así que RLS FORCE no es barrera; `pnpm --filter @fluvia/auth rotate:mfa-key`, **por lotes por keyset con cotas de tiempo por tx**, **resiliente a filas indescifrables** `failed`/`failedIds`, + **prueba de vuelo `--check` de SOLO LECTURA** `inspectMfaSecretKeys`) migra los secretos a la actual para poder eliminar la retirada. Runbook `mfa-key-rotation.md` a **4 fases sin ventana** (pre-siembra de la nueva como solo-descifra en toda la flota ANTES de promoverla). El secreto TOTP en claro NO cambia (los códigos del Authenticator verifican igual). Probado vs PG real (`mfa-crypto.test.ts` 7 + `mfa-rotate.test.ts` 8): trial-decrypt por tag, re-cifrado que preserva el claro, fila mixta, idempotencia, filtro por usuario, resiliencia `failed`, inspección de solo-lectura, y **end-to-end por el `AuthService` real** (un usuario bajo la clave RETIRADA se autentica en la ventana de rotación; tras el barrido se autentica con SOLO la actual — login→reto MFA→verificación de TOTP). Sin migración (keyring por config). El barrido re-cifra TODOS los usuarios con secreto TOTP (incl. soft-deleted) para que retirar la clave sea seguro. (18) **Secretos/cadena — rotación EFECTIVA del pepper HMAC de API keys** ✅ (ADR-0012, migr. 0044, TERCERA y última pata de rotación con CÓDIGO — cierra las TRES) — `API_KEY_HMAC_SECRET` ya se rota, con mecánica DISTINTA a las otras dos porque es HMAC **ONE-WAY**: un hash NO se puede re-hashear a un pepper nuevo sin el secreto en claro (que solo el llamador tiene al autenticar). Por eso el re-hash pepper-viejo→nuevo es **PEREZOSO**, dentro de `authenticate_api_key` (extiende el upgrade v1(sha256)→v2(hmac) que ya existía): el caller (`security.ts`) computa el hmac con el pepper ACTUAL + cada RETIRADO + el sha256 legado; la fila matchea por cualquiera y se RE-HASHEA al actual, fijando su **huella**. Para el retiro VERIFICABLE se añade `api_keys.key_hash_pepper_fp` — una huella one-way (128 bits) del pepper que produjo el `key_hash`. Límite honesto (surgido en revisión): SÍ es una exposición NUEVA — un oráculo que permite CONFIRMAR un pepper adivinado offline con un dump (el `key_hash` no daba eso, exige el secreto en claro), pero inexplotable contra un pepper aleatorio de 256 bits (confirmar exige adivinar los 256 bits; no estrecha la búsqueda) — trade-off aceptado por el gate verificable. `API_KEY_HMAC_SECRET_RETIRED` en config (anti-mezcla, mismo eval-order). El gate `--check` (`inspectApiKeyPepper`, SOLO LECTURA, rol admin — `api_keys` es RLS por-tenant como webhooks, cruzar tenants exige superusuario) cuenta las keys VIVAS por pepper (`underCurrent`/`underRetired`/`unmarked`/`unknown`); se retira un pepper cuando `underRetired=0 ∧ unmarked=0 ∧ unknown=0` — como NO hay barrido en bloque (one-way), las keys DORMIDAS bajo el viejo se **revocan+re-emiten** para llegar a 0. Un `backfillApiKeyPepperFp` (paso ÚNICO tras la migración) marca las filas v2 previas como bajo el actual. Runbook `api-key-pepper-rotation.md`: Fase 0 (backfill) + 4 fases sin ventana de autenticación (pre-sembrar el nuevo como solo-verifica → intercambio → ventana de re-hash perezoso → retirar el viejo tras el `--check`). Probado vs PG real (`api-keys.test.ts`, +4): create fija la huella; una key bajo el pepper RETIRADO autentica y se RE-HASHEA al actual (y luego autentica con solo el actual); el gate baja a 0 al re-hashear; un pepper fuera del keyring se reporta `unknown` y NO autentica; el backfill marca las filas sin huella. **Con esto las TRES patas de rotación de secretos de ADR-0012 quedan HECHAS.** (19) **Ledger — no-negatividad del motor a nivel BD (check [9])** ✅ — cierra el residual homónimo de TM-01/§5 con la EVIDENCIA a nivel BD que faltaba: un nuevo check **[9]** en `verify-ledger-invariants.sql` (CI por commit + copia del restore drill) rompe si una cuenta PROTEGIDA del chart tiene `available`/`pending` < 0 en `balance_projections`. EXENTAS solo las transitorias (`suspense`/`recon.differences`, que `postReconAdjustment` —posting sin guard cuyos dos asientos son justo estas dos— puede dejar negativas por diseño; `reverseTransaction` también postea sin guard pero HOY no tiene llamador de producción, y si lo tuviera y dejara negativa una cuenta protegida, [9] lo DETECTARÍA correctamente); y quedan fuera las cuentas NO-chart (p. ej. `conc.*` de tests de concurrencia con asientos crudos: en prod toda cuenta nace del chart). Se eligió VERIFICACIÓN (detección), NO un trigger de runtime, A PROPÓSITO: el guard ya lo GARANTIZA en app bajo locks (`nonNegativeAccounts`, service.ts), y un trigger que aborte postings arriesgaría el motor de dinero por un control ya resuelto — [9] lo respalda como evidencia (mismo patrón tamper-evidence que [7]/[8]) sin ese riesgo, y complementa a [6] (que solo exige que la proyección COINCIDA con los asientos, no que sea >= 0). Dientes probados (`ledger-hash-chain.test.ts`, +2): un negativo en cuenta protegida (merchant.available) rompe [9]; un `suspense` negativo NO (exención). Verificado verde sobre la BD poblada por la suite entera y sobre la copia del restore drill; sin cambio de esquema ni de runtime. (20) **Revisión de seguridad interna v1** ✅ (`docs/audits/security-review-v1.md`) — self-review adversarial de **7 lentes** sobre el sistema endurecido (pre-re-auditoría; NO sustituye la auditoría externa v1, que sigue 0 P1/0 P2). Cerró **2 HIGH** (auto-enrolamiento de MFA desde una sesión secuestrada que reabría TM-02 → step-up fresco para enrolar/activar/deshabilitar MFA; bypass SSRF por literales IPv6 no canónicos → normalización a bytes en `isPrivateIp`), **3 MEDIUM** (shadowing de `pg_temp` en los 21 `SECURITY DEFINER` → `REVOKE TEMPORARY`, migr. **0045**; evasión del lockout MFA por reset del contador al primer factor; dedup de webhooks por PK de fila en vez del `event_id` estable del sobre) y varios **LOW/INFO** (guard de no-negatividad en `reverseTransaction`; validación UUID + entero-seguro de amounts; hex de firma del SDK; CSP en checkout+dashboard; `REVOKE` de writes sobre `users`; lock en `verifyProjection`), con **residuales documentados** CON razón (enumeración por lockout = tradeoff aceptado acotado por rate-limit; Origin-check/CSP-nonce/`Secure`-por-entorno/aserción de `normal_side` en startup → PEND-006/F5). Verificado: suite completa + invariantes [1]–[9] + drills worker-down/restore verdes; migr. 0045 idempotente. Restan las patas mayores de F6: load-soak-chaos + `trustProxy` acotado (PEND-006), copia OFFSITE de los anchors (operador), cifrado field-level de credenciales de proveedor (F5), + la infra de backup de producción. (21) **Load-soak-chaos — drill de resiliencia bajo carga+fallo** ✅ (`drill:load-chaos`, corre en CI el ÚLTIMO — el más destructivo) — cierra la última pata GRANDE de F6 buildeable en sandbox, y **ENCONTRÓ Y CORRIGIÓ UN CRASH REAL**: `createPool` no ataba un handler `error`, así que una caída de conexión (failover de PG / kill administrativo / partición de red / reinicio de la BD) sobre CUALQUIER conexión del pool tumbaba TODO el proceso (API o worker) por la excepción no capturada de node-pg — un blip transitorio era un caído total. Ahora se atan handlers a nivel de POOL (conexión ociosa) y de CLIENTE (en `connect`, para una conexión TOMADA cuya query muere en vuelo); las queries en vuelo siguen rechazando por su cuenta (no se tragan errores). El drill ATACA las redes de V2-R1 sobre PG16 real: (a) saturación de pool — 80 postings concurrentes sobre un pool de 4 → TODOS drenan sin cuelgue, conservación exacta; (b) caos de conexión — mata backends `fluvia_app` EN VUELO (`pg_terminate_backend`) → los postings abortan LIMPIO (rollback atómico por el trigger de balanceo diferido, cero asientos huérfanos ni drift), el pool se recupera solo; (c) idempotencia bajo caos — 12 claves × 5 dups concurrentes con kills → cada clave aplicada A LO SUMO una vez (unicidad DB-enforced); (d) invariantes finales — Σdébitos == Σcréditos + proyección == recomputo en todas las cuentas. Teeth: `pool-resilience.test.ts` (una conexión muerta en vuelo rechaza su query, NO crashea el proceso, el pool se recupera). **Honestidad (Nivel A)**: el throughput logueado es una MEDICIÓN del sandbox mono-instancia (pool de 4, PG local) — NO un SLO de producción ni afirmación de escala; el valor probado es la INTEGRIDAD del dinero bajo fallo abrupto, medida de verdad. Con esto **NO queda ninguna pata GRANDE de F6 buildeable en sandbox** — el residual es de infra/despliegue (F5: cifrado field-level; `trustProxy`+Origin-check+CSP-nonce → PEND-006), del operador (copia OFFSITE de anchors) o del propietario (re-auditoría). **Fase 5** (primer proveedor real en sandbox) sigue **bloqueada** por la verificación legal de la matriz de jurisdicción Colombia + F0-VER + PEND-004 (decisión del propietario). La exposición pública / `live` sigue congelada hasta PEND-006.

### Historial de incrementos (Fase 3–4)

**Los 5 hallazgos P1 de la auditoría están cerrados con evidencia — el criterio de re-auditoría se cumplió por completo**: es el momento de que el propietario la solicite (el paquete de entrada es `docs/audits/audit-closure-register-v1.md` + `audit-integration-plan-v1.md`). F1-07 (observabilidad base) quedó completado el 2026-07-05: la pata de observabilidad de AUD-P2-007 está cubierta (restore/runbooks siguen en F4-06/F6) y el drift watcher/relay ya alimentan métricas con alertas baseline definidas (`observability.md` §4). F3-08 quedó completado: el ciclo de dinero es reversible extremo a extremo (refunds con asiento compensatorio, desenlaces conocido/desconocido probados) y **AUD-P1-002 queda cerrado por completo** — el núcleo de pagos real (intents + attempts + confirm + refunds) existe y está probado. Con esto los incrementos de dinero de la Fase 3 (F3-01/02/03/04/07/08) están cerrados. F3-05c-iv quedó completado: **F3-05 (checkout session app) está COMPLETO** — customers, recurso de checkout sessions, flujo alojado (status/confirm por client_secret), entrega garantizada de eventos y la UI Next.js alojada, verificada en navegador real. Es también el **primer app frontend del monorepo** y establece el patrón (Next.js 15 App Router, route handlers como proxy, i18n propio, WCAG AA, tests jsdom+axe en CI + E2E de navegador local). F3-06 quedó completado (backend): los payment links generan checkout sessions pagables bajo demanda, reutilizando TODO F3-05; probado end-to-end (abrir link → pagar → completado). F3-06-b quedó completado: la página pública `/l/[id]` del app de checkout genera la sesión server-side y **redirige** el navegador a `/c/{sessionId}#{clientSecret}` (el `client_secret` en el fragmento del `Location`, fuera de los logs) — **el payment link cierra end-to-end para el comprador**, verificado en navegador real (`/l/{id}` → `$ 77.000` → Pagar → completado). Con esto la superficie de checkout+links de la Fase 3 está completa de cara al comprador. F3-09a quedó completado: primera rebanada del dashboard como capacidad backend NUEVA — el read API de la cola de webhooks (`/v1/webhook_events`) + el **reenvío manual auditado de eventos `dead`** (clona el muerto como un evento `pending` fresco, con audit `webhook_event.resent` atómico), que cierra el diferido de F3-07. F3-09b-i quedó completado: el **plano de LECTURA del dashboard por sesión + membresía** (`/v1/organizations/:orgId/*` con el nuevo permiso `payments:read`), que reutiliza los servicios y serializers del plano de API key — es el prerequisito honesto (Nivel A) de cualquier UI, porque el operador es humano (sesión) y los recursos hasta ahora solo se leían por API key. F3-09b-ii quedó completado: el **frontend del dashboard** (`apps/dashboard`, SEGUNDO app frontend del monorepo) — login por sesión con cookie httpOnly → panel de solo lectura de las 5 vistas de operación consumiendo el plano F3-09b-i server-side, verificado en navegador real (login → panel con intent `$ 123.000` y link `$ 45.000`). Con esto el dashboard de operación es funcional de cara al operador (lectura). F3-09b-iii quedó completado: el **reenvío de eventos `dead` desde el panel** (permiso RBAC `webhooks:manage` en el plano de sesión + ruta `POST /v1/organizations/:orgId/webhook_events/:id/resend` + botón «Reenviar» en la cola), verificado en navegador real (clic → 201 → cola con el pending fresco). **Con esto F3-09 (dashboard mínimo) está COMPLETO** — plano de lectura de operación + la primera acción de operación (reenvío), sobre datos reales, por sesión humana. F3-10 quedó completado: el **SDK TypeScript** (`@fluvia/sdk`) del plano de integración por API key, con un contract test que lo mantiene sincronizado con `openapi.v1.json` y un e2e contra el API real en proceso. F3-11a quedó completado: **CORS de allowlist + cabeceras de seguridad del API** (cierra la parte de API de AUD-P2-016). F3-11b quedó completado: el **empaquetado de despliegue** (Dockerfile multi-stage no-root + compose profile `app` con migrate→api+worker), verificado con Docker real local. **Con esto F3-11 y el hardening de la Fase 3 están COMPLETOS**, y la Fase 3 (recursos + checkout + links + webhooks + dashboard + SDK + hardening) queda cerrada como bloque de producto. F4-01a quedó completado: el **motor de conciliación (núcleo)** — `@fluvia/reconciliation` casa los intentos `succeeded` contra el settlement report del proveedor y clasifica las 4 discrepancias, per-tenant bajo RLS + append-only, probado vs PG real. F4-01b quedó completado: la **API de gestión de conciliación** (plano de API key). F4-01c quedó completado: la **vista de conciliación en el dashboard** (lectura por sesión + páginas `/o/:orgId/reconciliation`), verificada en navegador real — **con esto el bucle de conciliación está cerrado de cara al operador** (ingesta por API key → motor → resumen + discrepancias visibles en el panel). F4-02 quedó completado: la **conciliación batch/continua** — el `ReconciliationWatchdog` del worker invoca `sweep_settlement_reports()` (0028, SECURITY DEFINER/`fluvia_worker`) en un intervalo y concilia automáticamente cross-tenant los reportes con periodo cerrado (`open` + `period_end <= now()`), con lease (`FOR UPDATE SKIP LOCKED`) + idempotencia con la conciliación manual + métricas por resultado + **alerta ante discrepancias**, probado vs PG real — la conciliación pasa de "a demanda" a "continua". F4-03a quedó completado: los **casos operativos** — un trigger materializa un `operational_case` por cada discrepancia (para el motor y el barrido) y `OperationalCaseService` gobierna su ciclo `open→acknowledged→resolved` con auditoría, per-tenant, expuesto por el plano de API key; **resolver es DOCUMENTAL, no mueve dinero**. F4-03b quedó completado (MOTOR): el **ajuste monetario con four-eyes** — `case_adjustments` con four-eyes por CHECK en la BD + `PostingService.postReconAdjustment` (asiento compensatorio real `recon.differences↔suspense`) + `CaseAdjustmentService` (propone un humano, aprueba un segundo humano distinto sobre umbral, postea el asiento + resuelve el caso atómico); máquina/IA rechazada. Con esto el **pipeline de conciliación está cerrado end-to-end**: detectar (F4-01a) → continua (F4-02) → caso rastreable (F4-03a) → ajuste autorizado con doble control humano + asiento (F4-03b). F4-03c quedó completado (backend): **casos y ajustes por SESIÓN** — permiso `reconciliation:manage` + rutas de sesión para trabajar casos y autorizar ajustes con **four-eyes real sobre HTTP** (dos miembros distintos), verificado con dos usuarios `finance`. Con esto el pipeline de conciliación es operable por un humano de punta a punta salvo la UI. Próximo incremento propuesto: **F4-03c-ii — UI del panel de casos/ajustes** en `apps/dashboard`: lista de casos + detalle con la evidencia de la discrepancia y el formulario de ajuste (proponer/aprobar/rechazar con el flujo four-eyes visible), como F4-01c cerró la conciliación de cara al operador; server components + i18n es/en + WCAG AA + E2E de navegador local. ALTERNATIVA: **F4-06/F6 — restore + runbooks** (la pata operativa pendiente de AUD-P2-007: backup/restore probado + runbooks/drills). NOTA: superficie pública congelada hasta re-auditoría + PEND-006; se construye en sandbox. Decisiones humanas abiertas: PEND-004, PEND-006 (PEND-002/pricing resuelto en 2% — decisión #25, F4-05c).
