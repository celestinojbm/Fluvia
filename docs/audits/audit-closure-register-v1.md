# Registro de cierre — Auditoría Independiente v1

Estado: Activo · Se actualiza con CADA cambio de estado de un hallazgo. Reglas:

1. El informe original (`independent-audit-v1/`) es inmutable; este registro es la única fuente del estado vivo de cada hallazgo.
2. **Ningún hallazgo pasa a RESUELTO sin evidencia individual verificable** (código + test + run). Prohibido cerrar en lote.
3. Estados: `RESUELTO` (evidencia completa) · `MITIGADO` (riesgo reducido, resto planificado) · `CERRADO POR DECISIÓN` (decisión documentada, sin código pendiente) · `ACEPTADO` (riesgo aceptado registrado en RISKS) · `PLANIFICADO` (abierto, con ítem de backlog y fase) · `ABIERTO` (sin plan — no debe existir ninguno en este estado).

Contexto: commit auditado `a665b4f` == HEAD al iniciar la integración (cero drift). Lote de remediación inmediata = **AUD-1** (migración `0008_audit_remediations.sql` + fixes de servicio + tests + docs, este commit). Evidencia CI: la URL del run verde del lote se registra en §Evidencia CI al final cuando exista.

## Hallazgos P1

| ID | Estado | Fecha | Evidencia / referencia |
|----|--------|-------|------------------------|
| AUD-P1-001 (FK cuenta-tenant-moneda) | **RESUELTO** (lote AUD-1) | 2026-07-04 | Migración `0008` §1: `UNIQUE (id, tenant_id, currency)` + FK compuesta `ledger_entries_account_coherence_fk` con validación previa de datos. Tests negativos SQL crudo (incluso superuser): `packages/db/test/ledger-invariants.test.ts` → "AUD-P1-001: an entry CANNOT reference another tenant's account" y "entry whose currency differs from its account is rejected" |
| AUD-P1-002 (payments core inexistente) | **RESUELTO** (F3-01→F3-08, decisión #24) | 2026-07-05 | Núcleo de pagos real y probado: `payment_intents`+`payment_attempts` con FSM en el motor (0017), `refunds` con FSM en el motor (0020), `@fluvia/payments-core` (intents/confirm en dos fases/attempts contra MockProvider sync+async/refunds E2E), robustez de attempts (barrido+breaker, F3-04) y webhooks salientes (F3-07). Endpoints idempotentes create/get/list/cancel/confirm de intents (F3-02/03) y create/get/list de refunds (F3-08) sobre HTTP. El ciclo de dinero completo — cobro con captura contable atómica y refund con asiento compensatorio por la vía normativa — está probado contra PG real con balances exactos y cero drift. Desviaciones (checkout/customers) registradas en BACKLOG; NADA declara capacidad inexistente. Exposición pública sigue congelada hasta re-auditoría + PEND-006 (decisión #24) |
| AUD-P1-003 (idempotencia API) | **RESUELTO** (F2-09/F2-10) | 2026-07-04 | `@fluvia/idempotency`: claim de la key en la MISMA transacción que el efecto (crash pre-COMMIT ⇒ rollback conjunto; post-COMMIT ⇒ replay exacto persistido); hash canónico sha256; los 5 casos del contrato de `idempotency.md` §3 probados (incl. 409 `processing_in_flight` por lock_timeout y 422 `idempotency_key_reuse`); carrera 8→1; property efectos==1; HTTP end-to-end con el sobre del catálogo. **Gate Idempotencia 🟢 técnico**. Cableado a endpoints de pagos = F3-02 con el patrón del test HTTP |
| AUD-P1-004 (outbox sin relay) | **RESUELTO** (F2-11) | 2026-07-04 | `@fluvia/outbox`: claim-lease `FOR UPDATE SKIP LOCKED`, publicación fuera de la tx (Nivel A), backoff exponencial con jitter, veneno→dead, barrido de zombies, replay auditado. CA probados: 2 workers concurrentes sin doble entrega ("CA Gate: two concurrent relays never double-deliver"), poison→dead+last_error, lease expirado re-elegible. Wired en `apps/worker` (publisher de log en sandbox; entrega real = F2-12/F3-07) |
| AUD-P1-005 (inbox/webhooks entrantes) | **RESUELTO** (F2-12) | 2026-07-04 | Migración `0010` + `@fluvia/inbox`: firma HMAC-SHA256 (timestamp firmado, tolerancia, tiempo constante) verificada ANTES de persistir; dedup `UNIQUE (provider, provider_event_id)` probado race-safe (6 entregas concurrentes ⇒ 1 fila); procesador claim-lease (2 procesadores sin doble procesamiento); veneno→dead+DLQ con redacción probada (`card_token`→`[REDACTED]`); `ignored_out_of_order` terminal; replay auditado. Handlers de negocio = F3-03 (el desbloqueo que este hallazgo pedía) |
| AUD-P1-006 (rate limiting + MFA) | **RESUELTO** (F1-04b) | 2026-07-04 | PEND-005 decidida (TOTP + backup codes, sin SMS). Migración `0014` + `packages/auth`: TOTP RFC 6238 propio (vectores del RFC en tests), secreto cifrado AES-256-GCM (clave `MFA_SECRET_KEY` anti-mezcla), reto post-password 5 min de un solo éxito, **anti-replay por step probado**, backup codes de un solo uso (sha256), fallos MFA → mismo lockout que password (probado hasta bloquear); **step-up** con frescura 15 min aplicado a `keys:manage` (403 `mfa_step_up_required` probado sobre HTTP, refrescado vía `/v1/auth/mfa/step-up`); **rate limiting** por email/IP en `/v1/auth/*` (429 `rate_limited`+Retry-After probado; in-memory documentado Nivel C → store compartido pre-sandbox compartido). 23 tests nuevos |
| AUD-P1-007 (worker BYPASSRLS/grants) | **RESUELTO** (F2-11, ADR-0011; mitigado antes por 0008) | 2026-07-04 | Migración `0009`: `fluvia_relay` SIN BYPASSRLS (visibilidad cross-tenant por políticas RLS explícitas SOLO en `outbox_events`; UPDATE por columna a los 6 campos de despacho; sin INSERT/DELETE); `fluvia_worker` → cascarón: `NOBYPASSRLS` + revoke total (tablas, secuencias y default privileges de 0002). Meta-tests permanentes: "NO runtime role has BYPASSRLS", "fluvia_worker … ZERO table privileges", "fluvia_relay holds ONLY outbox_events SELECT + column-scoped UPDATE" |
| AUD-P1-008 (evidencia CI no reproducible) | **RESUELTO** (evidencia existía; auditor sin acceso) | 2026-07-04 | GitHub Actions workflow `CI` corre por commit: install, lint, format-check, typecheck, migrate ×2 (no-op check), tests contra PG16 real, gitleaks, `pnpm audit --audit-level high`, SBOM SPDX. Runs #1–#10 verdes; **run #10 = commit auditado `a665b4f`, conclusion=success**. Adopción de proceso: cada handoff/cierre registra la URL del run (este registro, §Evidencia CI) |
| AUD-P1-009 (idempotency_keys sin endpoint) | **RESUELTO** (lote AUD-1) | 2026-07-04 | Migración `0008` §2: columna `endpoint` + PK `(tenant_id, endpoint, key)`; tabla sin consumidores previos (cambio seguro). Test: `ledger-invariants.test.ts` → "AUD-P1-009: idempotency keys are scoped per endpoint". Doc `idempotency.md` ↔ schema vuelven a coincidir |
| AUD-P1-010 (posting sin validación de saldo) | **RESUELTO** (lote AUD-1) | 2026-07-04 | `PostTransactionInput.nonNegativeAccounts` verificado DENTRO de la tx bajo locks de cuenta (race-safe), `InsufficientBalanceError` → rollback total; `PostingService.twoLegged` protege la cuenta debitada (release/refund/settleRefund). Tests: `ledger-service.test.ts` → describe "AUD-P1-010" (sobregiro rechazado sin efectos, frontera exacta en 0 pasa, opt-in explícito) y golden `posting.test.ts` → "cannot release more than merchant.pending nor refund more than merchant.available" |

## Hallazgos P2

| ID | Estado | Fecha | Evidencia / referencia |
|----|--------|-------|------------------------|
| AUD-P2-001 (replay sin metadata causal) | **RESUELTO** (lote AUD-1) | 2026-07-04 | `service.ts replay()` compara `reason`, `source_type`, `source_id`, `reverses_tx_id` contra la fila persistida además de la firma de entries; divergencia ⇒ `IdempotencyConflictError`. Tests: "rejects key reuse when only the REASON differs" y "…only the SOURCE differs" |
| AUD-P2-002 (payment_intents mínima) | **RESUELTO** (F3-01, decisión #24) | 2026-07-05 | Migración 0017: modelo real (guard: la tabla debía estar vacía — verificado en la propia migración), 12 estados, FK compuesta intent↔merchant↔tenant (probado que un intent no puede referenciar el merchant de otro tenant ni como superusuario), CHECKs `captured ≤ amount` y `refunded ≤ captured` (probados), tabla `payment_attempts` con su FSM, `@fluvia/payments-core` con `PaymentIntentService` (RLS, lock de fila, evento outbox en la misma tx) |
| AUD-P2-003 (API keys live sin frontera) | **RESUELTO** (lote AUD-1) | 2026-07-04 | `ApiKeyService.create` rechaza `environment='live'` con `LiveKeysDisabledError` (HTTP 403 `live_keys_disabled`). Opción más restrictiva y reversible de las dos que ofrecía el auditor; desbloqueo futuro = production gates + decisión humana **PEND-004**. Test: "refuses to create live-environment keys while no live plane exists" (verifica además que no persiste nada) |
| AUD-P2-004 (projections actualizables) | **RESUELTO** (F2-05; mitigado antes por 0008) | 2026-07-04 | Guard de versión + `verifyProjection` (previos) + 0008 (worker sin privilegios) + F2-05: `rebuildProjection` race-safe bajo lock de cuenta (property test con posting/rebuild concurrentes), `ledger_projection_drift()` (definer, solo worker) y `ProjectionDriftWatcher` programado en el worker con alerta por log (métricas formales = F1-07). Reparación siempre explícita |
| AUD-P2-005 (outbox sin envelope) | **RESUELTO** (F2-11) | 2026-07-04 | `@fluvia/events`: envelope `event_id (evt_uuid)/schema_version/occurred_at/producer/resource/data` con schema Zod estricto; el productor valida al emitir (`buildEnvelope`) y el relay re-valida al despachar (no conforme = veneno→dead). Ledger migrado al sobre (test del envelope en `ledger-service.test.ts`); contrato documentado en `outbox-inbox.md` §1 |
| AUD-P2-006 (conciliación) | PLANIFICADO | — | F4 (sin cambio de plan; ya bloqueaba proveedor real). `reconciliation.md` ahora declara explícitamente que nada está construido |
| AUD-P2-007 (observabilidad/restore/runbooks) | **PARCIAL** — pata de observabilidad cubierta (F1-07, 2026-07-05); restore (F4-06) y runbooks (F6) siguen planificados | — | F1-07: métricas Prometheus en API y worker (HTTP por ruta, relay delivered/retried/dead, drift gauge, heartbeats), health/readiness en ambos procesos y alertas baseline (`observability.md` §4 — drift>0, dead>0, worker sin latido, 5xx, p99, 429s). El hallazgo NO se declara resuelto: faltan restore drill y runbooks |
| AUD-P2-008 (passwords dev en migraciones) | **RESUELTO** (F1-09) | 2026-07-05 | `fluvia_assert_dev_role_creation()` invocado antes de CADA `CREATE ROLE ... PASSWORD` (0002/0004/0009/0010; meta-test: 5 altas, 5 con guard) + el runner inyecta `fluvia.environment`: fuera de local/test, un rol ausente ABORTA la migración exigiendo credenciales gestionadas (probado end-to-end en BD efímera: falla en 'production', aplica en 'test'; sin registro parcial). R-12 queda mitigado por código, no solo por documentación |
| AUD-P2-009 (taxonomía de errores) | **RESUELTO** (F1-08) | 2026-07-04 | Catálogo v1 cerrado (`error-catalog.ts`, 26 códigos) con golden comprometido como contrato (cambio breaking = test roto + bump consciente); sobre estable `{type, code, message, details?, request_id}`; **el `message` interno de dominio ya NO se expone** (va a logs; el cliente recibe el texto público del catálogo — probado con fuga sembrada); 5xx opacos; forma default de Fastify irrepresentable; `details` imposible fuera de códigos declarados. Doc: `api-errors.md` |
| AUD-P2-010 (SBOM/licencias) | **MITIGADO**; license report a F6 | 2026-07-04 | SBOM SPDX ya se genera como artifact en cada run de CI (auditor sin acceso a Actions). Pendiente: reporte de licencias transitivas + cierre F0-VER antes de release |
| AUD-P2-011 (FSM docs ↔ DDL sin meta-test) | **RESUELTO** (F3-01) | 2026-07-05 | `fsm-meta.test.ts`: triple comparación EXACTA doc(mermaid §1/§2/§3)↔mapa TS↔tablas DDL (seed generado desde el mapa por `gen-fsm-seed.ts` — una sola fuente de verdad) + **matriz completa a nivel de motor**: los 144 pares (from,to) de intents y 64 de attempts probados con SQL de superusuario — todo par fuera del mapa lanza `FLUVIA_INVALID_TRANSITION`; las tablas de transiciones son inmutables (triggers) |
| AUD-P2-012 (verify-ledger-invariants.sql) | **RESUELTO** (F2-06) | 2026-07-04 | `scripts/verify-ledger-invariants.sql` autocontenido (6 checks: doble partida por tx/moneda, montos>0, coherencia cuenta-tenant-moneda, cabeceras huérfanas, reversiones cross-tenant, drift de proyecciones) + paso de CI vía psql tras la suite. Tests: pasa en BD sana, detecta corrupción sembrada (proyección adulterada como superusuario), vuelve a pasar tras reparación explícita |
| AUD-P2-013 (bucket reserved vs cuentas) | **CERRADO POR DECISIÓN** | 2026-07-04 | Decisión: reservas = CUENTAS del Chart (`merchant.reserve`, `dispute.reserve`, `refund.liability`); los buckets expresan liquidez (`available`/`pending`), no propósito. `ledger-design.md` §2/§6 corregido (la mención a `reserved` era resto del spike). Si Fase 4 demuestra necesidad de bucket, exigirá ADR nuevo |
| AUD-P2-014 (dbUrls sin anti-mezcla) | **RESUELTO** (lote AUD-1) | 2026-07-04 | `dbUrlsFromEnv`: defaults dev SOLO con `FLUVIA_ENV`/`NODE_ENV` local/test; en cualquier otro entorno lanza `FLUVIA_CONFIG` listando las URLs faltantes (FLUVIA_ENV tiene precedencia). Tests: `packages/db/test/config.test.ts` (4 casos, incluye precedencia) |
| AUD-P2-015 (HMAC server-side keys) | **RESUELTO** | 2026-07-05 | Migración 0016: `key_hash_version` (1=sha256 legado, 2=HMAC-SHA256 con pepper `API_KEY_HMAC_SECRET`, 64 hex, anti-mezcla en config — falla fuera de local sin valor explícito). Claves nuevas nacen v2; `authenticate_api_key(hmac, legacy)` autentica ambas y PROMUEVE v1→v2 en la misma llamada (el caller demostró el plaintext; upgrade probado). Un dump de `api_keys` ya no permite validar claves offline: falta el pepper, que jamás se persiste (test: hash almacenado ≠ sha256 del secreto). Prerequisito técnico de PEND-006 cumplido |
| AUD-P2-016 (CORS/headers/OpenAPI/load) | PLANIFICADO | — | OpenAPI ya estaba en F3-02; CORS/security headers a F3 (primer consumo browser); load a F2-08/F6 |

## Hallazgos P3

| ID | Estado | Fecha | Evidencia / referencia |
|----|--------|-------|------------------------|
| AUD-P3-001 (README desactualizado) | **RESUELTO** (lote AUD-1) | 2026-07-04 | README reescrito: estado real (Fase 2), tabla completa de paquetes, PEND resueltas/abiertas, negativa explícita de capacidades productivas y live keys |
| AUD-P3-002 (docs sin estado de implementación) | **RESUELTO** (lote AUD-1) | 2026-07-04 | Bloque "Estado de implementación" fechado en: `webhook-delivery.md`, `reconciliation.md`, `payment-lifecycle.md`, `payment-state-machines.md`, `outbox-inbox.md`, `idempotency.md` — distinguen DISEÑO / PARCIAL / construido con referencias a fase |
| AUD-P3-003 (Dockerfile/compose completo) | PLANIFICADO | — | F3 (junto a E2E sandbox) |

## Decisiones humanas abiertas derivadas (no bloquean el plan)

- **PEND-004**: política definitiva de credenciales `live` (hoy: creación bloqueada por código). Propietario: humano.
- **PEND-005**: modelo MFA/recuperación de cuenta (TOTP propuesto). Propietario: humano.
- **PEND-006**: condiciones para abrir sandbox compartido (requiere F1-04b + AUD-P2-015). Propietario: humano.

## Evidencia CI

- Runs #1–#10: verdes (uno por commit hasta `a665b4f` = commit auditado, run #10).
- Runs #11–#12: rama documental de la auditoría (`9c17888`), verdes.
- **Lote AUD-1: run #13 VERDE** — commit `4cdfcb0`, `https://github.com/celestinojbm/Fluvia/actions/runs/28716967441` (install, lint, format, typecheck, migrate ×2 incl. 0008, 174 tests contra PG16, gitleaks, audit, SBOM).
- **F2-11 (cierra AUD-P1-004/007 y AUD-P2-005): run #15 VERDE** — commit `1ecc533`, `https://github.com/celestinojbm/Fluvia/actions/runs/28717692510` (migrate ×2 incl. 0009, 195 tests incl. relay multi-worker y meta-tests de roles).
- **F2-12 (cierra AUD-P1-005): run #18 VERDE** — commit `f391240`, `https://github.com/celestinojbm/Fluvia/actions/runs/28718633416` (migrate ×2 incl. 0010 sobre PG fresco, 215 tests incl. inbox). Nota de proceso: el run #17 (`bf72d98`) tuvo el job de calidad VERDE pero gitleaks marcó un falso positivo (fixture de test con aspecto de secreto); corregido con valor obviamente falso + `gitleaks:allow` puntual — no se silenció el escáner globalmente.
- **F2-05/F2-06 (cierran AUD-P2-004 y AUD-P2-012): run #20 VERDE** — commit `0ce18a9`, `https://github.com/celestinojbm/Fluvia/actions/runs/28719493607` (migrate ×2 incl. 0011, 224 tests, y el NUEVO paso de CI ejecutando `scripts/verify-ledger-invariants.sql` vía psql sobre la base poblada por la suite).
- **F2-07/F2-08 (Gate Ledger 🟢 técnico): run #22 VERDE** — commit `616138b`, `https://github.com/celestinojbm/Fluvia/actions/runs/28720890852` (migrate ×2 incl. 0012, 233 tests incl. reversiones bajo carrera y suite formal de concurrencia).
- **F1-08 (cierra AUD-P2-009): run #24 VERDE** — commit `4303a93`, `https://github.com/celestinojbm/Fluvia/actions/runs/28721370832` (241 tests incl. contrato golden del catálogo de errores y tests de no-fuga de mensajes internos).
- **F2-09/F2-10 (cierra AUD-P1-003; Gate Idempotencia 🟢; Fase 2 completa): run #26 VERDE** — commit `29695d9`, `https://github.com/celestinojbm/Fluvia/actions/runs/28721992797` (migrate ×2 incl. 0013, 255 tests incl. crash-recovery y carrera N→1 de la capa de idempotencia).
- **F1-04b (cierra AUD-P1-006; criterio de re-auditoría 5/5): run #29 VERDE** — commits `d8d1090` + `b65cb68`, `https://github.com/celestinojbm/Fluvia/actions/runs/28723083681` (migrate ×2 incl. 0014, 278 tests incl. vectores RFC 6238, anti-replay por step, lockout compartido, step-up HTTP y rate limiting 429). Nota de proceso: el run #28 (`d8d1090`) tuvo el job de calidad VERDE (lint, types, migrate ×2, 278 tests, invariantes) pero gitleaks marcó como falso positivo la clave de desarrollo local documentada (patrón `00112233…`, régimen R-12); anotada puntualmente con `gitleaks:allow` — el escáner no se silenció globalmente.
- **F1-07 (AUD-P2-007 → PARCIAL, pata de observabilidad): run #31 VERDE** — commit `4dd46c8`, `https://github.com/celestinojbm/Fluvia/actions/runs/28726862210` (298 tests incl. formato de exposición Prometheus, anti-cardinalidad por plantilla de ruta, /metrics sin identificadores de tenant y aislamiento de fallos del observador de métricas en relay/drift/heartbeat).
- **F1-09 (cierra AUD-P2-008): run #33 VERDE** — commit `1d17ec0`, `https://github.com/celestinojbm/Fluvia/actions/runs/28727371526` (migrate ×2 incl. 0015 y las migraciones de roles CON guard aplicadas sobre PG fresco; 310 tests incl. purga de alcance exacto, no-escape financiero con GUC activo, DELETE de superusuario bloqueado y el guard probado en ambos sentidos sobre BD efímera).
- **F1-10 (Fase 1 completa; sin hallazgo asociado — evidencia de proceso): run #35 VERDE** — commit `89169e8`, `https://github.com/celestinojbm/Fluvia/actions/runs/28727678396` (315 tests incl. seeds deterministas: segunda corrida con CERO filas nuevas y guard de entorno probado antes de tocar la BD).
- **AUD-P2-015 (HMAC server-side) + diseño F3-01: run #37 VERDE** — commit `24050a2`, `https://github.com/celestinojbm/Fluvia/actions/runs/28728095481` (migrate ×2 incl. 0016 sobre PG fresco; 316 tests incl. upgrade perezoso v1→v2 y dump-resistance del hash de API keys).
- **F3-01 (cierra AUD-P2-002 y AUD-P2-011; decisión #24): run #39 VERDE** — commit `2aac4c9`, `https://github.com/celestinojbm/Fluvia/actions/runs/28729119401` (migrate ×2 incl. 0017 sobre PG fresco; 332 tests incl. meta-test triple doc↔TS↔DDL y la matriz de motor completa — 144 pares de intent + 64 de attempt — con SQL de superusuario).
- **F3-02 (AUD-P1-002 → PARCIAL; endpoints idempotentes de payment intents): run #42 VERDE** — commits `53b878c` + `49d066f`, `https://github.com/celestinojbm/Fluvia/actions/runs/28729908202` (348 tests incl. replay exacto sobre HTTP, claim revertido sin quemar key, 409 `invalid_state_transition` sobre HTTP y contrato OpenAPI↔servidor). Nota de proceso: en el run #41 los 348 tests PASARON pero vitest marcó un evento 'error' sin handler en el teardown del test del guard de migraciones (DROP DATABASE WITH (FORCE) terminando un socket en drenaje — 57P01); corregido con handler explícito en ese pool efímero únicamente.
- **F3-03 parte síncrona (ciclo de pago vivo; AUD-P1-002 sigue PARCIAL): run #44 VERDE** — commit `c27ad69`, `https://github.com/celestinojbm/Fluvia/actions/runs/28730299677` (363 tests incl. captura contable atómica vía onPosted con re-ejecución no-op, rechazo sin asientos, timeout→indeterminate sin resolución por asunción y confirm idempotente end-to-end sobre HTTP).
- **F3-03b (F3-03 cerrado; primer handler real del inbox — completa el desbloqueo que pedía AUD-P1-005): run #46 VERDE** — commit `ee15d73`, `https://github.com/celestinojbm/Fluvia/actions/runs/28731084988` (372 tests incl. cadena asíncrona completa sobre HTTP: webhook firmado → ingesta durable → processor → captura atómica; firma inválida sin persistencia; tardíos ignored_out_of_order sin doble asiento).
- **F3-04 (robustez de attempts; evidencia de proceso): run #48 VERDE** — commit `95e8e4f`, `https://github.com/celestinojbm/Fluvia/actions/runs/28731534657` (migrate ×2 incl. 0018; 385 tests incl. barrido con lease + auditoría atómica, resolución post-barrido solo por fuente verificada, breaker con reloj inyectado y semántica circuito-abierto = fallo limpio sin asiento).
- **F3-07 (webhooks salientes; AUD-P1-002 avanza — solo falta refunds E2E): run #50 VERDE** — commit `a3ddab3`, `https://github.com/celestinojbm/Fluvia/actions/runs/28732295370` (migrate ×2 incl. 0019 con rol `fluvia_webhook` bajo el guard AUD-P2-008; 409 tests incl. fan-out con alcance por tenant/topic/estado, firma verificable con rotación dual, SSRF estricto sin tráfico de red, calendario de reintentos→dead y secreto entregado UNA sola vez por la API).
- **F3-08 (refunds E2E; AUD-P1-002 RESUELTO por completo): run #52 VERDE** — commit `a598bc1`, `https://github.com/celestinojbm/Fluvia/actions/runs/28733552846` (migrate ×2 incl. 0020 con FSM `refund_transitions` en el motor; 436 tests incl. asiento compensatorio por la vía normativa, matriz de 36 pares como superusuario, saldo insuficiente→canceled sin contactar al proveedor, throw/`pending`→`indeterminate` con reserva retenida y resolución verificada vía `resolveFromProvider`. Revisión adversarial multi-agente del flujo del dinero: cero defectos confirmados).
- **F3-05a (customers; fundación de checkout): run #54 VERDE** — commit `7297965`, `https://github.com/celestinojbm/Fluvia/actions/runs/28734624528` (migrate ×2 incl. 0021; 458 tests incl. email duplicado permitido, update parcial con `null` limpia + metadata reemplaza, softDelete idempotente, aislamiento cross-tenant 404 y OpenAPI +5 paths por el test de contrato).
- **F3-05b (checkout sessions — el recurso): run #56 VERDE** — commit `46a3627`, `https://github.com/celestinojbm/Fluvia/actions/runs/28748142399` (migrate ×2 incl. 0022 con FSM `checkout_session_transitions` en el motor; 476 tests incl. matriz de 9 pares como superusuario, create idempotente con replay exacto sobre HTTP, guard de intent resuelto→409, client_secret entregado una vez con solo su hash en reposo y aislamiento cross-tenant).
- **F3-05c-i (backend del flujo alojado): run #58 VERDE** — commit `671860f`, `https://github.com/celestinojbm/Fluvia/actions/runs/28748725989` (migrate ×2 incl. 0023 `checkout_session_authenticate` SECURITY DEFINER; 483 tests incl. auth cross-tenant por client_secret sin API key, vista redactada, completado/expiración con evento único + idempotencia, intent-succeeded gana sobre TTL, y ciclo completo sobre HTTP crear→confirmar→/status completed).
- **F3-05c-ii (entrega garantizada de eventos de checkout): run #60 VERDE** — commit `016178e`, `https://github.com/celestinojbm/Fluvia/actions/runs/28749057996` (migrate ×2 incl. 0024 `sweep_checkout_sessions()` SECURITY DEFINER; 489 tests incl. el sobre construido EN SQL validado contra el MISMO `EventEnvelopeSchema` del relay, completado/expiración de sesiones no consultadas vía rol `fluvia_worker`, ramas disjuntas, intent-succeeded gana sobre TTL, idempotencia con la vía perezosa).
- **F3-05c-iii (confirm alojado por client_secret): run #62 VERDE** — commit `487ab85`, `https://github.com/celestinojbm/Fluvia/actions/runs/28749600623` (496 tests incl. `POST /confirm` sin API key en dos fases, tok_approve→completed, tok_decline→intent failed+sesión open, doble submit idempotente sin segundo attempt, secreto malo→404, token faltante→400 — el flujo de pago alojado funcional extremo a extremo por backend).
- **F3-05c-iv (UI Next.js alojada; F3-05 COMPLETO): run #64 VERDE** — commit `1c9da26`, `https://github.com/celestinojbm/Fluvia/actions/runs/28750616451` (PRIMER app frontend del monorepo — Next.js 15/React 19; ambos jobs verdes: calidad (lint/format/build-tsc/test, 501 tests incl. 5 de componente jsdom+axe) Y seguridad (`pnpm audit --audit-level high` limpio con Next 15.5.16 — el advisory crítico de Next 14 motivó el bump). E2E de navegador full-stack verificado LOCALMENTE (Chromium real → API → PG; captura), documentado en `apps/checkout/e2e/README.md` porque el CI no tiene navegador).
- **F3-06 (payment links — plantilla "págame"): run #66 VERDE** — commit `c674aac`, `https://github.com/celestinojbm/Fluvia/actions/runs/28751293715` (515 tests; migración 0025 con `payment_link_resolve` SECURITY DEFINER que solo resuelve links ACTIVOS cross-tenant, `PaymentLinkService` que en la apertura pública genera payment_intent + checkout_session FRESCOS reutilizando F3-05 en UNA transacción atómica. Gestión idempotente (F2-09) con API key; apertura `POST :id/sessions` PÚBLICA sin API key. Tests: creación idempotente/replay, merchant ajeno→404 anti-enumeración, scope payments:write requerido, y apertura pública que genera una sesión pagable extremo a extremo hasta `completed`; link deshabilitado/inexistente→mismo 404).
- **F3-06-b (UI del payment link `/l/[id]`; el link cierra end-to-end para el comprador): run #68 VERDE** — commit `4f131fc`, `https://github.com/celestinojbm/Fluvia/actions/runs/28753699854` (521 tests; página pública `/l/[id]` en `apps/checkout` — server component que abre el link server-side (`POST :id/sessions`, F3-06) y REDIRIGE al flujo alojado `/c/{sessionId}#{clientSecret}` (F3-05c-iv); el `client_secret` viaja en el fragmento del `Location`, fuera de los logs de la navegación posterior. Link inválido/deshabilitado/API caída → misma vista de indisponibilidad, anti-enumeración. Lógica pura `resolveLinkSession` + vista de indisponibilidad probadas en CI sin navegador (jsdom + axe, 6 tests: destino con fragmento, `?lang=en` preservado, endpoint correcto, 404→null, API caída→null, body incompleto→null, a11y). La redirección real con fragmento a través del navegador verificada LOCALMENTE (Chromium: `/l/{id}` → 307 `Location: /c/{sid}#cs_…` → checkout carga `$ 77.000` → Pagar → completado), documentado en `apps/checkout/e2e/README.md`. Sin cambios de esquema ni dependencias).
- **F3-09a (visibilidad de la cola de webhooks + reenvío manual auditado de eventos `dead`; cierra el diferido de F3-07): run #70 VERDE** — commit `d590135`, `https://github.com/celestinojbm/Fluvia/actions/runs/28754828422` (536 tests; migración 0026 con columna `resent_from_event_id` + función SECURITY DEFINER acotada `webhook_event_resend` — `fluvia_app` tiene SELECT sobre la cola pero NO INSERT/UPDATE, revocados en 0019; el reenvío entra por la función. Reenviar NO resucita el evento muerto (estado terminal inmutable): clona (tenant, endpoint, topic, payload) como evento `pending` fresco enlazado vía `resent_from_event_id`, con audit event `webhook_event.resent` en la MISMA transacción. `GET /v1/webhook_events` (scope `read`, filtros endpoint/status), `GET :id` (con payload + historial de intentos), `POST :id/resend` (scope `webhooks:manage`; solo `dead` → si no, `invalid_state_transition`). 12 tests vs PG real: lectura RLS por tenant, clone+enlace+auditoría atómicos, aislamiento cross-tenant (404), guard de solo-`dead` (409), scope requerido. Sin códigos de catálogo nuevos (36), +2 domain codes → 48, golden regen; OpenAPI +3 paths).
- **F3-09b-i (plano de LECTURA del dashboard por sesión + membresía): run #72 VERDE** — commit `e5515d6`, `https://github.com/celestinojbm/Fluvia/actions/runs/28756147994` (541 tests; el dashboard es para un operador HUMANO → autentica por SESIÓN y su org=tenant sale de la MEMBRESÍA, no por API key; por Nivel A es el prerequisito de cualquier UI. Nuevo permiso RBAC `payments:read` (matriz + meta-test celda-a-celda + `access-control.md`; lo tiene todo rol — dato de tenant de solo lectura). Rutas `GET /v1/organizations/:orgId/{payment_intents,refunds,checkout_sessions,payment_links,webhook_events}` list+detail bajo `security.session`+`security.org('payments:read')`, reutilizando los MISMOS servicios y serializers que el plano de API key — una sola forma de recurso. Solo lectura: la escritura de operación (reenvío `dead`) sigue en el plano de API key F3-09a. 5 tests vs PG real: datos creados por API key y leídos por sesión, plano completo cableado+guardado, no-miembro→404, sin sesión→401, aislamiento por tenant. Sin migración ni deps nuevas; plano de sesión excluido del contrato OpenAPI como el resto).
- **F3-09b-ii (frontend del dashboard de operación; segundo app frontend del monorepo): run #74 VERDE** — commit `0e9e9b1`, `https://github.com/celestinojbm/Fluvia/actions/runs/28757381585` (553 tests; `apps/dashboard`, Next.js 15/React 19 patrón de `apps/checkout`. Login por sesión: `/login` → route handler `/api/session` que proxya a la API y fija la cookie **httpOnly** `fluvia_session` (el token NUNCA llega al JS del navegador; MFA fuera de alcance → se avisa, no se simula, Nivel A). `/` lista las organizaciones del operador (membresía) → `/o/[orgId]` (server component) trae las 5 vistas del plano de lectura F3-09b-i con el token de la cookie server-side y renderiza tablas accesibles (intents/refunds/checkout sessions/payment links/cola de webhooks) con montos por moneda/locale y badges por estado. SOLO lectura. i18n es/en, WCAG AA. 12 tests CI-gated: lógica pura del cliente API (login incl. MFA-required sin fingir sesión, orgs, agregación con degradación por recurso) + componente/accesibilidad (jsdom + axe). E2E de navegador full-stack verificado LOCALMENTE (Chromium real → cookie httpOnly → picker → panel con intent `$ 123.000` y link `$ 45.000`; captura), documentado en `apps/dashboard/e2e/README.md`. Sin migración; deps ya presentes).
- **F3-09b-iii (reenvío `dead` desde el panel; F3-09 dashboard mínimo COMPLETO): run #76 VERDE** — commit `7a057e4`, `https://github.com/celestinojbm/Fluvia/actions/runs/28758670786` (559 tests; el dashboard pasa a ser superficie de OPERACIÓN. Backend: nuevo permiso RBAC `webhooks:manage` (espeja el scope de API key homónimo en el plano de sesión; solo owner/admin/developer; matriz + meta-test + `access-control.md`), ruta `POST /v1/organizations/:orgId/webhook_events/:id/resend` (sesión + membresía + `webhooks:manage`) que reusa `WebhookEventService.resend` auditando como `actorType: user`. Frontend: botón «Reenviar» (client component) en la cola de webhooks solo para eventos `dead` y solo si el rol puede (hint `canResendRole`; el API da 403 a quien no); POSTea al route handler que reenvía la cookie de sesión → «Reenviado ✓» + recarga. 6 tests CI-gated (3 ruta HTTP admin→201/read_only→403/no-dead→409+no-miembro→404, 3 frontend). E2E de navegador verificado LOCALMENTE (Chromium: login owner → clic Reenviar en el `dead` → 201 → cola en 2 = dead + pending fresco; captura). Sin migración; deps ya presentes).
- **F3-10 (SDK TypeScript del contrato OpenAPI): run #78 VERDE** — commit `0309744`, `https://github.com/celestinojbm/Fluvia/actions/runs/28759189886` (569 tests; `@fluvia/sdk`, cliente tipado del plano de integración por API key SIN dependencias de runtime — `fetch` global inyectable. Cubre las 27 (método,ruta) del contrato con namespaces por recurso, tipos que espejan la forma pública, `FluviaApiError` (sobre estable code/request_id) e Idempotency-Key autogenerada. `contract.test.ts`: `SDK_ROUTES` == el conjunto (método,ruta) de `openapi.v1.json` en AMBAS direcciones — añadir una ruta al spec sin método en el SDK (o al revés) rompe CI, así el SDK jamás deriva del contrato. `sdk.test.ts`: ejerce el SDK contra el API REAL en proceso (fetch delega en `app.inject` → auth/idempotencia/dominio reales, no mock): intent→confirm(async)→refund, customers CRUD, payment links, rotación de webhook endpoints, errores tipados 404/401. 10 tests (2 contrato + 8 e2e); README. Sin migración ni deps externas nuevas).
- **F3-11a (CORS + cabeceras de seguridad del API; cierra la parte de API de AUD-P2-016): run #80 VERDE** — commit `5b9d398`, `https://github.com/celestinojbm/Fluvia/actions/runs/28759905591` (576 tests; CORS de allowlist explícita vía `CORS_ALLOWED_ORIGINS` (default VACÍO = ningún cross-origin — checkout/dashboard llaman al API server-side; `*` = todos): hook `onRequest` que hace eco del origen permitido con `Access-Control-Allow-Origin` + `Vary: Origin` y responde el preflight OPTIONS (204 con Allow-Methods/Headers/Max-Age) solo para orígenes permitidos. Cabeceras de seguridad en TODA respuesta (hook `onSend`): `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy: no-referrer`, CSP `default-src 'none'; frame-ancestors 'none'`, `Cross-Origin-Resource-Policy: same-origin`, y HSTS solo fuera de local/test. 7 tests. Sin migración ni deps nuevas. **AUD-P2-016**: la parte de API queda cubierta; el resto (empaquetado/despliegue) es F3-11b/AUD-P3-003).
- **F3-11b (empaquetado de despliegue; cierra AUD-P3-003 y con F3-11a completa F3-11): run #82 VERDE** — commit `f0ff680`, `https://github.com/celestinojbm/Fluvia/actions/runs/28760659009` (576 tests, sin cambios de código; solo `Dockerfile`/`.dockerignore`/`docker-compose.yml`). `Dockerfile` multi-stage único del backend (API+worker comparten imagen; el servicio elige la app con `command`): build instala `--frozen-lockfile` + valida tipos (`pnpm build`), runtime slim que corre como usuario **no-root** (`node`), pnpm pineado horneado en la base. `docker-compose.yml` con profile `app` (el `up` por defecto sigue siendo PG+Redis): `migrate` one-shot (crea roles de mínimo privilegio + esquema) del que dependen `api` (:3000) y `worker`. **Verificado con Docker REAL local** (el CI no corre docker): `docker build` OK, `migrate` aplicó las migraciones en el contenedor contra PG containerizado, el API arrancó no-root y sirvió `/health`+`/ready` con los security headers de F3-11a, el worker arrancó limpio (watchdogs/deliverer + métricas). **AUD-P3-003 cubierto; F3-11 y el hardening de Fase 3 COMPLETOS**).
- **F4-01a (motor de conciliación — núcleo; arranca la Fase 4): run #84 VERDE** — commit `39edb4b`, `https://github.com/celestinojbm/Fluvia/actions/runs/28761911229` (580 tests; migración 0027 con `settlement_reports` + `settlement_lines` + `reconciliation_entries` (RLS por tenant + append-only). `@fluvia/reconciliation` `ReconciliationService`: `createReport`/`addLines` (idempotente por `(report, provider_ref)`)/`reconcile`/`getReport`/`listEntries`. El motor casa las líneas del settlement report del proveedor contra los intentos `succeeded` del tenant (mismo proveedor/moneda, dentro del periodo) vía FULL OUTER JOIN por `provider_ref` y clasifica: matched/amount_mismatch/missing_in_ledger/missing_at_provider; persiste las entries + marca el reporte `reconciled` en UNA tx. Append-only: se concilia una sola vez. Per-tenant bajo RLS, sin definer. 4 tests vs PG real: 4 clases de discrepancia + periodo respetado, idempotencia de addLines, guard de conciliar-una-vez, aislamiento por tenant. Sin deps externas nuevas).
- **F4-01b (gestión de conciliación — API de integración): run #86 VERDE** — commit `3b088a1`, `https://github.com/celestinojbm/Fluvia/actions/runs/28762516533` (584 tests; expone el motor F4-01a por el plano de API key: `POST /v1/settlement_reports` (crea, payments:write), `POST :id/lines` (carga líneas del proveedor, ≤1000, idempotente), `POST :id/reconcile` (dispara el motor, resumen de discrepancias), `GET :id` (reporte + resumen), `GET` (lista), `GET :id/entries` (discrepancias, filtro por status; read). `SettlementReportNotFoundError`→not_found, `ReportAlreadyReconciledError`→invalid_state_transition (sin códigos de catálogo nuevos, 36; +2 domain → 50; golden regen). 4 tests de ruta HTTP: cargar→conciliar→resumen 4 clases + GET reconciled con la discrepancia y su monto, reconciliar-dos-veces→409, scope payments:write + aislamiento por tenant, listar. Fuera del contrato OpenAPI v1 por ahora — no cascada al SDK. Sin migración ni deps nuevas).
- **F4-01c (vista de conciliación en el dashboard; cierra el bucle de conciliación de cara al operador): run #88 VERDE** — commit `2cf61e0`, `https://github.com/celestinojbm/Fluvia/actions/runs/28763304377` (591 tests; plano de lectura por sesión `GET /v1/organizations/:orgId/settlement_reports` (+`/:id` con resumen, +`/:id/entries`) bajo `security.session`+`payments:read`, reutilizando `ReconciliationService`. Frontend `apps/dashboard`: enlace «Conciliación» → `/o/[orgId]/reconciliation` (lista) → `/o/[orgId]/reconciliation/[reportId]` (resumen por clase + tabla de discrepancias con montos ledger/proveedor). i18n es/en, WCAG AA. 7 tests CI-gated (2 ruta de sesión + aislamiento por tenant, 5 frontend componente/axe/fetchers). E2E de navegador verificado LOCALMENTE (Chromium: login → Conciliación → reporte → detalle con `matched` $60.000/$60.000 y `missing_in_ledger` `er_phantom` proveedor $9.000; captura). Sin migración; deps ya presentes).
- **F4-02 (conciliación batch/continua — worker; de "a demanda" a "continua"): run #90 VERDE** — commit `e6fe863`, `https://github.com/celestinojbm/Fluvia/actions/runs/28764919421` (597 tests; migración 0028 con `sweep_settlement_reports()` SECURITY DEFINER sin parámetros — misma familia que `sweep_payment_attempts`/`sweep_checkout_sessions` —, EXECUTE solo para `fluvia_worker` (sin privilegios de tabla). Concilia cross-tenant, en UN statement set-based, los reportes con periodo cerrado (`open` + `period_end <= now()` — disparador natural de "reporte sellado"): FULL OUTER JOIN líneas del proveedor ⋈ intentos `succeeded` del ledger, clasificación idéntica al motor per-tenant F4-01a, inserta `reconciliation_entries` + marca `reconciled` TODOS los vencidos (incl. vacíos). Lease `FOR UPDATE SKIP LOCKED`; idempotente con la conciliación manual (ambas bajo el guard `status='open'`). `apps/worker` `ReconciliationWatchdog` (gate `RECONCILIATION_WATCHDOG_ENABLED`/`_INTERVAL_MS`, patrón attempts/checkout: `runOnce`/`start`/`stop`, observer que no rompe el job) invoca la función, expone `fluvia_settlement_reports_reconciled_total`/`fluvia_reconciliation_entries_total{status}`/`fluvia_reconciliation_discrepancies_last` y ALERTA ante discrepancias (entry ≠ matched = dinero real sin cuadrar). 6 tests vs PG real: enteros ≥0, sella cross-tenant con las 4 clases, NO toca periodo abierto, idempotencia (no re-barre ni duplica), `discrepancyCount`, observer que explota no rompe el job. Migrate ×2 idempotente incl. 0028; ledger invariants verdes. Docs espejo `reconciliation.md` §4/§5 + `observability.md` §2/§4. Sin deps externas nuevas).
- **F4-03a (casos operativos de conciliación — materialización + ciclo): run #92 VERDE** — commit `ca06f16`, `https://github.com/celestinojbm/Fluvia/actions/runs/28765612058` (605 tests; migración 0029 con `operational_cases` (RLS por tenant, mutable con ciclo `open→acknowledged→resolved` + `version`; sin DELETE/TRUNCATE) enlazada por FK compuesta por tenant a `reconciliation_entries` (nueva UNIQUE `(id,tenant_id)`) + snapshot de evidencia. Trigger `AFTER INSERT` `materialize_reconciliation_case` sobre `reconciliation_entries`: un caso por cada entry `!= matched`, automático para el motor F4-01a (fluvia_app) y el barrido F4-02 (dentro del definer como postgres), atómico + idempotente (`UNIQUE(reconciliation_entry_id)` + `ON CONFLICT DO NOTHING`). Severidad: `missing_in_ledger`→critical, `amount_mismatch`/`missing_at_provider`→high. `OperationalCaseService` (`list`/`get`/`acknowledge`/`resolve`) bajo RLS con auditoría `operational_case.acknowledged`/`.resolved`. **Nivel A: resolver es DOCUMENTAL — NO mueve dinero**; el ajuste con four-eyes es F4-03b. Plano API key `GET /v1/operational_cases`(+`:id`)/`POST :id/acknowledge`/`POST :id/resolve` (payments:write). `OperationalCaseNotFoundError`→not_found, `InvalidCaseTransitionError`→invalid_state_transition (sin códigos de catálogo nuevos, 36; +2 domain → 52; golden regen). 8 tests vs PG real (5 servicio: materialización un-caso-por-discrepancia + severidad + matched no crea caso, ack→resolve con nota + auditoría, actor api_key deja resolved_by null, re-resolver→error, aislamiento por tenant; 3 ruta HTTP: materializar→ack→resolve→409, scope + resolución vacía→400, aislamiento 404). Migrate ×2 idempotente incl. 0029; ledger invariants verdes. Docs espejo `reconciliation.md` §3 + `data-model.md` §4).
- **F4-03b (ajuste monetario con four-eyes — Nivel C; cierra el §30): run #94 VERDE** — commit `fb2b105`, `https://github.com/celestinojbm/Fluvia/actions/runs/28766249286` (611 tests; migración 0030 con `case_adjustments` (RLS por tenant, mutable `proposed→applied|rejected` + `version`; sin DELETE/TRUNCATE) enlazada al caso por FK compuesta por tenant (nueva UNIQUE `(id,tenant_id)` en `operational_cases`). **FOUR-EYES EN LA BD**: CHECK `approved_by_user_id <> proposed_by_user_id`, `proposed_by_user_id` NOT NULL, índice parcial único `WHERE status<>'rejected'` (un ajuste vivo por caso). `PostingService.postReconAdjustment` postea un asiento compensatorio REAL `recon.differences↔suspense` (platform/transitorias del chart existente, sin merchant; `reason='reconciliation'`, `source_type='case_adjustment'`). `CaseAdjustmentService` propone (humano)/aprueba (segundo humano DISTINTO sobre `FOUR_EYES_THRESHOLD_MINOR`, default 0=siempre)/rechaza — al aprobar postea el asiento + resuelve el caso + auditoría, TODO atómico vía `onPosted` del ledger e idempotente por `case_adj:{id}` (doble aprobación no duplica el asiento). Nivel A: `api_key`/IA→`HumanActorRequiredError`, autoaprobación sobre umbral→`SelfApprovalError`. 3 acciones de auditoría nuevas. 6 tests vs PG real: máquina rechazada, sobre-umbral proponente-no-autoaprueba + segundo humano aplica con asiento balanceado (débitos==créditos) + caso resuelto, bajo-umbral autoaprueba, re-aprobar aplicado→error con UN solo asiento, rechazo libera para nueva propuesta, aislamiento por tenant. Migrate ×2 idempotente incl. 0030; ledger invariants verdes (el asiento recon balancea). Entregado como MOTOR sin rutas (como F4-01a); la exposición sesión+UI es F4-03c. Docs espejo `reconciliation.md` §3 + `data-model.md` §4 + `ledger-chart-of-accounts.md`).
- **F4-03c (casos y ajustes por SESIÓN — four-eyes real sobre HTTP; backend): run #96 VERDE** — commit `e8720e4`, `https://github.com/celestinojbm/Fluvia/actions/runs/28766802843` (615 tests; permiso RBAC nuevo `reconciliation:manage` (owner/admin/finance; matriz + meta-test celda-a-celda + `access-control.md`). Rutas de sesión en `dashboard.ts` bajo `security.session`+membresía: `operational_cases` list/detail(+ajustes)/acknowledge/resolve (`reconciliation:manage`) y `case_adjustments` propose/approve/reject. **FOUR-EYES REAL SOBRE HTTP**: proponente=`req.identity.userId`, aprobador distinto sobre umbral; `SelfApprovalError`→409 `four_eyes_required` (código de catálogo NUEVO; 37 catálogo, +4 domain→56; golden regen). API key jamás alcanza este plano (autorizar dinero es humano). `CaseAdjustmentService` cableado en `app.ts` con `postingService`+`config.fourEyesThresholdMinor`. 4 tests HTTP vs PG real con DOS miembros `finance` distintos: U1 acknowledge→propone→U1 auto-aprueba→409 `four_eyes_required`→U2 aplica (200, `ledger_transaction_id` no nulo)→caso `resolved` con el ajuste en el detalle; read_only propone→403 `insufficient_permissions`; no-miembro→404; rechazo por U2 + read_only lista (payments:read). Sin migración ni deps nuevas. Falta solo la UI (F4-03c-ii). Docs espejo `access-control.md` + `reconciliation.md` §3).
- **F4-03c-ii (UI del panel de casos/ajustes con four-eyes; frontend) → cierra F4-03c: run #98 VERDE** — commit `7e8295b`, `https://github.com/celestinojbm/Fluvia/actions/runs/28769854547` (CI completo verde: lint + format:check + build ×21 paquetes + suite; 17 tests nuevos del dashboard → 37, jsdom + axe. `apps/dashboard`: «Casos» → `/o/:orgId/cases` (lista filtrable por estado + severidad) → `/cases/:caseId` (discrepancia + tabla de ajustes + acciones). Las acciones son islas cliente que POSTean a route handlers server-side que reenvían la cookie httpOnly `fluvia_session` como Bearer — el navegador JAMÁS toca el token ni la URL de la API (mismo patrón que el reenvío `dead` de F3-09b-iii): acknowledge/resolve (documental) del caso y propose/**approve**/reject del ajuste. **FOUR-EYES EN LA UI**: aprobar el propio ajuste → 409 `four_eyes_required` transmitido TAL CUAL por el proxy y mostrado como mensaje específico (no genérico); acciones solo a roles con `reconciliation:manage` (hint `canManageReconciliation`; el API decide 403); propuesta oculta si hay ajuste vivo (espeja el índice único parcial de la BD). Un **review adversarial** (5 dimensiones, verificación escéptica por hallazgo) confirmó y corrigió 1 issue de accesibilidad (aria-label del nav de filtros decía «Severidad» pero filtra por estado) y descartó un cambio que habría dejado ajustes huérfanos. `next build` valida las fronteras server/client + los 5 route handlers. E2E de navegador full-stack pendiente LOCAL (el CI no tiene navegador). Sin migración ni deps nuevas. Docs espejo `reconciliation.md` §3).
- **F4-04a (panel admin — «buscar comercios»; frontend): run #100 VERDE** — commit `a9aa92d` (validado por el run del tip `6ed0be9`), `https://github.com/celestinojbm/Fluvia/actions/runs/28770947990` (CI completo verde; +7 tests del dashboard → 44. Enlace «Comercios» → `/o/:orgId/merchants`: vista de solo lectura por sesión sobre el `GET .../merchants` existente (`merchants:read`, que tienen todos los roles); tabla nombre/id/país/moneda/estado (active/frozen traducido)/creado. Búsqueda accesible **sin JS** (`<form method=get>` → `?q=`, filtrado server-side `filterMerchants` por nombre/id/país/moneda), dos estados vacíos (sin comercios vs sin coincidencias). Server component puro — el token nunca sale del servidor. `next build` compila la ruta. Sin migración ni deps nuevas).
- **F4-04b (panel admin — «ver eventos»; frontend) → panel admin mínimo COMPLETO: run #100 VERDE** — commit `ba59ca3` (mismo run del tip `6ed0be9`), `https://github.com/celestinojbm/Fluvia/actions/runs/28770947990` (CI completo verde; +6 tests del dashboard → 50. Enlace «Eventos» (gateado por `canReadAudit` = owner/admin/finance/analyst) → `/o/:orgId/events`: vista por sesión sobre `GET .../audit-events` (`audit:read`) — la auditoría append-only de F1-05; tabla fecha/actor/acción/recurso/resultado (success/failure)/riesgo (low/medium/high), paginación por cursor `?before=` sin JS (preserva `lang`). Con esto las 4 piezas del **panel admin mínimo** (`mvp-scope.md` §1: buscar comercios · ver eventos · reprocesar webhooks · casos de conciliación) quedan entregadas — cierra **F4-04**. Sin migración ni deps nuevas. Docs espejo `mvp-scope.md`).
- **F4-06a (runbooks operativos; docs) → primer paso de F4-06: run #102 VERDE** — commit `c0782ee`, `https://github.com/celestinojbm/Fluvia/actions/runs/28771809633` (CI completo verde; **docs-only, sin código**. `docs/ops/runbooks/`: índice + 7 runbooks anclados a métricas (`observability.md` §4), endpoints y mecanismos REALES (inventario del código verificado): discrepancia de conciliación→caso→ajuste four-eyes · drift contable (SEV-1, investigar antes de rebuild; `rebuildProjection` única reparación sancionada) · eventos `dead` outbox/inbox→replay auditado (`replayDeadOutboxEvents`/`replayDeadProviderEvents`, `reason` obligatorio, admin pool, `platform.operation`) · pago `indeterminate`→`resolveFromProvider` por fuente verificada (nunca por asunción) · webhook `dead`→reenvío desde el panel · worker caído/watcher detenido→reinicio+drenado · investigación por auditoría. Cada uno Diagnóstico→Resolución→Verificación→Escalación + sección **Drill** pendiente. Cross-link desde `incident-response.md` y `observability.md` §4. Documenta honestamente que replay/rebuild son métodos de servicio sobre el admin pool SIN ruta HTTP/script aún (F6). El **drill ejecutable + evidencia** es F4-06b (cierra el criterio de salida de Fase 4). Sin código ni deps).
- **F4-06b (drill del runbook de conciliación — ejecutado PASS; código): run #108 VERDE** — commit `868f18c`, `https://github.com/celestinojbm/Fluvia/actions/runs/28772769374` (CI completo verde: build/lint/format/test — el drill es código tipado con override eslint `**/drills/**`. `apps/api/drills/reconciliation-drill.ts` (`pnpm --filter @fluvia/api run drill:reconciliation`) levanta la API real en proceso y la conduce **sobre HTTP** con dos operadores `finance` distintos, ensayando `reconciliation-discrepancy.md` end-to-end: siembra `missing_in_ledger` → concilia → caso `critical` materializado → U1 reconoce/propone → **four-eyes** U1 auto-aprueba→409 `four_eyes_required` → U2 aprueba→asiento+resuelto → verifica rastro de auditoría (`acknowledged`/`adjustment_proposed`/`adjustment_applied`) + **balance del asiento** (débitos==créditos). **Ejecución LOCAL: PASS 9/9** vs Postgres 16, con `verify-ledger-invariants.sql`→`FLUVIA_INVARIANTS_OK` (el CI valida el CÓDIGO del drill; su ejecución es local, como los E2E de navegador del dashboard). Cierra el criterio de salida de Fase 4 «runbooks probados en drill» para el flujo crítico; los demás runbooks con cobertura de facto por sus suites. Docs `docs/ops/runbooks/*`).
- **F4-05a (abstracción contable de reserves; ledger): run #112 VERDE** — commit `ff55f9b` (su run propio #110 quedó `cancelled` por el push de bookkeeping siguiente — concurrencia de GitHub; el CÓDIGO se valida por el run del tip `abf28b6`, que lo incluye como ancestro y corre la suite completa + migrate), `https://github.com/celestinojbm/Fluvia/actions/runs/28775115735` (CI completo verde: build ×21 + lint + format + suite + migrate. Migración 0031: añade `'reserve'` a las causas del ledger (idempotente `DROP IF EXISTS`+`ADD`) + `LEDGER_REASONS`. `PostingService.holdReserve`/`releaseReserve`: reclasificación `merchant.available ↔ merchant.reserve` (dos pasivos del comercio → la obligación TOTAL con el comercio no cambia, solo se aparta/libera para riesgo/disputas), reusando `twoLegged` con el guard de no-negatividad AUD-P1-010 (no reservar más de lo disponible ni liberar más de lo reservado). 3 golden tests vs PG real: round-trip capture→settle→hold→release con la invariante `available+reserve` constante + cero drift, guards de no-negatividad sin efectos, idempotencia de replay. Migrate ×2 idempotente incl. 0031; invariantes del ledger verdes (`FLUVIA_INVARIANTS_OK`). **Adelanta la parte DESBLOQUEADA de F4-05** (no depende de pricing): payout queda pendiente de diseño (falta cuenta de caja/banco en el chart), fees bloqueado por PEND-002. Docs espejo `ledger-chart-of-accounts.md`).
- **F4-05b (abstracción contable de payout; ledger): run #116 VERDE** — commit `1368294`, `https://github.com/celestinojbm/Fluvia/actions/runs/28777461604` (CI completo verde: build ×21 + lint + format + suite + migrate. Decisión contable tomada — no depende de pricing, es flujo de fondos. Migración 0032: corrige `payout.in_transit` de activo→PASIVO (tratamiento estándar de "payout en tránsito"; guard duro: aborta si hay algún asiento — la cuenta era placeholder sin usar) + backfill de `platform.cash` (activo, caja/banco de Fluvia) para charts existentes; el chart pasa a **14 cuentas** (9 platform + 5 merchant). Reglas de posting: `receiveProviderSettlement` (`debit platform.cash`/`credit provider.clearing` — el proveedor liquida a caja), `emitPayout` (`debit merchant.available`/`credit payout.in_transit` — no double-spend de fondos en vuelo), `settlePayout` (`debit payout.in_transit`/`credit platform.cash` — el banco confirma), `failPayout` (reverso). **Mejora de corrección**: `twoLegged` generaliza el guard AUD-P1-010 a TODA cuenta que decrece (calculado desde `normal_side`), cerrando el hueco donde el riesgo estaba en la cuenta ACREDITADA (`provider.clearing` en provider.settle/refund.settle; `platform.cash` en payout.settle — ahora imposible sobregirar caja o clearing). 4 golden tests vs PG real: flujo completo captura→proveedor-liquida→disponible→emit→settle con balances exactos + cero drift, fallo que devuelve íntegro al comercio, los 3 guards (provider.settle>clearing, emit>available, settle>cash), idempotencia. Suites afectadas verdes (payments-core 79, reconciliation 15, seeds 5); migrate ×2 idempotente incl. 0032; invariantes del ledger verdes. **Con F4-05b, solo resta F4-05c (fees), bloqueado por PEND-002.** Docs espejo `ledger-chart-of-accounts.md`).
- **F4-05c (motor de fees; cierra PEND-002 → 2% por transacción, decisión #25; payments-core): run #122 VERDE** — commit del motor `da373ff` (su run propio #120/#121 quedó ROJO: al cablear el 2% por defecto en `buildApp`, la suite de contrato de refunds `refund-routes.test.ts` (F3-08) sembraba liberando el BRUTO capturado a `available` mientras la captura ahora deposita `monto − Ff` en `pending` → underflow del guard AUD-P1-010; sólo caía el primer test, los siguientes heredaban `pending` acumulado. Corregido en `bda717a` fijando `PLATFORM_FEE_BPS=0` en esa suite — los refunds están acotados por `amount_captured` (bruto) y el disponible del ledger es neto, así que con fee>0 ambos divergen: la interacción refund↔fee es trabajo futuro, fuera del alcance del motor de fees). Validado por el run del tip `bda717a`, que incluye el motor como ancestro y corre la suite completa, `https://github.com/celestinojbm/Fluvia/actions/runs/28805898970` (CI completo verde: build ×21 + lint + format + suite PG real + migrate ×2 + `FLUVIA_INVARIANTS_OK`). `packages/payments-core/src/pricing.ts`: `FeeSchedule` + `FlatBpsFeeSchedule` (bps ∈ [0,10000] entero; `platformFee(monto) = monto.allocate([bps, 10000−bps])[0]` → redondeo por mayor residuo, invariante `0 ≤ Ff ≤ monto`) + `ZERO_FEE_SCHEDULE`. `PaymentConfirmationService` recibe el `FeeSchedule` como 5.º parámetro **REQUERIDO** (fail-fast: sin fee-cero silencioso en producción = pérdida de ingresos) y devenga `platform.fees = platformFee(monto)` en `recordApproved` (único camino de captura, sync + async). Producción (`apps/api`, `apps/worker`) instancia `FlatBpsFeeSchedule(config.platformFeeBps)`; env `PLATFORM_FEE_BPS` (default 200) en `@fluvia/config`. Tests: `pricing.test.ts` (6: 2% exacto, redondeo por mayor residuo + barrido de invariante, bps=0/ZERO, monto cero, moneda, bounds) + integración de fee al 2% por deltas en `confirmation.test.ts`; las 6 suites que instancian el servicio pasan `ZERO_FEE_SCHEDULE`. Suite total verde: payments-core 86, config 12, apps/api 163. **Con F4-05c, F4-05 (settlement/reserves/payout/fees) queda COMPLETO y los cuatro pilares contables de Fase 4 construidos.** Sin migración ni deps nuevas. Docs espejo `ledger-chart-of-accounts.md`, `DECISIONS.md` (#25), `BACKLOG.md`, `STATE.md`).
- **F4-07a (payouts como recurso gestionado — motor; payments-core): run #128 VERDE** — commit del motor `d20faaa` (su run propio quedó ROJO por un test de collation: `fsm-meta` comparaba el `ORDER BY from_status` de Postgres contra `transitionPairs` (localeCompare de Node), que discrepan en `in_transit` vs `indeterminate` — el guion bajo ordena ANTES de las letras en C.UTF-8 (la BD local, que coincidía con Node) pero DESPUÉS bajo el locale glibc de la imagen de Postgres de CI, así que CI devolvió `indeterminate` primero. Ningún otro FSM tiene un par guion-bajo↔letra, sólo este test se vio afectado; corregido en `bda…`→`259c08a` quitando el `ORDER BY` y ordenando en JS con el MISMO comparador → el test compara el CONJUNTO de transiciones, independiente del collation). Validado por el run del tip `259c08a`, que incluye el motor como ancestro y corre la suite completa, `https://github.com/celestinojbm/Fluvia/actions/runs/28810120789` (CI completo verde: build ×21 + lint + format + suite PG real + migrate ×2 incl. 0033 + `FLUVIA_INVARIANTS_OK`). Envuelve las primitivas de F4-05b (emit/settle/fail) como recurso: migración 0033 (`payouts` RLS/no-delete/FK de coherencia por tenant + `payout_transitions` INMUTABLE sembrada desde `PAYOUT_TRANSITIONS` + trigger de validación) con FSM `requested → in_transit → paid | failed | indeterminate`; `PayoutService` en dos fases (como F3-08): `create` valida fundabilidad (disponible − payouts `requested`; el guard AUD-P1-010 es el backstop atómico) y nace `requested`; `execute` emitPayout (sin disponible→`failed`) y submitPayout al banco (nuevo método opcional del `PaymentProvider` + `MockPaymentProvider`) → aprobado settlePayout→`paid`, rechazado/circuito failPayout (fondos íntegros de vuelta)→`failed`, throw/timeout/pending→`indeterminate` (fondos retenidos, V4 §23) que SOLO `resolveFromProvider` cierra; emite `payout.<estado>` al outbox. 8 golden tests vs PG real (ciclo money-out con balances exactos + cero drift incl. la caja del proveedor como precondición, fundabilidad, carrera→failed sin mover dinero, rechazo/circuito devuelven, indeterminate+resolución verificada, idempotencia) + fsm-meta extendido (doc §6 ↔ TS ↔ DDL + los 25 pares del motor + inmutabilidad). Motor SIN endpoints/UI (slices siguientes: API key + worker + dashboard); payouts públicos/reales bloqueados hasta gates. Docs espejo `payment-state-machines.md` §6 + `ledger-chart-of-accounts.md` + `BACKLOG.md`).
- **F4-07b (payouts — plano de API key; apps/api): run #134 VERDE** — commit `d66ed18`, validado por el run del tip `18129c5` (que lo incluye como ancestro y corre la suite completa), `https://github.com/celestinojbm/Fluvia/actions/runs/28811991914` (CI completo verde: build ×21 + lint + format + suite PG real + migrate ×2 + `FLUVIA_INVARIANTS_OK`). Expone el motor de F4-07a al integrador, espejando refunds (F3-08). Rutas `apps/api/src/routes/payouts.ts`: `POST /v1/payouts` (scope `payments:write`, Idempotency-Key obligatoria, contrato ASÍNCRONO: `beginIn` dentro de la tx de la key → `requested`, dispara `execute` fuera de tx; replay exacto), `GET /v1/payouts/:id`, `GET /v1/payouts` (por `merchant_id`, `read`), aislamiento por tenant (RLS). `ResilientProvider` gana `submitPayout` (mismo circuito/timeout que submit/refund — CircuitOpen→`failed provider_unavailable`, timeout→`indeterminate`, ya manejados por el motor). Catálogo: `payout_amount_exceeds_balance` (422, código nuevo; 38 catálogo) + `PayoutNotFoundError`→not_found, `InsufficientPayoutBalanceError`→422 (+2 domain → 58; golden regen). 5 tests de ruta HTTP vs PG real: crear→GET `paid`→lista + replay exacto, fundabilidad 422, Idempotency-Key requerida 400, read-only 403 + aislamiento (otro tenant no puede drenar el comercio ajeno: disponible 0 bajo su RLS → 422, y GET ajeno → 404). Fuera de OpenAPI v1 — no toca el SDK. Sin migración ni deps nuevas. Docs espejo `BACKLOG.md`.
- **F4-07c (payouts — robustez en el worker/barrido; apps/worker): run #134 VERDE** — commit `18129c5` (el tip; su propio run corre la suite completa), `https://github.com/celestinojbm/Fluvia/actions/runs/28811991914` (CI completo verde: build ×21 + lint + format + suite PG real + migrate ×2 incl. 0034 + `FLUVIA_INVARIANTS_OK`). Espeja el watchdog de attempts (F3-04). Migración 0034: `sweep_payouts()` SECURITY DEFINER sin parámetros (EXECUTE solo `fluvia_worker`) barre `in_transit` past-lease (5 min) → `indeterminate` (el proceso murió entre el emit y el registro del desenlace del banco: desenlace DESCONOCIDO, fondos RETENIDOS en tránsito, JAMÁS `failed` por asunción — V4 §23) con auditoría atómica `payout.swept_indeterminate`; NO fija `failure_code` (no es un fallo; el rastro vive en la auditoría). Salud: `indeterminate_total`/`indeterminate_aged` (>30 min) + `requested_stuck` (payouts cuyo `execute` nunca corrió, >5 min — sin dinero en riesgo, el banco jamás fue contactado; se SURFACEAN, no se re-ejecutan para no arriesgar doble envío). `apps/worker` `PayoutsWatchdog` (gate `PAYOUTS_WATCHDOG_ENABLED`/`_INTERVAL_MS`) invoca la función, expone `fluvia_payouts_swept_total`/`_indeterminate`/`_aged`/`_requested_stuck` y ALERTA. 6 tests vs PG real: barre SOLO in_transit past-lease (el fresco intacto) con auditoría atómica que lista los ids + `failure_code` NULL, indeterminados envejecidos contados, `requested` atascado surfaceado sin moverse, solo `fluvia_worker` ejecuta (app/relay → permission denied), runOnce enteros ≥0, observador que explota no rompe el job. Migrate ×2 idempotente incl. 0034; invariantes verdes. Docs espejo `observability.md` + `payment-state-machines.md` §6. **Restan F4-07c-ii (webhook del banco → resolución automática de indeterminados) y F4-07d (dashboard UI).**
- **F4-07c-ii (webhook del banco resuelve payouts indeterminados; payments-core): run #138 VERDE** — commit `d71eef7`, validado por el run del tip `eb9e04c` (que lo incluye como ancestro y corre la suite completa), `https://github.com/celestinojbm/Fluvia/actions/runs/28814937327` (CI completo verde: build ×21 + lint + format + suite PG real + migrate ×2 + `FLUVIA_INVARIANTS_OK`). Cierra el ciclo de robustez de payouts (espeja F3-03b). El mismo webhook FIRMADO del proveedor 'mock' (endpoint `/v1/providers/mock/webhook`, HMAC + dedup + DLQ ya existentes) transporta también `payout.paid`/`payout.failed`: `MockWebhookEventSchema` → `z.discriminatedUnion('type', …)` (payment.* con `attempt_id` + payout.* con `payout_id`) y `createMockInboxRegistration(confirmation, payouts)` DESPACHA por tipo → payout.* a `PayoutService.resolveFromProvider` (cierra `in_transit`/`indeterminate` — p. ej. los barridos por F4-07c — por fuente verificada, jamás por asunción, V4 §23). El worker cablea un `PayoutService` en el `InboxProcessor`. 3 tests de payout vs PG real EN `webhook-ingest.test.ts` (junto a los 4 de pagos, **un solo processor**: el claim del inbox no filtra por provider, así que dos processors 'mock' concurrentes competirían — por eso ambos flujos comparten archivo/registro): payout.paid firmado → durable 200 → `runOnce` → `paid` + settle (caja neta 0); payout.failed → `failed` + fondos de vuelta; redelivery → duplicate. Sin migración ni deps nuevas.
- **F4-07d (UI del dashboard de payouts; apps/dashboard + apps/api): run #138 VERDE** → **F4-07 COMPLETO** — commit del tip `eb9e04c`, `https://github.com/celestinojbm/Fluvia/actions/runs/28814937327` (CI completo verde: build ×21 incl. **`next build`** + lint + format + suite PG real + migrate ×2 + `FLUVIA_INVARIANTS_OK`). Vista de SOLO LECTURA (server components puros) espejando conciliación F4-01c. Backend: rutas de sesión `GET /v1/organizations/:orgId/payouts`(+`/:id`) bajo `security.session`+`payments:read`, reutilizando `PayoutService` + el serializer `publicPayout` de F4-07b; `payoutService` cableado en `registerDashboardRoutes`. Frontend: enlace «Payouts» → `/o/[orgId]/payouts` (lista id/comercio/monto/estado/creado con badge) → `/o/[orgId]/payouts/[payoutId]` (detalle con pista específica cuando `indeterminate`: fondos retenidos en tránsito). Lee la API directamente server-side (token de sesión jamás sale del servidor; sin route handler). i18n es/en, WCAG AA. 5 tests jsdom+axe (`payouts-view.test.tsx`) + `payouts` en el plano de lectura de `dashboard-routes.test.ts`. Sin migración ni deps nuevas. **Con F4-07d, F4-07 (motor a + API b + watchdog c + webhook c-ii + UI d) queda COMPLETO: el lado money-out está construido de punta a punta en sandbox.**
- **F4-07e (re-drive seguro de `requested` atascados; apps/worker + packages/db): run #143 VERDE** — commit `62b37f8`, `https://github.com/celestinojbm/Fluvia/actions/runs/28816633302` (CI completo verde: build ×21 + lint + format + suite PG real + migrate ×2 incl. 0035 + `FLUVIA_INVARIANTS_OK`). Cierra el hueco que F4-07c dejó abierto: los payouts cuyo `execute` fire-and-forget nunca corrió (el proceso murió tras crear la fila `requested`, antes de emitir — el banco JAMÁS fue contactado) se SURFACEABAN pero no se re-conducían. Migración 0035: `claim_stuck_payouts(max_batch)` SECURITY DEFINER (EXECUTE solo `fluvia_worker`) reclama cross-tenant los `requested` atascados (`created_at`>5 min) tomando un LEASE (toca `updated_at`, sin cambiar status) bajo `FOR UPDATE SKIP LOCKED` + predicado de lease (`updated_at`>5 min) — el lease + skip SERIALIZAN el re-drive (dos workers jamás conducen el mismo payout: lo único que podría doble-contactar al banco, ya que tras el `emit` idempotente ambos llamarían a `submitPayout`), auditoría atómica `payout.redrive_claimed`, devuelve `(id, tenant_id)`. **Seguro por diseño**: en `requested` el emit aún no ocurrió ⇒ re-ejecutar no puede doble-pagar (`emit` idempotente por su key); un re-drive que cae deja el payout en `requested` con el lease vencido y otra corrida lo reintenta. `PayoutsRedriver` (gate `PAYOUTS_REDRIVER_ENABLED`/`_INTERVAL_MS`) reclama en el pool worker y llama `PayoutService.execute` por fila con aislamiento de error por payout; expone `fluvia_payouts_redriven_total{result=redriven|failed}`. El worker reusa UN `PayoutService` sobre el `appPool` para el webhook del banco (F4-07c-ii) y el redriver. 5 tests vs PG real: claim reclama SOLO past-lease requested (el fresco fuera) + toma el lease (segundo claim inmediato lo excluye) + auditoría atómica + no cambia status, solo `fluvia_worker` ejecuta (app/relay→permission denied), re-drive requested→paid con balances exactos (caja neta 0), idempotencia, observador que explota no rompe el job. Docs espejo `observability.md`. **El plano de payouts queda robusto de punta a punta: barrido `in_transit` (F4-07c) + resolución verificada (F4-07c-ii) + re-drive `requested` (F4-07e).**
- **F4-08a (motor de disputas/chargebacks + FSM; packages/db + ledger + payments-core): run #147 VERDE** — commit `027e63c`, `https://github.com/celestinojbm/Fluvia/actions/runs/28818148238` (CI completo verde: build ×21 + lint + format + suite PG real + migrate ×2 incl. 0036 + `FLUVIA_INVARIANTS_OK`; su run PR gemelo #144 también verde). Activa la cuenta `dispute.reserve` que ya vivía en el Chart of Accounts esperando su motor; espeja refunds F3-08 / payouts F4-07a; sandbox. Migración 0036: añade `'dispute'` a las causas del ledger (idempotente, como 0031) + `LEDGER_REASONS` + tabla `disputes` (RLS por tenant, sin DELETE, FK de coherencia por tenant a `merchants`) + FSM `open → under_review → won | lost` hecha cumplir EN el motor por `dispute_transitions` (INMUTABLE, sembrada desde el mapa TS `DISPUTE_TRANSITIONS` vía `gen-fsm-seed.ts`, patrón golden) + trigger de validación. Primitivas contables (`PostingService`, `reason='dispute'`, guard AUD-P1-010 generalizado): `openDispute` (available→dispute.reserve — el banco abre y se aparta el disputado, no double-spend), `winDispute` (dispute.reserve→available, ganada: vuelve íntegro), `loseDispute` (dispute.reserve→provider.clearing, perdida: se va de vuelta vía el proveedor, como un refund forzado). `DisputeService`: `open` crea la fila `open` y aparta en la MISMA tx (atómico; pre-chequeo + `InsufficientDisputeBalanceError`, el guard es el backstop atómico; en sandbox v1 exige disponible suficiente — el saldo deudor es decisión mayor futura), `submitEvidence` (open→under_review, sin mover dinero), `resolve` por FUENTE VERIFICADA (won/lost, jamás por asunción — V4 §23; `ignored`/`ignored_out_of_order` para eventos tardíos). Emite `dispute.<estado>` al outbox (sin estado interno silencioso). No hay `indeterminate` (la disputa no hace llamada saliente cuyo resultado se desconozca; nos lo empujan). 6 golden tests vs PG real (open→won devuelve íntegro + clearing intacto, open→lost forfeita a clearing, open→under_review→lost, no-double-spend con dos disputas que agotan el disponible, abrir por encima rechaza sin crear, idempotencia/fuera-de-orden + inexistente) + FSM meta-test extendido (doc §4 ↔ TS ↔ DDL + los 16 pares del motor + inmutabilidad). Suite total verde: payments-core 112, ledger 49. Motor SIN endpoints/UI (slices siguientes: API key + webhook del banco + dashboard); disputas públicas/reales bloqueadas hasta gates. Docs espejo `payment-state-machines.md` §4 (el placeholder pasa a FSM real) + `ledger-chart-of-accounts.md` + `BACKLOG.md`.
- **F4-08b (disputas — plano de API key; apps/api): run #151 VERDE** — commit `252bf13`, `https://github.com/celestinojbm/Fluvia/actions/runs/28821760763` (CI completo verde: build ×21 + lint + format + suite PG real + migrate ×2 + `FLUVIA_INVARIANTS_OK`; run push gemelo también verde). Expone el motor de F4-08a al integrador. A diferencia de payouts/refunds, la disputa la INICIA el banco: el integrador NO la crea, así que la superficie es de LECTURA + una única acción mutante — RESPONDER con evidencia. Rutas `apps/api/src/routes/disputes.ts`: `GET /v1/disputes/:id` y `GET /v1/disputes` (lista por `merchant_id`, scope `read`), `POST /v1/disputes/:id/evidence` (scope `payments:write`) → `submitEvidence` (`open → under_review`). Envío de evidencia **idempotente por diseño** (sin Idempotency-Key): `submitEvidence` toma `FOR UPDATE`, devuelve el estado actual si ya está `under_review` (serializa envíos concurrentes) y lanza `InvalidStateTransitionError` sobre una disputa terminal. `DisputeNotFoundError`→not_found (sin código de catálogo nuevo, 38; +1 domain → 59; golden regen). La apertura y la resolución (won/lost) NO están en este plano: llegan por fuente verificada (el webhook del banco — F4-08c). 4 tests de ruta HTTP vs PG real (disputa sembrada por el motor como la apertura real del banco): GET detalle/lista por tenant + aislamiento (otro tenant → 404/lista vacía), 404 not_found, evidencia `open→under_review` + re-envío idempotente + read-only 403, evidencia sobre disputa resuelta → 409 `invalid_state_transition`. Fuera del contrato OpenAPI v1 — no toca el SDK. Sin migración ni deps nuevas. Suite total verde: apps/api 175, payments-core 106. **Restan F4-08c (webhook del banco → apertura + resolución por fuente verificada) y F4-08d (dashboard UI).**
- **F4-08c (webhook del banco abre + resuelve disputas; payments-core + apps/worker + packages/db): run #155 VERDE** — commit `7b2b494`, `https://github.com/celestinojbm/Fluvia/actions/runs/28824161187` (CI completo verde: build ×21 + lint + format + suite PG real + migrate ×2 incl. 0037 + `FLUVIA_INVARIANTS_OK`; run push gemelo #154 también verde). El punto de entrada REAL de las disputas (espeja F4-07c-ii). El mismo webhook FIRMADO del proveedor 'mock' (endpoint `/v1/providers/mock/webhook`, HMAC + dedup + DLQ ya existentes) transporta ahora también `dispute.opened`/`dispute.won`/`dispute.lost`: `MockWebhookEventSchema` gana tres literales (open con `merchant_id`+`amount`+`currency`+`provider_ref`; won/lost con `dispute_id`), y `createMockInboxRegistration(confirmation, payouts, disputes)` DESPACHA por tipo — `dispute.opened` → `DisputeService.openFromProvider` (el banco ABRE y aparta fondos), `dispute.won/lost` → `DisputeService.resolve` (fuente verificada, jamás por asunción — V4 §23). **Idempotencia del dinero (Nivel A)**: el inbox es at-least-once (el marcado del evento no es atómico con el efecto), así que `openFromProvider` es idempotente por `provider_ref` — camino rápido `findByProviderRef`, backstop DURO el índice único parcial `disputes_provider_ref_uniq` (migración 0037: `UNIQUE (tenant_id, provider, provider_ref) WHERE provider_ref IS NOT NULL`); un `dispute.opened` reprocesado JAMÁS doble-abre (doble-hold). El worker cablea un `DisputeService` en el `InboxProcessor`. 3 tests de disputa vs PG real EN `webhook-ingest.test.ts` (junto a los 4 de pagos + 3 de payouts, **un solo processor**): `dispute.opened` → durable → `runOnce` → `open` + hold, luego `dispute.won` → íntegro de vuelta; `dispute.lost` → forfeit; dos `dispute.opened` con distinto `event_id` y mismo `provider_ref` → UNA disputa, UN hold (idempotente) + 1 test de motor `openFromProvider`. Sin deps nuevas; migrate ×2 idempotente incl. 0037. Suite total verde: apps/api 178, payments-core 107, apps/worker 35.
- **F4-08d (UI del dashboard de disputas; apps/dashboard + apps/api): run #157 VERDE** → **F4-08 COMPLETO** — commit `ec79e33`, `https://github.com/celestinojbm/Fluvia/actions/runs/28824728479` (CI completo verde: build ×21 incl. **`next build`** + lint + format + suite PG real + migrate ×2 + `FLUVIA_INVARIANTS_OK`; run push gemelo #156 también verde). Vista de SOLO LECTURA (server components puros) espejando payouts F4-07d. Backend: rutas de sesión `GET /v1/organizations/:orgId/disputes`(+`/:id`) bajo `security.session`+`payments:read`, reutilizando `DisputeService` + el serializer `publicDispute` de F4-08b; `disputeService` cableado en `registerDashboardRoutes`. Frontend: enlace «Disputas» → `/o/[orgId]/disputes` (lista id/comercio/monto/estado/creado con badge) → `/o/[orgId]/disputes/[disputeId]` (detalle con motivo + ref. del banco + pista de fondos apartados cuando NO es terminal). Lee la API server-side (token de sesión jamás sale del servidor; sin route handler). i18n es/en, WCAG AA. Badges CSS nuevos `won`(ok)/`lost`(bad)/`under_review`(warn). 5 tests jsdom+axe (`disputes-view.test.tsx`) + `disputes` en el plano de lectura de `dashboard-routes.test.ts`. Sin migración ni deps nuevas. Suite total verde: dashboard 60, apps/api 178. **Con F4-08d, F4-08 (motor a + API b + webhook c + UI d) queda COMPLETO: el ciclo de disputas/chargebacks está construido de punta a punta en sandbox — el banco las abre y resuelve por fuente verificada, con idempotencia del dinero (Nivel A). Con F4-07 (payouts money-out) + F4-08 (disputas money-clawed-back), ambos flujos están completos.**

## Criterio de re-auditoría (adoptado del auditor)

Criterio del auditor: cerrar P1-003, P1-004, P1-005, P1-006 y P1-007. **Estado: 5 de 5 CERRADOS** (~~P1-003~~ ✓ ~~P1-004~~ ✓ ~~P1-005~~ ✓ ~~P1-006~~ ✓ ~~P1-007~~ ✓) con evidencia individual y runs de CI registrados arriba. **El proyecto está LISTO para la re-auditoría externa** — este registro + el plan de integración son el paquete de entrada.
