# Registro de cierre — Auditoría Independiente v1

Estado: Activo · Se actualiza con CADA cambio de estado de un hallazgo. Reglas:

1. El informe original (`independent-audit-v1/`) es inmutable; este registro es la única fuente del estado vivo de cada hallazgo.
2. **Ningún hallazgo pasa a RESUELTO sin evidencia individual verificable** (código + test + run). Prohibido cerrar en lote.
3. Estados: `RESUELTO` (evidencia completa) · `MITIGADO` (riesgo reducido, resto planificado) · `CERRADO POR DECISIÓN` (decisión documentada, sin código pendiente) · `ACEPTADO` (riesgo aceptado registrado en RISKS) · `PLANIFICADO` (abierto, con ítem de backlog y fase) · `ABIERTO` (sin plan — no debe existir ninguno en este estado).

Contexto: commit auditado `a665b4f` == HEAD al iniciar la integración (cero drift). Lote de remediación inmediata = **AUD-1** (migración `0008_audit_remediations.sql` + fixes de servicio + tests + docs, este commit). Evidencia CI: la URL del run verde del lote se registra en §Evidencia CI al final cuando exista.

## Estado para re-auditoría (2026-07-07) — Fase 4 CERRADA

**Fases 1, 2, 3 y 4 completas.** El **Gate Conciliación** de la Fase 4 (`phase-plan.md`: «Gate Conciliación en verde; runbooks probados en drill») está **cumplido**: conciliación (motor + continua + casos + four-eyes), panel admin (F4-04), reserves/fees/settlement/payout/disputas como abstracciones contables (F4-05/07/08), observabilidad + watchdogs (F4-10) y **los 7 runbooks operativos ejecutados en drill** (F4-06a/b — 6 accionables PASS + `audit-investigation` de facto). Todo contra Postgres 16 real, con `verify-ledger-invariants.sql`→`FLUVIA_INVARIANTS_OK` por incremento y evidencia de CI verde run-a-run en §Evidencia CI.

Estado de los hallazgos de la auditoría v1 tras la Fase 4: **0 P1 abiertos** (10/10 RESUELTOS), **0 P2 ABIERTOS** — todos RESUELTOS / MITIGADOS / CERRADOS POR DECISIÓN / PLANIFICADOS con fase. El último pendiente acotado, el **restore drill** de AUD-P2-007, quedó **ejecutado (F6 · Gate Restore, PASS 7/7)** — con esto AUD-P2-007 pasa a **RESUELTO** en sus tres patas (observabilidad + runbooks + restore). La **exposición pública** de la API de pagos/checkout/webhooks y todo uso `live` siguen **congelados** por decisión #24 hasta esta re-auditoría + PEND-006 (todo se construyó en sandbox, jamás expuesto).

**Paquete de entrada para el auditor**: este registro (estado vivo por hallazgo + §Evidencia CI run-a-run) + `audit-integration-plan-v1.md` (reconciliación evidencia-por-hallazgo) + `docs/agents/STATE.md` (qué existe de verdad) + `docs/ops/runbooks/` (procedimientos + estado de drill). El informe original `independent-audit-v1/` permanece inmutable.

## Hallazgos P1

| ID                                           | Estado                                                  | Fecha      | Evidencia / referencia                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| -------------------------------------------- | ------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AUD-P1-001 (FK cuenta-tenant-moneda)         | **RESUELTO** (lote AUD-1)                               | 2026-07-04 | Migración `0008` §1: `UNIQUE (id, tenant_id, currency)` + FK compuesta `ledger_entries_account_coherence_fk` con validación previa de datos. Tests negativos SQL crudo (incluso superuser): `packages/db/test/ledger-invariants.test.ts` → "AUD-P1-001: an entry CANNOT reference another tenant's account" y "entry whose currency differs from its account is rejected"                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| AUD-P1-002 (payments core inexistente)       | **RESUELTO** (F3-01→F3-08, decisión #24)                | 2026-07-05 | Núcleo de pagos real y probado: `payment_intents`+`payment_attempts` con FSM en el motor (0017), `refunds` con FSM en el motor (0020), `@fluvia/payments-core` (intents/confirm en dos fases/attempts contra MockProvider sync+async/refunds E2E), robustez de attempts (barrido+breaker, F3-04) y webhooks salientes (F3-07). Endpoints idempotentes create/get/list/cancel/confirm de intents (F3-02/03) y create/get/list de refunds (F3-08) sobre HTTP. El ciclo de dinero completo — cobro con captura contable atómica y refund con asiento compensatorio por la vía normativa — está probado contra PG real con balances exactos y cero drift. Desviaciones (checkout/customers) registradas en BACKLOG; NADA declara capacidad inexistente. Exposición pública sigue congelada hasta re-auditoría + PEND-006 (decisión #24) |
| AUD-P1-003 (idempotencia API)                | **RESUELTO** (F2-09/F2-10)                              | 2026-07-04 | `@fluvia/idempotency`: claim de la key en la MISMA transacción que el efecto (crash pre-COMMIT ⇒ rollback conjunto; post-COMMIT ⇒ replay exacto persistido); hash canónico sha256; los 5 casos del contrato de `idempotency.md` §3 probados (incl. 409 `processing_in_flight` por lock_timeout y 422 `idempotency_key_reuse`); carrera 8→1; property efectos==1; HTTP end-to-end con el sobre del catálogo. **Gate Idempotencia 🟢 técnico**. Cableado a endpoints de pagos = F3-02 con el patrón del test HTTP                                                                                                                                                                                                                                                                                                                     |
| AUD-P1-004 (outbox sin relay)                | **RESUELTO** (F2-11)                                    | 2026-07-04 | `@fluvia/outbox`: claim-lease `FOR UPDATE SKIP LOCKED`, publicación fuera de la tx (Nivel A), backoff exponencial con jitter, veneno→dead, barrido de zombies, replay auditado. CA probados: 2 workers concurrentes sin doble entrega ("CA Gate: two concurrent relays never double-deliver"), poison→dead+last_error, lease expirado re-elegible. Wired en `apps/worker` (publisher de log en sandbox; entrega real = F2-12/F3-07)                                                                                                                                                                                                                                                                                                                                                                                                 |
| AUD-P1-005 (inbox/webhooks entrantes)        | **RESUELTO** (F2-12)                                    | 2026-07-04 | Migración `0010` + `@fluvia/inbox`: firma HMAC-SHA256 (timestamp firmado, tolerancia, tiempo constante) verificada ANTES de persistir; dedup `UNIQUE (provider, provider_event_id)` probado race-safe (6 entregas concurrentes ⇒ 1 fila); procesador claim-lease (2 procesadores sin doble procesamiento); veneno→dead+DLQ con redacción probada (`card_token`→`[REDACTED]`); `ignored_out_of_order` terminal; replay auditado. Handlers de negocio = F3-03 (el desbloqueo que este hallazgo pedía)                                                                                                                                                                                                                                                                                                                                 |
| AUD-P1-006 (rate limiting + MFA)             | **RESUELTO** (F1-04b)                                   | 2026-07-04 | PEND-005 decidida (TOTP + backup codes, sin SMS). Migración `0014` + `packages/auth`: TOTP RFC 6238 propio (vectores del RFC en tests), secreto cifrado AES-256-GCM (clave `MFA_SECRET_KEY` anti-mezcla), reto post-password 5 min de un solo éxito, **anti-replay por step probado**, backup codes de un solo uso (sha256), fallos MFA → mismo lockout que password (probado hasta bloquear); **step-up** con frescura 15 min aplicado a `keys:manage` (403 `mfa_step_up_required` probado sobre HTTP, refrescado vía `/v1/auth/mfa/step-up`); **rate limiting** por email/IP en `/v1/auth/*` (429 `rate_limited`+Retry-After probado; in-memory documentado Nivel C → store compartido pre-sandbox compartido). 23 tests nuevos                                                                                                   |
| AUD-P1-007 (worker BYPASSRLS/grants)         | **RESUELTO** (F2-11, ADR-0011; mitigado antes por 0008) | 2026-07-04 | Migración `0009`: `fluvia_relay` SIN BYPASSRLS (visibilidad cross-tenant por políticas RLS explícitas SOLO en `outbox_events`; UPDATE por columna a los 6 campos de despacho; sin INSERT/DELETE); `fluvia_worker` → cascarón: `NOBYPASSRLS` + revoke total (tablas, secuencias y default privileges de 0002). Meta-tests permanentes: "NO runtime role has BYPASSRLS", "fluvia_worker … ZERO table privileges", "fluvia_relay holds ONLY outbox_events SELECT + column-scoped UPDATE"                                                                                                                                                                                                                                                                                                                                               |
| AUD-P1-008 (evidencia CI no reproducible)    | **RESUELTO** (evidencia existía; auditor sin acceso)    | 2026-07-04 | GitHub Actions workflow `CI` corre por commit: install, lint, format-check, typecheck, migrate ×2 (no-op check), tests contra PG16 real, gitleaks, `pnpm audit --audit-level high`, SBOM SPDX. Runs #1–#10 verdes; **run #10 = commit auditado `a665b4f`, conclusion=success**. Adopción de proceso: cada handoff/cierre registra la URL del run (este registro, §Evidencia CI)                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| AUD-P1-009 (idempotency_keys sin endpoint)   | **RESUELTO** (lote AUD-1)                               | 2026-07-04 | Migración `0008` §2: columna `endpoint` + PK `(tenant_id, endpoint, key)`; tabla sin consumidores previos (cambio seguro). Test: `ledger-invariants.test.ts` → "AUD-P1-009: idempotency keys are scoped per endpoint". Doc `idempotency.md` ↔ schema vuelven a coincidir                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| AUD-P1-010 (posting sin validación de saldo) | **RESUELTO** (lote AUD-1)                               | 2026-07-04 | `PostTransactionInput.nonNegativeAccounts` verificado DENTRO de la tx bajo locks de cuenta (race-safe), `InsufficientBalanceError` → rollback total; `PostingService.twoLegged` protege la cuenta debitada (release/refund/settleRefund). Tests: `ledger-service.test.ts` → describe "AUD-P1-010" (sobregiro rechazado sin efectos, frontera exacta en 0 pasa, opt-in explícito) y golden `posting.test.ts` → "cannot release more than merchant.pending nor refund more than merchant.available"                                                                                                                                                                                                                                                                                                                                   |

## Hallazgos P2

| ID                                           | Estado                                                                                                  | Fecha      | Evidencia / referencia                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AUD-P2-001 (replay sin metadata causal)      | **RESUELTO** (lote AUD-1)                                                                               | 2026-07-04 | `service.ts replay()` compara `reason`, `source_type`, `source_id`, `reverses_tx_id` contra la fila persistida además de la firma de entries; divergencia ⇒ `IdempotencyConflictError`. Tests: "rejects key reuse when only the REASON differs" y "…only the SOURCE differs"                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| AUD-P2-002 (payment_intents mínima)          | **RESUELTO** (F3-01, decisión #24)                                                                      | 2026-07-05 | Migración 0017: modelo real (guard: la tabla debía estar vacía — verificado en la propia migración), 12 estados, FK compuesta intent↔merchant↔tenant (probado que un intent no puede referenciar el merchant de otro tenant ni como superusuario), CHECKs `captured ≤ amount` y `refunded ≤ captured` (probados), tabla `payment_attempts` con su FSM, `@fluvia/payments-core` con `PaymentIntentService` (RLS, lock de fila, evento outbox en la misma tx)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| AUD-P2-003 (API keys live sin frontera)      | **RESUELTO** (lote AUD-1)                                                                               | 2026-07-04 | `ApiKeyService.create` rechaza `environment='live'` con `LiveKeysDisabledError` (HTTP 403 `live_keys_disabled`). Opción más restrictiva y reversible de las dos que ofrecía el auditor; desbloqueo futuro = production gates + decisión humana **PEND-004**. Test: "refuses to create live-environment keys while no live plane exists" (verifica además que no persiste nada)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| AUD-P2-004 (projections actualizables)       | **RESUELTO** (F2-05; mitigado antes por 0008)                                                           | 2026-07-04 | Guard de versión + `verifyProjection` (previos) + 0008 (worker sin privilegios) + F2-05: `rebuildProjection` race-safe bajo lock de cuenta (property test con posting/rebuild concurrentes), `ledger_projection_drift()` (definer, solo worker) y `ProjectionDriftWatcher` programado en el worker con alerta por log (métricas formales = F1-07). Reparación siempre explícita                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| AUD-P2-005 (outbox sin envelope)             | **RESUELTO** (F2-11)                                                                                    | 2026-07-04 | `@fluvia/events`: envelope `event_id (evt_uuid)/schema_version/occurred_at/producer/resource/data` con schema Zod estricto; el productor valida al emitir (`buildEnvelope`) y el relay re-valida al despachar (no conforme = veneno→dead). Ledger migrado al sobre (test del envelope en `ledger-service.test.ts`); contrato documentado en `outbox-inbox.md` §1                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| AUD-P2-006 (conciliación)                    | **RESUELTO** (F4-01→F4-03)                                                                              | 2026-07-07 | El pipeline de conciliación existe y está probado vs PG real: motor `@fluvia/reconciliation` (F4-01a: casa `succeeded` vs settlement report del proveedor, clasifica las 4 discrepancias, RLS + append-only) → API de gestión por API key (F4-01b) + vista de dashboard por sesión (F4-01c) → conciliación **continua** por `ReconciliationWatchdog`/`sweep_settlement_reports()` (F4-02, 0028, definer/worker cross-tenant) → **casos operativos** materializados por discrepancia (F4-03a, 0029) → **ajuste monetario con four-eyes** (F4-03b, 0030: dos humanos distintos por CHECK en BD, asiento compensatorio `recon.differences↔suspense`) operable por sesión (F4-03c). Runbook `reconciliation-discrepancy.md` **probado en drill PASS 9/9** (F4-06b, four-eyes sobre HTTP real + `FLUVIA_INVARIANTS_OK`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| AUD-P2-007 (observabilidad/restore/runbooks) | **RESUELTO** — observabilidad ✅ (F1-07) + runbooks ✅ (F4-06a/b) + **restore drill ✅ (F6, PASS 7/7)** | 2026-07-07 | F1-07: métricas Prometheus en API y worker (HTTP por ruta, relay/inbox por resultado, drift gauge, watchdogs de attempts/payouts/disputas, heartbeats), health/readiness en ambos procesos y alertas baseline (`observability.md` §4). **Runbooks (F4-06a) + drills (F4-06b)**: `docs/ops/runbooks/` con runbooks anclados a métricas/endpoints/mecanismos REALES; los **6 accionables ejecutados en drill** contra PG16 real (`apps/api/drills/`, cada uno con invariantes verdes): `reconciliation-discrepancy` 9/9, `aged-disputes` 7/7, `indeterminate-payment` 6/6, `outbox-inbox-stuck` 7/7, `worker-down` 5/5, `webhook-dead-letter` 5/5; `audit-investigation` de facto. **Restore drill (F6 · Gate Restore)**: `drill:restore` (`apps/api/drills/restore-drill.ts`, **PASS 7/7**) — `pg_dump -Fc` → `pg_restore --exit-on-error` (code=0 asserteado) en base fresca → `verify-ledger-invariants.sql`→`FLUVIA_INVARIANTS_OK` sobre la copia → paridad fuente↔copia (conteos de 4 tablas + Σamount + saldo del marcador + checksums row-level de asientos y transacciones, RPO=0) → drift inyectado detectado + `rebuildProjection` explícito → RLS FORZADO + políticas preservadas; runbook `backup-restore.md`. **Las tres patas cerradas**; la infra de backup de producción (cadencia/offsite/PITR) es decisión de despliegue (F6/F7, `production-gates.md` §Gate Restore 🟡) con el mismo criterio ya probado |
| AUD-P2-008 (passwords dev en migraciones)    | **RESUELTO** (F1-09)                                                                                    | 2026-07-05 | `fluvia_assert_dev_role_creation()` invocado antes de CADA `CREATE ROLE ... PASSWORD` (0002/0004/0009/0010; meta-test: 5 altas, 5 con guard) + el runner inyecta `fluvia.environment`: fuera de local/test, un rol ausente ABORTA la migración exigiendo credenciales gestionadas (probado end-to-end en BD efímera: falla en 'production', aplica en 'test'; sin registro parcial). R-12 queda mitigado por código, no solo por documentación                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| AUD-P2-009 (taxonomía de errores)            | **RESUELTO** (F1-08)                                                                                    | 2026-07-04 | Catálogo v1 cerrado (`error-catalog.ts`, 26 códigos) con golden comprometido como contrato (cambio breaking = test roto + bump consciente); sobre estable `{type, code, message, details?, request_id}`; **el `message` interno de dominio ya NO se expone** (va a logs; el cliente recibe el texto público del catálogo — probado con fuga sembrada); 5xx opacos; forma default de Fastify irrepresentable; `details` imposible fuera de códigos declarados. Doc: `api-errors.md`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| AUD-P2-010 (SBOM/licencias)                  | **MITIGADO**; license report a F6                                                                       | 2026-07-04 | SBOM SPDX ya se genera como artifact en cada run de CI (auditor sin acceso a Actions). Pendiente: reporte de licencias transitivas + cierre F0-VER antes de release                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| AUD-P2-011 (FSM docs ↔ DDL sin meta-test)    | **RESUELTO** (F3-01)                                                                                    | 2026-07-05 | `fsm-meta.test.ts`: triple comparación EXACTA doc(mermaid §1/§2/§3)↔mapa TS↔tablas DDL (seed generado desde el mapa por `gen-fsm-seed.ts` — una sola fuente de verdad) + **matriz completa a nivel de motor**: los 144 pares (from,to) de intents y 64 de attempts probados con SQL de superusuario — todo par fuera del mapa lanza `FLUVIA_INVALID_TRANSITION`; las tablas de transiciones son inmutables (triggers)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| AUD-P2-012 (verify-ledger-invariants.sql)    | **RESUELTO** (F2-06)                                                                                    | 2026-07-04 | `scripts/verify-ledger-invariants.sql` autocontenido (6 checks: doble partida por tx/moneda, montos>0, coherencia cuenta-tenant-moneda, cabeceras huérfanas, reversiones cross-tenant, drift de proyecciones) + paso de CI vía psql tras la suite. Tests: pasa en BD sana, detecta corrupción sembrada (proyección adulterada como superusuario), vuelve a pasar tras reparación explícita                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| AUD-P2-013 (bucket reserved vs cuentas)      | **CERRADO POR DECISIÓN**                                                                                | 2026-07-04 | Decisión: reservas = CUENTAS del Chart (`merchant.reserve`, `dispute.reserve`, `refund.liability`); los buckets expresan liquidez (`available`/`pending`), no propósito. `ledger-design.md` §2/§6 corregido (la mención a `reserved` era resto del spike). Si Fase 4 demuestra necesidad de bucket, exigirá ADR nuevo                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| AUD-P2-014 (dbUrls sin anti-mezcla)          | **RESUELTO** (lote AUD-1)                                                                               | 2026-07-04 | `dbUrlsFromEnv`: defaults dev SOLO con `FLUVIA_ENV`/`NODE_ENV` local/test; en cualquier otro entorno lanza `FLUVIA_CONFIG` listando las URLs faltantes (FLUVIA_ENV tiene precedencia). Tests: `packages/db/test/config.test.ts` (4 casos, incluye precedencia)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| AUD-P2-015 (HMAC server-side keys)           | **RESUELTO**                                                                                            | 2026-07-05 | Migración 0016: `key_hash_version` (1=sha256 legado, 2=HMAC-SHA256 con pepper `API_KEY_HMAC_SECRET`, 64 hex, anti-mezcla en config — falla fuera de local sin valor explícito). Claves nuevas nacen v2; `authenticate_api_key(hmac, legacy)` autentica ambas y PROMUEVE v1→v2 en la misma llamada (el caller demostró el plaintext; upgrade probado). Un dump de `api_keys` ya no permite validar claves offline: falta el pepper, que jamás se persiste (test: hash almacenado ≠ sha256 del secreto). Prerequisito técnico de PEND-006 cumplido                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| AUD-P2-016 (CORS/headers/OpenAPI/load)       | PLANIFICADO                                                                                             | —          | OpenAPI ya estaba en F3-02; CORS/security headers a F3 (primer consumo browser); load a F2-08/F6                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |

## Hallazgos P3

| ID                                             | Estado                    | Fecha      | Evidencia / referencia                                                                                                                                                                                                                               |
| ---------------------------------------------- | ------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AUD-P3-001 (README desactualizado)             | **RESUELTO** (lote AUD-1) | 2026-07-04 | README reescrito: estado real (Fase 2), tabla completa de paquetes, PEND resueltas/abiertas, negativa explícita de capacidades productivas y live keys                                                                                               |
| AUD-P3-002 (docs sin estado de implementación) | **RESUELTO** (lote AUD-1) | 2026-07-04 | Bloque "Estado de implementación" fechado en: `webhook-delivery.md`, `reconciliation.md`, `payment-lifecycle.md`, `payment-state-machines.md`, `outbox-inbox.md`, `idempotency.md` — distinguen DISEÑO / PARCIAL / construido con referencias a fase |
| AUD-P3-003 (Dockerfile/compose completo)       | PLANIFICADO               | —          | F3 (junto a E2E sandbox)                                                                                                                                                                                                                             |

## Re-auditoría v2 (2026-07-07) — hallazgos y estado de remediación

Segunda auditoría independiente (read-only) sobre el commit base `e7539f5`. **Veredicto: las remediaciones P1 de la v1 quedan confirmadas ✅, todo el código nuevo evaluado es 🟢 Sólido, y NO se hallaron P0/P1 nuevos.** Informe: `docs/audits/re-audit-v2-delta.md` (inmutable, como el de v1).

**Alcance:** el commit auditado (`e7539f5`) llega hasta conciliación (F4-03). Los incrementos apilados de payouts/disputas/webhooks-fan-out/observabilidad y los 6 drills de runbook (F4-05/07/08/09/10, migraciones 0031-0038) quedaron **fuera del alcance de v2** (se construyeron después del base auditado, sobre la rama del PR); su evidencia vive en §Evidencia CI (runs de esos incrementos) y en los drills PASS.

Los 11 hallazgos vigentes del informe v2 y su estado tras este lote de remediación (verificado contra PG16 real: suite completa + `migrate` ×2 + `verify-ledger-invariants.sql`→`FLUVIA_INVARIANTS_OK` + los 6 drills + revisión adversarial en workflow):

| ID (sev.)                                                         | Estado                                          | Evidencia / referencia                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ----------------------------------------------------------------- | ----------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| V2-R1 (P2) — `withTenantTransaction` sin cotas de tiempo          | **RESUELTO**                                    | `pool.ts`: `SET LOCAL statement_timeout`/`lock_timeout`/`idle_in_transaction_session_timeout` (defaults 30 s/15 s/60 s, sobreescribibles por tx) inyectados junto al tenant en un solo `set_config` parametrizado. 4 tests nuevos (`packages/db/test/tx-timeouts.test.ts`) incl. abort REAL de una sentencia lenta (SQLSTATE `57014`). Suite de concurrencia del ledger (120 transferencias + deadlock pressure) verde ⇒ el `lock_timeout` no rompe la contención legítima                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| V2-R2 (P2 latente) — `entriesSignature` con separadores `\|`/`\n` | **RESUELTO**                                    | `service.ts`: firma ahora es `JSON.stringify` de tuplas posicionales ordenadas por su forma canónica — ningún campo puede provocar colisión de firma. Se recomputa en AMBOS lados de la comparación de replay y NUNCA se persiste ⇒ cambio de formato interno, sin migración. Suite de replay/idempotencia (49 tests) verde                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| V2-R3 (P3 perf) — constraint trigger `FOR EACH ROW` redundante    | **ACEPTADO**                                    | La garantía por-fila es defensa-en-profundidad deliberada (balance verificado en cada inserción, no solo al final de la sentencia); el costo es marginal frente al valor de la invariante. Registrado como riesgo aceptado                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| V2-R4 (P2) — `SUM(BIGINT)` sin protección de overflow             | **RESUELTO**                                    | `verifyProjection`/`rebuildProjection` (`service.ts`) e invariantes SQL: recomputo en `::numeric` (`SUM(bigint)` ya es `numeric` sin overflow; el `::bigint` explícito abortaría en el borde). Verificado empíricamente que `(N::bigint)::numeric::text == (N::bigint)::text` para positivo/negativo/cero ⇒ la comparación contra la proyección (bigint) sigue exacta, sin false-fail                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| V2-R5 (P3) — `Money.allocate` sesgo hacia índice 0                | **DOCUMENTADO**                                 | El reparto round-robin del residuo empezando en índice 0 es DETERMINISTA a propósito (reproducible en tests/auditoría); documentado en JSDoc con la guía para callers que necesiten repartir el sesgo. Comportamiento sin cambio (invariante suma-de-partes intacta)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| V2-R6 (P3) — `applyProjectionDeltas` fallback `?? {0n,0n}`        | **RESUELTO** (aclarado)                         | Comentarios en `service.ts` distinguen los dos `??`: uno es la INICIALIZACIÓN del acumulador por cuenta; el otro es un cero DEFENSIVO explícito (cuenta bloqueada sin movimiento solo sube `version`) — ninguno enmascara un bug. `accountIds == cuentas de input.entries`, así que `deltas` siempre cubre cada id                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| V2-R7 (P3) — SQLSTATE 40001 inalcanzable con READ COMMITTED       | **DOCUMENTADO**                                 | Comentario en `RETRYABLE_SQLSTATES`: `40001` es forward-looking (listo para cuando alguna operación opte por SERIALIZABLE); el deadlock `40P01` SÍ ocurre y se reintenta. Sin cambio de comportamiento                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| V2-N1 (P2 inferido) — `WebhookDeliverer` fallback a clave DEV     | **VERIFICADO — no explotable** (falso positivo) | La clave DEV solo se retorna en `local`/`test`: `required()` (anti-mezcla V4 §43, `config/src/index.ts:205`) LANZA `ConfigError` fuera de local/test en vez de devolver el default. Ambos call sites de producción inyectan el valor guardado (`worker/main.ts:284`, `api/app.ts:255` → `config.webhookSecretEncKey`). El `?? DEV_..._HEX` del constructor es patrón UNIFORME del codebase (idéntico en `identity/api-keys.ts:140` y `auth/service.ts:173`), guardado en el borde de config — exactamente el mecanismo AUD-P2-008 que el auditor hipotetizó pero no pudo verificar                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| V2-N2 (P3) — `resolveSafeWebhookTarget` solo valida la primera IP | **RESUELTO (F6)**                               | Precisión previa: validaba TODAS las IPs pero pineaba solo `ips[0]`. Ahora el target lleva todas las IPs validadas (`SafeWebhookTarget.ips`) y el deliverer hace failover entre ellas SOLO cuando la conexión jamás se estableció (cap 3, sin re-resolver — re-resolver reabriría la ventana de rebinding); jamás si el socket conectó o hubo respuesta HTTP — el borde "conectó pero calló ⇒ no re-enviar el mismo intento firmado a otra IP" lo encontró la revisión adversarial del propio bundle y se corrigió antes de mergear. Cerrado dentro del bundle F6 de SSRF: `rejectUnauthorized:true` explícito (inmune a `NODE_TLS_REJECT_UNAUTHORIZED=0`, probado contra receptor TLS self-signed que jamás ve el payload), rate limit por IP del ingest público en `onRequest` (backend inyectable TM-03; el keying por `req.ip` exige `trustProxy` acotado al fronting con proxy — registrado en threat model §5 como bloqueante de PEND-006), test HTTP del body >1 MiB (413, cero persistencia). Tests: `delivery.test.ts`, `webhook-ingest.test.ts` |
| V2-N3 (P3) — `refund.execute()` lee fuera de tx                   | **DOCUMENTADO**                                 | Comentario en `refunds.ts`: la lectura es best-effort e intencional (solo decide SI ejecutar); la seguridad la da el `UPDATE ... WHERE id=$1` bajo el trigger FSM — si el estado cambió, 0 filas ⇒ no-op, jamás doble asiento                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| V2-N4 (P2) — umbral four-eyes validado solo en servicio           | **DIFERIDO / DOCUMENTADO**                      | El guard de DOS HUMANOS distintos SÍ está en la BD (`0030` CHECK: `requires_second_approval` ⇒ `approved_by_user_id <> proposed_by_user_id`). Lo service-computado es solo el FLAG `requires_second_approval` (`amount >= threshold`), precisamente porque el umbral es config-driven por despliegue (`adjustments.ts:141-149`, `fourEyesThresholdMinor`), lo que hace un CHECK estático sobre el umbral awkward. Recomputarlo en BD exige el umbral como setting/tabla (cambio de diseño); diferido para evaluación. La invariante núcleo (dos humanos) es DB-enforced                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |

**Verificación adversarial de la propia remediación (workflow):** el diff de remediación se sometió a una revisión adversarial multi-agente (dimensiones: regresión de timeouts, preservación de idempotencia/replay, corrección numérica SQL; cada hallazgo verificado por un refutador independiente). Surgieron y se corrigieron **4 hallazgos de seguimiento CONFIRMADOS** (introducidos por V2-R1/V2-R4 mismos), en un commit posterior:

| #         | Hallazgo (sev.)                                                                                                                                                                                                                                                                          | Corrección                                                                                                                                                                                                            |
| --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| RR-1 (P2) | El `statement_timeout=30 s` por defecto de V2-R1 abortaría el `SUM` de historial completo en `rebuildProjection`/`verifyProjection` (único camino de reparación de drift) y en `reconcile` (FULL OUTER JOIN de un reporte mensual grande) en cuentas de plataforma con millones de filas | Override por-tx ACOTADO de 10 min en esas tres operaciones pesadas (`HEAVY_RECOMPUTE_TX_TIMEOUTS`/`RECONCILE_TX_TIMEOUTS`) — completan sin reintroducir la espera infinita                                            |
| RR-2 (P2) | V2-R4 endureció `verifyProjection`/`invariants.sql` a `::numeric` pero NO `ledger_projection_drift()` (0011), el detector CONTINUO del worker — donde un `::bigint` que aborte ciega al watchdog frente a TODAS las cuentas                                                              | Migración `0039`: `ledger_projection_drift()` recomputa y retorna `numeric` (DROP+CREATE por cambio de firma). `drift.ts` ya lee vía `::text` ⇒ sin cambio de código                                                  |
| RR-3 (P3) | El `lock_timeout=15 s` de V2-R1 introduce `55P03` (lock_not_available), ausente de `RETRYABLE_SQLSTATES` ⇒ una contención transitoria en cuenta caliente que antes esperaba ahora falla duro con 500                                                                                     | `55P03` agregado a `RETRYABLE_SQLSTATES`: se reintenta con backoff (cada intento acotado por el propio lock_timeout, total limitado por `maxRetries`) — no se cae un asiento legítimo, sin reabrir la espera infinita |
| RR-4 (P3) | `ms()` (pool.ts) solo acotaba el borde inferior; un override > INT_MAX o en notación exponencial haría que `set_config` lance `22023` y aborte la tx en el setup                                                                                                                         | `ms()` acota a `[1, 2^31-1]`; test de regresión (un override enorme resuelve en vez de abortar)                                                                                                                       |

**Cobertura del auditor (no hallazgos):** v2 marcó `AUD-P1-006` (rate limiting + MFA) y `AUD-P2-014` (dbUrls anti-mezcla) como "no verificados en profundidad" por falta de lectura, no por defecto. Ambos ya están **RESUELTOS** con evidencia v1 (run #29 para MFA/rate-limiting; `config.ts` `required()`/anti-mezcla V4 §43 para dbUrls, citado arriba en V2-N1).

## Re-auditoría F6 delta (2026-07-10) — hallazgos RA-F6-\*

Delta audit externo sobre el baseline mergeado `claude/new-session-haeo7h` @ `e6a185d876cdb04a9c4a0e3da3020adcce0a3763`. Veredicto: **0 P0, 1 P1** (RA-F6-001). El P1 **bloquea el F6 gate, la Fase 5, la exposición pública y producción** hasta que el delta audit confirme su cierre. F6 **NO se declara aprobado** hasta esa confirmación.

| ID | Sev | Hallazgo | Estado |
| --- | --- | --- | --- |
| RA-F6-001 | P1 | **Idempotencia financiera sin cotas transaccionales completas**: `IdempotencyService.execute()` (`packages/idempotency/src/index.ts`) abría una transacción MANUAL con solo `SET LOCAL lock_timeout` (interpolado), sin `statement_timeout` ni `idle_in_transaction_session_timeout` — divergía de la defensa sistémica V2-R1 de `withTenantTransaction` (`packages/db/src/pool.ts`): un handler patológico o una sesión idle dentro de la tx podía acaparar una conexión del pool sin límite, en el camino del dinero | **REMEDIADO — pendiente de confirmación por el delta audit** (PR aislado `fix/ra-f6-001-idempotency-transaction-timeouts`). Fix: `execute()` corre ahora por `withTenantTransaction` (las TRES cotas como `set_config(..., true)` parametrizado + clamping; se elimina el `BEGIN` manual y la interpolación `${}` del `lock_timeout` — la entrada del allowlist del guard estático se retira). Opciones nuevas validadas `statementTimeoutMs` (default 30 s) e `idleInTxTimeoutMs` (default 60 s) con la regla `statement > lock` que preserva el contrato 55P03 → 409. Semántica intacta: el COMMIT temprano del replay (solo lectura) pasa al COMMIT del helper (equivalente observacional); mapeo 55P03 → `processing_in_flight` conservado; mismo client/tx para el handler. Teeth: `packages/idempotency/test/tx-timeouts.test.ts` (5 tests vs PG16: tres cotas activas + tenant intacto; 57014 con rollback atómico y retry limpio; idle-kill sin estado huérfano y pool usable; secuencia de fallos sin agotar un pool max=2; validación del constructor). Los 13 tests existentes del contrato pasan sin cambios. Evidencia CI: se registra abajo al verde |
| RA-F6-002 | — | Reportado por el delta audit según instrucción del propietario; **el detalle del informe aún NO fue entregado al repo** | **PENDIENTE de integración** — NO abordado en este PR (aislamiento del fix) |
| RA-F6-003 | — | Ídem | **PENDIENTE de integración** |
| RA-F6-004 | — | Ídem | **PENDIENTE de integración** |
| RA-F6-005 | — | Ídem | **PENDIENTE de integración** |

## Decisiones humanas abiertas derivadas (no bloquean el plan)

- **PEND-004**: política definitiva de credenciales `live` (hoy: creación bloqueada por código). Propietario: humano.
- **PEND-005**: modelo MFA/recuperación de cuenta (TOTP propuesto). Propietario: humano.
- **PEND-006**: condiciones para abrir sandbox compartido (requiere F1-04b + AUD-P2-015). Propietario: humano.

## Evidencia CI

- Runs #1–#10: verdes (uno por commit hasta `a665b4f` = commit auditado, run #10).
- Runs #11–#12: rama documental de la auditoría (`9c17888`), verdes.
- **Lote AUD-1: run #13 VERDE** — commit `4cdfcb0`, `https://github.com/celestinojbm/Fluvia/actions/runs/28716967441` (install, lint, format, typecheck, migrate ×2 incl. 0008, 174 tests contra PG16, gitleaks, audit, SBOM).
- **F2-11 (cierra AUD-P1-004/007 y AUD-P2-005): run #15 VERDE** — commit `1ecc533`, `https://github.com/celestinojbm/Fluvia/actions/runs/28717692510` (migrate ×2 incl. 0009, 195 tests incl. relay multi-worker y meta-tests de roles).
- **F2-12 (cierra AUD-P1-005): run #18 VERDE** — commit `f391240`, `https://github.com/celestinojbm/Fluvia/actions/runs/28718633416` (migrate ×2 incl. 0010 sobre PG fresco, 215 tests incl. inbox). Nota de proceso: el run #17 (`bf72d98`) tuvo el job de calidad VERDE pero gitleaks marcó un falso positivo (fixture de test con aspecto de secreto); corregido con valor obviamente falso + `gitleaks:allow` puntual — no se silenció el escáner globalmente.
- **F2-05/F2-06 (cierran AUD-P2-004 y AUD-P2-012): run #20 VERDE** — commit `0ce18a9`, `https://github.com/celestinojbm/Fluvia/actions/runs/28719493607` (migrate ×2 incl. 0011, 224 tests, y el NUEVO paso de CI ejecutando `scripts/verify-ledger-invariants.sql` vía psql sobre la base poblada por la suite).
- **F2-07/F2-08 (Gate Ledger 🟢 técnico): run #22 VERDE** — commit `616138b`, `https://github.com/celestinojbm/Fluvia/actions/runs/28720890852` (migrate ×2 incl. 0012, 233 tests incl. reversiones bajo carrera y suite formal de concurrencia).
- **F1-08 (cierra AUD-P2-009): run #24 VERDE** — commit `4303a93`, `https://github.com/celestinojbm/Fluvia/actions/runs/28721370832` (241 tests incl. contrato golden del catálogo de errores y tests de no-fuga de mensajes internos).
- **F2-09/F2-10 (cierra AUD-P1-003; Gate Idempotencia 🟢; Fase 2 completa): run #26 VERDE** — commit `29695d9`, `https://github.com/celestinojbm/Fluvia/actions/runs/28721992797` (migrate ×2 incl. 0013, 255 tests incl. crash-recovery y carrera N→1 de la capa de idempotencia).
- **F1-04b (cierra AUD-P1-006; criterio de re-auditoría 5/5): run #29 VERDE** — commits `d8d1090` + `b65cb68`, `https://github.com/celestinojbm/Fluvia/actions/runs/28723083681` (migrate ×2 incl. 0014, 278 tests incl. vectores RFC 6238, anti-replay por step, lockout compartido, step-up HTTP y rate limiting 429). Nota de proceso: el run #28 (`d8d1090`) tuvo el job de calidad VERDE (lint, types, migrate ×2, 278 tests, invariantes) pero gitleaks marcó como falso positivo la clave de desarrollo local documentada (patrón `00112233…`, régimen R-12); anotada puntualmente con `gitleaks:allow` — el escáner no se silenció globalmente.
- **F1-07 (AUD-P2-007 → PARCIAL, pata de observabilidad): run #31 VERDE** — commit `4dd46c8`, `https://github.com/celestinojbm/Fluvia/actions/runs/28726862210` (298 tests incl. formato de exposición Prometheus, anti-cardinalidad por plantilla de ruta, /metrics sin identificadores de tenant y aislamiento de fallos del observador de métricas en relay/drift/heartbeat).
- **F1-09 (cierra AUD-P2-008): run #33 VERDE** — commit `1d17ec0`, `https://github.com/celestinojbm/Fluvia/actions/runs/28727371526` (migrate ×2 incl. 0015 y las migraciones de roles CON guard aplicadas sobre PG fresco; 310 tests incl. purga de alcance exacto, no-escape financiero con GUC activo, DELETE de superusuario bloqueado y el guard probado en ambos sentidos sobre BD efímera).
- **F1-10 (Fase 1 completa; sin hallazgo asociado — evidencia de proceso): run #35 VERDE** — commit `89169e8`, `https://github.com/celestinojbm/Fluvia/actions/runs/28727678396` (315 tests incl. seeds deterministas: segunda corrida con CERO filas nuevas y guard de entorno probado antes de tocar la BD).
- **AUD-P2-015 (HMAC server-side) + diseño F3-01: run #37 VERDE** — commit `24050a2`, `https://github.com/celestinojbm/Fluvia/actions/runs/28728095481` (migrate ×2 incl. 0016 sobre PG fresco; 316 tests incl. upgrade perezoso v1→v2 y dump-resistance del hash de API keys).
- **F3-01 (cierra AUD-P2-002 y AUD-P2-011; decisión #24): run #39 VERDE** — commit `2aac4c9`, `https://github.com/celestinojbm/Fluvia/actions/runs/28729119401` (migrate ×2 incl. 0017 sobre PG fresco; 332 tests incl. meta-test triple doc↔TS↔DDL y la matriz de motor completa — 144 pares de intent + 64 de attempt — con SQL de superusuario).
- **F3-02 (AUD-P1-002 → PARCIAL; endpoints idempotentes de payment intents): run #42 VERDE** — commits `53b878c` + `49d066f`, `https://github.com/celestinojbm/Fluvia/actions/runs/28729908202` (348 tests incl. replay exacto sobre HTTP, claim revertido sin quemar key, 409 `invalid_state_transition` sobre HTTP y contrato OpenAPI↔servidor). Nota de proceso: en el run #41 los 348 tests PASARON pero vitest marcó un evento 'error' sin handler en el teardown del test del guard de migraciones (DROP DATABASE WITH (FORCE) terminando un socket en drenaje — 57P01); corregido con handler explícito en ese pool efímero únicamente.
- **F3-03 parte síncrona (ciclo de pago vivo; AUD-P1-002 sigue PARCIAL): run #44 VERDE** — commit `c27ad69`, `https://github.com/celestinojbm/Fluvia/actions/runs/28730299677` (363 tests incl. captura contable atómica vía onPosted con re-ejecución no-op, rechazo sin asientos, timeout→indeterminate sin resolución por asunción y confirm idempotente end-to-end sobre HTTP).
- **F3-03b (F3-03 cerrado; primer handler real del inbox — completa el desbloqueo que pedía AUD-P1-005): run #46 VERDE** — commit `ee15d73`, `https://github.com/celestinojbm/Fluvia/actions/runs/28731084988` (372 tests incl. cadena asíncrona completa sobre HTTP: webhook firmado → ingesta durable → processor → captura atómica; firma inválida sin persistencia; tardíos ignored_out_of_order sin doble asiento).
- **F3-04 (robustez de attempts; evidencia de proceso): run #48 VERDE** — commit `95e8e4f`, `https://github.com/celestinojbm/Fluvia/actions/runs/28731534657` (migrate ×2 incl. 0018; 385 tests incl. barrido con lease + auditoría atómica, resolución post-barrido solo por fuente verificada, breaker con reloj inyectado y semántica circuito-abierto = fallo limpio sin asiento).
- **F3-07 (webhooks salientes; AUD-P1-002 avanza — solo falta refunds E2E): run #50 VERDE** — commit `a3ddab3`, `https://github.com/celestinojbm/Fluvia/actions/runs/28732295370` (migrate ×2 incl. 0019 con rol `fluvia_webhook` bajo el guard AUD-P2-008; 409 tests incl. fan-out con alcance por tenant/topic/estado, firma verificable con rotación dual, SSRF estricto sin tráfico de red, calendario de reintentos→dead y secreto entregado UNA sola vez por la API).
- **F3-08 (refunds E2E; AUD-P1-002 RESUELTO por completo): run #52 VERDE** — commit `a598bc1`, `https://github.com/celestinojbm/Fluvia/actions/runs/28733552846` (migrate ×2 incl. 0020 con FSM `refund_transitions` en el motor; 436 tests incl. asiento compensatorio por la vía normativa, matriz de 36 pares como superusuario, saldo insuficiente→canceled sin contactar al proveedor, throw/`pending`→`indeterminate` con reserva retenida y resolución verificada vía `resolveFromProvider`. Revisión adversarial multi-agente del flujo del dinero: cero defectos confirmados).
- **F3-05a (customers; fundación de checkout): run #54 VERDE** — commit `7297965`, `https://github.com/celestinojbm/Fluvia/actions/runs/28734624528` (migrate ×2 incl. 0021; 458 tests incl. email duplicado permitido, update parcial con `null` limpia + metadata reemplaza, softDelete idempotente, aislamiento cross-tenant 404 y OpenAPI +5 paths por el test de contrato).
- **F3-05b (checkout sessions — el recurso): run #56 VERDE** — commit `46a3627`, `https://github.com/celestinojbm/Fluvia/actions/runs/28748142399` (migrate ×2 incl. 0022 con FSM `checkout_session_transitions` en el motor; 476 tests incl. matriz de 9 pares como superusuario, create idempotente con replay exacto sobre HTTP, guard de intent resuelto→409, client_secret entregado una vez con solo su hash en reposo y aislamiento cross-tenant).
- **F3-05c-i (backend del flujo alojado): run #58 VERDE** — commit `671860f`, `https://github.com/celestinojbm/Fluvia/actions/runs/28748725989` (migrate ×2 incl. 0023 `checkout_session_authenticate` SECURITY DEFINER; 483 tests incl. auth cross-tenant por client_secret sin API key, vista redactada, completado/expiración con evento único + idempotencia, intent-succeeded gana sobre TTL, y ciclo completo sobre HTTP crear→confirmar→/status completed).
- **F3-05c-ii (entrega garantizada de eventos de checkout): run #60 VERDE** — commit `016178e`, `https://github.com/celestinojbm/Fluvia/actions/runs/28749057996` (migrate ×2 incl. 0024 `sweep_checkout_sessions()` SECURITY DEFINER; 489 tests incl. el sobre construido EN SQL validado contra el MISMO `EventEnvelopeSchema` del relay, completado/expiración de sesiones no consultadas vía rol `fluvia_worker`, ramas disjuntas, intent-succeeded gana sobre TTL, idempotencia con la vía perezosa).
- **F3-05c-iii (confirm alojado por client_secret): run #62 VERDE** — commit `487ab85`, `https://github.com/celestinojbm/Fluvia/actions/runs/28749600623` (496 tests incl. `POST /confirm` sin API key en dos fases, tok_approve→completed, tok_decline→intent failed+sesión open, doble submit idempotente sin segundo attempt, secreto malo→404, token faltante→400 — el flujo de pago alojado funcional extremo a extremo por backend).
- **F3-05c-iv (UI Next.js alojada; F3-05 COMPLETO): run #64 VERDE** — commit `1c9da26`, `https://github.com/celestinojbm/Fluvia/actions/runs/28750616451` (PRIMER app frontend del monorepo — Next.js 15/React 19; ambos jobs verdes: calidad (lint/format/build-tsc/test, 501 tests incl. 5 de componente jsdom+axe) Y seguridad (`pnpm audit --audit-level high` limpio con Next 15.5.16 — el advisory crítico de Next 14 motivó el bump). E2E de navegador full-stack verificado LOCALMENTE (Chromium real → API → PG; captura), documentado en `apps/checkout/e2e/README.md` porque el CI no tiene navegador).
- **F3-06 (payment links — plantilla "págame"): run #66 VERDE** — commit `c674aac`, `https://github.com/celestinojbm/Fluvia/actions/runs/28751293715` (515 tests; migración 0025 con `payment_link_resolve` SECURITY DEFINER que solo resuelve links ACTIVOS cross-tenant, `PaymentLinkService` que en la apertura pública genera payment_intent + checkout_session FRESCOS reutilizando F3-05 en UNA transacción atómica. Gestión idempotente (F2-09) con API key; apertura `POST :id/sessions` PÚBLICA sin API key. Tests: creación idempotente/replay, merchant ajeno→404 anti-enumeración, scope payments:write requerido, y apertura pública que genera una sesión pagable extremo a extremo hasta `completed`; link deshabilitado/inexistente→mismo 404).
- **F3-06-b (UI del payment link `/l/[id]`; el link cierra end-to-end para el comprador): run #68 VERDE** — commit `4f131fc`, `https://github.com/celestinojbm/Fluvia/actions/runs/28753699854` (521 tests; página pública `/l/[id]` en `apps/checkout` — server component que abre el link server-side (`POST :id/sessions`, F3-06) y REDIRIGE al flujo alojado `/c/{sessionId}#{clientSecret}` (F3-05c-iv); el `client_secret` viaja en el fragmento del `Location`, fuera de los logs de la navegación posterior. Link inválido/deshabilitado/API caída → misma vista de indisponibilidad, anti-enumeración. Lógica pura `resolveLinkSession` + vista de indisponibilidad probadas en CI sin navegador (jsdom + axe, 6 tests: destino con fragmento, `?lang=en` preservado, endpoint correcto, 404→null, API caída→null, body incompleto→null, a11y). La redirección real con fragmento a través del navegador verificada LOCALMENTE (Chromium: `/l/{id}` → 307 `Location: /c/{sid}#cs_…` → checkout carga `$ 77.000` → Pagar → completado), documentado en `apps/checkout/e2e/README.md`. Sin cambios de esquema ni dependencias).
- **F3-09a (visibilidad de la cola de webhooks + reenvío manual auditado de eventos `dead`; cierra el diferido de F3-07): run #70 VERDE** — commit `d590135`, `https://github.com/celestinojbm/Fluvia/actions/runs/28754828422` (536 tests; migración 0026 con columna `resent_from_event_id` + función SECURITY DEFINER acotada `webhook_event_resend` — `fluvia_app` tiene SELECT sobre la cola pero NO INSERT/UPDATE, revocados en 0019; el reenvío entra por la función. Reenviar NO resucita el evento muerto (estado terminal inmutable): clona (tenant, endpoint, topic, payload) como evento `pending` fresco enlazado vía `resent_from_event_id`, con audit event `webhook_event.resent` en la MISMA transacción. `GET /v1/webhook_events` (scope `read`, filtros endpoint/status), `GET :id` (con payload + historial de intentos), `POST :id/resend` (scope `webhooks:manage`; solo `dead` → si no, `invalid_state_transition`). 12 tests vs PG real: lectura RLS por tenant, clone+enlace+auditoría atómicos, aislamiento cross-tenant (404), guard de solo-`dead` (409), scope requerido. Sin códigos de catálogo nuevos (36), +2 domain codes → 48, golden regen; OpenAPI +3 paths).
- **F3-09b-i (plano de LECTURA del dashboard por sesión + membresía): run #72 VERDE** — commit `e5515d6`, `https://github.com/celestinojbm/Fluvia/actions/runs/28756147994` (541 tests; el dashboard es para un operador HUMANO → autentica por SESIÓN y su org=tenant sale de la MEMBRESÍA, no por API key; por Nivel A es el prerequisito de cualquier UI. Nuevo permiso RBAC `payments:read` (matriz + meta-test celda-a-celda + `access-control.md`; lo tiene todo rol — dato de tenant de solo lectura). Rutas `GET /v1/organizations/:orgId/{payment_intents,refunds,checkout_sessions,payment_links,webhook_events}` list+detail bajo `security.session`+`security.org('payments:read')`, reutilizando los MISMOS servicios y serializers que el plano de API key — una sola forma de recurso. Solo lectura: la escritura de operación (reenvío `dead`) sigue en el plano de API key F3-09a. 5 tests vs PG real: datos creados por API key y leídos por sesión, plano completo cableado+guardado, no-miembro→404, sin sesión→401, aislamiento por tenant. Sin migración ni deps nuevas; plano de sesión excluido del contrato OpenAPI como el resto).
- **F3-09b-ii (frontend del dashboard de operación; segundo app frontend del monorepo): run #74 VERDE** — commit `0e9e9b1`, `https://github.com/celestinojbm/Fluvia/actions/runs/28757381585` (553 tests; `apps/dashboard`, Next.js 15/React 19 patrón de `apps/checkout`. Login por sesión: `/login` → route handler `/api/session` que proxya a la API y fija la cookie **httpOnly** `fluvia_session` (el token NUNCA llega al JS del navegador; MFA fuera de alcance → se avisa, no se simula, Nivel A). `/` lista las organizaciones del operador (membresía) → `/o/[orgId]` (server component) trae las 5 vistas del plano de lectura F3-09b-i con el token de la cookie server-side y renderiza tablas accesibles (intents/refunds/checkout sessions/payment links/cola de webhooks) con montos por moneda/locale y badges por estado. SOLO lectura. i18n es/en, WCAG AA. 12 tests CI-gated: lógica pura del cliente API (login incl. MFA-required sin fingir sesión, orgs, agregación con degradación por recurso) + componente/accesibilidad (jsdom + axe). E2E de navegador full-stack verificado LOCALMENTE (Chromium real → cookie httpOnly → picker → panel con intent `$ 123.000` y link `$ 45.000`; captura), documentado en `apps/dashboard/e2e/README.md`. Sin migración; deps ya presentes).
- **F3-09b-iii (reenvío `dead` desde el panel; F3-09 dashboard mínimo COMPLETO): run #76 VERDE** — commit `7a057e4`, `https://github.com/celestinojbm/Fluvia/actions/runs/28758670786` (559 tests; el dashboard pasa a ser superficie de OPERACIÓN. Backend: nuevo permiso RBAC `webhooks:manage` (espeja el scope de API key homónimo en el plano de sesión; solo owner/admin/developer; matriz + meta-test + `access-control.md`), ruta `POST /v1/organizations/:orgId/webhook_events/:id/resend` (sesión + membresía + `webhooks:manage`) que reusa `WebhookEventService.resend` auditando como `actorType: user`. Frontend: botón «Reenviar» (client component) en la cola de webhooks solo para eventos `dead` y solo si el rol puede (hint `canResendRole`; el API da 403 a quien no); POSTea al route handler que reenvía la cookie de sesión → «Reenviado ✓» + recarga. 6 tests CI-gated (3 ruta HTTP admin→201/read_only→403/no-dead→409+no-miembro→404, 3 frontend). E2E de navegador verificado LOCALMENTE (Chromium: login owner → clic Reenviar en el `dead` → 201 → cola en 2 = dead + pending fresco; captura). Sin migración; deps ya presentes).
- **F3-10 (SDK TypeScript del contrato OpenAPI): run #78 VERDE** — commit `0309744`, `https://github.com/celestinojbm/Fluvia/actions/runs/28759189886` (569 tests; `@fluvia/sdk`, cliente tipado del plano de integración por API key SIN dependencias de runtime — `fetch` global inyectable. Cubre las 27 (método,ruta) del contrato con namespaces por recurso, tipos que espejan la forma pública, `FluviaApiError` (sobre estable code/request_id) e Idempotency-Key autogenerada. `contract.test.ts`: `SDK_ROUTES` == el conjunto (método,ruta) de `openapi.v1.json` en AMBAS direcciones — añadir una ruta al spec sin método en el SDK (o al revés) rompe CI, así el SDK jamás deriva del contrato. `sdk.test.ts`: ejerce el SDK contra el API REAL en proceso (fetch delega en `app.inject` → auth/idempotencia/dominio reales, no mock): intent→confirm(async)→refund, customers CRUD, payment links, rotación de webhook endpoints, errores tipados 404/401. 10 tests (2 contrato + 8 e2e); README. Sin migración ni deps externas nuevas).
- **F3-11a (CORS + cabeceras de seguridad del API; cierra la parte de API de AUD-P2-016): run #80 VERDE** — commit `5b9d398`, `https://github.com/celestinojbm/Fluvia/actions/runs/28759905591` (576 tests; CORS de allowlist explícita vía `CORS_ALLOWED_ORIGINS` (default VACÍO = ningún cross-origin — checkout/dashboard llaman al API server-side; `*` = todos): hook `onRequest` que hace eco del origen permitido con `Access-Control-Allow-Origin` + `Vary: Origin` y responde el preflight OPTIONS (204 con Allow-Methods/Headers/Max-Age) solo para orígenes permitidos. Cabeceras de seguridad en TODA respuesta (hook `onSend`): `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy: no-referrer`, CSP `default-src 'none'; frame-ancestors 'none'`, `Cross-Origin-Resource-Policy: same-origin`, y HSTS solo fuera de local/test. 7 tests. Sin migración ni deps nuevas. **AUD-P2-016**: la parte de API queda cubierta; el resto (empaquetado/despliegue) es F3-11b/AUD-P3-003).
- **F3-11b (empaquetado de despliegue; cierra AUD-P3-003 y con F3-11a completa F3-11): run #82 VERDE** — commit `f0ff680`, `https://github.com/celestinojbm/Fluvia/actions/runs/28760659009` (576 tests, sin cambios de código; solo `Dockerfile`/`.dockerignore`/`docker-compose.yml`). `Dockerfile` multi-stage único del backend (API+worker comparten imagen; el servicio elige la app con `command`): build instala `--frozen-lockfile` + valida tipos (`pnpm build`), runtime slim que corre como usuario **no-root** (`node`), pnpm pineado horneado en la base. `docker-compose.yml` con profile `app` (el `up` por defecto sigue siendo PG+Redis): `migrate` one-shot (crea roles de mínimo privilegio + esquema) del que dependen `api` (:3000) y `worker`. **Verificado con Docker REAL local** (el CI no corre docker): `docker build` OK, `migrate` aplicó las migraciones en el contenedor contra PG containerizado, el API arrancó no-root y sirvió `/health`+`/ready` con los security headers de F3-11a, el worker arrancó limpio (watchdogs/deliverer + métricas). **AUD-P3-003 cubierto; F3-11 y el hardening de Fase 3 COMPLETOS**).
- **F4-01a (motor de conciliación — núcleo; arranca la Fase 4): run #84 VERDE** — commit `39edb4b`, `https://github.com/celestinojbm/Fluvia/actions/runs/28761911229` (580 tests; migración 0027 con `settlement_reports` + `settlement_lines` + `reconciliation_entries` (RLS por tenant + append-only). `@fluvia/reconciliation` `ReconciliationService`: `createReport`/`addLines` (idempotente por `(report, provider_ref)`)/`reconcile`/`getReport`/`listEntries`. El motor casa las líneas del settlement report del proveedor contra los intentos `succeeded` del tenant (mismo proveedor/moneda, dentro del periodo) vía FULL OUTER JOIN por `provider_ref` y clasifica: matched/amount_mismatch/missing_in_ledger/missing_at_provider; persiste las entries + marca el reporte `reconciled` en UNA tx. Append-only: se concilia una sola vez. Per-tenant bajo RLS, sin definer. 4 tests vs PG real: 4 clases de discrepancia + periodo respetado, idempotencia de addLines, guard de conciliar-una-vez, aislamiento por tenant. Sin deps externas nuevas).
- **F4-01b (gestión de conciliación — API de integración): run #86 VERDE** — commit `3b088a1`, `https://github.com/celestinojbm/Fluvia/actions/runs/28762516533` (584 tests; expone el motor F4-01a por el plano de API key: `POST /v1/settlement_reports` (crea, payments:write), `POST :id/lines` (carga líneas del proveedor, ≤1000, idempotente), `POST :id/reconcile` (dispara el motor, resumen de discrepancias), `GET :id` (reporte + resumen), `GET` (lista), `GET :id/entries` (discrepancias, filtro por status; read). `SettlementReportNotFoundError`→not_found, `ReportAlreadyReconciledError`→invalid_state_transition (sin códigos de catálogo nuevos, 36; +2 domain → 50; golden regen). 4 tests de ruta HTTP: cargar→conciliar→resumen 4 clases + GET reconciled con la discrepancia y su monto, reconciliar-dos-veces→409, scope payments:write + aislamiento por tenant, listar. Fuera del contrato OpenAPI v1 por ahora — no cascada al SDK. Sin migración ni deps nuevas).
- **F4-01c (vista de conciliación en el dashboard; cierra el bucle de conciliación de cara al operador): run #88 VERDE** — commit `2cf61e0`, `https://github.com/celestinojbm/Fluvia/actions/runs/28763304377` (591 tests; plano de lectura por sesión `GET /v1/organizations/:orgId/settlement_reports` (+`/:id` con resumen, +`/:id/entries`) bajo `security.session`+`payments:read`, reutilizando `ReconciliationService`. Frontend `apps/dashboard`: enlace «Conciliación» → `/o/[orgId]/reconciliation` (lista) → `/o/[orgId]/reconciliation/[reportId]` (resumen por clase + tabla de discrepancias con montos ledger/proveedor). i18n es/en, WCAG AA. 7 tests CI-gated (2 ruta de sesión + aislamiento por tenant, 5 frontend componente/axe/fetchers). E2E de navegador verificado LOCALMENTE (Chromium: login → Conciliación → reporte → detalle con `matched` $60.000/$60.000 y `missing_in_ledger` `er_phantom` proveedor $9.000; captura). Sin migración; deps ya presentes).
- **F4-02 (conciliación batch/continua — worker; de "a demanda" a "continua"): run #90 VERDE** — commit `e6fe863`, `https://github.com/celestinojbm/Fluvia/actions/runs/28764919421` (597 tests; migración 0028 con `sweep_settlement_reports()` SECURITY DEFINER sin parámetros — misma familia que `sweep_payment_attempts`/`sweep_checkout_sessions` —, EXECUTE solo para `fluvia_worker` (sin privilegios de tabla). Concilia cross-tenant, en UN statement set-based, los reportes con periodo cerrado (`open` + `period_end <= now()` — disparador natural de "reporte sellado"): FULL OUTER JOIN líneas del proveedor ⋈ intentos `succeeded` del ledger, clasificación idéntica al motor per-tenant F4-01a, inserta `reconciliation_entries` + marca `reconciled` TODOS los vencidos (incl. vacíos). Lease `FOR UPDATE SKIP LOCKED`; idempotente con la conciliación manual (ambas bajo el guard `status='open'`). `apps/worker` `ReconciliationWatchdog` (gate `RECONCILIATION_WATCHDOG_ENABLED`/`_INTERVAL_MS`, patrón attempts/checkout: `runOnce`/`start`/`stop`, observer que no rompe el job) invoca la función, expone `fluvia_settlement_reports_reconciled_total`/`fluvia_reconciliation_entries_total{status}`/`fluvia_reconciliation_discrepancies_last` y ALERTA ante discrepancias (entry ≠ matched = dinero real sin cuadrar). 6 tests vs PG real: enteros ≥0, sella cross-tenant con las 4 clases, NO toca periodo abierto, idempotencia (no re-barre ni duplica), `discrepancyCount`, observer que explota no rompe el job. Migrate ×2 idempotente incl. 0028; ledger invariants verdes. Docs espejo `reconciliation.md` §4/§5 + `observability.md` §2/§4. Sin deps externas nuevas).
- **F4-03a (casos operativos de conciliación — materialización + ciclo): run #92 VERDE** — commit `ca06f16`, `https://github.com/celestinojbm/Fluvia/actions/runs/28765612058` (605 tests; migración 0029 con `operational_cases` (RLS por tenant, mutable con ciclo `open→acknowledged→resolved` + `version`; sin DELETE/TRUNCATE) enlazada por FK compuesta por tenant a `reconciliation_entries` (nueva UNIQUE `(id,tenant_id)`) + snapshot de evidencia. Trigger `AFTER INSERT` `materialize_reconciliation_case` sobre `reconciliation_entries`: un caso por cada entry `!= matched`, automático para el motor F4-01a (fluvia_app) y el barrido F4-02 (dentro del definer como postgres), atómico + idempotente (`UNIQUE(reconciliation_entry_id)` + `ON CONFLICT DO NOTHING`). Severidad: `missing_in_ledger`→critical, `amount_mismatch`/`missing_at_provider`→high. `OperationalCaseService` (`list`/`get`/`acknowledge`/`resolve`) bajo RLS con auditoría `operational_case.acknowledged`/`.resolved`. **Nivel A: resolver es DOCUMENTAL — NO mueve dinero**; el ajuste con four-eyes es F4-03b. Plano API key `GET /v1/operational_cases`(+`:id`)/`POST :id/acknowledge`/`POST :id/resolve` (payments:write). `OperationalCaseNotFoundError`→not_found, `InvalidCaseTransitionError`→invalid_state_transition (sin códigos de catálogo nuevos, 36; +2 domain → 52; golden regen). 8 tests vs PG real (5 servicio: materialización un-caso-por-discrepancia + severidad + matched no crea caso, ack→resolve con nota + auditoría, actor api_key deja resolved_by null, re-resolver→error, aislamiento por tenant; 3 ruta HTTP: materializar→ack→resolve→409, scope + resolución vacía→400, aislamiento 404). Migrate ×2 idempotente incl. 0029; ledger invariants verdes. Docs espejo `reconciliation.md` §3 + `data-model.md` §4).
- **F4-03b (ajuste monetario con four-eyes — Nivel C; cierra el §30): run #94 VERDE** — commit `fb2b105`, `https://github.com/celestinojbm/Fluvia/actions/runs/28766249286` (611 tests; migración 0030 con `case_adjustments` (RLS por tenant, mutable `proposed→applied|rejected` + `version`; sin DELETE/TRUNCATE) enlazada al caso por FK compuesta por tenant (nueva UNIQUE `(id,tenant_id)` en `operational_cases`). **FOUR-EYES EN LA BD**: CHECK `approved_by_user_id <> proposed_by_user_id`, `proposed_by_user_id` NOT NULL, índice parcial único `WHERE status<>'rejected'` (un ajuste vivo por caso). `PostingService.postReconAdjustment` postea un asiento compensatorio REAL `recon.differences↔suspense` (platform/transitorias del chart existente, sin merchant; `reason='reconciliation'`, `source_type='case_adjustment'`). `CaseAdjustmentService` propone (humano)/aprueba (segundo humano DISTINTO sobre `FOUR_EYES_THRESHOLD_MINOR`, default 0=siempre)/rechaza — al aprobar postea el asiento + resuelve el caso + auditoría, TODO atómico vía `onPosted` del ledger e idempotente por `case_adj:{id}` (doble aprobación no duplica el asiento). Nivel A: `api_key`/IA→`HumanActorRequiredError`, autoaprobación sobre umbral→`SelfApprovalError`. 3 acciones de auditoría nuevas. 6 tests vs PG real: máquina rechazada, sobre-umbral proponente-no-autoaprueba + segundo humano aplica con asiento balanceado (débitos==créditos) + caso resuelto, bajo-umbral autoaprueba, re-aprobar aplicado→error con UN solo asiento, rechazo libera para nueva propuesta, aislamiento por tenant. Migrate ×2 idempotente incl. 0030; ledger invariants verdes (el asiento recon balancea). Entregado como MOTOR sin rutas (como F4-01a); la exposición sesión+UI es F4-03c. Docs espejo `reconciliation.md` §3 + `data-model.md` §4 + `ledger-chart-of-accounts.md`).
- **F4-03c (casos y ajustes por SESIÓN — four-eyes real sobre HTTP; backend): run #96 VERDE** — commit `e8720e4`, `https://github.com/celestinojbm/Fluvia/actions/runs/28766802843` (615 tests; permiso RBAC nuevo `reconciliation:manage` (owner/admin/finance; matriz + meta-test celda-a-celda + `access-control.md`). Rutas de sesión en `dashboard.ts` bajo `security.session`+membresía: `operational_cases` list/detail(+ajustes)/acknowledge/resolve (`reconciliation:manage`) y `case_adjustments` propose/approve/reject. **FOUR-EYES REAL SOBRE HTTP**: proponente=`req.identity.userId`, aprobador distinto sobre umbral; `SelfApprovalError`→409 `four_eyes_required` (código de catálogo NUEVO; 37 catálogo, +4 domain→56; golden regen). API key jamás alcanza este plano (autorizar dinero es humano). `CaseAdjustmentService` cableado en `app.ts` con `postingService`+`config.fourEyesThresholdMinor`. 4 tests HTTP vs PG real con DOS miembros `finance` distintos: U1 acknowledge→propone→U1 auto-aprueba→409 `four_eyes_required`→U2 aplica (200, `ledger_transaction_id` no nulo)→caso `resolved` con el ajuste en el detalle; read_only propone→403 `insufficient_permissions`; no-miembro→404; rechazo por U2 + read_only lista (payments:read). Sin migración ni deps nuevas. Falta solo la UI (F4-03c-ii). Docs espejo `access-control.md` + `reconciliation.md` §3).
- **F4-03c-ii (UI del panel de casos/ajustes con four-eyes; frontend) → cierra F4-03c: run #98 VERDE** — commit `7e8295b`, `https://github.com/celestinojbm/Fluvia/actions/runs/28769854547` (CI completo verde: lint + format:check + build ×21 paquetes + suite; 17 tests nuevos del dashboard → 37, jsdom + axe. `apps/dashboard`: «Casos» → `/o/:orgId/cases` (lista filtrable por estado + severidad) → `/cases/:caseId` (discrepancia + tabla de ajustes + acciones). Las acciones son islas cliente que POSTean a route handlers server-side que reenvían la cookie httpOnly `fluvia_session` como Bearer — el navegador JAMÁS toca el token ni la URL de la API (mismo patrón que el reenvío `dead` de F3-09b-iii): acknowledge/resolve (documental) del caso y propose/**approve**/reject del ajuste. **FOUR-EYES EN LA UI**: aprobar el propio ajuste → 409 `four_eyes_required` transmitido TAL CUAL por el proxy y mostrado como mensaje específico (no genérico); acciones solo a roles con `reconciliation:manage` (hint `canManageReconciliation`; el API decide 403); propuesta oculta si hay ajuste vivo (espeja el índice único parcial de la BD). Un **review adversarial** (5 dimensiones, verificación escéptica por hallazgo) confirmó y corrigió 1 issue de accesibilidad (aria-label del nav de filtros decía «Severidad» pero filtra por estado) y descartó un cambio que habría dejado ajustes huérfanos. `next build` valida las fronteras server/client + los 5 route handlers. E2E de navegador full-stack pendiente LOCAL (el CI no tiene navegador). Sin migración ni deps nuevas. Docs espejo `reconciliation.md` §3).
- **F4-04a (panel admin — «buscar comercios»; frontend): run #100 VERDE** — commit `a9aa92d` (validado por el run del tip `6ed0be9`), `https://github.com/celestinojbm/Fluvia/actions/runs/28770947990` (CI completo verde; +7 tests del dashboard → 44. Enlace «Comercios» → `/o/:orgId/merchants`: vista de solo lectura por sesión sobre el `GET .../merchants` existente (`merchants:read`, que tienen todos los roles); tabla nombre/id/país/moneda/estado (active/frozen traducido)/creado. Búsqueda accesible **sin JS** (`<form method=get>` → `?q=`, filtrado server-side `filterMerchants` por nombre/id/país/moneda), dos estados vacíos (sin comercios vs sin coincidencias). Server component puro — el token nunca sale del servidor. `next build` compila la ruta. Sin migración ni deps nuevas).
- **F4-04b (panel admin — «ver eventos»; frontend) → panel admin mínimo COMPLETO: run #100 VERDE** — commit `ba59ca3` (mismo run del tip `6ed0be9`), `https://github.com/celestinojbm/Fluvia/actions/runs/28770947990` (CI completo verde; +6 tests del dashboard → 50. Enlace «Eventos» (gateado por `canReadAudit` = owner/admin/finance/analyst) → `/o/:orgId/events`: vista por sesión sobre `GET .../audit-events` (`audit:read`) — la auditoría append-only de F1-05; tabla fecha/actor/acción/recurso/resultado (success/failure)/riesgo (low/medium/high), paginación por cursor `?before=` sin JS (preserva `lang`). Con esto las 4 piezas del **panel admin mínimo** (`mvp-scope.md` §1: buscar comercios · ver eventos · reprocesar webhooks · casos de conciliación) quedan entregadas — cierra **F4-04**. Sin migración ni deps nuevas. Docs espejo `mvp-scope.md`).
- **F4-06a (runbooks operativos; docs) → primer paso de F4-06: run #102 VERDE** — commit `c0782ee`, `https://github.com/celestinojbm/Fluvia/actions/runs/28771809633` (CI completo verde; **docs-only, sin código**. `docs/ops/runbooks/`: índice + 7 runbooks anclados a métricas (`observability.md` §4), endpoints y mecanismos REALES (inventario del código verificado): discrepancia de conciliación→caso→ajuste four-eyes · drift contable (SEV-1, investigar antes de rebuild; `rebuildProjection` única reparación sancionada) · eventos `dead` outbox/inbox→replay auditado (`replayDeadOutboxEvents`/`replayDeadProviderEvents`, `reason` obligatorio, admin pool, `platform.operation`) · pago `indeterminate`→`resolveFromProvider` por fuente verificada (nunca por asunción) · webhook `dead`→reenvío desde el panel · worker caído/watcher detenido→reinicio+drenado · investigación por auditoría. Cada uno Diagnóstico→Resolución→Verificación→Escalación + sección **Drill** pendiente. Cross-link desde `incident-response.md` y `observability.md` §4. Documenta honestamente que replay/rebuild son métodos de servicio sobre el admin pool SIN ruta HTTP/script aún (F6). El **drill ejecutable + evidencia** es F4-06b (cierra el criterio de salida de Fase 4). Sin código ni deps).
- **F4-06b (drill del runbook de conciliación — ejecutado PASS; código): run #108 VERDE** — commit `868f18c`, `https://github.com/celestinojbm/Fluvia/actions/runs/28772769374` (CI completo verde: build/lint/format/test — el drill es código tipado con override eslint `**/drills/**`. `apps/api/drills/reconciliation-drill.ts` (`pnpm --filter @fluvia/api run drill:reconciliation`) levanta la API real en proceso y la conduce **sobre HTTP** con dos operadores `finance` distintos, ensayando `reconciliation-discrepancy.md` end-to-end: siembra `missing_in_ledger` → concilia → caso `critical` materializado → U1 reconoce/propone → **four-eyes** U1 auto-aprueba→409 `four_eyes_required` → U2 aprueba→asiento+resuelto → verifica rastro de auditoría (`acknowledged`/`adjustment_proposed`/`adjustment_applied`) + **balance del asiento** (débitos==créditos). **Ejecución LOCAL: PASS 9/9** vs Postgres 16, con `verify-ledger-invariants.sql`→`FLUVIA_INVARIANTS_OK` (el CI valida el CÓDIGO del drill; su ejecución es local, como los E2E de navegador del dashboard). Cierra el criterio de salida de Fase 4 «runbooks probados en drill» para el flujo crítico; los demás runbooks con cobertura de facto por sus suites. Docs `docs/ops/runbooks/*`).
- **F4-05a (abstracción contable de reserves; ledger): run #112 VERDE** — commit `ff55f9b` (su run propio #110 quedó `cancelled` por el push de bookkeeping siguiente — concurrencia de GitHub; el CÓDIGO se valida por el run del tip `abf28b6`, que lo incluye como ancestro y corre la suite completa + migrate), `https://github.com/celestinojbm/Fluvia/actions/runs/28775115735` (CI completo verde: build ×21 + lint + format + suite + migrate. Migración 0031: añade `'reserve'` a las causas del ledger (idempotente `DROP IF EXISTS`+`ADD`) + `LEDGER_REASONS`. `PostingService.holdReserve`/`releaseReserve`: reclasificación `merchant.available ↔ merchant.reserve` (dos pasivos del comercio → la obligación TOTAL con el comercio no cambia, solo se aparta/libera para riesgo/disputas), reusando `twoLegged` con el guard de no-negatividad AUD-P1-010 (no reservar más de lo disponible ni liberar más de lo reservado). 3 golden tests vs PG real: round-trip capture→settle→hold→release con la invariante `available+reserve` constante + cero drift, guards de no-negatividad sin efectos, idempotencia de replay. Migrate ×2 idempotente incl. 0031; invariantes del ledger verdes (`FLUVIA_INVARIANTS_OK`). **Adelanta la parte DESBLOQUEADA de F4-05** (no depende de pricing): payout queda pendiente de diseño (falta cuenta de caja/banco en el chart), fees bloqueado por PEND-002. Docs espejo `ledger-chart-of-accounts.md`).
- **F4-05b (abstracción contable de payout; ledger): run #116 VERDE** — commit `1368294`, `https://github.com/celestinojbm/Fluvia/actions/runs/28777461604` (CI completo verde: build ×21 + lint + format + suite + migrate. Decisión contable tomada — no depende de pricing, es flujo de fondos. Migración 0032: corrige `payout.in_transit` de activo→PASIVO (tratamiento estándar de "payout en tránsito"; guard duro: aborta si hay algún asiento — la cuenta era placeholder sin usar) + backfill de `platform.cash` (activo, caja/banco de Fluvia) para charts existentes; el chart pasa a **14 cuentas** (9 platform + 5 merchant). Reglas de posting: `receiveProviderSettlement` (`debit platform.cash`/`credit provider.clearing` — el proveedor liquida a caja), `emitPayout` (`debit merchant.available`/`credit payout.in_transit` — no double-spend de fondos en vuelo), `settlePayout` (`debit payout.in_transit`/`credit platform.cash` — el banco confirma), `failPayout` (reverso). **Mejora de corrección**: `twoLegged` generaliza el guard AUD-P1-010 a TODA cuenta que decrece (calculado desde `normal_side`), cerrando el hueco donde el riesgo estaba en la cuenta ACREDITADA (`provider.clearing` en provider.settle/refund.settle; `platform.cash` en payout.settle — ahora imposible sobregirar caja o clearing). 4 golden tests vs PG real: flujo completo captura→proveedor-liquida→disponible→emit→settle con balances exactos + cero drift, fallo que devuelve íntegro al comercio, los 3 guards (provider.settle>clearing, emit>available, settle>cash), idempotencia. Suites afectadas verdes (payments-core 79, reconciliation 15, seeds 5); migrate ×2 idempotente incl. 0032; invariantes del ledger verdes. **Con F4-05b, solo resta F4-05c (fees), bloqueado por PEND-002.** Docs espejo `ledger-chart-of-accounts.md`).
- **F4-05c (motor de fees; cierra PEND-002 → 2% por transacción, decisión #25; payments-core): run #122 VERDE** — commit del motor `da373ff` (su run propio #120/#121 quedó ROJO: al cablear el 2% por defecto en `buildApp`, la suite de contrato de refunds `refund-routes.test.ts` (F3-08) sembraba liberando el BRUTO capturado a `available` mientras la captura ahora deposita `monto − Ff` en `pending` → underflow del guard AUD-P1-010; sólo caía el primer test, los siguientes heredaban `pending` acumulado. Corregido en `bda717a` fijando `PLATFORM_FEE_BPS=0` en esa suite — los refunds están acotados por `amount_captured` (bruto) y el disponible del ledger es neto, así que con fee>0 ambos divergen: la interacción refund↔fee es trabajo futuro, fuera del alcance del motor de fees). Validado por el run del tip `bda717a`, que incluye el motor como ancestro y corre la suite completa, `https://github.com/celestinojbm/Fluvia/actions/runs/28805898970` (CI completo verde: build ×21 + lint + format + suite PG real + migrate ×2 + `FLUVIA_INVARIANTS_OK`). `packages/payments-core/src/pricing.ts`: `FeeSchedule` + `FlatBpsFeeSchedule` (bps ∈ [0,10000] entero; `platformFee(monto) = monto.allocate([bps, 10000−bps])[0]` → redondeo por mayor residuo, invariante `0 ≤ Ff ≤ monto`) + `ZERO_FEE_SCHEDULE`. `PaymentConfirmationService` recibe el `FeeSchedule` como 5.º parámetro **REQUERIDO** (fail-fast: sin fee-cero silencioso en producción = pérdida de ingresos) y devenga `platform.fees = platformFee(monto)` en `recordApproved` (único camino de captura, sync + async). Producción (`apps/api`, `apps/worker`) instancia `FlatBpsFeeSchedule(config.platformFeeBps)`; env `PLATFORM_FEE_BPS` (default 200) en `@fluvia/config`. Tests: `pricing.test.ts` (6: 2% exacto, redondeo por mayor residuo + barrido de invariante, bps=0/ZERO, monto cero, moneda, bounds) + integración de fee al 2% por deltas en `confirmation.test.ts`; las 6 suites que instancian el servicio pasan `ZERO_FEE_SCHEDULE`. Suite total verde: payments-core 86, config 12, apps/api 163. **Con F4-05c, F4-05 (settlement/reserves/payout/fees) queda COMPLETO y los cuatro pilares contables de Fase 4 construidos.** Sin migración ni deps nuevas. Docs espejo `ledger-chart-of-accounts.md`, `DECISIONS.md` (#25), `BACKLOG.md`, `STATE.md`).
- **F4-07a (payouts como recurso gestionado — motor; payments-core): run #128 VERDE** — commit del motor `d20faaa` (su run propio quedó ROJO por un test de collation: `fsm-meta` comparaba el `ORDER BY from_status` de Postgres contra `transitionPairs` (localeCompare de Node), que discrepan en `in_transit` vs `indeterminate` — el guion bajo ordena ANTES de las letras en C.UTF-8 (la BD local, que coincidía con Node) pero DESPUÉS bajo el locale glibc de la imagen de Postgres de CI, así que CI devolvió `indeterminate` primero. Ningún otro FSM tiene un par guion-bajo↔letra, sólo este test se vio afectado; corregido en `bda…`→`259c08a` quitando el `ORDER BY` y ordenando en JS con el MISMO comparador → el test compara el CONJUNTO de transiciones, independiente del collation). Validado por el run del tip `259c08a`, que incluye el motor como ancestro y corre la suite completa, `https://github.com/celestinojbm/Fluvia/actions/runs/28810120789` (CI completo verde: build ×21 + lint + format + suite PG real + migrate ×2 incl. 0033 + `FLUVIA_INVARIANTS_OK`). Envuelve las primitivas de F4-05b (emit/settle/fail) como recurso: migración 0033 (`payouts` RLS/no-delete/FK de coherencia por tenant + `payout_transitions` INMUTABLE sembrada desde `PAYOUT_TRANSITIONS` + trigger de validación) con FSM `requested → in_transit → paid | failed | indeterminate`; `PayoutService` en dos fases (como F3-08): `create` valida fundabilidad (disponible − payouts `requested`; el guard AUD-P1-010 es el backstop atómico) y nace `requested`; `execute` emitPayout (sin disponible→`failed`) y submitPayout al banco (nuevo método opcional del `PaymentProvider` + `MockPaymentProvider`) → aprobado settlePayout→`paid`, rechazado/circuito failPayout (fondos íntegros de vuelta)→`failed`, throw/timeout/pending→`indeterminate` (fondos retenidos, V4 §23) que SOLO `resolveFromProvider` cierra; emite `payout.<estado>` al outbox. 8 golden tests vs PG real (ciclo money-out con balances exactos + cero drift incl. la caja del proveedor como precondición, fundabilidad, carrera→failed sin mover dinero, rechazo/circuito devuelven, indeterminate+resolución verificada, idempotencia) + fsm-meta extendido (doc §6 ↔ TS ↔ DDL + los 25 pares del motor + inmutabilidad). Motor SIN endpoints/UI (slices siguientes: API key + worker + dashboard); payouts públicos/reales bloqueados hasta gates. Docs espejo `payment-state-machines.md` §6 + `ledger-chart-of-accounts.md` + `BACKLOG.md`).
- **F4-07b (payouts — plano de API key; apps/api): run #134 VERDE** — commit `d66ed18`, validado por el run del tip `18129c5` (que lo incluye como ancestro y corre la suite completa), `https://github.com/celestinojbm/Fluvia/actions/runs/28811991914` (CI completo verde: build ×21 + lint + format + suite PG real + migrate ×2 + `FLUVIA_INVARIANTS_OK`). Expone el motor de F4-07a al integrador, espejando refunds (F3-08). Rutas `apps/api/src/routes/payouts.ts`: `POST /v1/payouts` (scope `payments:write`, Idempotency-Key obligatoria, contrato ASÍNCRONO: `beginIn` dentro de la tx de la key → `requested`, dispara `execute` fuera de tx; replay exacto), `GET /v1/payouts/:id`, `GET /v1/payouts` (por `merchant_id`, `read`), aislamiento por tenant (RLS). `ResilientProvider` gana `submitPayout` (mismo circuito/timeout que submit/refund — CircuitOpen→`failed provider_unavailable`, timeout→`indeterminate`, ya manejados por el motor). Catálogo: `payout_amount_exceeds_balance` (422, código nuevo; 38 catálogo) + `PayoutNotFoundError`→not_found, `InsufficientPayoutBalanceError`→422 (+2 domain → 58; golden regen). 5 tests de ruta HTTP vs PG real: crear→GET `paid`→lista + replay exacto, fundabilidad 422, Idempotency-Key requerida 400, read-only 403 + aislamiento (otro tenant no puede drenar el comercio ajeno: disponible 0 bajo su RLS → 422, y GET ajeno → 404). Fuera de OpenAPI v1 — no toca el SDK. Sin migración ni deps nuevas. Docs espejo `BACKLOG.md`.
- **F4-07c (payouts — robustez en el worker/barrido; apps/worker): run #134 VERDE** — commit `18129c5` (el tip; su propio run corre la suite completa), `https://github.com/celestinojbm/Fluvia/actions/runs/28811991914` (CI completo verde: build ×21 + lint + format + suite PG real + migrate ×2 incl. 0034 + `FLUVIA_INVARIANTS_OK`). Espeja el watchdog de attempts (F3-04). Migración 0034: `sweep_payouts()` SECURITY DEFINER sin parámetros (EXECUTE solo `fluvia_worker`) barre `in_transit` past-lease (5 min) → `indeterminate` (el proceso murió entre el emit y el registro del desenlace del banco: desenlace DESCONOCIDO, fondos RETENIDOS en tránsito, JAMÁS `failed` por asunción — V4 §23) con auditoría atómica `payout.swept_indeterminate`; NO fija `failure_code` (no es un fallo; el rastro vive en la auditoría). Salud: `indeterminate_total`/`indeterminate_aged` (>30 min) + `requested_stuck` (payouts cuyo `execute` nunca corrió, >5 min — sin dinero en riesgo, el banco jamás fue contactado; se SURFACEAN, no se re-ejecutan para no arriesgar doble envío). `apps/worker` `PayoutsWatchdog` (gate `PAYOUTS_WATCHDOG_ENABLED`/`_INTERVAL_MS`) invoca la función, expone `fluvia_payouts_swept_total`/`_indeterminate`/`_aged`/`_requested_stuck` y ALERTA. 6 tests vs PG real: barre SOLO in_transit past-lease (el fresco intacto) con auditoría atómica que lista los ids + `failure_code` NULL, indeterminados envejecidos contados, `requested` atascado surfaceado sin moverse, solo `fluvia_worker` ejecuta (app/relay → permission denied), runOnce enteros ≥0, observador que explota no rompe el job. Migrate ×2 idempotente incl. 0034; invariantes verdes. Docs espejo `observability.md` + `payment-state-machines.md` §6. **Restan F4-07c-ii (webhook del banco → resolución automática de indeterminados) y F4-07d (dashboard UI).**
- **F4-07c-ii (webhook del banco resuelve payouts indeterminados; payments-core): run #138 VERDE** — commit `d71eef7`, validado por el run del tip `eb9e04c` (que lo incluye como ancestro y corre la suite completa), `https://github.com/celestinojbm/Fluvia/actions/runs/28814937327` (CI completo verde: build ×21 + lint + format + suite PG real + migrate ×2 + `FLUVIA_INVARIANTS_OK`). Cierra el ciclo de robustez de payouts (espeja F3-03b). El mismo webhook FIRMADO del proveedor 'mock' (endpoint `/v1/providers/mock/webhook`, HMAC + dedup + DLQ ya existentes) transporta también `payout.paid`/`payout.failed`: `MockWebhookEventSchema` → `z.discriminatedUnion('type', …)` (payment.* con `attempt_id` + payout.* con `payout_id`) y `createMockInboxRegistration(confirmation, payouts)` DESPACHA por tipo → payout.* a `PayoutService.resolveFromProvider` (cierra `in_transit`/`indeterminate` — p. ej. los barridos por F4-07c — por fuente verificada, jamás por asunción, V4 §23). El worker cablea un `PayoutService` en el `InboxProcessor`. 3 tests de payout vs PG real EN `webhook-ingest.test.ts` (junto a los 4 de pagos, **un solo processor**: el claim del inbox no filtra por provider, así que dos processors 'mock' concurrentes competirían — por eso ambos flujos comparten archivo/registro): payout.paid firmado → durable 200 → `runOnce` → `paid` + settle (caja neta 0); payout.failed → `failed` + fondos de vuelta; redelivery → duplicate. Sin migración ni deps nuevas.
- **F4-07d (UI del dashboard de payouts; apps/dashboard + apps/api): run #138 VERDE** → **F4-07 COMPLETO** — commit del tip `eb9e04c`, `https://github.com/celestinojbm/Fluvia/actions/runs/28814937327` (CI completo verde: build ×21 incl. **`next build`** + lint + format + suite PG real + migrate ×2 + `FLUVIA_INVARIANTS_OK`). Vista de SOLO LECTURA (server components puros) espejando conciliación F4-01c. Backend: rutas de sesión `GET /v1/organizations/:orgId/payouts`(+`/:id`) bajo `security.session`+`payments:read`, reutilizando `PayoutService` + el serializer `publicPayout` de F4-07b; `payoutService` cableado en `registerDashboardRoutes`. Frontend: enlace «Payouts» → `/o/[orgId]/payouts` (lista id/comercio/monto/estado/creado con badge) → `/o/[orgId]/payouts/[payoutId]` (detalle con pista específica cuando `indeterminate`: fondos retenidos en tránsito). Lee la API directamente server-side (token de sesión jamás sale del servidor; sin route handler). i18n es/en, WCAG AA. 5 tests jsdom+axe (`payouts-view.test.tsx`) + `payouts` en el plano de lectura de `dashboard-routes.test.ts`. Sin migración ni deps nuevas. **Con F4-07d, F4-07 (motor a + API b + watchdog c + webhook c-ii + UI d) queda COMPLETO: el lado money-out está construido de punta a punta en sandbox.**
- **F4-07e (re-drive seguro de `requested` atascados; apps/worker + packages/db): run #143 VERDE** — commit `62b37f8`, `https://github.com/celestinojbm/Fluvia/actions/runs/28816633302` (CI completo verde: build ×21 + lint + format + suite PG real + migrate ×2 incl. 0035 + `FLUVIA_INVARIANTS_OK`). Cierra el hueco que F4-07c dejó abierto: los payouts cuyo `execute` fire-and-forget nunca corrió (el proceso murió tras crear la fila `requested`, antes de emitir — el banco JAMÁS fue contactado) se SURFACEABAN pero no se re-conducían. Migración 0035: `claim_stuck_payouts(max_batch)` SECURITY DEFINER (EXECUTE solo `fluvia_worker`) reclama cross-tenant los `requested` atascados (`created_at`>5 min) tomando un LEASE (toca `updated_at`, sin cambiar status) bajo `FOR UPDATE SKIP LOCKED` + predicado de lease (`updated_at`>5 min) — el lease + skip SERIALIZAN el re-drive (dos workers jamás conducen el mismo payout: lo único que podría doble-contactar al banco, ya que tras el `emit` idempotente ambos llamarían a `submitPayout`), auditoría atómica `payout.redrive_claimed`, devuelve `(id, tenant_id)`. **Seguro por diseño**: en `requested` el emit aún no ocurrió ⇒ re-ejecutar no puede doble-pagar (`emit` idempotente por su key); un re-drive que cae deja el payout en `requested` con el lease vencido y otra corrida lo reintenta. `PayoutsRedriver` (gate `PAYOUTS_REDRIVER_ENABLED`/`_INTERVAL_MS`) reclama en el pool worker y llama `PayoutService.execute` por fila con aislamiento de error por payout; expone `fluvia_payouts_redriven_total{result=redriven|failed}`. El worker reusa UN `PayoutService` sobre el `appPool` para el webhook del banco (F4-07c-ii) y el redriver. 5 tests vs PG real: claim reclama SOLO past-lease requested (el fresco fuera) + toma el lease (segundo claim inmediato lo excluye) + auditoría atómica + no cambia status, solo `fluvia_worker` ejecuta (app/relay→permission denied), re-drive requested→paid con balances exactos (caja neta 0), idempotencia, observador que explota no rompe el job. Docs espejo `observability.md`. **El plano de payouts queda robusto de punta a punta: barrido `in_transit` (F4-07c) + resolución verificada (F4-07c-ii) + re-drive `requested` (F4-07e).**
- **F4-08a (motor de disputas/chargebacks + FSM; packages/db + ledger + payments-core): run #147 VERDE** — commit `027e63c`, `https://github.com/celestinojbm/Fluvia/actions/runs/28818148238` (CI completo verde: build ×21 + lint + format + suite PG real + migrate ×2 incl. 0036 + `FLUVIA_INVARIANTS_OK`; su run PR gemelo #144 también verde). Activa la cuenta `dispute.reserve` que ya vivía en el Chart of Accounts esperando su motor; espeja refunds F3-08 / payouts F4-07a; sandbox. Migración 0036: añade `'dispute'` a las causas del ledger (idempotente, como 0031) + `LEDGER_REASONS` + tabla `disputes` (RLS por tenant, sin DELETE, FK de coherencia por tenant a `merchants`) + FSM `open → under_review → won | lost` hecha cumplir EN el motor por `dispute_transitions` (INMUTABLE, sembrada desde el mapa TS `DISPUTE_TRANSITIONS` vía `gen-fsm-seed.ts`, patrón golden) + trigger de validación. Primitivas contables (`PostingService`, `reason='dispute'`, guard AUD-P1-010 generalizado): `openDispute` (available→dispute.reserve — el banco abre y se aparta el disputado, no double-spend), `winDispute` (dispute.reserve→available, ganada: vuelve íntegro), `loseDispute` (dispute.reserve→provider.clearing, perdida: se va de vuelta vía el proveedor, como un refund forzado). `DisputeService`: `open` crea la fila `open` y aparta en la MISMA tx (atómico; pre-chequeo + `InsufficientDisputeBalanceError`, el guard es el backstop atómico; en sandbox v1 exige disponible suficiente — el saldo deudor es decisión mayor futura), `submitEvidence` (open→under_review, sin mover dinero), `resolve` por FUENTE VERIFICADA (won/lost, jamás por asunción — V4 §23; `ignored`/`ignored_out_of_order` para eventos tardíos). Emite `dispute.<estado>` al outbox (sin estado interno silencioso). No hay `indeterminate` (la disputa no hace llamada saliente cuyo resultado se desconozca; nos lo empujan). 6 golden tests vs PG real (open→won devuelve íntegro + clearing intacto, open→lost forfeita a clearing, open→under_review→lost, no-double-spend con dos disputas que agotan el disponible, abrir por encima rechaza sin crear, idempotencia/fuera-de-orden + inexistente) + FSM meta-test extendido (doc §4 ↔ TS ↔ DDL + los 16 pares del motor + inmutabilidad). Suite total verde: payments-core 112, ledger 49. Motor SIN endpoints/UI (slices siguientes: API key + webhook del banco + dashboard); disputas públicas/reales bloqueadas hasta gates. Docs espejo `payment-state-machines.md` §4 (el placeholder pasa a FSM real) + `ledger-chart-of-accounts.md` + `BACKLOG.md`.
- **F4-08b (disputas — plano de API key; apps/api): run #151 VERDE** — commit `252bf13`, `https://github.com/celestinojbm/Fluvia/actions/runs/28821760763` (CI completo verde: build ×21 + lint + format + suite PG real + migrate ×2 + `FLUVIA_INVARIANTS_OK`; run push gemelo también verde). Expone el motor de F4-08a al integrador. A diferencia de payouts/refunds, la disputa la INICIA el banco: el integrador NO la crea, así que la superficie es de LECTURA + una única acción mutante — RESPONDER con evidencia. Rutas `apps/api/src/routes/disputes.ts`: `GET /v1/disputes/:id` y `GET /v1/disputes` (lista por `merchant_id`, scope `read`), `POST /v1/disputes/:id/evidence` (scope `payments:write`) → `submitEvidence` (`open → under_review`). Envío de evidencia **idempotente por diseño** (sin Idempotency-Key): `submitEvidence` toma `FOR UPDATE`, devuelve el estado actual si ya está `under_review` (serializa envíos concurrentes) y lanza `InvalidStateTransitionError` sobre una disputa terminal. `DisputeNotFoundError`→not_found (sin código de catálogo nuevo, 38; +1 domain → 59; golden regen). La apertura y la resolución (won/lost) NO están en este plano: llegan por fuente verificada (el webhook del banco — F4-08c). 4 tests de ruta HTTP vs PG real (disputa sembrada por el motor como la apertura real del banco): GET detalle/lista por tenant + aislamiento (otro tenant → 404/lista vacía), 404 not_found, evidencia `open→under_review` + re-envío idempotente + read-only 403, evidencia sobre disputa resuelta → 409 `invalid_state_transition`. Fuera del contrato OpenAPI v1 — no toca el SDK. Sin migración ni deps nuevas. Suite total verde: apps/api 175, payments-core 106. **Restan F4-08c (webhook del banco → apertura + resolución por fuente verificada) y F4-08d (dashboard UI).**
- **F4-08c (webhook del banco abre + resuelve disputas; payments-core + apps/worker + packages/db): run #155 VERDE** — commit `7b2b494`, `https://github.com/celestinojbm/Fluvia/actions/runs/28824161187` (CI completo verde: build ×21 + lint + format + suite PG real + migrate ×2 incl. 0037 + `FLUVIA_INVARIANTS_OK`; run push gemelo #154 también verde). El punto de entrada REAL de las disputas (espeja F4-07c-ii). El mismo webhook FIRMADO del proveedor 'mock' (endpoint `/v1/providers/mock/webhook`, HMAC + dedup + DLQ ya existentes) transporta ahora también `dispute.opened`/`dispute.won`/`dispute.lost`: `MockWebhookEventSchema` gana tres literales (open con `merchant_id`+`amount`+`currency`+`provider_ref`; won/lost con `dispute_id`), y `createMockInboxRegistration(confirmation, payouts, disputes)` DESPACHA por tipo — `dispute.opened` → `DisputeService.openFromProvider` (el banco ABRE y aparta fondos), `dispute.won/lost` → `DisputeService.resolve` (fuente verificada, jamás por asunción — V4 §23). **Idempotencia del dinero (Nivel A)**: el inbox es at-least-once (el marcado del evento no es atómico con el efecto), así que `openFromProvider` es idempotente por `provider_ref` — camino rápido `findByProviderRef`, backstop DURO el índice único parcial `disputes_provider_ref_uniq` (migración 0037: `UNIQUE (tenant_id, provider, provider_ref) WHERE provider_ref IS NOT NULL`); un `dispute.opened` reprocesado JAMÁS doble-abre (doble-hold). El worker cablea un `DisputeService` en el `InboxProcessor`. 3 tests de disputa vs PG real EN `webhook-ingest.test.ts` (junto a los 4 de pagos + 3 de payouts, **un solo processor**): `dispute.opened` → durable → `runOnce` → `open` + hold, luego `dispute.won` → íntegro de vuelta; `dispute.lost` → forfeit; dos `dispute.opened` con distinto `event_id` y mismo `provider_ref` → UNA disputa, UN hold (idempotente) + 1 test de motor `openFromProvider`. Sin deps nuevas; migrate ×2 idempotente incl. 0037. Suite total verde: apps/api 178, payments-core 107, apps/worker 35.
- **F4-08d (UI del dashboard de disputas; apps/dashboard + apps/api): run #157 VERDE** → **F4-08 COMPLETO** — commit `ec79e33`, `https://github.com/celestinojbm/Fluvia/actions/runs/28824728479` (CI completo verde: build ×21 incl. **`next build`** + lint + format + suite PG real + migrate ×2 + `FLUVIA_INVARIANTS_OK`; run push gemelo #156 también verde). Vista de SOLO LECTURA (server components puros) espejando payouts F4-07d. Backend: rutas de sesión `GET /v1/organizations/:orgId/disputes`(+`/:id`) bajo `security.session`+`payments:read`, reutilizando `DisputeService` + el serializer `publicDispute` de F4-08b; `disputeService` cableado en `registerDashboardRoutes`. Frontend: enlace «Disputas» → `/o/[orgId]/disputes` (lista id/comercio/monto/estado/creado con badge) → `/o/[orgId]/disputes/[disputeId]` (detalle con motivo + ref. del banco + pista de fondos apartados cuando NO es terminal). Lee la API server-side (token de sesión jamás sale del servidor; sin route handler). i18n es/en, WCAG AA. Badges CSS nuevos `won`(ok)/`lost`(bad)/`under_review`(warn). 5 tests jsdom+axe (`disputes-view.test.tsx`) + `disputes` en el plano de lectura de `dashboard-routes.test.ts`. Sin migración ni deps nuevas. Suite total verde: dashboard 60, apps/api 178. **Con F4-08d, F4-08 (motor a + API b + webhook c + UI d) queda COMPLETO: el ciclo de disputas/chargebacks está construido de punta a punta en sandbox — el banco las abre y resuelve por fuente verificada, con idempotencia del dinero (Nivel A). Con F4-07 (payouts money-out) + F4-08 (disputas money-clawed-back), ambos flujos están completos.**
- **F4-09 (topics de webhook para payouts + disputas; packages/webhooks): run #161 VERDE** — commit `175d0f8`, `https://github.com/celestinojbm/Fluvia/actions/runs/28827024957` (CI completo verde: build ×21 + lint + format + suite PG real + migrate ×2 + `FLUVIA_INVARIANTS_OK`; run push gemelo también verde). Cierra un cabo suelto de correctitud detectado al revisar Fase 4: los motores de F4-07/F4-08 emitían `payout.<estado>`/`dispute.<estado>` al outbox, pero `createWebhookFanoutPublisher` DESCARTA todo topic fuera de `WEBHOOK_TOPICS` — y esos topics estaban como «Reservados post-MVP» en `webhook-delivery.md` §5. Resultado: los eventos de dinero se emitían pero JAMÁS llegaban al comercio por webhook (el operador los veía en el dashboard; el integrador no recibía nada). Graduación: `WEBHOOK_TOPICS` (`events.ts`) gana `payout.requested|in_transit|paid|failed` + `dispute.open|under_review|won|lost` (exactamente lo que emiten los motores; `payout.indeterminate` queda interno/silente), y §5 los mueve de reservados a activos — el **meta-test `contract.test.ts`** exige doc↔código idénticos. Test end-to-end nuevo en `delivery.test.ts`: un endpoint suscrito a `payout.paid`+`dispute.won` recibe filas en `webhook_events` por el fan-out real (antes de F4-09 no generaban nada). Sin migración ni deps nuevas. Suite verde: build ×21, webhooks 21, apps/api webhook-endpoint-routes 6, lint + format. Docs espejo `webhook-delivery.md` §5. **Con F4-09, los eventos de payouts y disputas son entregables a los comercios por webhook firmado — la entrega de ambos flujos de dinero queda completa de extremo a extremo (motor → outbox → fan-out → webhook firmado).**
- **F4-10 (observabilidad + watchdog de disputas; apps/worker + packages/db): run #165 VERDE** — commit `642540b`, `https://github.com/celestinojbm/Fluvia/actions/runs/28829443989` (CI completo verde: build ×21 + lint + format + suite PG real + migrate ×2 incl. 0038 + `FLUVIA_INVARIANTS_OK`; run push gemelo #164 también verde). Cierra el hueco de robustez money-held: las disputas eran el ÚNICO recurso money-held sin monitoreo worker-side (payouts ya tenían watchdog + métricas + alerta + runbook). Espeja F4-07c PERO sin barrido — una disputa `open`/`under_review` retiene fondos en `dispute.reserve` y su desenlace llega SOLO de fuente verificada (V4 §23), así que NADA se transiciona. Migración 0038: `sweep_disputes()` SECURITY DEFINER sin parámetros (EXECUTE solo `fluvia_worker` — el rol no puede SELECT `disputes` bajo RLS; el definer cuenta cross-tenant por él) que SURFACEA `held_total` + `held_aged` (>7d) + índice parcial `disputes_held_idx`. `DisputesWatchdog` (gate `DISPUTES_WATCHDOG_ENABLED`/`_INTERVAL_MS`, mismo patrón que payouts/attempts) expone gauges `fluvia_disputes_held`/`fluvia_disputes_aged` + ALERTA ante envejecidas. 5 tests vs PG real (held vs aged sin transicionar, resuelta no cuenta, solo `fluvia_worker` ejecuta, observador que explota no rompe el job) + runbook `aged-disputes.md` + `observability.md`. Sin deps nuevas. Suite verde: apps/worker 40, config 12.
- **F4-08e (acción de evidencia de disputa en el dashboard; apps/dashboard + apps/api): run #167 VERDE** — commit `0b398c3`, `https://github.com/celestinojbm/Fluvia/actions/runs/28831160333` (CI completo verde: build ×21 incl. **`next build`** + lint + format + suite PG real + migrate ×2 + `FLUVIA_INVARIANTS_OK`; run push gemelo #166 también verde). Cierra el ciclo HUMANO de disputas: RESPONDER con evidencia (`open→under_review`) pasa de ser solo una llamada cruda de API key a una acción del panel — hace `aged-disputes.md` accionable desde la UI. Espeja `case-actions` (F4-03c-ii): ruta de sesión `POST /v1/organizations/:orgId/disputes/:id/evidence` (guard `reconciliation:manage` — owner/admin/finance; el endpoint de API key sigue en `payments:write`) → `submitEvidence`; route handler Next reenvía la cookie httpOnly `fluvia_session` (el navegador jamás sostiene el token); isla cliente `RespondWithEvidence` solo visible mientras la disputa vive + idempotente en la UI (sobre `under_review` muestra «Evidencia enviada»). El desenlace won/lost sigue siendo SOLO por el webhook del banco (V4 §23). i18n es/en + WCAG (axe verde incl. la isla). Tests: `dispute-actions.test.tsx` (POST + error + ya-respondida), `disputes-view.test.tsx` (acción solo con held+canManage, oculta en terminal), `dashboard-routes.test.ts` (finance→under_review idempotente; read_only→403; no-miembro→404). Sin migración ni deps nuevas. Suite verde: dashboard 64, apps/api 181.
- **F4-06b (drill del runbook `aged-disputes` — ejecutado PASS; código): run #169 VERDE** — commit `ab0410f`, `https://github.com/celestinojbm/Fluvia/actions/runs/28831928047` (CI completo verde: build/lint/format/test — el drill es código tipado con override eslint `**/drills/**`; run push gemelo #168 también verde). Segundo drill del Gate Conciliación tras `reconciliation-discrepancy` (9/9). `apps/api/drills/disputes-drill.ts` (`pnpm --filter @fluvia/api run drill:disputes`) conduce el ciclo money-clawed-back contra un stack real (Postgres + API en proceso, **sobre HTTP** para la acción del operador), cruzando los tres planos: el banco ABRE idempotente (reingerir el mismo `provider_ref` NO doble-abre — inbox at-least-once) + aparta → envejecer >7d → `sweep_disputes()` marca `held_aged` (alerta) → **NIVEL A**: el watchdog NO transiciona (sigue `open`) → gate RBAC `read_only`→403 → el operador `finance` RESPONDE con evidencia por sesión (F4-08e)→`under_review` → el banco entrega `dispute.won`→fondos íntegros / `dispute.lost`→forfeit, asientos balanceados (débitos==créditos por disputa vía `ledger_entries`). **Ejecución LOCAL: PASS 7/7** vs Postgres 16, con `verify-ledger-invariants.sql`→`FLUVIA_INVARIANTS_OK` (el CI valida el CÓDIGO; la ejecución es local, como el drill de conciliación). La ingesta HTTP→inbox del webhook la cubre `webhook-ingest.test.ts`; el drill representa el banco por `DisputeService`. Docs `runbooks/README.md` + sección Drill de `aged-disputes.md` a ✅ PASS 7/7. Sin migración ni deps.
- **F4-06b (drill del runbook `indeterminate-payment` — ejecutado PASS; código): run #171 VERDE** — commit `f59c52e`, `https://github.com/celestinojbm/Fluvia/actions/runs/28832789555` (CI completo verde: build/lint/format/test — drill tipado, override `**/drills/**`; run push gemelo #170 también verde). Tercer drill del Gate Conciliación — el segundo flujo money-in-doubt (tras disputas). `apps/api/drills/indeterminate-payment-drill.ts` (`pnpm --filter @fluvia/api run drill:indeterminate`) conduce el ciclo contra un stack real con el INGRESO del pago **SOBRE HTTP** (`tok_timeout` → el MockProvider lanza `ProviderTimeoutError`), cruzando dos planos (proveedor verificado + worker); a diferencia de `aged-disputes` NO hay acción de operador (resolución solo por fuente verificada, por diseño): confirmar→attempt `indeterminate` (NO `failed`; ≠ circuito abierto = fallo limpio `provider_unavailable`) → envejecer >30min → `sweep_payment_attempts()` marca `indeterminate_aged` (alerta) → **NIVEL A**: el watchdog NO resuelve → `resolveFromProvider` `succeeded` (captura ATÓMICA idempotente `attempt:<id>:capture`, intent `succeeded`+`amount_captured`, webhook tardío→`ignored_out_of_order` sin doble captura) / `failed` (SIN asiento) / referencia inexistente→`ignored`. **Ejecución LOCAL: PASS 6/6** vs Postgres 16, con `FLUVIA_INVARIANTS_OK`. Docs `runbooks/README.md` + sección Drill de `indeterminate-payment.md` a ✅ PASS 6/6. Sin migración ni deps. **Con estos dos drills, los TRES flujos money-critical (conciliación four-eyes + disputas money-clawed-back + pagos money-in-doubt) quedan ensayados end-to-end; restan `outbox-inbox-stuck`/`worker-down`/`webhook-dead-letter` con cobertura de facto por sus suites.**
- **F4-06b (drills restantes `outbox-inbox-stuck`+`worker-down`+`webhook-dead-letter` — ejecutados PASS; código): run #175 VERDE** — commit `84810a6`, `https://github.com/celestinojbm/Fluvia/actions/runs/28842286533` (CI completo verde: build/lint/format/test — los drills son código tipado con override eslint `**/drills/**`; run push gemelo #174 también verde). Completa el set de drills del Gate Conciliación: con estos TRES, TODOS los runbooks accionables tienen drill ejecutado (+ `audit-investigation` de facto), y el criterio de salida de Fase 4 «runbooks probados en drill» queda **CUMPLIDO**. Tres drills nuevos en `apps/api/drills/` + scripts `drill:outbox-inbox`/`drill:worker-down`/`drill:webhook-dead-letter`; se añaden `@fluvia/outbox`+`@fluvia/events` como deps de `@fluvia/api` (tooling de drills). **Ejecución LOCAL**: `outbox-inbox-stuck` **PASS 7/7** (replay AUDITADO en las dos direcciones — el único camino sancionado para `dead→pending`, ADR-0011: `reason` obligatoria → sin ella `PlatformReasonRequiredError`, `platform.operation` auditado con `reason`+`replayed_ids`, el relay/inbox retoman el re-encolado → delivered/processed, idempotente sobre filas no-dead); `worker-down` **PASS 5/5** (simula la caída con backlog outbox+inbox y una proyección corrupta acumulados → `checkReady` responde → el relay/inbox **drenan sus backlogs solos** → el `ProjectionDriftWatcher` **detecta** el drift → `rebuildProjection` (reparación EXPLÍCITA, nunca automática, V4 §30) lo limpia; ensaya de paso `ledger-drift`); `webhook-dead-letter` **PASS 5/5** (reenvío por SESIÓN sobre HTTP: gate `webhooks:manage` read_only→403 → **201** con un `pending` fresco enlazado por `resent_from_event_id` (CLONA, no resucita) → el `dead` sigue terminal → audit `webhook_event.resent` → reenviar un no-dead → **409**). Cada uno cierra con `verify-ledger-invariants.sql`→`FLUVIA_INVARIANTS_OK` (el CI valida el CÓDIGO del drill; la ejecución es local, como los demás drills y los E2E de navegador). Docs: tabla de `runbooks/README.md` + secciones Drill de los tres runbooks a ✅ PASS; `ledger-drift` a «ejercido en worker-down». Sin migraciones. **Con esto la Fase 4 (Gate Conciliación) queda cerrada; el cierre documental + audit-readiness va en el commit `57d08f2` (docs-only, bookkeeping — no requiere evidencia propia).**

- **Re-auditoría v2 — remediación de hallazgos accionables (código): run #184 VERDE (base) + run #186 VERDE (seguimiento)** — la remediación de los hallazgos v2 (`docs/audits/re-audit-v2-delta.md`) va en dos commits de código. **Base** commit `d079a37` (V2-R1 cotas de tiempo `SET LOCAL` por-tx + V2-R2 `entriesSignature` JSON canónico + V2-R4 `::numeric` en recompute e invariantes SQL + V2-R6 comentarios; +4 tests de timeouts incl. abort real `57014`): el push gemelo (#181/#182) falló SOLO por `prettier` sobre el test nuevo; corregido en `1b03dec` (style-only, sin cambio de comportamiento) → `https://github.com/celestinojbm/Fluvia/actions/runs/28849494915` (run #184, CI completo verde: build/lint/format/test/migrate ×2/invariantes; push gemelo #183 también verde). **Seguimiento** commit `29414e6`, `https://github.com/celestinojbm/Fluvia/actions/runs/28850278142` (run #186, verde; push gemelo #185 también verde): corrige **4 hallazgos CONFIRMADOS por una revisión adversarial multi-agente del propio diff** (0 refutados) — **RR-1** (override por-tx acotado de 10 min en `rebuildProjection`/`verifyProjection`/`reconcile` para que el `statement_timeout=30s` de V2-R1 no aborte agregados de historial completo en cuentas con millones de filas) · **RR-2** (migración `0039`: `ledger_projection_drift()` recomputa/retorna `numeric` — el detector CONTINUO del worker que V2-R4 había omitido; `drift.ts` lee vía `::text` ⇒ sin cambio de código) · **RR-3** (`55P03` lock_timeout ahora en `RETRYABLE_SQLSTATES`, reintento con backoff acotado) · **RR-4** (`ms()` acota a `[1, 2^31-1]`, evita `22023` en el setup; +test de regresión). **Verificación LOCAL**: suite completa verde (db 57, ledger 49, reconciliation 15, worker 40, payments-core 107, api 181, dashboard 64, …), `migrate` ×2 idempotente incl. `0039`, `verify-ledger-invariants.sql`→`FLUVIA_INVARIANTS_OK`, drills `worker-down` 5/5 + `reconciliation-discrepancy` 9/9 PASS. Estado por hallazgo v2 (incl. V2-N1 verificado no-explotable, V2-N4 dos-humanos DB-enforced, V2-R3/N2 diferidos) + tabla RR-1..RR-4 en §Re-auditoría v2 arriba.
- **F6 · Gate Restore — restore drill (código; cierra AUD-P2-007): run #190 VERDE** — commit `501c073`, `https://github.com/celestinojbm/Fluvia/actions/runs/28882444641` (run #190 pull_request, CI completo verde: build/lint/format/test/migrate ×2/invariantes + secret-scan/SBOM; push gemelo run #28882440238 también verde). Ensaya el runbook de recuperación de desastre contra Postgres 16 real: `pg_dump -Fc` → `pg_restore --exit-on-error` (`code=0` **asserteado** — un restore parcial FALLA el drill) en base fresca → `verify-ledger-invariants.sql`→`FLUVIA_INVARIANTS_OK` sobre la copia → paridad fuente↔copia (conteos de 4 tablas + Σamount + saldo del marcador + **checksums row-level** `md5(string_agg(row::text ORDER BY id))` de asientos y transacciones) → drift inyectado en la copia DETECTADO → `rebuildProjection` (rol `fluvia_app`, explícito) → invariantes verdes → RLS FORZADO + políticas preservadas. `apps/api/drills/restore-drill.ts` + `drill:restore`. **Ejecución LOCAL: PASS 7/7** (el CI valida el CÓDIGO del drill; la ejecución —crea/dropea bases, shell-ea `pg_dump`/`pg_restore`— es local, como los demás drills). **Una revisión adversarial del propio drill halló 2 P2 de falso-verde (exit code de pg_restore descartado + paridad demasiado estrecha para detectar una `ledger_transactions` perdida); ambos corregidos en el mismo commit** (`--exit-on-error` asserteado + paridad de `ledger_transactions` + checksums row-level). Con esto **AUD-P2-007 queda RESUELTO** en sus tres patas (observabilidad + runbooks + restore) y el **Gate Restore pasa a 🟡** (procedimiento drilled; la infra de backup de prod —cadencia/offsite/PITR— es decisión de despliegue F6/F7). Sin migración. `production-gates.md`, `runbooks/backup-restore.md` + README, `STATE.md`, `phase-plan.md` actualizados.
- **F6 · TM-01 (threat model §5) — gate de cobertura del guard de no-negatividad (código): run #196 VERDE** — commit `00e2fc0`, `https://github.com/celestinojbm/Fluvia/actions/runs/28892741999` (run #196 pull_request, CI completo verde; push gemelo #195 también verde). Convierte la invariante de dinero de AUD-P1-010 en **gate de build**: `packages/ledger/test/non-negativity-guard.test.ts` (**18 tests**, suite del ledger 49→67) exige que TODA operación de posting que DEBITA una cuenta de custodia del comercio (pasivo `scope=merchant`, derivado del chart — no hardcodeado) la incluya en `nonNegativeAccounts` (el guard race-safe del motor); un **reflection gate** asegura que la tabla de operaciones cubre EXACTAMENTE los métodos públicos de `PostingService` (una operación futura no puede saltarse la cobertura), con **prueba de dientes** (el detector señala un débito de custodia sin guard) y **prueba de no-vacuidad** (≥10 operaciones ejercen el camino). Relevado durante el threat model: NO hay bug vigente (toda salida de custodia pasa por `twoLegged`, que deriva el guard del chart) — este test lo hace permanente. Sin cambio de código de producto; threat model TM-01 → MITIGADO (código), backstop a nivel de BD registrado como opción aparte (las cuentas transitorias deben poder ir negativas).

- **F6 · TM-04 — restore drill EJECUTADO EN CI por commit (Gate Restore → 🟢 técnico): run #200 VERDE** — commit `aa2e5d7` (los runs propios #197/#198 fueron cancelados por concurrencia al llegar el commit siguiente; lo valida el run del tip que lo contiene: `a7a851a`, `https://github.com/celestinojbm/Fluvia/actions/runs/28893552052`, run #200 pull_request, verde; push gemelo #199 también verde). Añade al job `quality` de CI un paso que EJECUTA `pnpm --filter @fluvia/api run drill:restore` tras la suite y las invariantes (sobre la BD poblada por los tests — una copia realista) + un safety-net que asegura `pg_dump 16+`. **Validación definitiva en el entorno REAL de GitHub Actions, verificada en el log del job**: `pg_dump major=16` y los 7 pasos del drill ✓ (backup → `pg_restore --exit-on-error` → `FLUVIA_INVARIANTS_OK` sobre la copia → paridad con checksums row-level → drift detectado + `rebuildProjection` → RLS preservado) → `✅ DRILL PASS`. Con esto el **Gate Restore pasa de 🟡 a 🟢 técnico** (el procedimiento de recuperación se verifica POR COMMIT, no solo local; resta únicamente la infra de backup de producción — cadencia/offsite/PITR, decisión de despliegue F6/F7). De-risked antes de enviar: el drill pasó localmente contra una BD de 9.7k asientos/6.3k cuentas/1.1k orgs en ~5 s (la forma exacta del CI post-suite). threat model TM-04 → RESUELTO (restore); `drill:worker-down` en CI queda como seguimiento.
- **F6 · TM-06 — guard de datos de tarjeta en el borde (Gate PCI; `pci-scope.md` §3 pasa de afirmación a capacidad): run #204 VERDE** — commit `2ff86f8`, `https://github.com/celestinojbm/Fluvia/actions/runs/28894693612` (run #204 pull_request, CI completo verde incl. el restore drill por commit; push gemelo #203 también verde). El §3 de `pci-scope.md` AFIRMABA que el API rechaza estructuras con datos de tarjeta — el guard no existía (hallazgo TM-06 del threat model, gap doc/código Nivel A). Construido: `apps/api/src/card-data-guard.ts` + hook `preValidation` en `app.ts` — corre con el body parseado **ANTES de auth, Zod o cualquier handler**: `pan_value` (13–19 dígitos tras quitar espacios/guiones + **IIN de marca** Visa/MC/Amex/Discover/Diners/JCB + **Luhn** — un epoch-ms de 13 dígitos NO dispara), `pan_field` (nombre inequívoco `card_number`/`pan`/`numero_tarjeta`… con 12–19 dígitos), `cvv_field` (nombre de código de seguridad con 3–4 dígitos), a cualquier profundidad con recorrido ACOTADO (anti-DoS). Al detectar: **422 `card_data_not_allowed`** (código ADITIVO nuevo en el catálogo, golden regenerado en el mismo commit) + **log de incidente que JAMÁS incluye el valor** (solo la ruta del campo) + métrica `fluvia_card_data_rejected_total{kind}`. **12 tests** (heurística pura + HTTP real: rechazo pre-auth —422 sin Authorization, no 401—, gana la carrera a Zod —campo `cvv` desconocido → 422, no `validation_error`—, el PAN no se refleja en respuesta ni `/metrics`); **suite del api 181→193, cero falsos positivos** (los 181 tests preexistentes pasan con el guard activo sobre cada payload real del sistema). Docs: `pci-scope.md` §3 cita el guard construido; threat model **TM-06 → RESUELTO**; `api-errors.md` gana la fila `unprocessable_error | 422` que faltaba. Sin migración.

- **F6 · TM-02 + TM-05 + TM-03 — cierre de los P2 restantes del threat model §5 (código): run #212 VERDE** — tres commits apilados validados por el run del tip `5fa27fa` (`https://github.com/celestinojbm/Fluvia/actions/runs/28898577776`, run #212 pull_request, CI completo verde incl. migrate ×2 con 0040, invariantes y el restore drill por commit; push gemelo #211 también verde; los runs propios de los dos primeros fueron cancelados por concurrencia — precedente establecido). **(a) TM-02, commit `34cf6c6`** — step-up por re-autenticación de PASSWORD para usuarios sin MFA (el guard era un no-op documentado): migración `0040` (`sessions.password_verified_at`, columna separada de `mfa_verified_at` a propósito — forense), `AuthService.stepUpWithPassword` (comparte el MISMO lockout que el login, igualación de costo temporal, auditado atómico con 2 acciones nuevas), `POST /v1/auth/step-up/password` rate-limited; **el password JAMÁS sustituye al TOTP con MFA (probado)**; el test viejo que codificaba el hueco fue reemplazado por el flujo completo (403→401+403→200→201). Suite api 193→194. **(b) TM-05, commit `faf97ff`** — derecho al olvido por PSEUDONIMIZACIÓN (mandato de `data-classification.md` que no estaba construido): `CustomerService.erase` sobrescribe irreversiblemente email/name/phone/description/metadata + baja lógica + audit `customer.pii_erased` (riesgo alto) en la MISMA tx, fila/FKs/contabilidad INTACTAS, idempotente; `POST /v1/customers/:id/erase` + OpenAPI + SDK `customers.erase` (ambos contratos anti-drift extendidos); probado que la PII no queda en NINGUNA columna. Suite api →197, sdk 10, identity 36. **(c) TM-03, commit `5fa27fa` (tip)** — rate limiter en store COMPARTIDO (ADR-0002): interfaz `RateLimiter` + `RedisFixedWindowLimiter` (Lua INCR+PEXPIRE atómico, sin carrera de expiración, claves con namespace, puerto estrecho stubbeable), **FAIL-OPEN con log** (control de abuso, no invariante — Redis caído no tumba el login, pero jamás en silencio), dep `redis@6.1.0` exacta, `server.ts` lo cablea con `config.redisUrl`; **CI gana un service container `redis:7`** y el log del job del run #212 confirma `✓ rate-limit-redis.test.ts (4 tests)` EJECUTADOS (no saltados) en el entorno real: fail-open unit, ventana COMPARTIDA entre dos instancias del limiter, expiración, y el e2e con DOS apps Fastify donde el 3er intento de login da **429 en la OTRA instancia**. Suite api →201. El circuit breaker del proveedor queda como seguimiento (disponibilidad, al escalar el worker). **Con esto los 6 P2 del threat model §5 (TM-01…TM-06) están cerrados con evidencia.**

- **F6 · Bundle SSRF/webhooks — cierra V2-N2 (re-auditoría v2) + los residuos Webhooks/SSRF del threat model §5: run #218 VERDE** — commit `33432f1` (sus runs propios #215/#216 fueron cancelados por concurrencia al llegar `89aaf86`, docs-only `.gitignore`; lo valida el run del tip que lo contiene: `https://github.com/celestinojbm/Fluvia/actions/runs/28904131705`, run #218 pull_request, CI completo verde incl. migrate ×2, invariantes, restore drill y redis:7; push gemelo #217 también verde). **(a) V2-N2 → RESUELTO** (V2-N4 sigue diferido/documentado con su invariante núcleo DB-enforced): `SafeWebhookTarget` lleva TODAS las IPs validadas y el deliverer hace failover entre ellas SOLO cuando la conexión jamás se estableció (cap 3, sin re-resolver — re-resolver reabriría la ventana de rebinding); jamás si el socket conectó o hubo respuesta HTTP (aun 5xx) — ese borde («conectó pero calló ⇒ re-enviar el mismo intento firmado a otra IP sería doble entrega») lo encontró la **revisión adversarial multi-agente del propio bundle** (un verificador reprodujo 3 POSTs idénticos en un intento con la versión previa) y se corrigió ANTES de mergear; si ninguna IP conecta, el error del attempt registra todas las inalcanzables. **(b) TLS estricto**: `rejectUnauthorized: true` EXPLÍCITO (el test activa `NODE_TLS_REJECT_UNAUTHORIZED=0` y prueba que el explícito gana) + SNI solo con nombre DNS (RFC 6066). **(c) Rate limit del ingest público** por IP en `onRequest` (antes de bufferizar y del HMAC), mismo backend inyectable de TM-03; caveat de topología REGISTRADO en threat model §5: keying por `req.ip` exige `trustProxy` acotado al fronting con proxy/LB (bloqueante de PEND-006, hallazgo de la misma revisión). **(d) Camino >1 MiB probado sobre HTTP** (413 `payload_too_large` con body FIRMADO, cero persistencia; capa servicio cubierta por unit test en `inbox.test.ts`). **Validación definitiva en el log del job del run #218**: `✓ test/delivery.test.ts (11 tests)` — el test TLS self-signed EJECUTÓ (no saltado; el cert se genera con openssl y en CI un fallo de generación rompe el build) — y `✓ test/webhook-ingest.test.ts (12 tests)`; de paso `✓ rate-limit-redis.test.ts (4 tests)` y `✅ DRILL PASS` siguen verdes. Suites: webhooks 24→25, api →203.

- **F6 · Bundle de cadena de suministro — cierra la fila «Secretos/cadena» del threat model §5 (salvo secret-manager): run #222 VERDE** — commit `2d5d264`, `https://github.com/celestinojbm/Fluvia/actions/runs/28906320347` (run #222 pull_request; push gemelo #221 también verde; CI completo incl. restore drill y redis). **(a) Grype gated**: `anchore/scan-action@v6` escanea el árbol y ROMPE el build ante High/Critical no aceptadas — **validado en el log del job security**: grype v0.97.1 ejecutó, catalogó 534 paquetes, 1 match por debajo del cutoff ⇒ verde legítimo (el SBOM syft/SPDX sigue publicándose como artefacto para el auditor); registro de aceptación versionado `.grype.yaml` (vacío) + proceso en `docs/security/vulnerability-acceptance.md` (acotado al paquete, justificación + fecha de revisión, por PR). **(b) `.gitleaks.toml` versionado** (hereda default, excepciones anidadas por review, cero entradas vigentes) + `GITLEAKS_VERSION=8.24.3` explícito — log: `gitleaks version: 8.24.3` → `no leaks found`. **(c) Dockerfile pineado por digest** (sha256 del índice multi-arch verificado) + **(d) Dependabot** semanal npm/actions/docker (su check de config pasó en el mismo tip). **(e) Redacción del logger probada**: log del job quality `✓ test/log-redaction.test.ts (3 tests)` — objeto `LOG_REDACT` compartido path a path, **probe de cableado con dientes de mutación** (quitar `redact:` de `buildApp` rompe el test — hallazgo P2 de la revisión adversarial del bundle, corregido antes de mergear junto con un allowlist sin anclar más ancho que el default y dos comentarios de esquema falsos) y no-fuga end-to-end. Residual documentado en §5: SHA-pinning de actions de CI + frontend BuildKit; secret-manager (ADR). Suite api →206.

- **F6 · Suite SQLi/parametrización + roles deny-by-default — cierra la fila «Multi-tenant» del threat model §5: run #226 VERDE** — commit `1ca7fff`, `https://github.com/celestinojbm/Fluvia/actions/runs/28908853646` (run #226 pull_request; push gemelo #225 también verde; CI completo incl. migrate ×2, invariantes, restore drill y redis). Respalda el límite documentado del RLS (no cubre SQL arbitrario con el rol app; defensa = queries 100% parametrizadas) con tres gates ejecutables. **(a) Candado ESTÁTICO** (`sql-parameterization.test.ts`): en un literal SQL de sentencia completa toda `${…}` debe ser un identificador vetado (lista de columnas constante o el `SET` desde campos literales) o el número de config no parametrizable de `SET LOCAL` — nunca un valor; ADEMÁS prohíbe los dos vectores que un candado de solo-literales no vería (concatenación `.query('…'+x)` y composición desde un fragmento `${…}` inicial) vía un extractor del primer argumento de cada `.query()/.execute()`. **(b) Suite BEHAVIORAL** (`sql-injection.test.ts`): 9 payloads hostiles por los sinks de texto libre de CustomerService y el builder dinámico de `update` → round-trip exacto como dato, tabla intacta (`to_regclass`), sin filas colaterales, RLS respetado, Zod bloquea los campos tipados. **(c) Meta-tests de roles DENY-BY-DEFAULT** (`tenant-escape.test.ts`): enumeran TODOS los `fluvia_%` de `pg_catalog` (rol nuevo = build roto hasta clasificarlo); ninguno super/bypassrls/createrole/createdb, y ninguno con DELETE/TRUNCATE **efectivo** (`has_table_privilege` — cubre grants a PUBLIC y heredados, no solo directos por nombre) + lint de que ninguna ruta instancia el pool admin. **Guiado por revisión adversarial multi-agente**: los dos vectores de evasión (concat/fragmento) y el gap PUBLIC/heredado del barrido de roles fueron hallazgos CONFIRMADOS que se cerraron antes de mergear, junto con correcciones de sobreafirmación en docs (el número de `SET LOCAL` es un valor no un identificador; el alcance «sentencia completa»; la ref rota `multi-tenancy.md §7`→§6.5 y su «sin SQL dinámico» reconciliado con el builder de `SET` identifier-safe). **Validación en el log del job quality del run #226**: `✓ test/sql-parameterization.test.ts (5 tests)`, `✓ test/tenant-escape.test.ts (22 tests)`, `✓ test/sql-injection.test.ts (6 tests)` — todos EJECUTADOS. Solo-tests + docs, sin migración.

- **F6 · Idempotencia ENDURECIDA (fila del threat model §5): run #230 VERDE** — commit `61f0e49`, `https://github.com/celestinojbm/Fluvia/actions/runs/28914110419` (run #230 pull_request; push gemelo #229 también verde; CI completo). Endurece (no cierra — el valor de retención queda como decisión del dueño antes de PEND-006) la fila «Idempotencia»: **(a)** migración `0041` — `sweep_idempotency_orphans()` (SECURITY DEFINER, EXECUTE solo `fluvia_worker`) que surfacea `in_progress_total` + `in_progress_aged` (>1 h) + índice parcial (con nota de deploy: no-CONCURRENTLY toma ShareLock; ventana de mantenimiento en tablas grandes); **(b)** `IdempotencyWatchdog` (espeja `DisputesWatchdog`) → gauges `fluvia_idempotency_in_progress[_aged]` + alerta baseline sobre huérfanos envejecidos que bloqueaban su `(tenant, endpoint, key)` hasta la purga; NO los auto-resuelve (podrían ser una op multi-paso externa viva → doble ejecución); **(c)** retención configurable — el servicio fija `expires_at` explícito desde `IDEMPOTENCY_RETENTION_HOURS` (default 24 h, rango 1–720, validado) con la regla documentada «>= ventana de retry del cliente» (una key expirada re-ejecuta el efecto). **Guiado por revisión adversarial** (11 agentes): cazó un `else` mal atribuido en `main.ts` (el gate de idempotency robaba el log «disputes disabled»), un `stop()` faltante en shutdown, la sobreafirmación «cierra»→«endurece» en STATE, una frase stale en `idempotency.md` y la nota del índice — todos corregidos antes de mergear. **Validación en el log del job quality del run #230**: `0041` aplicado + 2ª corrida de migrate `nothing applied` (idempotente), `✓ test/idempotency.test.ts (13 tests)` y `✓ test/idempotency-watchdog.test.ts (2 tests)` EJECUTADOS, restore drill PASS. Suites: idempotency 11→13, worker +2.

- **F6 · Auth — ciclo de vida de sesión endurecido (fila Auth del threat model §5, PARCIAL): run #234 VERDE** — commit `9a6aa06`, `https://github.com/celestinojbm/Fluvia/actions/runs/28915717000` (run #234 pull_request; push gemelo #233 también verde; CI completo, sin migración nueva). Endurece PARCIAL la fila «Auth» con las dos piezas sin dependencia del canal de email: **(a)** **idle-timeout** de sesión — `authenticateSession` exige `last_seen_at > now() - SESSION_IDLE_TIMEOUT_MS` (default 30 min); el `WHERE` ve el `last_seen_at` PREVIO al `SET`, así que una sesión ociosa es inválida antes del expiry absoluto de 24 h y el `SET` refresca el reloj en cada uso (una sesión fresca pasa por el `DEFAULT now()` de 0004); config-driven. **(b)** **`POST /v1/auth/logout-all`** — autentica la sesión y revoca TODAS las sesiones del usuario (incluida la actual), auditado `auth.sessions_revoked`; es el control «cerrar en todos los dispositivos» que faltaba y el hook para revoke-all-al-cambiar-credencial. **Diferido con honestidad** (documentado): neutralizar el oráculo de enumeración de `register` + el flujo de recovery están acoplados al **canal de email** (sin él, un register neutral dejaría al usuario legítimo sin señal ni cuenta — regresión); revoke-all-automático necesita primero un flujo de cambio de credencial; rotación de pepper/MFA-key es operacional. El comportamiento `email_taken` existente queda intacto. **Revisión adversarial** (8 agentes): encontró el CÓDIGO LIMPIO (semántica del idle WHERE, borde de sesión fresca y precisión de `make_interval` verificados; 4 hallazgos rechazados por falsos); único confirmado = un doc-drift stale en `access-control.md` (MFA/step-up listados como pendientes pese a estar hechos), corregido antes de mergear. **Validación en el log del job quality del run #234**: `✓ test/auth-service.test.ts (17 tests)` (incluye el idle-timeout) y `✓ test/auth-routes.test.ts (9 tests)` (incluye logout-all + requires-session) EJECUTADOS, restore drill PASS. Suites: auth 15→17, auth-routes 7→9.

- **F6 · Drill de resiliencia «worker-down» en CI (fila TM-04 / Gate Restore — cierra el último drill manual): run #240 VERDE** — commit `5e931fe` (validado en el tip `a87196a`), `https://github.com/celestinojbm/Fluvia/actions/runs/28917498673` (run #240 pull_request; push gemelo #239 también verde; CI completo, sin migración). Cablea `drill:worker-down` en el job `quality` tras la suite, junto al restore drill — era el ÚNICO drill operativo que quedaba manual (TM-04 decía «Pendiente: `drill:worker-down` en CI»). El drill ensaya el runbook «worker caído» sobre PG16 real con roles de mínimo privilegio (solo-PG, sin Redis): reinicio (`checkReady`) → el relay del outbox y el processor del inbox DRENAN su backlog solos → el `ProjectionDriftWatcher` DETECTA el drift acumulado durante la caída → `rebuildProjection` EXPLÍCITO lo limpia; deja la BD consistente para el restore drill que corre a continuación. **Endurecido antes de mergear**: el drenado del backlog pasó de un cap CIEGO de iteraciones a uno DIRIGIDO POR OBJETIVO (espera a que los eventos que el drill sembró queden `delivered`/`processed`, con techo de seguridad que da FAIL claro, nunca falso-verde ni cuelgue) — un cap fijo se volvería flaky sobre la BD poblada por la suite (~700 eventos de outbox pendientes hoy) a medida que el backlog heredado crezca. **Validación en el log del job quality del run #240**: `✓ PASO 1..5` + `✅ DRILL PASS — runbook worker-down` con `PASO 2: … 3/3 delivered` (drenado dirigido por objetivo sobre la BD poblada), seguido del restore drill también PASS 7/7 (coexistencia confirmada). Verificado además local sobre la BD poblada por la suite completa.

- **F6 · SHA-pinning COMPLETO de la cadena de CI/build (fila «Secretos/cadena» del threat model §5): run #240 VERDE** — commit `a87196a` (= tip), `https://github.com/celestinojbm/Fluvia/actions/runs/28917498673` (run #240 pull_request; push gemelo #239 también verde; CI completo, sin migración). Cierra el residual de SHA-pinning de la fila «Secretos/cadena» (queda solo secret-manager): las **6 actions de `ci.yml`** ancladas por SHA de commit con comentario de versión — `actions/checkout` v4.3.1, `pnpm/action-setup` v4.3.0, `actions/setup-node` v4.4.0, `gitleaks/gitleaks-action` v2.3.9, `anchore/sbom-action` v0.24.0, `anchore/scan-action` v6.5.1 — y el **frontend BuildKit** `# syntax=docker/dockerfile:1` → `1.24.0@sha256:87999aa3…` (el digest índice AUTORITATIVO que `:1` resuelve hoy, verificado contra el registry; BuildKit lo DESCARGA Y EJECUTA para parsear el Dockerfile ⇒ input de cadena de suministro ejecutable). Cada SHA/digest es EXACTAMENTE lo que el tag mutable resuelve hoy (**freeze verificado tag-a-tag, no upgrade** — CI se comporta idéntico); `GITLEAKS_VERSION: 8.24.3` sigue fijando aparte el binario del escáner. Dependabot (github-actions) bumpea el SHA + comentario de las actions; la directiva `# syntax=` se revisa/bumpea a mano (Dependabot solo parsea `FROM`) — nota honesta en el Dockerfile. **Validación en el log de los jobs del run #240**: `Download action repository 'actions/checkout@34e114876…' (SHA:34e114876…)` + `pnpm/action-setup@b906affc…` + `setup-node@49933ea…` (job quality) y `gitleaks-action@ff98106e…` + `sbom-action@e22c3899…` + `scan-action@1638637d…` (job security) — las 6 DESCARGADAS Y EJECUTADAS por su SHA exacto, ambos jobs verdes, `gitleaks version: 8.24.3` intacto. (Warning pre-existente Node-20 en checkout/setup-node/pnpm — no es fallo ni lo introduce este bundle; Dependabot lo resolverá al bumpear a versiones con runtime Node-24.)

- **F6 · Ledger — hash-chain de tamper-evidence (fila Ledger del threat model §5): run #248 VERDE** — commits `3b94433` (bundle) + `909d902` (remediación de la revisión adversarial) + `fc7d802` (último hallazgo + handoff), validado en el tip `fc7d802`, `https://github.com/celestinojbm/Fluvia/actions/runs/28923678725` (run #248 pull_request; push gemelo #247 también verde; CI completo, **migración 0042**). ENDURECE la fila «Ledger»: el «superuser salta triggers» pasa de indetectable a DETECTABLE — checkpoints append-only encadenados por sha256 sobre `ledger_entries` + campos de su cabecera (orden total por `seq` GENERATED ALWAYS), sellado LAZY en dos fases con **horizonte de txid** (un candidato solo finaliza cuando toda tx en vuelo terminó — cero falsos positivos por commits fuera de orden, cero contención en el posting), sellador `LedgerCheckpointer` en el worker (gauges `fluvia_ledger_chain_*`), y verificación como check **[7]** de `verify-ledger-invariants.sql` (autocontenido: CI por commit + copia del restore drill). **Revisión adversarial** (23 agentes, 5 dimensiones, 18 hallazgos → **10 confirmados, 8 rechazados**) — TODOS los confirmados remediados ANTES de cerrar: **(P1)** advisory lock a nivel de tx que serializa selladores concurrentes (dos workers podían forjar un checkpoint con el `prev_chain_hash` del abuelo equivocado — [7] roto PERMANENTE sobre tabla append-only); **(P1)** los tests de dientes pasaron de `ALTER TABLE … DISABLE TRIGGER` GLOBAL (que corría con los ~9 archivos de test de `packages/db` en paralelo → `rls.test.ts` podía mutar el ledger en la ventana y romper la cadena sin culpable) a `SET LOCAL session_replication_role = replica` de SESIÓN — verificado corriendo el paquete **3× en paralelo con la cadena intacta**; **(P1)** auto-sanado tras DR — un candidato con `horizon_txid` de otro cluster (restore lógico) se evicta en vez de atascar el sellado para siempre; **(P2)** el canónico ahora incluye `t.tenant_id` — sin él, re-atribuir una cabecera sellada a otro tenant era invisible para [7] Y para [1..6] (probado); **(P2)** gauge `fluvia_ledger_chain_unsealed_seq` + alerta de ESTANCAMIENTO (el sellador falla en silencio: los gauges se congelan y la alerta de gauge-que-cae no dispara); **(P3)** `clock_timestamp()` para la edad del candidato (el cinturón se erosionaba con `now()`); **(P3)** doc-overclaim corregido (el truncado de sufijo es igual de invisible que borrar la cadena entera — ambos cubiertos por la alerta de gauge). **Límites honestos documentados**: tamper-EVIDENCE (no prevention); borrar/truncar la cadena → [7] verde trivial, cubierto por la alerta de gauge + el anclaje EXTERNO del `chain_hash` como siguiente pata. **Validación en el log del job quality del run #248**: `0042` aplicado sobre BD fresca + 2ª corrida `nothing applied` (idempotente), `✓ test/ledger-hash-chain.test.ts (13 tests)` (incluye encubrimiento perfecto, re-tenant de cabecera, borrado de checkpoint intermedio, horizonte con tx abierta, restore-eviction) y `✓ test/ledger-checkpointer.test.ts (2 tests)` EJECUTADOS — los 12 «[7] hash-chain broken» del log son los tampers INTENCIONALES de los tests (cada uno tamperea→asserta [7]→repara), y el `FLUVIA_INVARIANTS_OK` POST-suite prueba que todos se repararon; worker-down + restore drills PASS, y `verify-ledger-invariants.sql → FLUVIA_INVARIANTS_OK` sobre la COPIA restaurada (la cadena + [7] + pgcrypto sobreviven pg_dump/restore). Suites: db 66→79 (+13), worker +2.
- **F6 · Ledger — anclaje EXTERNO del `chain_hash` (siguiente pata de la fila Ledger del §5): run #252 VERDE** — commit `c004d8d`, `https://github.com/celestinojbm/Fluvia/actions/runs/28927875328` (run #252 pull_request; push gemelo #251 también verde; CI completo, **migración 0043**; sobre la rama APILADA `claude/external-chain-hash-anchor-7sc0oj`, PR #3 con base `session-direction-check-rnrwv6`). Cierra el hueco honesto de [7]: borrar la cadena ENTERA o truncar su sufijo HASTA el último tip anclado dejaba [7] verde trivial (solo lo cubría la alerta de gauge-que-cae). `anchor_ledger_chain()` (DEFINER, EXECUTE solo del worker) publica el tip (`upto_seq`, `chain_hash`) a un almacén append-only SEPARADO (`ledger_chain_anchors` + evento de plataforma `ledger.chain_anchored` en `audit_events`), y el nuevo check **[8]** de `verify-ledger-invariants.sql` rompe DURO cuando un anchor no tiene checkpoint vivo (truncado/borrado) o su `chain_hash` difiere (re-sellado). `LedgerChainAnchorer` en el worker loguea el tip (artefacto exportable offsite) + gauges `fluvia_ledger_chain_anchors_total`/`_anchored_upto_seq`; el estancamiento del anchorador se detecta CRUZANDO gauges (`_sealed_upto_seq` sigue creciendo aunque el anchorador caiga). **Revisión adversarial** (4 lentes + verificación escéptica) — todos los hallazgos remediados ANTES de mergear: **(HIGH)** gauge `anchor_gap` INERTE eliminado (el anchorador ancla el tip actual en UNA llamada ⇒ su rezago auto-reportado es siempre 0; el estancamiento se detecta cruzando gauges); **(MEDIUM)** inanición mutua de los tests de sellado del worker de-flakeada (un candidato agrupado da UN solo evento finalize; anchorer + checkpointer ahora esperan al estado ACUMULADO `sealed_upto_seq`, robusto a quién selló) — verificado worker **3×** + db **2×** estable; **(LOW)** alcance de [8] precisado (protege HASTA el último anchor; el horizonte aún no anclado lo cubre la alerta de rezago); lente de seguridad: **0 defectos** (meta-tests empíricos, DEFINER como 0034/0042, sin fuga ni inyección). **Límite honesto (V4 Nivel A)**: el anchor vive EN la BD — un superusuario que manipule los TRES almacenes (cadena + anchors + audit) sigue indetectable sin una copia OFFSITE (del operador); no hay infra externa en sandbox (sin capacidad simulada). **Validación en el log del job quality del run #252**: `0043` aplicado sobre BD fresca + 2ª corrida `nothing applied` (idempotente), `✓ test/ledger-hash-chain.test.ts (18 tests)` (+5: anclaje + [8]-only borrando la cadena ENTERA deja [1..7] verdes y solo [8] rompe + min-privilegio + append-only) y `✓ test/ledger-chain-anchorer.test.ts (2 tests)` EJECUTADOS; worker-down + restore drills PASS, y `verify-ledger-invariants.sql → FLUVIA_INVARIANTS_OK` sobre la COPIA restaurada (los anchors + [8] sobreviven pg_dump/restore). Suites: db 79→84 (+5), worker 44→46 (+2).
- **F6 · Secretos/cadena — ADR-0012 secret-manager (docs, Propuesto): run #256 VERDE** — commit `2c43942`, `https://github.com/celestinojbm/Fluvia/actions/runs/28931064650` (run #256 pull_request; push gemelo #255 también verde; sobre `claude/secret-manager-adr`, PR #4 apilado sobre el #3). **Solo-docs** (ADR-0012 + índice ADR con fix de doc-drift 0011 + `secrets-management.md` regla 6 + threat-model §5 «Secretos/cadena» + STATE/HANDOFF): sin migración ni código ⇒ **suite INTACTA**, el CI completo (lint/format/typecheck/migrate ×2/tests/invariantes/drills/grype/gitleaks/SBOM) reconfirma verde. Decide el ENFOQUE de secret-manager: inyección desde el manager como **variables de entorno al arranque** (app agnóstica de proveedor, sin SDK en runtime; contrato de env validado + anti-mezcla ya existente) + rotación por **clave versionada** (patrón v1→v2 de API keys) por clase de secreto — cierra la parte «ADR» del residual «Secretos/cadena». **Nivel A**: artefacto de DISEÑO; el runtime del manager + el vendor concreto van al despliegue real (F5), no se simula infra en sandbox. Restan: runbooks de rotación EFECTIVOS + cifrado field-level de credenciales de proveedor (F5).
- **F6 · Secretos/cadena — rotación EFECTIVA de `WEBHOOK_SECRET_ENC_KEY` (PRIMERA pata de rotación con CÓDIGO de ADR-0012): run #260 VERDE** — commit `1d2fe51`, `https://github.com/celestinojbm/Fluvia/actions/runs/28964468520` (run #260 pull_request; push gemelo #259 también verde; CI completo, **sin migración** — keyring por config; sobre la rama APILADA `claude/webhook-enc-key-rotation`, PR #5 con base `claude/secret-manager-adr` = PR #4). Convierte el enfoque de ADR-0012 en capacidad: `WEBHOOK_SECRET_ENC_KEY` (AES-256-GCM que cifra EN REPOSO los `whsec_` de firma) ya se rota SIN downtime ni cambio de esquema. Un **keyring** (clave ACTUAL + RETIRADAS) descifra PROBANDO cada clave — el TAG de AES-GCM disambigua (una clave ajena jamás autentica, salvo forja 2⁻¹²⁸) ⇒ los blobs no necesitan versión ni columna nueva; `WEBHOOK_SECRET_ENC_KEY_RETIRED` en config (anti-mezcla: `required()` fuera de local/test, `ADMIN_DATABASE_URL` sigue lanzando PRIMERO) alimenta al deliverer; `reencryptWebhookSecrets` (CLI admin `rotate:webhook-key`, cross-tenant) migra los blobs a la clave actual para poder eliminar la retirada. El `whsec_` del comercio NO cambia (solo su cifrado en reposo). **Revisión adversarial** (4 lentes + verificación escéptica) — TODOS los hallazgos remediados ANTES de mergear: **(MEDIUM)** el barrido crudo `pool.connect()` con un `FOR UPDATE` de TODA la tabla saltaba las cotas de tiempo por tx del repo (V2-R1) y bloqueaba la gestión de endpoints durante el barrido entero → reescrito a **lotes por keyset**, cada uno su propia tx con `SET LOCAL` statement/lock/idle (los locks se sueltan entre lotes); **(MEDIUM · honestidad)** el «sin downtime» de 3 fases tenía una ventana de indescifrable en el rolling restart de la flota mixta (se promovía la clave nueva antes de que toda instancia viva pudiera descifrarla) → **runbook a 4 fases** que pre-siembra la clave nueva como solo-descifra en TODA la flota ANTES de promoverla (la afirmación ahora es REAL, no solo disclosed); **(LOW-MEDIUM)** una sola fila indescifrable abortaba el barrido entero (rotación atascada plataforma-wide) → **resiliencia por fila** (`failed`/`failedIds`, el barrido sigue); **(MEDIUM)** el gate irreversible «borrar una clave» solo tenía como prueba el CLI mutante → **prueba de vuelo de SOLO LECTURA** `inspectWebhookSecretKeys` / `rotate:webhook-key --check` (`underRetired=0`/`undecryptable=0`); **(LOW)** `tryDecrypt` enmascaraba el error de FORMATO de clave como «clave equivocada» → ahora surge; **(gaps de test)** null-prev a nivel BD, fila mixta, conteos acoplados al orden → añadidos/endurecidos. Confirmados **no-issues** en la revisión: orden de evaluación de config, bordes del parseo de retiradas, sin ciclo de dependencias (`@fluvia/config` es hoja), sin fuga de claro a logs, manejo de `prev` null. **Límite honesto (Nivel A)**: el secret-manager de ADR-0012 NO está integrado en el sandbox (no hay infra externa; no se simula) — hoy las claves llegan por env validadas; el vendor concreto va al despliegue real (F5). MFA-key (TOTP) y pepper de API keys quedan como patas aparte. **Validación**: el run #260 verde reconfirma el CI completo (lint/format/typecheck/migrate ×2/tests/invariantes/drills/grype/gitleaks/SBOM); local vs PG16 real: `@fluvia/webhooks` 34→**38** (`crypto.test.ts` 6→7 incl. clave mal formada; `rotate.test.ts` 3→6 incl. fila mixta + resiliencia `failed` + inspección de solo-lectura), `tsc` build 21/21, `verify-ledger-invariants.sql → FLUVIA_INVARIANTS_OK`, worker-down + restore drills PASS.
- **F6 · Secretos/cadena — rotación EFECTIVA de `MFA_SECRET_KEY` (SEGUNDA pata de rotación con CÓDIGO de ADR-0012): run #264 VERDE** — commit `92cee31`, `https://github.com/celestinojbm/Fluvia/actions/runs/28967954615` (run #264 pull_request; push gemelo #263 también verde; CI completo, **sin migración** — keyring por config; sobre la rama APILADA `claude/mfa-secret-key-rotation`, PR #6 con base `claude/webhook-enc-key-rotation` = PR #5). Reutiliza el mecanismo probado en la pata webhook para rotar SIN downtime ni cambio de esquema la clave AES-256-GCM que cifra EN REPOSO el secreto TOTP de cada usuario (`users.totp_secret_enc` / `totp_pending_secret_enc`); el secreto TOTP en claro NO cambia (los códigos del Authenticator siguen verificando). El **keyring** `MfaEncKeyring` (ACTUAL + RETIRADAS) descifra PROBANDO cada clave — el TAG de AES-GCM disambigua; `MFA_SECRET_KEY_RETIRED` en config (anti-mezcla: 64-hex, `!=` current, sin dups; `ADMIN_DATABASE_URL` sigue lanzando PRIMERO). `reencryptMfaSecrets` corre como **`fluvia_auth`** (mínimo privilegio, NO superusuario — `users` es global y `fluvia_auth` ya lee/escribe la tabla entera, a diferencia de los `webhook_endpoints` tenant-scoped): lotes por keyset (200), cada uno su tx con `SET LOCAL` statement/lock/idle; filas indescifrables reportadas (`failed`/`failedIds`), no abortan el barrido; incluye usuarios soft-deleted para que borrar la clave sea seguro; solo toca las dos columnas enc (anti-replay `totp_last_used_step` y `totp_enabled_at` preservados); SQL 100% literal (pasa el candado de parametrización estática). Read-only `inspectMfaSecretKeys` (`rotate:mfa-key --check`) gatea el borrado de clave. **Revisión adversarial** — hallazgos remediados ANTES de mergear con el mismo patrón que la pata webhook: barrido por lotes con cotas de tiempo por tx (no `FOR UPDATE` global), runbook a **4 fases** (pre-siembra la clave nueva como solo-descifra en TODA la flota ANTES de promoverla — la afirmación «sin downtime» es REAL, no solo disclosed), resiliencia por fila, prueba de vuelo de solo-lectura como gate irreversible, y `tryDecryptMfa` que surge el error de FORMATO de clave en vez de enmascararlo. El runbook nota que durante una rotación fallida los backup codes TAMBIÉN quedan indisponibles (recuperación = re-añadir la clave). **Límite honesto (Nivel A)**: el secret-manager de ADR-0012 NO está integrado en el sandbox; hoy las claves llegan por env validadas (el vendor concreto va a F5). **Validación**: el run #264 verde reconfirma el CI completo (lint/format/typecheck/tests/invariantes/drills/grype/gitleaks/SBOM); local vs PG16 real como `fluvia_auth`: `mfa-crypto.test.ts` 7 (incl. clave mal formada) + `mfa-rotate.test.ts` 8 (dos end-to-end vía el `AuthService` real: un usuario bajo la clave RETIRADA autentica durante la ventana de rotación y, tras el barrido, autentica solo con la ACTUAL), suite `@fluvia/auth` →**58**, `verify-ledger-invariants.sql → FLUVIA_INVARIANTS_OK`, worker-down + restore drills PASS.
- **F6 · Secretos/cadena — rotación EFECTIVA del pepper `API_KEY_HMAC_SECRET` (TERCERA y ÚLTIMA pata de rotación con CÓDIGO de ADR-0012 — cierra las tres): run #266 VERDE** — commit `0492263`, `https://github.com/celestinojbm/Fluvia/actions/runs/28971469429` (run #266 pull_request; push gemelo #265 también verde; CI completo, **migración 0044**; sobre la rama APILADA `claude/api-key-pepper-rotation`, PR #7 con base `claude/mfa-secret-key-rotation` = PR #6). El pepper es un HMAC de UNA vía (`key_hash = hmac(pepper, secret)`) ⇒ a diferencia de las patas AES-GCM NO hay barrido reversible masivo: el re-hash es **LAZY** dentro de `authenticate_api_key`, extendiendo el upgrade existente `v1(sha256)→v2(hmac)` para cubrir también `pepper-viejo→pepper-nuevo`. El `fluvia_sk_` del comercio NO cambia. **Migración 0044**: añade `api_keys.key_hash_pepper_fp` (fingerprint de UNA vía del pepper, por fila) y reescribe `authenticate_api_key` (SECURITY DEFINER) a 4 args (hmac-actual, sha256-legacy, hmacs-retirados[], fp-actual) — matchea por hash y re-hashea a la clave actual + estampa su fingerprint cuando el match vino por un pepper retirado o legacy; REVOKE/GRANT a `fluvia_app`. `inspectApiKeyPepper` (gate de SOLO LECTURA `rotate:api-key-pepper --check`; buckets `underCurrent/underRetired/unmarked/unknown` sobre keys VIVAS) + `backfillApiKeyPepperFp` (marca de una vez las filas v2 pre-0044 como bajo el pepper actual); cross-tenant vía el pool admin (RLS de `api_keys` es tenant-scoped, como webhooks). **Revisión adversarial** (2 lentes de agente + 2 inline) — hallazgos remediados ANTES de mergear: **(MEDIUM)** un `--backfill` corrido a mitad de rotación envenenaría el gate → el CLI RECHAZA en duro `--backfill` cuando hay peppers retirados configurados (no solo un warning en prosa); **(LOW)** `tenantIds:[]` pasado como filtro real → tratado como «todos» (fail-open, sin falso «safe») + test; **(LOW · honestidad)** el fingerprint ES un nuevo oráculo de verificación pepper-only (inviable vs un pepper de 256 bits) → wording corregido y **ensanchado a 128 bits** para que las colisiones del gate sean negligibles; **(LOW)** ventana de orden de despliegue por el cambio de aridad de 0044 → documentada; lente DEFINER: sin bypass de auth / sin falso-accept (redes de seguridad GCM/UNIQUE verificadas). El runbook añade la Fase 0 (backfill) + 4 fases sin ventana de auth; un pepper retirado se descarta solo cuando `underRetired=0` (las keys dormidas se revocan+reemiten, ya que un hash de una vía no se puede migrar en masa). **Límite honesto (Nivel A)**: el fingerprint es un oráculo pepper-only (no expone el pepper); el secret-manager de ADR-0012 sigue sin integrarse en el sandbox (F5). **Validación**: el run #266 verde reconfirma el CI completo (incl. migrate ×2 idempotente — 0044 aplicado + 2ª corrida `nothing applied`); local vs PG16 real: `api-keys.test.ts` +5 (create estampa el fp; una key bajo el pepper RETIRADO autentica y se re-hashea a la actual; el gate llega a 0 vía re-hash lazy; un pepper fuera del keyring es «unknown» y NO autentica; backfill marca las filas unmarked; `tenantIds:[]` = todos), suite `@fluvia/identity` →**47**, `verify-ledger-invariants.sql → FLUVIA_INVARIANTS_OK`, worker-down + restore drills PASS. **Con esta pata, las TRES rotaciones EFECTIVAS de ADR-0012 (webhook enc-key + MFA-key + pepper) quedan CERRADAS.**
- **F6 · Ledger — invariante de no-negatividad a nivel BD [9] (defensa en profundidad; cierra el residuo «no-negatividad de motor a nivel BD» de TM-01 / §5): run #268 VERDE** — commit `438f1e0`, `https://github.com/celestinojbm/Fluvia/actions/runs/28974923085` (run #268 pull_request; push gemelo #267 también verde; CI completo, **sin migración/runtime**; sobre la rama APILADA `claude/ledger-nonneg-invariant`, PR #8 con base `claude/api-key-pepper-rotation` = PR #7). Cierra el residuo como EVIDENCIA, no como trigger de runtime: el nuevo check **[9]** de `verify-ledger-invariants.sql` (CI por commit + copia del restore drill) rompe si cualquier cuenta PROTEGIDA del chart tiene `available` o `pending < 0` en `balance_projections`. **Por qué verificación y no trigger**: el motor ya garantiza la no-negatividad en runtime bajo locks de cuenta (`nonNegativeAccounts`, `service.ts`) — un trigger que abortara postings arriesgaría el motor de dinero por un control ya resuelto; [9] lo respalda como tamper-evidence (mismo patrón que [7]/[8]) con CERO riesgo de runtime, y complementa a [6] (que solo exige que la proyección COINCIDA con los asientos, no que sea `>= 0`). **Exentas**: las cuentas transitorias `suspense`/`recon.differences`, que `postReconAdjustment` (un posting SIN el guard) puede dejar negativas por diseño; las cuentas fuera-de-chart (p.ej. `conc.*` de tests de concurrencia con asientos crudos) quedan fuera del whitelist de 12 códigos protegidos — en producción toda cuenta nace del chart. **Verify-first** (disciplina del hilo) rechazó la variante de trigger de runtime (riesgo de motor) y fijó el set exento EXACTO leyendo el guard y cada camino de posting. **Revisión adversarial** confirmó que ningún falso positivo es alcanzable en producción (`capturePayment` es solo-incremento; cada op `twoLegged` guarda sus cuentas decrecientes; el bucket `pending` es uniformemente 0 para las cuentas del motor) y precisó la honestidad del comentario (`reverseTransaction` es un SEGUNDO posting sin guard, hoy sin callers de producción; [9] disparando sobre él sería detección correcta). **Validación**: el run #268 verde reconfirma el CI completo; empíricamente [9] VERDE sobre la BD entera poblada por la suite (todos los flujos de pago) Y sobre la copia del restore drill — fuerte evidencia de cero falsos positivos; teeth tests en `ledger-hash-chain.test.ts` (+2): un negativo en una cuenta protegida (`merchant.available`) rompe [9]; un `suspense` negativo NO (exención) con [6] aún presente (reparación por `UPDATE ... SET available=0, pending=0`, porque `balance_projections` es append-only). Suite `@fluvia/db` 84→**86** (+2), worker-down + restore drills PASS, `verify-ledger-invariants.sql → FLUVIA_INVARIANTS_OK`.
- **F6 · Revisión de seguridad interna v1 — cierra 2 HIGH + 3 MEDIUM + LOW/INFO de un self-review adversarial de 7 lentes: run #274 VERDE** — commit `8cd9264` (tip; el increment es `4b5df7b` + el fix de formato/lint `8cd9264`), `https://github.com/celestinojbm/Fluvia/actions/runs/28979786026` (run #274 pull_request; push gemelo #273 también verde; CI completo, **migración 0045**; sobre la rama APILADA `claude/security-review-f6`, PR #9 con base `claude/ledger-nonneg-invariant` = PR #8). Self-review adversarial (7 lentes sobre el código real; NO sustituye la auditoría externa v1, que sigue 0 P1/0 P2) — cada hallazgo verificado contra el código y remediado con teeth test. **2 HIGH**: (a) una sesión secuestrada SIN MFA se auto-enrolaba un factor propio (`/mfa/setup`+`/activate` solo exigían sesión, `activateMfa` sellaba `mfa_verified_at`) y pasaba el step-up para acuñar keys → **reabría TM-02**; fix: step-up FRESCO (`assertFreshStepUp`) para enrolar/activar/deshabilitar MFA; (b) la denylist SSRF comparaba IPv6 por STRING (`=== '::1'`, `startsWith('fe80')`), dejando pasar literales no canónicos (`0:0:0:0:0:0:0:1`, `::ffff:7f00:1`, el resto de `fe80::/10`) → fix: normalización a 16 bytes + rangos numéricos. **3 MEDIUM**: (c) los 21 `SECURITY DEFINER` shadowables por `pg_temp` (que `search_path = public` NO excluye) → migr. **0045** `REVOKE TEMPORARY … FROM PUBLIC` (quita la primitiva de objetos temporales a los roles de runtime — root-cause); (d) evasión del lockout MFA por reset del contador al PRIMER factor → reset solo al COMPLETAR el login; (e) dedup de webhooks por PK de fila en vez del `event_id` ESTABLE del sobre → el deliverer emite el `event_id` del sobre. **LOW/INFO**: guard de no-negatividad en `reverseTransaction`; validación UUID de params (500→400); `Number.isSafeInteger` en `amount` de 5 rutas; hex en el verificador de firma del SDK; **CSP** en checkout+dashboard (`connect-src 'self'`); `REVOKE` de writes sobre `users` (0045); lock en `verifyProjection`. Residuales documentados CON razón (enumeración por lockout = tradeoff aceptado; Origin-check / CSP-nonce / `Secure`-por-entorno / aserción `normal_side` en startup → PEND-006/F5). Informe completo en `docs/audits/security-review-v1.md`; STATE/HANDOFF/threat-model (TM-02 + Webhooks/SSRF + Multi-tenant) actualizados. **Nota CI honesta**: el run #272 (sobre `4b5df7b`) FALLÓ por formato/lint (corrí `tsc`+tests localmente pero no `lint`/`format`; ediciones multilínea de prettier + `process` no-declarado en los `next.config.mjs` no-TS); corregido en `8cd9264` (sin cambio de comportamiento) → run #274 verde. **Validación**: build 21/21, suite completa verde vs PG16 (`@fluvia/api` 211, `@fluvia/db` 88, `@fluvia/auth` 59, `@fluvia/ledger` 68, `@fluvia/webhooks` 40, …), `verify-ledger-invariants.sql → FLUVIA_INVARIANTS_OK` (checks [1]–[9]), migr. 0045 idempotente (migrate ×2 `nothing applied`), drills worker-down (5/5) + restore (7/7) — el restore verifica los invariantes sobre la COPIA (0045 sobrevive pg_dump/restore, RLS preservado).
- **F6 · Load-soak-chaos — drill de resiliencia bajo carga+fallo (cierra la ÚLTIMA pata GRANDE de F6 buildeable en sandbox) + FIX de un crash real: run #278 VERDE** — commit `6735d69`, `https://github.com/celestinojbm/Fluvia/actions/runs/28981379691` (run #278 pull_request; push gemelo #277 también verde; CI completo; sobre la rama APILADA `claude/load-chaos-stress-f6`, PR #10 con base `claude/security-review-f6` = PR #9). **BUG REAL encontrado y corregido POR el drill de caos** (`packages/db/src/pool.ts`): `createPool` no ataba un handler `error`, así que node-pg lanza un `'error'` NO capturado cuando CUALQUIER conexión del pool muere (failover de PG / kill administrativo / partición de red / reinicio de la BD) → el proceso ENTERO (API o worker) CRASHEA; un blip de conexión transitorio era un caído total. Ahora se manejan ambos casos: `pool.on('error')` (conexión OCIOSA) + `client.on('error')` atado en `connect` (conexión TOMADA cuya query muere EN VUELO); las queries en vuelo siguen rechazando por su cuenta (NO se traga ningún error de query). El `drill:load-chaos` (nuevo gate en CI, corre el ÚLTIMO — el más destructivo) ataca las redes de V2-R1 sobre PG16 real: (a) saturación de pool — 80 postings concurrentes sobre un pool de 4 → TODOS drenan sin cuelgue, conservación exacta; (b) caos de conexión — `pg_terminate_backend` sobre backends `fluvia_app` EN VUELO → los postings abortan LIMPIO (rollback atómico por el trigger de balanceo diferido, CERO asientos huérfanos ni drift), el pool se recupera solo; (c) idempotencia bajo caos — 12 claves × 5 dups concurrentes con kills → cada clave aplicada A LO SUMO una vez (unicidad DB-enforced); (d) invariantes finales — Σdébitos == Σcréditos + proyección == recomputo. Teeth: `pool-resilience.test.ts` (una conexión muerta en vuelo rechaza su query, NO crashea el proceso, el pool se recupera). **Honestidad (Nivel A)**: el throughput logueado es una MEDICIÓN del sandbox mono-instancia (pool de 4, PG local) — NO un SLO de producción ni afirmación de escala; el valor PROBADO es la INTEGRIDAD del dinero bajo fallo abrupto, medida de verdad. STATE/HANDOFF/threat-model (TM-04) actualizados. **Validación**: build 21/21, suite completa verde vs PG16 (`@fluvia/db` 88→**89** con `pool-resilience.test.ts`; `payouts-redriver` es el flake conocido, 46/46 aislado), lint + format limpios, `verify-ledger-invariants.sql → FLUVIA_INVARIANTS_OK` (checks [1]–[9]), drills worker-down (5/5) + restore (7/7) + **load-chaos (4/4)** — coexisten en secuencia en el job quality de CI. **Con esto NO queda ninguna pata GRANDE de F6 buildeable en sandbox** (rotación×3 + [9] + revisión de seguridad + load-soak-chaos, TODO verde en CI con evidencia registrada); el residual es de infra/despliegue (F5/PEND-006), del operador (copia OFFSITE de anchors) o del propietario (re-auditoría). Cierre limpio para pedir la re-auditoría externa.
- **MERGE DEL STACK COMPLETO (PRs #2–#10) a `claude/new-session-haeo7h` — 9/9 runs de push VERDES (2026-07-09)** — el propietario autorizó el merge («Opción A»): merge commits (sin squash), orden estricto #2→#3→#4→#5→#6→#7→#8→#9→#10, re-apuntando la base del siguiente PR tras cada merge y esperando CI verde en la rama por defecto antes de continuar. Evidencia por merge (commit de merge → run de push verde): PR #2 `efaa789` → **#281**; PR #3 `d0dc9b5` → **#304**; PR #4 `e5c7991` → **#305**; PR #5 `01e115b` → **#306**; PR #6 `1c5d061` → **#309**; PR #7 `ebd4527` → **#314**; PR #8 `dba92c6` → **#317**; PR #9 `53d1e30` → **#318**; PR #10 `d60e4595` → **#319** (`https://github.com/celestinojbm/Fluvia/actions/runs/29056398115`). Cada run es el pipeline completo (lint, format, typecheck, migrate ×2 idempotente sobre BD fresca — 0001–0045 —, suite vs PG16, invariantes [1]–[9] `FLUVIA_INVARIANTS_OK`, drills worker-down + restore + load-chaos, gitleaks, dependency audit, SBOM). Pre-flight verificado antes del primer merge: historia 100% lineal (cadena de ancestros confirmada con `merge-base --is-ancestor` en los 9 eslabones), simulación `git merge-tree` limpia contra la punta de la rama por defecto (cuyo único commit extra, `62f7a94` — el delta de la re-auditoría v2 —, no intersecta ningún archivo del stack), y hashes SHA-256 de `docs/audits/independent-audit-v1/` intactos contra el manifest. **Baseline de re-auditoría: `claude/new-session-haeo7h` @ `d60e45956bef4c1cc696ff4e3cca31c8bc8346d1` (run #319)**. Nota: los PRs de dependabot que aparecieron tras el merge del stack quedan SIN mergear a propósito (decisión #26 — no mover el baseline durante la ventana de re-auditoría).

## Criterio de re-auditoría (adoptado del auditor)

Criterio del auditor: cerrar P1-003, P1-004, P1-005, P1-006 y P1-007. **Estado: 5 de 5 CERRADOS** (~~P1-003~~ ✓ ~~P1-004~~ ✓ ~~P1-005~~ ✓ ~~P1-006~~ ✓ ~~P1-007~~ ✓) con evidencia individual y runs de CI registrados arriba. **El proyecto está LISTO para la re-auditoría externa** — este registro + el plan de integración son el paquete de entrada.
