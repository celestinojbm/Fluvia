# Modelo de amenazas

Estado: Activo · **Fase 6 · actualizado 2026-07-07** · Revisión obligatoria ante cambios de superficie y antes de abrir el sandbox compartido (PEND-006) / integrar el proveedor real (Fase 5).

> **Qué cambió respecto a la versión Fase 0.** Aquella era un plan («Diseñado/Parcial»); esta refleja lo **implementado y verificado** tras las Fases 1–4 y las dos auditorías independientes (v1 + re-auditoría v2, ambas remediadas — `docs/audits/`). Cada control se ancla en código/migración/test concretos, con su forma de verificación. El §5 es el **backlog de endurecimiento de la Fase 6** derivado de este análisis (riesgo residual real, priorizado). Método: STRIDE por frontera de confianza. Inventario base: 89 controles + 34 riesgos residuales relevados con evidencia (subsistemas: aislamiento multi-tenant, integridad del ledger, idempotencia/concurrencia, authn/authz, webhooks/SSRF, secretos/datos/cadena de suministro).

Alcance: sandbox, sin dinero real ni datos de tarjeta reales. Aun así el modelo se construye como si fueran reales — los controles Nivel A son los mismos que exigirá producción. La exposición pública de la API de pagos/checkout/webhooks y todo uso `live` siguen **congelados** (decisión #24) hasta re-auditoría + PEND-006.

## 1. Contexto, actores y fronteras de confianza

Monolito modular (`system-overview.md`): `apps/api` (Fastify) + frontends Next.js (dashboard, checkout, admin) que importan paquetes de dominio (sin HTTP interno); `apps/worker` corre relay/jobs y hace las ÚNICAS llamadas de red salientes (webhooks, proveedor); PostgreSQL 16 es la fuente de verdad; Redis es cache/coordinación (jamás fuente duradera).

| Actor / entrada                | Frontera cruzada                                     | Credencial                               | Confianza                                          |
| ------------------------------ | ---------------------------------------------------- | ---------------------------------------- | -------------------------------------------------- |
| Integrador (API key)           | Internet → `apps/api` `/v1`                          | `fluvia_sk_*` (Bearer, HMAC server-side) | No confiable; scoped por tenant+permiso            |
| Comprador (checkout)           | Internet → `/v1/checkout_sessions/:id/*` sin API key | `client_secret` `cs_` (en el fragment)   | No confiable; solo su sesión                       |
| Operador (dashboard/admin)     | Internet → sesión + membresía                        | Cookie httpOnly `fluvia_session` + RBAC  | Semi-confiable; RBAC + four-eyes                   |
| Proveedor (webhooks entrantes) | Proveedor → inbox `/v1/providers/mock/webhook`       | HMAC del proveedor                       | No confiable hasta verificar firma                 |
| Endpoints de webhook salientes | `apps/worker` → Internet                             | — (nosotros firmamos)                    | **Superficie SSRF**: destino potencialmente hostil |
| DBA / superusuario / infra     | Fuera del proceso                                    | Acceso a cluster/secret manager          | **Tier de confianza superior** (ver §4)            |

Fronteras duras de runtime (`system-overview.md` §3): ninguna llamada de red dentro de una tx SQL (outbox/inbox es el único puente); todo dato externo pasa por Zod en el borde; contexto de tenant solo por `set_config(...,local)` en tx; dinero solo como `Money` (bigint); estados de FSM solo mutables por su servicio de dominio.

## 2. Activos

Fondos contables (ledger append-only + proyecciones), credenciales (API keys, sesiones, secretos de webhook, seed TOTP, futuras credenciales de proveedor), PII de customers, integridad y no-repudio de eventos y del `audit_log`, disponibilidad del pipeline de pagos, y la confianza/reputación de la plataforma.

## 3. Análisis STRIDE (controles implementados)

### 3.1 Spoofing (suplantación)

| Amenaza                                                | Control implementado                                                                                                                                      | Evidencia                                                                    | Verificación                                                |
| ------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- | ----------------------------------------------------------- |
| Cracking offline de passwords desde un dump            | scrypt (N=2¹⁵) con salt por hash + compare timing-safe; formato versionado                                                                                | `packages/auth/src/passwords.ts:17-72`                                       | auth tests                                                  |
| Password robado ⇒ acceso                               | MFA TOTP RFC 6238 (impl propia, anti-replay por step); un password válido con MFA activo solo emite un challenge                                          | `packages/auth/src/totp.ts:45-112`, `service.ts:322-345,474-480`; migr. 0014 | auth/mfa tests                                              |
| Robo de token de sesión / exposición a JS              | Token opaco; la BD guarda solo `sha256(token)`; el dashboard lo tiene en cookie **httpOnly** SameSite; proxy server-side (el navegador nunca ve el token) | `service.ts:365-379`, `apps/dashboard/app/api/session/route.ts:30-36`        | dashboard e2e                                               |
| Contexto de tenant suplantado / fuga por reuse de pool | `withTenantTransaction`: `set_config('app.tenant_id',$1,true)` parametrizado (SET LOCAL, muere con la tx) — único camino de datos                         | `packages/db/src/pool.ts:65-98`                                              | `tenant-escape.test.ts:117-139` (pool no arrastra contexto) |
| IDOR: el cliente dicta su tenant en el payload         | Tenant derivado SOLO de la identidad autenticada (`authenticate_api_key` DEFINER pre-tenant; sesión por membresía bajo RLS)                               | `apps/api/src/security.ts:105-133`; migr. 0002/0003                          | `rls.test.ts:123-150`                                       |
| Webhook entrante forjado                               | HMAC-SHA256 sobre `${ts}.${rawBody}` con compare constante, ANTES de procesar; firma sobre los BYTES crudos                                               | `packages/inbox/src/signature.ts:22-56`, `ingest.ts:86-95`                   | inbox/contract tests                                        |

### 3.2 Tampering (manipulación)

| Amenaza                                               | Control implementado                                                                                                                                        | Evidencia                                           | Verificación                                          |
| ----------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------- | ----------------------------------------------------- |
| Edición/borrado de historia financiera                | Append-only a nivel MOTOR: triggers `fluvia_forbid_mutation` BEFORE UPDATE/DELETE/TRUNCATE (disparan incluso para superuser); sin GRANT DELETE a ningún rol | migr. `0001_foundation.sql:167-210`, `0002:50-55`   | `rls.test.ts:76-120`, `tenant-escape.test.ts:197-204` |
| Asiento desbalanceado / header sin entradas           | Constraint triggers DIFERIDOS `ledger_assert_balanced` + `ledger_assert_nonempty` a COMMIT (por moneda)                                                     | migr. `0007_ledger_core.sql:82-126`                 | `ledger-invariants.test.ts`                           |
| Asiento cruzado a cuenta de otro tenant/moneda        | UNIQUE `(id,tenant_id,currency)` + FK compuesta en `ledger_entries` (AUD-P1-001) — ni el superuser puede                                                    | migr. `0008_audit_remediations.sql:18-40`           | `ledger-invariants.test.ts`                           |
| Doble reversión (duplica el efecto opuesto)           | Índice único parcial `ledger_transactions_reverses_once` decide la carrera EN el motor                                                                      | migr. `0012:26-28`; `service.ts:505-583`            | `reversal.test.ts`                                    |
| Manipulación de precisión monetaria                   | `Money`: bigint unidades menores, inmutable, exige misma moneda; CHECK `amount>0`                                                                           | `packages/money/src/money.ts:16-189`; migr. 0001:79 | `money.test.ts` (20)                                  |
| Firma sobre re-serialización en vez de bytes de cable | Body capturado como string crudo (`parseAs:'string'`); HMAC sobre esos bytes exactos                                                                        | `apps/api/src/routes/provider-webhooks.ts:29-35`    | inbox tests                                           |

### 3.3 Repudiation (repudio)

| Amenaza                                        | Control implementado                                                                                                            | Evidencia                                                                                  | Verificación                                   |
| ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ | ---------------------------------------------- |
| Negar una acción sensible (auth, keys, ajuste) | `audit_log` append-only, escrito en la MISMA tx que la acción; correlación por `request_id` extremo a extremo                   | `packages/auth/src/service.ts` (insertAuditEvent), `api-keys.ts:172-182`, `adjustments.ts` | audit/contract tests                           |
| Bypass de aislamiento sin rastro               | `withPlatformOperation` = único camino fuera del tenant; exige `reason`, audita atómico (riesgo alto) y hace rollback conjunto  | `packages/audit/src/index.ts:169-206`; callers en replay/inbox                             | audit tests                                    |
| Fraude de un solo actor sobre dinero           | **Four-eyes** en ajustes sobre umbral en DOS capas: CHECK en BD (`approved_by<>proposed_by`) + guard de servicio + actor humano | migr. `0030:47-50`; `reconciliation/src/adjustments.ts:135-138,238-240`                    | `adjustments` tests + drill reconciliation 9/9 |

### 3.4 Information disclosure (divulgación)

| Amenaza                                        | Control implementado                                                                                                                                                 | Evidencia                                                                | Verificación                                                      |
| ---------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ | ----------------------------------------------------------------- |
| Tenant escape (lectura/escritura cross-tenant) | RLS ENABLE+**FORCE** en toda tabla con `tenant_id`; policy `USING`+`WITH CHECK` con el idioma `NULLIF(current_setting('app.tenant_id',true),'')::uuid` (fail-closed) | migr. `0002:61-106`, `0003`, `0019`                                      | `tenant-escape.test.ts:40-114` (read/write/join/EXISTS/agregados) |
| Robo de credenciales vía plano de negocio      | Plano de auth separado: solo `fluvia_auth` toca `users/sessions/tokens/mfa`; app/worker/relay/inbox con CERO privilegios ahí (REVOKE ALL)                            | migr. `0004:74-105`, `0014:47-71`                                        | `tenant-escape.test.ts:206-214`                                   |
| Seed TOTP / secretos de webhook desde un dump  | TOTP AES-256-GCM (`MFA_SECRET_KEY`); `whsec_` AES-256-GCM (`WEBHOOK_SECRET_ENC_KEY`); API keys HMAC con pepper fuera de la BD                                        | `totp.ts:114-146`, `webhooks/src/crypto.ts:11-43`, `api-keys.ts:111-113` | auth/webhooks tests                                               |
| Fuga de secretos en logs/errores               | Catálogo de errores sin detalle interno (golden test); redacción en el logger; `/metrics` sin ids de tenant                                                          | `error-catalog.ts`, `packages/observability`                             | golden error test, metrics test                                   |
| Claves DEV alcanzando un entorno real          | `required()` (anti-mezcla V4 §43) LANZA fuera de local/test; el default DEV solo se retorna en local/test                                                            | `packages/config/src/index.ts:203-232`                                   | config tests + guard AUD-P2-008                                   |
| Enumeración de cuentas en login                | `InvalidCredentialsError` uniforme + hash dummy memoizado (email inexistente vs password errado indistinguibles)                                                     | `auth/src/service.ts:278-282`                                            | auth tests                                                        |

### 3.5 Denial of service (denegación)

| Amenaza                                              | Control implementado                                                                                          | Evidencia                                                | Verificación                                   |
| ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------- | ---------------------------------------------- |
| Query patológica / lock / tx colgada acapara el pool | `SET LOCAL statement_timeout=30s / lock_timeout=15s / idle_in_tx=60s` en cada `withTenantTransaction` (V2-R1) | `packages/db/src/pool.ts:65-98`                          | `tx-timeouts.test.ts` (incl. abort real 57014) |
| Body entrante gigante agota memoria/BD               | Cap de 1 MiB antes de firmar/persistir (`PayloadTooLargeError`)                                               | `packages/inbox/src/ingest.ts:75,82-85`                  | ⚠️ sin test del camino >1 MiB (ver §5)         |
| Payload veneno bloquea la cola                       | Inbox async: claim-lease `FOR UPDATE SKIP LOCKED`, validación Zod, veneno → DLQ (no bloquea)                  | `packages/inbox/src/processor.ts:128-346`                | inbox tests                                    |
| Fuerza bruta en login/register/MFA                   | Rate limiter de ventana fija por email+IP en `/v1/auth/*` + lockout por usuario                               | `apps/api/src/rate-limit.ts:26-90`, `service.ts:284-311` | auth tests · ⚠️ in-memory mono-instancia (§5)  |
| Proveedor caído satura reintentos                    | `ResilientProvider`: timeout duro + circuit breaker (abierto = fallo LIMPIO `provider_unavailable`)           | `packages/payments-core` (F3-04); `AttemptsWatchdog`     | watchdog tests                                 |

### 3.6 Elevation of privilege (elevación)

| Amenaza                                                 | Control implementado                                                                                                                               | Evidencia                                                                   | Verificación                                                       |
| ------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| Rol de fondo con visión cross-tenant oculta (BYPASSRLS) | **NINGÚN** rol de runtime tiene BYPASSRLS (ADR-0011); la visión cross-tenant del relay/inbox/webhook es política RLS EXPLÍCITA acotada a UNA tabla | migr. `0009:43` (`NOBYPASSRLS`), `0009:29-38`, `0010:54-78`, `0019:141-168` | `tenant-escape.test.ts:216-225` (los 6 roles `rolbypassrls=false`) |
| Rol de cola sobre-privilegiado muta payload/tenant      | GRANTs por COLUMNA: relay/inbox/webhook solo SELECT + UPDATE de columnas de despacho de su ÚNICA tabla; worker = cascarón sin privilegios          | migr. `0009/0010/0019`                                                      | `tenant-escape.test.ts:227-302`                                    |
| `SET ROLE` a un rol más poderoso                        | `fluvia_app` es LOGIN plano sin membresía en postgres/worker/auth                                                                                  | modelo de roles 0002/0004/0009                                              | `tenant-escape.test.ts:141-150` (SET ROLE rechazado)               |
| Sesión secuestrada acuña/revoca API keys                | Step-up en `keys:manage`: si el usuario tiene MFA, exige `mfa_verified_at` fresco                                                                  | `apps/api/src/security.ts:82-89`; migr. 0014:26-27                          | ⚠️ **no-op si el usuario NO tiene MFA** (§5, P2)                   |
| BOLA / acceso a otra organización                       | Toda ruta `/v1/organizations/:orgId` corre `session()`+`org(permission)`; no-miembro → 404 (anti-enumeración) vs 403                               | `apps/api/src/security.ts:91-103`; `tenant-service.ts:190-198`              | route tests (403/404)                                              |
| API key escala a gestión de cuenta                      | Dos planos NO intercambiables (sesión vs API key); scopes disjuntos                                                                                | `apps/api/src/security.ts:61-135`; `access-control.md`                      | route tests                                                        |
| RBAC drift / escalada silenciosa                        | Matriz RBAC declarativa (7 roles × 10 permisos) como fuente única + meta-test celda-a-celda                                                        | `packages/identity/src/rbac.ts:8-85`                                        | rbac meta-test                                                     |

### 3.7 Doble movimiento de dinero (transversal a T/E — crítico para un ledger)

| Amenaza                                      | Control implementado                                                                                                                        | Evidencia                                                                     | Verificación                                                            |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| Retry mueve dinero dos veces (nivel API)     | `idempotency_keys` PK `(tenant_id,endpoint,key)`; claim+efecto+respuesta en UNA tx; mismo key+payload → replay, key+payload distinto → 422  | migr. `0001:115-125`,`0008:46-49`; `packages/idempotency/src/index.ts:66-192` | idempotency + crash-recovery tests                                      |
| Doble posting en el motor                    | Idempotencia por clave natural: `ledger_transactions` UNIQUE `(tenant_id,idempotency_key)` ON CONFLICT DO NOTHING + replay con firma causal | `service.ts:198-215,365-431`                                                  | ledger tests                                                            |
| Doble captura (confirm sync + webhook async) | Ambos caminos postean con la MISMA clave `attempt:{id}:capture`                                                                             | `confirmation.ts:239-271`                                                     | drill indeterminate 6/6                                                 |
| Sobre-refund                                 | Cupo = capturado − aplicado − en vuelo, bajo `FOR UPDATE` del intent                                                                        | `refunds.ts:128-178`                                                          | payments-core tests (107)                                               |
| Webhook duplicado re-abre disputa            | Índice único parcial `disputes_provider_ref_uniq`                                                                                           | migr. `0037:17-19`                                                            | disputes tests + drill 7/7                                              |
| Resultado ambiguo asumido                    | Desenlaces desconocidos → `indeterminate` con reserva RETENIDA (nunca «failed por si acaso»); cierra solo por fuente verificada             | `confirmation.ts:113-174,185-236`                                             | drill indeterminate 6/6                                                 |
| Saldo negativo (double-spend contable)       | Guard de no-negatividad race-safe bajo lock de cuenta (AUD-P1-010) para toda cuenta que DECRECE                                             | `service.ts:334-361`, `posting.ts:370-401`                                    | `posting.test.ts` · ⚠️ es guard de app, no invariante de motor (§5, P2) |
| Corrupción silenciosa de proyección          | Proyección reconstruible desde asientos; `verifyProjection`/`rebuildProjection` + drift watcher + `ledger_projection_drift()`               | `service.ts:450-648`, `drift.ts`; migr. 0011/0039                             | drift property test + drill worker-down 5/5                             |
| Corrupción indetectable / restore infiel     | Invariantes EXTERNOS `verify-ledger-invariants.sql` (6 checks, autocontenido) en CI + drill de restore                                      | `scripts/verify-ledger-invariants.sql`; ci.yml:70-71                          | drill restore 7/7                                                       |

## 4. Supuestos de confianza (tiers)

- **Tier runtime (modelado a fondo)**: los roles `fluvia_app/worker/relay/inbox/auth/webhook` — sin BYPASSRLS, mínimo privilegio, RLS forzado. Es la superficie principal del modelo.
- **Tier DBA / superusuario / infraestructura (confianza superior, explícito)**: el superusuario de PG puede saltar triggers/constraints vía `session_replication_role` y leer todo; la infra (secret manager, red) es de confianza. En Fases 1–4 (local/CI) queda fuera del alcance activo; **en Fase 6/producción entra** con secret manager, acceso mínimo, y —como tamper-evidence opcional— archivado append-only/hash-chain de `ledger_entries` (ver §5).
- **MockProvider**: es código propio pero NO se le confía distinto — pasa por el MISMO inbox verificado (firma + dedup), así el pipeline queda probado como si fuera externo.
- **Congelado por decisión**: exposición pública y `live` bloqueados (decisión #24) hasta re-auditoría + PEND-006; payouts reales bloqueados por diseño en el MVP.

## 5. Riesgo residual y backlog de endurecimiento (Fase 6)

Derivado del inventario (34 residuales, deduplicados y priorizados). Ninguno es P0/P1; la exposición está congelada. Los **P2** se abordan antes de abrir el sandbox compartido (PEND-006) o integrar el proveedor real (F5).

### P2 — antes del sandbox compartido / proveedor real

| #     | Riesgo residual                                                                                                                                                                                                                                                 | Recomendación                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Gate/Fase                       |
| ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------- |
| TM-01 | **No-negatividad es guard de APP** (`nonNegativeAccounts`), no invariante de MOTOR: hoy toda salida de custodia pasa por `twoLegged` (deriva el guard del chart — sin bug vigente), pero un posting FUTURO/multi-pata podría olvidarlo, y no hay backstop de BD | **MITIGADO (código)**: meta-test `non-negativity-guard.test.ts` convierte el invariante en GATE — toda operación de posting que DEBITA una cuenta de pasivo `scope=merchant` debe guardarla; una operación nueva sin cobertura o sin guard rompe el build (con reflection gate + prueba de dientes). **Pendiente**: backstop a nivel de BD (columna `non_negative` + trigger) — más costoso/sutil (las cuentas transitorias SÍ pueden ir negativas), evaluado aparte | F6 · Gate Ledger                |
| TM-02 | **Step-up es no-op sin MFA**: una sesión secuestrada de un usuario sin MFA acuña/revoca keys sin prueba fresca                                                                                                                                                  | MFA obligatorio, o fallback de re-auth por password para `keys:manage`, antes del sandbox compartido                                                                                                                                                                                                                                                                                                                                                                 | F6 · PEND-006                   |
| TM-03 | **Rate limiter + circuit breaker in-memory mono-instancia** (Nivel C): se reinician por proceso y no coordinan entre instancias (no afectan la garantía de no-duplicación — PG es la fuente)                                                                    | Mover a store compartido (Redis, ADR-0002) antes de cualquier despliegue multi-instancia/compartido                                                                                                                                                                                                                                                                                                                                                                  | F6 · PEND-006                   |
| TM-04 | **Drills de restore/worker-down son manuales**, no corren en CI ⇒ Gate Restore no se vuelve verde solo                                                                                                                                                          | Cablear `drill:restore` (y `drill:worker-down`) en un job programado/nightly contra PG16 efímero con client tools                                                                                                                                                                                                                                                                                                                                                    | F6 · Gate Restore               |
| TM-05 | **Derecho al olvido / pseudonimización de PII documentado pero no construido** (`data-classification.md` lo exige)                                                                                                                                              | Rutina auditada que sobrescribe campos PII manteniendo la fila+FKs; + test                                                                                                                                                                                                                                                                                                                                                                                           | F6 (o F5 con PII del proveedor) |
| TM-06 | **`pci-scope.md` §3 afirma un guard de datos de tarjeta que NO existe** en código                                                                                                                                                                               | Construir el guard conservador (heurística nombre-de-campo + Luhn en endpoints no permitidos, con audit de incidente + test), o degradar la afirmación del doc                                                                                                                                                                                                                                                                                                       | F5/F6 · Gate PCI                |

### P3 — endurecimiento y exactitud (Fase 6)

| Área            | Riesgo residual                                                                                                                                                                                                                                                                                                       | Recomendación                                                                                                                                                                                                                                    |
| --------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Multi-tenant    | RLS no protege contra SQL arbitrario con el rol app (cambia el GUC); meta-tests de roles no son deny-by-default; `adminPool` instanciable desde rutas                                                                                                                                                                 | Suite de SQL-injection/parametrización (F6); reescribir meta-tests para consultar TODOS los roles `fluvia\_%`; lint de arquitectura: ninguna ruta importa `adminPool`                                                                            |
| Ledger          | Superuser puede saltar triggers (tier superior); drift watcher mono-instancia (setInterval); `verify-invariants` §6 no marca cuentas con asientos sin proyección                                                                                                                                                      | Cron DB-side de invariantes + leader election; check de visibilidad de proyecciones faltantes; considerar hash-chain de `ledger_entries` para tamper-evidence                                                                                    |
| Idempotencia    | Retención de keys (24h) < ventana máxima de retry documentable; filas `in_progress` huérfanas (24h)                                                                                                                                                                                                                   | Fijar retención ≥ ventana de retry antes del sandbox; sweep/alerta de `in_progress` envejecidos                                                                                                                                                  |
| Auth            | Cobertura de step-up más estrecha que lo documentado; sin flujo de recuperación de cuenta; `register` es oráculo de enumeración (409 `email_taken`); sin rotación de pepper/MFA-key; sesiones sin idle-timeout ni revocación global al cambiar credencial                                                             | Alinear step-up con `access-control.md`; flujo de recovery (token único, TTL corto, revoke-all) con canal de email; respuesta neutral en register; versión+rotación de pepper/MFA-key; idle-timeout + revoke-all-on-change                       |
| Webhooks/SSRF   | **V2-N2**: `resolveSafeWebhookTarget` valida TODAS las IPs pero pinea solo `ips[0]` (sin failover — robustez, no hueco de seguridad); `rejectUnauthorized` implícito sin test; ingest entrante sin rate limit; secreto de mock único sin rotación; camino >1 MiB sin test; deliverer sin cap de concurrencia por host | Iterar sobre las IPs ya validadas en error de conexión; fijar `rejectUnauthorized:true` explícito + test TLS self-signed; rate-limit del ingest (PEND-006); per-provider secret en F5; test del cuerpo gigante; concurrencia por host al escalar |
| Secretos/cadena | Redacción del logger estrecha y sin test; SBOM generado pero no escaneado/gated; imagen base por tag mutable; sin `.gitleaks.toml` versionado; secret-manager + cifrado field-level de credenciales de proveedor no aterrizados                                                                                       | Test de redacción + ampliar paths; escaneo de vulnerabilidades del SBOM (grype) + Dependabot/Renovate; pinear imagen por digest; `.gitleaks.toml` con allowlist revisable; ADR de secret-manager + runbooks de rotación antes del sandbox        |
| Docs            | `multi-tenancy.md §4` y ADR-0005 declaraban `fluvia_worker` con `BYPASSRLS` (contradice ADR-0011/realidad)                                                                                                                                                                                                            | **`multi-tenancy.md §4` corregido en este incremento**; ADR-0005 queda supersedido por ADR-0011 (nota en su encabezado, vía proceso ADR)                                                                                                         |

## 6. Verificación y mantenimiento del modelo

- **El modelo se mantiene honesto por prueba, no por afirmación**: cada control del §3 cita test/CI/drill; los invariantes del ledger corren en CI (`verify-ledger-invariants.sql`) y en el drill de restore; los meta-tests estructurales en `pg_catalog` cubren tablas y roles FUTUROS (una tabla nueva sin RLS, o un rol nuevo con BYPASSRLS/DELETE, rompe el build).
- **Dos auditorías independientes** (v1 + re-auditoría v2) están integradas y remediadas (`docs/audits/audit-closure-register-v1.md`: 0 P1 / 0 P2 abiertos); sus hallazgos son entradas de este modelo.
- **Disparadores de revisión**: nuevo endpoint mutante de dinero, nuevo rol/tabla, integración del proveedor real (F5), apertura del sandbox compartido (PEND-006), o cualquier hallazgo de auditoría. Los ítems del §5 se rastrean como backlog de Fase 6 con evidencia por cierre.
- **Correcciones sancionadas** (nunca SQL manual sobre dinero): compensación/ajuste con caso + four-eyes, replay auditado de outbox/inbox, rebuild explícito de proyecciones (`incident-response.md`, `runbooks/`).
